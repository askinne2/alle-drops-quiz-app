---
phase: 06-purchase-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/sense-buy-buttons-excerpt.liquid
  - tests/sense-atc-selector-contract.test.ts
autonomous: true
requirements: [SHOP-03]
tags: [d-02, selector-contract, sense-fixture, wave-0]

must_haves:
  truths:
    - "CI fails loudly if Sense's .product-form__submit class disappears from the vendored buy-buttons excerpt"
    - "CI fails loudly if the express-checkout payment_button conditional disappears from the vendored excerpt"
    - "The selector contract is green against the checked-in fixture without needing the sibling theme path"
  artifacts:
    - path: "tests/fixtures/sense-buy-buttons-excerpt.liquid"
      provides: "Vendored Sense ATC + express-checkout needles for D-02"
      contains: "product-form__submit"
    - path: "tests/sense-atc-selector-contract.test.ts"
      provides: "D-02 compensating control — mandatory for fail-open gate"
      contains: "product-form__submit"
  key_links:
    - from: "tests/sense-atc-selector-contract.test.ts"
      to: "tests/fixtures/sense-buy-buttons-excerpt.liquid"
      via: "readFileSync of vendored fixture"
      pattern: "sense-buy-buttons-excerpt\\.liquid"
---

<objective>
Install the mandatory D-02 compensating control: a checked-in Sense buy-buttons excerpt and a Vitest
contract that fails when `.product-form__submit` or the `payment_button` express path drifts.

Purpose: Fail-open ATC gating (D-02) is only safe if CI screams when Sense renames the class.
Without this, a theme update silently turns the honor-system gate into dead UI.
Output: `tests/fixtures/sense-buy-buttons-excerpt.liquid` + `tests/sense-atc-selector-contract.test.ts`,
both green under `npx vitest run`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-purchase-prerequisites/06-CONTEXT.md
@.planning/phases/06-purchase-prerequisites/06-RESEARCH.md
@.planning/phases/06-purchase-prerequisites/06-PATTERNS.md
@.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md
@tests/liquid-block-contract.test.ts

<interfaces>
Source of truth for the fixture (theme repo, do not import at runtime in CI):
  /Users/andrewskinner/Local Sites/allergist-on-demand/snippets/buy-buttons.liquid
  — class product-form__submit at the available-variant button and the sold-out button
  — show_dynamic_checkout conditional wrapping form | payment_button

Contract style analog: tests/liquid-block-contract.test.ts (readFileSync + toContain / toMatch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Sense buy-buttons excerpt for D-02</name>
  <files>tests/fixtures/sense-buy-buttons-excerpt.liquid</files>
  <read_first>
    - `/Users/andrewskinner/Local Sites/allergist-on-demand/snippets/buy-buttons.liquid` lines 73–114 (both product-form__submit buttons and the show_dynamic_checkout / payment_button conditional)
    - `.planning/phases/06-purchase-prerequisites/06-RESEARCH.md` § Pattern 3 and § D-02 CI fixture strategy
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-01 and D-02
  </read_first>
  <action>
    Create tests/fixtures/sense-buy-buttons-excerpt.liquid by copying the ATC-relevant excerpt from the Sense theme file buy-buttons.liquid (available-variant submit button with class product-form__submit, sold-out submit button with the same class, and the show_dynamic_checkout conditional that outputs form | payment_button). Do not copy the entire snippet — only the product-form buttons region that contains those needles.

    Add a one-line HTML comment at the top naming the source path and that this fixture is the D-02 CI contract, not live theme code. Do not rewrite class names. Do not soft-code the selector string — the literal product-form__submit must appear twice (matching Sense's two buttons). The payment_button filter invocation must appear inside the show_dynamic_checkout conditional exactly as in Sense.

    Do not read or write allergist-on-demand/config/settings_data.json. Do not run shopify theme push (D-03).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('tests/fixtures/sense-buy-buttons-excerpt.liquid','utf8'); const n=s.split('product-form__submit').length-1; const p=s.split('payment_button').length-1; if(n&lt;2||p&lt;1) process.exit(1); console.log('submit='+n+' payment='+p)"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/fixtures/sense-buy-buttons-excerpt.liquid exists under the quiz-app repo.
    - Occurrence count of product-form__submit via split(needle).length-1 is at least 2.
    - Occurrence count of payment_button is at least 1.
    - File contains show_dynamic_checkout.
    - No shopify theme push was run; SUMMARY states that explicitly.
  </acceptance_criteria>
  <done>Vendored Sense ATC/express excerpt is committed in-repo for CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green Vitest contract for Sense ATC selector needles</name>
  <files>tests/sense-atc-selector-contract.test.ts</files>
  <read_first>
    - `tests/fixtures/sense-buy-buttons-excerpt.liquid` (Task 1 output)
    - `tests/liquid-block-contract.test.ts` (contract-test style: readFileSync, describe/it, toContain)
    - `.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md` Interaction Contract item 3 (fail-open + CI mandatory)
  </read_first>
  <behavior>
    - Reading the vendored fixture finds class product-form__submit at least twice.
    - Fixture contains the show_dynamic_checkout conditional and payment_button.
    - Test does not require the sibling theme path to exist (CI-safe).
  </behavior>
  <action>
    Create tests/sense-atc-selector-contract.test.ts using vitest describe/it/expect and node:fs readFileSync of tests/fixtures/sense-buy-buttons-excerpt.liquid relative to process.cwd().

    Assert: (1) split-based occurrence count of product-form__submit is greater than or equal to 2; (2) fixture includes show_dynamic_checkout; (3) fixture includes payment_button; (4) fixture includes type="submit" or type='submit' on a product-form__submit button context. Optional non-blocking comment may note a local hash check against the sibling theme path is developer-machine only — do not make CI depend on that path.

    Name the top-level describe something like Sense ATC selector contract (D-02 compensating control). Per D-02 this is the mandatory compensating control for fail-open gating — do not weaken assertions to soft-match regexes that could pass without the real class string.
  </action>
  <verify>
    <automated>npx vitest run tests/sense-atc-selector-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/sense-atc-selector-contract.test.ts exits 0.
    - Test file imports from vitest and reads only the vendored fixture path under tests/fixtures/.
    - No assertion depends on /Users/andrewskinner/Local Sites/allergist-on-demand existing.
  </acceptance_criteria>
  <done>D-02 CI selector contract is green and CI-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme snippet → CI fixture | Untrusted theme updates can rename ATC classes; fixture detects drift |
| Honor-system gate → storefront sales | Fail-open without detection is silent policy bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Sense .product-form__submit selector | mitigate | This plan's contract fails CI when the vendored class needle drifts (D-02) |
| T-6-02 | Denial of service | Fail-closed ATC on missing selector | accept | Explicitly rejected by D-02 — fail-open is required; compensated by T-6-01 |
| T-6-03 | Information disclosure | Theme push of drifted settings_data.json | mitigate | Plan actions forbid shopify theme push (D-03); no theme files modified |
</threat_model>

<verification>
npx vitest run tests/sense-atc-selector-contract.test.ts exits 0; fixture contains both ATC and express needles.
</verification>

<success_criteria>
D-02's mandatory CI compensating control exists and is green without sibling-theme CI access.
</success_criteria>

<output>
After completion, create `.planning/phases/06-purchase-prerequisites/06-01-SUMMARY.md`
</output>
