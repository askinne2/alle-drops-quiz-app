---
phase: 06-purchase-prerequisites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md
autonomous: false
requirements: [SHOP-01, SHOP-05]
tags: [d-04, wave-0, liquid-render, admin-inventory]

must_haves:
  truths:
    - "TN and TX SLIT products use the regional-drops product template; consult does not"
    - "SHOP-05 paste targets on Basic/Grow are inventoried before any copy draft is written"
    - "Liquid render of customer.metafields.alledrops.quiz_count is measured on authenticated served bytes for a known customer"
  artifacts:
    - path: ".planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md"
      provides: "templateSuffix table, SHOP-05 surface map, Liquid render measurement record"
      contains: "templateSuffix"
  key_links:
    - from: "06-02-SUMMARY.md"
      to: "Shopify Admin product templateSuffix"
      via: "human Admin verification recorded verbatim"
      pattern: "tennessee-alledrops|texas-alledrops"
---

<objective>
Close the three Wave 0 human gates that block safe placement and honest SHOP-05 drafting: D-04
product→template assignment, SHOP-05 admin surface inventory, and the owed SHOP-01 Liquid render
measurement on served bytes.

Purpose: Building or placing the gate without D-04 can block the $99 consult (clinical-access
regression). Drafting SHOP-05 without an inventory targets fictional admin fields. Treating
metafield definitions as proof of Liquid behavior repeats the SHOP-01 spike failure mode.
Output: A SUMMARY that records templateSuffix for all three products, the real SHOP-05 paste
surfaces, and a non-empty Liquid quiz_count measurement for a logged-in customer with values.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-purchase-prerequisites/06-CONTEXT.md
@.planning/phases/06-purchase-prerequisites/06-RESEARCH.md
@.planning/phases/06-purchase-prerequisites/06-SPIKE-SHOP-01.md
@.planning/phases/06-purchase-prerequisites/06-VALIDATION.md
@HANDOFF.md
@app/lib/shopify/metafields.ts

<interfaces>
Known product handles (from Phase 1 / DEF-03): tennessee-alledrops, texas-alledrops.
Consult handle historically allergy-consultation — confirm live handle in Admin.
Expected SLIT template suffix: regional-drops (maps to product.regional-drops.json).
Consult must NOT use regional-drops for the purchase-prerequisites block (D-04).
Metafield keys (read-only): alledrops.quiz_count (integer), alledrops.last_completed_at (date_time).
Analytics filter on both definitions must remain OFF (06-SPIKE-SHOP-01.md).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify D-04 product templateSuffix assignments</name>
  <files>.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-04
    - `.planning/phases/06-purchase-prerequisites/06-RESEARCH.md` Assumption A1 and Open Question 1
    - `.planning/phases/06-purchase-prerequisites/06-SPIKE-SHOP-01.md` (context only — not a substitute for D-04)
  </read_first>
  <action>
    In Shopify Admin, open each of these products and record Theme template / templateSuffix: tennessee-alledrops, texas-alledrops, and the live $99 consult product (confirm handle — historically allergy-consultation). Alternatively run Admin GraphQL product { handle templateSuffix } for the same three handles.

    Required outcome per D-04: both SLIT SKUs use regional-drops (product.regional-drops.json). The consult product must NOT use regional-drops. If either SLIT product is on product.json or another suffix, STOP and report — do not invent placement. If consult is on regional-drops, STOP — gating it would be a clinical-access regression.

    Write the three-row table into 06-02-SUMMARY.md with columns handle, templateSuffix, gate_eligible (yes only for regional-drops SLIT). Never run shopify theme push (D-03).
  </action>
  <verify>
    <automated>MISSING — human Admin read; gate is the SUMMARY table with three handles and templateSuffix values</automated>
  </verify>
  <acceptance_criteria>
    - 06-02-SUMMARY.md contains a table with tennessee-alledrops, texas-alledrops, and the consult handle.
    - Both SLIT rows show templateSuffix regional-drops (or plan halts with STOP recorded).
    - Consult row shows a templateSuffix that is not regional-drops (or plan halts with STOP).
    - SUMMARY states no shopify theme push was used.
  </acceptance_criteria>
  <done>D-04 verified on live Admin assignments and recorded.</done>
  <resume-signal>Type approved with the templateSuffix table pasted, or describe STOP findings</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Inventory SHOP-05 Basic/Grow admin paste surfaces</name>
  <files>.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-11
    - `.planning/phases/06-purchase-prerequisites/06-RESEARCH.md` Pattern 6 SHOP-05 admin surface map and Assumption A2
    - `.planning/REQUIREMENTS.md` SHOP-05 ownership note
  </read_first>
  <action>
    Spend about 15 minutes in Shopify Admin and record which free-text surfaces actually accept the SHOP-05 substance on this shop's Basic/Grow plan. Minimum inventory: Settings → Notifications → Order confirmation (body editable?); Settings → Policies → Refund policy (exists?); Online Store → Themes → Edit default theme content (any checkout-related string that can carry the prerequisites sentence). Explicitly mark checkout information/shipping/payment UI extension targets as Plus-only and out of scope for paste drafts (D-09 / D-11).

    Append a surface map to 06-02-SUMMARY.md with columns: surface_path, editable_on_basic_grow (yes/no), draft_target_for_06-05 (yes/no), notes. Do not draft final copy in this task — inventory only. Do not enable Analytics filtering on metafields.
  </action>
  <verify>
    <automated>MISSING — human Admin inventory; SUMMARY surface map is the artifact</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY lists Order confirmation notification with editable yes/no.
    - SUMMARY lists Refund policy with editable yes/no and notes SPEC-only ownership (William / D-11).
    - SUMMARY states Plus-only checkout-step targets are not draft targets.
    - At least one draft_target_for_06-05 = yes row exists, or SUMMARY documents that SHOP-04 + notifications + policies cover SHOP-05 intent with zero mid-checkout field.
  </acceptance_criteria>
  <done>SHOP-05 paste targets are known before plan 06-05 drafts copy.</done>
  <resume-signal>Type approved after the surface map is in SUMMARY</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Measure SHOP-01 Liquid metafield render on served bytes</name>
  <files>.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-SPIKE-SHOP-01.md` section Still unproven
    - `.planning/phases/06-purchase-prerequisites/06-RESEARCH.md` Pattern 2 SHOP-02 Wave 0 owed measurement
    - `HANDOFF.md` notes on claude-in-chrome vs chrome-devtools for Shopify sessions
    - `app/lib/shopify/metafields.ts` (confirm namespace alledrops keys quiz_count and last_completed_at only)
  </read_first>
  <action>
    Using Andrew's logged-in Chrome session (claude-in-chrome — chrome-devtools MCP has no Shopify session), prove Liquid can render customer.metafields.alledrops.quiz_count for one of the four customers who already have values.

    Method: place a temporary Custom Liquid (or theme-editor Custom liquid block) on a product template that outputs a visible marker plus the integer, e.g. a data attribute or plain text containing the quiz_count value — then fetch authenticated, cache-busted served HTML for that URL while logged in as that customer. Count with split(needle).length-1, never grep -c. Confirm non-empty integer render. Prefer also confirming last_completed_at renders via date filter if practical in the same probe.

    Record in SUMMARY: customer used (id or email redacted to non-PHI opaque id if preferred — do not paste clinical answers), URL fetched, needles counted, measured integer presence (yes/no). Remove the temporary Custom Liquid probe after measurement unless plan 06-06 will replace it with the real block immediately in the same session.

    Confirm Analytics Filter or group data remains OFF on both metafield definitions — do not change access settings. This closes the owed SHOP-01 empirical step assigned to SHOP-02's first implementation measurement.
  </action>
  <verify>
    <automated>MISSING — served-bytes measurement requires logged-in storefront session; record counts in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records a served-bytes measurement with split-based occurrence evidence that quiz_count rendered non-empty for a logged-in customer.
    - Measurement was not taken solely from theme editor preview.
    - SUMMARY confirms Analytics filter still OFF on both metafield definitions.
    - No PHI (answers, score, bracket, DOB) appears in SUMMARY.
  </acceptance_criteria>
  <done>SHOP-01 Liquid readability is empirically proven on served bytes.</done>
  <resume-signal>Type approved with measurement numbers, or report failure (fallback design would be required)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify Admin → placement decisions | Wrong templateSuffix gates consult or misses SLIT |
| Storefront Liquid → customer metafields | Only non-PHI allowlisted fields; Analytics segmentation must stay off |
| Agent docs → PHI | SUMMARY must not record clinical content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Elevation of privilege (policy) | Consult product gated by SLIT prerequisites | mitigate | D-04 STOP if consult uses regional-drops; placement plan 06-06 must respect table |
| T-6-05 | Information disclosure | Metafield Analytics segmentation | mitigate | Reconfirm Filter or group data in Analytics OFF; never enable |
| T-6-06 | Information disclosure | SUMMARY recording PHI | mitigate | Acceptance criteria forbid answers/score/bracket/DOB in SUMMARY |
| T-6-07 | Spoofing | Treating Admin API metafield presence as Liquid proof | mitigate | This plan's Task 3 measures served-bytes Liquid render explicitly |
</threat_model>

<verification>
06-02-SUMMARY.md contains D-04 table, SHOP-05 surface map, and Liquid render measurement with split counts.
</verification>

<success_criteria>
Wave 0 human gates cleared: templates known, SHOP-05 targets known, Liquid metafield render proven.
</success_criteria>

<output>
After completion, create `.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md`
</output>
