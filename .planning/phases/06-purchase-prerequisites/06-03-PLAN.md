---
phase: 06-purchase-prerequisites
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - extensions/purchase-prerequisites/shopify.extension.toml
  - extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid
  - extensions/purchase-prerequisites/assets/purchase-prerequisites.js
  - extensions/purchase-prerequisites/assets/purchase-prerequisites.css
  - extensions/purchase-prerequisites/locales/en.default.json
  - tests/purchase-prerequisites-block-contract.test.ts
autonomous: true
requirements: [SHOP-02, SHOP-03]
tags: [theme-extension, honor-system, d-01, d-02, d-05, d-06, d-07, d-08]

must_haves:
  truths:
    - "A logged-in customer with quiz_count greater than or equal to 1 sees Symptom assessment complete pre-checked, locked, and dated"
    - "Logged-out or unmatched shoppers see two interactive checkboxes plus the D-08 login line"
    - "Add-to-cart .product-form__submit is disabled until both confirmations are satisfied when the selector is found"
    - "If .product-form__submit is missing, confirmations still render and buttons are left alone (fail-open)"
    - "Testing confirmation uses acknowledgment copy only — no third metafield and no Fly fetch"
  artifacts:
    - path: "extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid"
      provides: "SHOP-02/03 Liquid UI + metafield credit"
      contains: "customer.metafields.alledrops.quiz_count"
    - path: "extensions/purchase-prerequisites/assets/purchase-prerequisites.js"
      provides: "D-01 disable + D-02 fail-open gate"
      contains: "product-form__submit"
    - path: "tests/purchase-prerequisites-block-contract.test.ts"
      provides: "Source contract for metafields, selector, fail-open, schema"
  key_links:
    - from: "purchase-prerequisites.js"
      to: ".product-form__submit"
      via: "closest scoped querySelectorAll"
      pattern: "product-form__submit"
    - from: "purchase-prerequisites.liquid"
      to: "alledrops.quiz_count / last_completed_at"
      via: "Liquid .value reads"
      pattern: "metafields\\.alledrops"
---

<objective>
Ship the new theme app extension purchase-prerequisites: one Liquid block with two checkboxes
(returning-patient credit + testing acknowledgment) and asset JS that disables Sense
.product-form__submit until both confirmations are satisfied, failing open if the selector is missing.

Purpose: Delivers SHOP-02 (completion state at purchase) and SHOP-03 (honor-system ATC
prerequisites) per locked D-01, D-02, D-05–D-08 and 06-UI-SPEC.md.
Output: extensions/purchase-prerequisites/* plus a green purchase-prerequisites-block-contract test.
Editor placement and show_dynamic_checkout OFF are plan 06-06 (D-03) — not this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-purchase-prerequisites/06-CONTEXT.md
@.planning/phases/06-purchase-prerequisites/06-RESEARCH.md
@.planning/phases/06-purchase-prerequisites/06-PATTERNS.md
@.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md
@.planning/phases/06-purchase-prerequisites/06-01-SUMMARY.md
@extensions/quiz-block/shopify.extension.toml
@extensions/quiz-block/blocks/symptom-quiz.liquid
@app/lib/shopify/metafields.ts
@tests/liquid-block-contract.test.ts
@tests/sense-atc-selector-contract.test.ts

<interfaces>
UI-SPEC locked copy (exact strings):
  Panel heading: Before you order
  Credited quiz: Symptom assessment complete
  Credited date: Completed {Month D, YYYY} via date: "%B %d, %Y"
  Uncredited quiz label: I completed the AlleDrops symptom assessment
  Testing (D-06): I understand AOD will not ship until allergy testing results are on file.
  Login (D-08): Already completed your assessment? Log in to see it here.
  Login href: /account/login?return_to={{ product.url | url_encode }}  (relative product.url only)
  Helper: Confirm both items above to add to cart.

Schema skeleton (RESEARCH Pattern 1):
  name Purchase prerequisites; target section; stylesheet purchase-prerequisites.css;
  javascript purchase-prerequisites.js; enabled_on.templates product; settings empty array ok.

CSS prefix: purchasePrerequisites__*
Do not modify app/lib/shopify/metafields.ts (D-05 — no third metafield).
Do not add a block under extensions/quiz-block/.
Never shopify theme push (D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold theme extension and Liquid credit/acknowledgment UI</name>
  <files>extensions/purchase-prerequisites/shopify.extension.toml, extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid, extensions/purchase-prerequisites/locales/en.default.json</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md` Copywriting Contract + Component Inventory §1
    - `.planning/phases/06-purchase-prerequisites/06-PATTERNS.md` sections for shopify.extension.toml and purchase-prerequisites.liquid
    - `extensions/quiz-block/shopify.extension.toml`
    - `extensions/quiz-block/blocks/symptom-quiz.liquid` (schema/header conventions only)
    - `app/lib/shopify/metafields.ts` (namespace/keys — read-only)
  </read_first>
  <action>
    Create a new theme extension directory extensions/purchase-prerequisites/ using shopify app generate extension (theme type) so CLI assigns uid, or hand-mirror quiz-block's minimal TOML (name, uid, type = theme) if generate is unavailable — prefer CLI. Do not add this block inside quiz-block.

    Implement blocks/purchase-prerequisites.liquid per 06-UI-SPEC: panel with heading Before you order; assign quiz_count from customer.metafields.alledrops.quiz_count.value default 0; when customer exists and quiz_count greater than or equal to 1, render credited row — checkbox checked disabled, label Symptom assessment complete, caption Completed with last_completed_at.value formatted date "%B %d, %Y" (D-07). Else render interactive quiz checkbox with label I completed the AlleDrops symptom assessment plus D-08 login line with Log in linking to /account/login?return_to= product.url url_encode (relative only — no absolute external return_to).

    Always render testing checkbox unchecked by default with D-06 acknowledgment label exactly: I understand AOD will not ship until allergy testing results are on file. Never read testing_status, never call Fly, never introduce a third metafield (D-05). Missing metafields equal uncredited, never an error banner.

    Schema JSON: name Purchase prerequisites, target section, stylesheet and javascript asset attrs pointing at purchase-prerequisites.css / .js, enabled_on templates product only, settings array may be empty. Add locales/en.default.json minimal stub. Class names use purchasePrerequisites__* prefix. Spacing/color tokens per UI-SPEC (panel fill rgba foreground 0.04, border 0.12, md 16px padding, lg 24px gap intent via CSS in Task 2).

    Banned copy: no if approved, unlocked, cleared to purchase, or I've submitted my allergy testing results.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid'; const s=fs.readFileSync(p,'utf8'); const need=['Before you order','alledrops.quiz_count','alledrops.last_completed_at','I understand AOD will not ship until allergy testing results are on file','Already completed your assessment','purchase-prerequisites.js']; for (const n of need){ if(!s.includes(n)) { console.error('missing '+n); process.exit(1);} } console.log('liquid-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid contains customer.metafields.alledrops.quiz_count and last_completed_at with .value usage.
    - Credited and uncredited branches both exist; testing label matches D-06 string exactly.
    - Schema parses as JSON between schema tags; enabled_on includes product; javascript and stylesheet attrs present.
    - File does not contain fetch(, testing_status, or a third alledrops. metafield key beyond quiz_count and last_completed_at.
    - No shopify theme push in the session log.
  </acceptance_criteria>
  <done>Liquid block implements SHOP-02 credit states and SHOP-03 confirmation markup.</done>
</task>

<task type="auto">
  <name>Task 2: Gate JS fail-open disable plus UI-SPEC stylesheet</name>
  <files>extensions/purchase-prerequisites/assets/purchase-prerequisites.js, extensions/purchase-prerequisites/assets/purchase-prerequisites.css</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md` JS gate behavior table + Spacing/Color/Typography
    - `.planning/phases/06-purchase-prerequisites/06-PATTERNS.md` assets/purchase-prerequisites.js section
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-01 and D-02
    - `tests/fixtures/sense-buy-buttons-excerpt.liquid` (selector string must match)
  </read_first>
  <action>
    Create assets/purchase-prerequisites.js that: finds the block root; scopes with closest to product-info, .product, or form[action*="/cart/add"] — never document.querySelectorAll globally; within that root querySelectorAll .product-form__submit. If zero matches, console.warn once with a stable AlleDrops purchase prerequisites message and return without disabling anything (D-02 fail-open). If matches exist, set disabled on those buttons until both confirmations are satisfied — credited locked quiz checkbox counts as satisfied for the quiz half; testing must be checked by the shopper. When incomplete and buttons were found, show helper text Confirm both items above to add to cart. When complete, enable the matched submit buttons (do not fight theme sold-out disabled state incorrectly — if theme already disabled for sold-out, leave sold-out disabled).

    Create assets/purchase-prerequisites.css with purchasePrerequisites__* rules: panel secondary fill/border per UI-SPEC, typography sizes 14/16/18 and weights 400/600 only, checkbox row min hit area 44px via label padding, muted caption color, accent only on focus-visible and login link. Do not restyle Sense .button geometry — only the disabled attribute from JS.

    Wire change listeners on both checkboxes. No analytics, no network calls, no PHI in logs (log selector-miss only).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const js=fs.readFileSync('extensions/purchase-prerequisites/assets/purchase-prerequisites.js','utf8'); const css=fs.readFileSync('extensions/purchase-prerequisites/assets/purchase-prerequisites.css','utf8'); if(!js.includes('product-form__submit')) process.exit(1); if(!js.includes('console.warn')) process.exit(1); if(!js.includes('closest')) process.exit(1); if(js.includes('document.querySelectorAll('.product-form__submit')')||js.includes('document.querySelectorAll(\".product-form__submit\")')) process.exit(1); if(!css.includes('purchasePrerequisites')) process.exit(1); console.log('js-css-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - JS contains product-form__submit and a fail-open console.warn path when zero buttons found.
    - JS uses closest scoping; does not call document.querySelectorAll('.product-form__submit') at document scope.
    - CSS uses purchasePrerequisites prefix and does not set fixed width/height on Sense .button classes.
    - Helper copy Confirm both items above to add to cart is present in JS or Liquid for the incomplete+selector-found state.
  </acceptance_criteria>
  <done>D-01 disable path and D-02 fail-open path exist in extension assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Green purchase-prerequisites block source contract</name>
  <files>tests/purchase-prerequisites-block-contract.test.ts</files>
  <read_first>
    - `tests/liquid-block-contract.test.ts` (schema parse + toContain patterns)
    - `extensions/purchase-prerequisites/blocks/purchase-prerequisites.liquid`
    - `extensions/purchase-prerequisites/assets/purchase-prerequisites.js`
    - `.planning/phases/06-purchase-prerequisites/06-VALIDATION.md` row for block contract
  </read_first>
  <behavior>
    - Liquid contains metafield .value paths for quiz_count and last_completed_at.
    - Liquid contains D-06 acknowledgment substring and D-08 login offer.
    - Schema JSON parses; enabled_on product; javascript/stylesheet attrs present.
    - JS contains product-form__submit and fail-open console.warn marker.
    - Sources contain neither fetch( to fly.dev nor testing_status nor approval-promise banned phrases.
  </behavior>
  <action>
    Create tests/purchase-prerequisites-block-contract.test.ts that readFileSyncs the Liquid and JS assets and asserts the behaviors above. Parse {% schema %} JSON like liquid-block-contract. Use split-based counts where occurrence matters. Include absence assertions for banned approval-promise fragments: if approved, unlocked, cleared to purchase, and I've submitted my allergy testing results — remember grep-gate hygiene: do not quote those banned strings in comments inside the Liquid/JS production files; the test file may contain them as assertion needles only.

    Prove RED briefly if writing tests before files exist is needed within this plan, then GREEN against Task 1–2 output. Run the verify command to exit 0.
  </action>
  <verify>
    <automated>npx vitest run tests/purchase-prerequisites-block-contract.test.ts tests/sense-atc-selector-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/purchase-prerequisites-block-contract.test.ts exits 0.
    - sense-atc-selector-contract from plan 01 still exits 0 in the same command.
    - Contract covers metafield needles, selector string, fail-open warn, and schema parse.
  </acceptance_criteria>
  <done>SHOP-02/03 source contracts are enforced in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Liquid block → customer metafields | Only quiz_count / last_completed_at; no PHI |
| Block JS → Sense product form | Cross-boundary DOM coupling; fail-open required |
| Login return_to → navigation | Open redirect if absolute external URL allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-08 | Information disclosure | Third metafield or Fly fetch for testing | mitigate | D-05 — Task 1 forbids; contract asserts no testing_status / fetch |
| T-6-09 | Spoofing | return_to open redirect | mitigate | Only product.url relative encode per UI-SPEC; contract can assert /account/login?return_to= |
| T-6-10 | Tampering | Silent fail-open without detection | mitigate | Plan 01 CI + this plan's JS console.warn + contract |
| T-6-11 | Elevation of privilege (policy) | Approval-promise copy | mitigate | UI-SPEC banned list; contract absence assertions |
| T-6-12 | Denial of service | Fail-closed missing selector | mitigate | D-02 fail-open implemented in Task 2 |
</threat_model>

<verification>
Block contract + sense selector contract green; Liquid/JS contain locked needles; no theme push.
</verification>

<success_criteria>
Theme extension implements honor-system ATC gating and returning-patient credit in source, ready for editor placement in 06-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-purchase-prerequisites/06-03-SUMMARY.md`
</output>
