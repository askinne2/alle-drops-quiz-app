---
phase: 06-purchase-prerequisites
plan: 06
type: execute
wave: 3
depends_on: ["02", "03", "04"]
files_modified:
  - .planning/phases/06-purchase-prerequisites/06-06-SUMMARY.md
autonomous: false
requirements: [SHOP-01, SHOP-02, SHOP-03, SHOP-04]
tags: [d-01, d-03, d-04, editor-placement, uat, shopify-app-deploy]

user_setup:
  - service: shopify
    why: "Theme editor placement + checkout editor + authorized shopify app deploy"
    dashboard_config:
      - task: "Authorize shopify app deploy for purchase-prerequisites + order-review-notice"
        location: "Shopify Partner / app deploy confirmation"
      - task: "Turn Show dynamic checkout buttons OFF on regional-drops Buy buttons"
        location: "Sense theme editor → product template Regional drops"
      - task: "Place Purchase prerequisites app block above Buy buttons on regional-drops only"
        location: "Sense theme editor → main-product"
      - task: "Place Order review notice on thank-you and order-status"
        location: "Checkout editor"

must_haves:
  truths:
    - "Purchase prerequisites block is live on TN and TX SLIT PDPs and absent from the consult PDP"
    - "Show dynamic checkout is OFF on regional-drops so Shop Pay cannot bypass the gate"
    - "Logged-in credited and logged-out shoppers both behave per UI-SPEC on served storefront"
    - "Thank-you and order-status show the static review notice"
    - "No shopify theme push occurred during this phase"
  artifacts:
    - path: ".planning/phases/06-purchase-prerequisites/06-06-SUMMARY.md"
      provides: "Deploy version, editor checklist, UAT evidence, served-bytes counts"
      contains: "show_dynamic_checkout"
  key_links:
    - from: "theme editor"
      to: "extensions/purchase-prerequisites"
      via: "app block placement above buy_buttons"
      pattern: "Purchase prerequisites|Before you order"
    - from: "checkout editor"
      to: "extensions/order-review-notice"
      via: "thank-you + order-status app blocks"
      pattern: "What happens next"
---

<objective>
Deploy the two new extensions (Shopify channel only), place them via editors without theme push,
flip show_dynamic_checkout OFF on the SLIT template, and complete the blocking human UAT that proves
SHOP-01…04 on served bytes.

Purpose: Source-green extensions do not change patient experience until deployed and editor-placed.
D-03 forbids theme push because drifted settings_data.json would re-enable Klaviyo on the PHI quiz
page.
Output: 06-06-SUMMARY.md with deploy provenance, editor checklist, UAT results, and explicit
no-theme-push attestation. No Fly deploy expected.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md
@.planning/phases/06-purchase-prerequisites/06-03-SUMMARY.md
@.planning/phases/06-purchase-prerequisites/06-04-SUMMARY.md
@.planning/phases/06-purchase-prerequisites/06-CONTEXT.md
@.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md
@.planning/phases/06-purchase-prerequisites/06-VALIDATION.md
@HANDOFF.md
@CLAUDE.md

<interfaces>
D-04 table from 06-02-SUMMARY.md is authoritative for which products receive the block.
D-01 editor action: Buy buttons → Show dynamic checkout buttons = OFF on product.regional-drops.
D-03: NEVER shopify theme push from allergist-on-demand (Klaviyo disabled:false locally).
Deploy: shopify app deploy only, with Andrew authorization — not fly deploy.
UAT surfaces: /products/tennessee-alledrops, /products/texas-alledrops, consult PDP (must NOT show gate), thank-you + order-status after test order.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authorized extension deploy and editor-only placement</name>
  <files>.planning/phases/06-purchase-prerequisites/06-06-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md` D-04 templateSuffix table
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-01, D-03, D-04
    - `CLAUDE.md` deploy notes (shopify app deploy ≠ fly deploy)
    - `HANDOFF.md` Klaviyo / theme-push warning
  </read_first>
  <action>
    After Andrew explicitly authorizes deploy: from alle-drops-quiz-app on the feature branch (or main per Andrew), run shopify app deploy to ship purchase-prerequisites and order-review-notice. Record the resulting app version name/id in SUMMARY. Do not run fly deploy unless Andrew separately requires it for an unrelated reason — Phase 6 expects none.

    Theme editor (Sense, Regional drops / product.regional-drops only): set Buy buttons Show dynamic checkout buttons to OFF (D-01). Add Purchase prerequisites app block inside main-product above buy_buttons. Do not place the block on product.telehealth-appointment or the consult product (D-04). Do not place via shopify theme push (D-03).

    Checkout editor: enable Order review notice on thank-you and order-status surfaces (D-09).

    Record a checklist in SUMMARY with checkmarks for: deploy version, dynamic checkout off, block on TN, block on TX, block absent on consult, thank-you placement, order-status placement, explicit sentence No shopify theme push was run.
  </action>
  <verify>
    <automated>MISSING — editor + Partner deploy are human-authorized; SUMMARY checklist is the gate</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY names the Shopify app version produced by shopify app deploy.
    - SUMMARY records show_dynamic_checkout OFF on regional-drops via editor.
    - SUMMARY records block present on both SLIT products and absent on consult.
    - SUMMARY contains the exact attestation that shopify theme push was not run.
    - No fly deploy claimed as required for this phase.
  </acceptance_criteria>
  <done>Extensions deployed; editor placement complete without theme push.</done>
  <resume-signal>Type approved after SUMMARY checklist is filled</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking storefront and checkout UAT on served bytes</name>
  <files>.planning/phases/06-purchase-prerequisites/06-06-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md` Interaction Contract + state table
    - `.planning/phases/06-purchase-prerequisites/06-VALIDATION.md` Manual-Only Verifications
    - `.planning/phases/06-purchase-prerequisites/06-02-SUMMARY.md` Liquid measurement baseline
  </read_first>
  <what-built>
    Live purchase-prerequisites gate on SLIT PDPs, express checkout disabled, order-review-notice on thank-you/order-status, metafield credit path.
  </what-built>
  <how-to-verify>
    1. Authenticated cache-busted fetch of TN and TX SLIT PDPs: confirm Before you order present; confirm Shop Pay / dynamic checkout / payment_button output absent (split counts, not grep -c).
    2. Logged-out: both boxes unchecked, D-08 login line visible, ATC disabled until both checked; checking both enables ATC.
    3. Logged-in as a customer with quiz_count greater than or equal to 1: quiz row shows Symptom assessment complete checked+disabled with Completed date; only testing acknowledgment required to enable ATC.
    4. Consult PDP: Before you order block must be absent.
    5. Fail-open smoke (optional if safe): temporarily misplace is NOT required — instead confirm console.warn path exists in source from 06-03; do not break production placement.
    6. Place a test order (or checkout editor preview): thank-you and order-status show What happens next with 2–3 business days substance; no score/bracket displayed.
    7. Reconfirm Analytics filter still OFF on metafield definitions.
    8. Record all split-based counts and pass/fail in SUMMARY.
  </how-to-verify>
  <action>
    Execute the how-to-verify steps using Andrew's logged-in Chrome. Write results into 06-06-SUMMARY.md. Any fail blocks phase closure — do not mark SHOP requirements complete in REQUIREMENTS.md from this plan without Andrew approval; record raw evidence first.
  </action>
  <verify>
    <automated>MISSING — human browser + served-bytes UAT; SUMMARY evidence table is required</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY has pass evidence for credited logged-in PDP and logged-out PDP on at least one SLIT product (preferably both TN and TX).
    - SUMMARY records absence of express/dynamic checkout on SLIT PDPs via served-bytes split counts.
    - SUMMARY records consult PDP does not show the prerequisites panel.
    - SUMMARY records thank-you or order-status notice visible with What happens next.
    - SUMMARY reconfirms no theme push and Analytics filter OFF.
  </acceptance_criteria>
  <done>SHOP-01…04 behaviors verified on live surfaces.</done>
  <resume-signal>Type approved or list defects</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local theme repo → live Sense theme | theme push can re-enable Klaviyo on PHI quiz page |
| Editor placement → consult product | Mis-placement blocks telehealth access |
| Deployed extension → storefront | Honor-system UI only — fulfillment remains SHOP-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-19 | Information disclosure | shopify theme push re-enables Klaviyo | mitigate | D-03 — Task 1 forbids theme push; SUMMARY attestation required |
| T-6-20 | Denial of service / access | Gate placed on consult PDP | mitigate | D-04 table + UAT step requiring consult absence |
| T-6-21 | Tampering | Express checkout left on | mitigate | Editor OFF + served-bytes absence check in Task 2 |
| T-6-22 | Information disclosure | Analytics segmentation on metafields | mitigate | Reconfirm Analytics filter OFF during UAT |
</threat_model>

<verification>
SUMMARY completes deploy + editor + UAT evidence; phase may proceed to verify-work only after approved resume-signal.
</verification>

<success_criteria>
Live SLIT purchase path shows prerequisites and returning-patient credit; consult ungated; thank-you/order-status notice live; no theme push.
</success_criteria>

<output>
After completion, create `.planning/phases/06-purchase-prerequisites/06-06-SUMMARY.md`
</output>
