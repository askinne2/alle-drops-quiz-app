---
phase: 06-purchase-prerequisites
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/order-review-notice/shopify.extension.toml
  - extensions/order-review-notice/package.json
  - extensions/order-review-notice/src/ThankYou.jsx
  - extensions/order-review-notice/src/OrderStatus.jsx
  - extensions/order-review-notice/src/ReviewNotice.jsx
  - tests/order-review-notice-extension-contract.test.ts
autonomous: true
requirements: [SHOP-04]
tags: [checkout-ui-extension, d-09, d-10, thank-you, order-status]

must_haves:
  truths:
    - "One checkout UI extension targets both purchase.thank-you.block.render and customer-account.order-status.block.render"
    - "Notice explains clinical review typically 2–3 business days plus how to get testing results on file"
    - "Extension performs no network fetch of quiz score, bracket, answers, or Fly APIs"
  artifacts:
    - path: "extensions/order-review-notice/shopify.extension.toml"
      provides: "Dual targeting per D-09"
      contains: "purchase.thank-you.block.render"
    - path: "extensions/order-review-notice/src/ReviewNotice.jsx"
      provides: "Shared static copy component"
      contains: "What happens next"
    - path: "tests/order-review-notice-extension-contract.test.ts"
      provides: "CI guard — both targets, no PHI fetch"
  key_links:
    - from: "shopify.extension.toml"
      to: "ThankYou.jsx / OrderStatus.jsx"
      via: "separate module per targeting entry"
      pattern: "\\[\\[extensions\\.targeting\\]\\]"
    - from: "ReviewNotice.jsx"
      to: "UI-SPEC copy"
      via: "static strings only"
      pattern: "2–3 business days|2-3 business days|allergy testing results are not yet on file|order confirmation email"
---

<objective>
Add this app's first thank-you / order-status checkout UI extension with static clinical-review
notice copy (SHOP-04), registering both targets with separate modules sharing one ReviewNotice
component.

Purpose: Patients who close the thank-you tab still see the review expectation on order status;
D-10 closes the loop opened by the PDP testing acknowledgment.
Output: extensions/order-review-notice/* and a green order-review-notice-extension-contract test.
Checkout editor placement is plan 06-06. No Fly deploy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-purchase-prerequisites/06-CONTEXT.md
@.planning/phases/06-purchase-prerequisites/06-RESEARCH.md
@.planning/phases/06-purchase-prerequisites/06-PATTERNS.md
@.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md
@extensions/quiz-history/shopify.extension.toml
@extensions/quiz-history/src/QuizHistoryBlock.jsx
@extensions/quiz-history/package.json

<interfaces>
D-09 targets (locked intent; official docs require separate modules):
  purchase.thank-you.block.render → ./src/ThankYou.jsx
  customer-account.order-status.block.render → ./src/OrderStatus.jsx
api_version: 2026-01 (match quiz-history)
Capabilities: do NOT enable network_access or api_access (static only; quiz-history needs them — this extension must not copy that).

UI-SPEC copy:
  Title: What happens next
  Review body: Dr. Sullivan reviews your intake before your order ships. This typically takes 2–3 business days.
  Testing follow-up: If allergy testing results are not yet on file, complete your symptom assessment upload (or finish the assessment) before we can ship. Need help? Contact the clinic using the support details on your order confirmation email.
  Banner status: info (not critical/warning)

Banned: if approved, unlocked, cleared to purchase, invented testing@ email domains.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold order-review-notice with dual targeting TOML</name>
  <files>extensions/order-review-notice/shopify.extension.toml, extensions/order-review-notice/package.json</files>
  <read_first>
    - `extensions/quiz-history/shopify.extension.toml`
    - `extensions/quiz-history/package.json`
    - `.planning/phases/06-purchase-prerequisites/06-RESEARCH.md` Pattern 4 (multi-page + separate modules correction)
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-09
  </read_first>
  <action>
    From the quiz-app repo root, run shopify app generate extension and choose Checkout UI / thank-you style targets, naming the extension order-review-notice (or rename after generate). Align api_version to 2026-01. Configure shopify.extension.toml with type ui_extension, handle order-review-notice, and exactly two [[extensions.targeting]] entries: module ./src/ThankYou.jsx target purchase.thank-you.block.render; module ./src/OrderStatus.jsx target customer-account.order-status.block.render.

    Mirror quiz-history package.json dependency style for @shopify/ui-extensions and preact as needed by the generated starter, but do not copy quiz-history's network_access or api_access capability flags — omit [extensions.capabilities] or leave capabilities empty/false so no network. Do not add Fly URL env vars.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('extensions/order-review-notice/shopify.extension.toml','utf8'); if(!t.includes('purchase.thank-you.block.render')) process.exit(1); if(!t.includes('customer-account.order-status.block.render')) process.exit(1); if(!t.includes('ThankYou.jsx')) process.exit(1); if(!t.includes('OrderStatus.jsx')) process.exit(1); if(/network_access\s*=\s*true/.test(t)) process.exit(1); console.log('toml-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - TOML lists both D-09 targets with distinct module paths ThankYou.jsx and OrderStatus.jsx.
    - api_version is 2026-01.
    - network_access is not true.
    - Extension lives under extensions/order-review-notice/, not inside quiz-history.
  </acceptance_criteria>
  <done>Dual-target checkout UI extension scaffold exists per D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Shared ReviewNotice plus thin thank-you and order-status entries</name>
  <files>extensions/order-review-notice/src/ReviewNotice.jsx, extensions/order-review-notice/src/ThankYou.jsx, extensions/order-review-notice/src/OrderStatus.jsx</files>
  <read_first>
    - `.planning/phases/06-purchase-prerequisites/06-UI-SPEC.md` thank-you + order-status Copywriting Contract and Component Inventory §2
    - `.planning/phases/06-purchase-prerequisites/06-CONTEXT.md` D-10
    - `extensions/quiz-history/src/QuizHistoryBlock.jsx` (Preact/bootstrap style only — do not copy fetch logic)
    - Generated starter files from Task 1 if present
  </read_first>
  <action>
    Implement ReviewNotice.jsx as a static shared component using Checkout UI primitives (Banner status info and/or BlockStack + Text). Title What happens next. Body paragraphs exactly per UI-SPEC review sentence (2–3 business days) and testing follow-up (generic support via order confirmation email — do not invent testing@alledrops.com or any non-AOD inbox). Do not read order id for clinical lookup. Do not call fetch. Do not reference score, bracket, answers_json, or quiz_count.

    ThankYou.jsx and OrderStatus.jsx are thin entry modules that render ReviewNotice using the CLI-generated bootstrap pattern for their targets (align to generated starter, not a hand-rolled wrong API). Both import the same ReviewNotice. No PHI, no network.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const rn=fs.readFileSync('extensions/order-review-notice/src/ReviewNotice.jsx','utf8'); const files=['extensions/order-review-notice/src/ReviewNotice.jsx','extensions/order-review-notice/src/ThankYou.jsx','extensions/order-review-notice/src/OrderStatus.jsx']; let all=''; for (const f of files) all+=fs.readFileSync(f,'utf8'); if(!rn.includes('What happens next')) process.exit(1); if(!/2–3 business days|2-3 business days/.test(rn)) process.exit(1); if(!rn.includes('allergy testing results are not yet on file')) process.exit(1); if(!rn.includes('order confirmation email')) process.exit(1); if(/fetch\s*\(/.test(all)) process.exit(1); if(/score_bracket|answers_json|quiz_count/.test(all)) process.exit(1); console.log('src-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - ReviewNotice contains UI-SPEC title and both body substances (review timing + testing follow-up).
    - No fetch( in any of the three src files.
    - No score_bracket, answers_json, or quiz_count identifiers in src.
    - ThankYou.jsx and OrderStatus.jsx both import or render ReviewNotice.
  </acceptance_criteria>
  <done>SHOP-04 static notice implemented for both surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Green order-review-notice extension contract</name>
  <files>tests/order-review-notice-extension-contract.test.ts</files>
  <read_first>
    - `extensions/order-review-notice/shopify.extension.toml`
    - `extensions/order-review-notice/src/ReviewNotice.jsx`
    - `tests/liquid-block-contract.test.ts` (style analog)
    - `.planning/phases/06-purchase-prerequisites/06-VALIDATION.md` SHOP-04 contract row
  </read_first>
  <behavior>
    - TOML includes both targeting strings and both module filenames.
    - network_access is not enabled as true.
    - Src has no fetch( and no score/bracket/answers_json/quiz_count.
    - ReviewNotice includes What happens next, 2–3 business days substance, and D-10 testing follow-up needles from 06-UI-SPEC.md: "allergy testing results are not yet on file" and "order confirmation email".
  </behavior>
  <action>
    Write tests/order-review-notice-extension-contract.test.ts reading TOML + src files with the assertions above. Assert ReviewNotice.jsx contains UI-SPEC D-10 testing follow-up substrings exactly: allergy testing results are not yet on file and order confirmation email (in addition to title + 2–3 business days). Run until green. Do not require a live checkout order in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/order-review-notice-extension-contract.test.ts && node -e "const fs=require('fs'); const t=fs.readFileSync('tests/order-review-notice-extension-contract.test.ts','utf8'); if(!t.includes('allergy testing results are not yet on file')) process.exit(1); if(!t.includes('order confirmation email')) process.exit(1); console.log('contract-needles-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/order-review-notice-extension-contract.test.ts exits 0.
    - Contract test source asserts D-10 testing follow-up needles (allergy testing results are not yet on file; order confirmation email).
    - Test fails if either D-09 target is removed (implied by positive contains assertions covering both).
  </acceptance_criteria>
  <done>SHOP-04 source contract enforced in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkout extension → Shopify order context | Order id may exist — must not become a PHI lookup key |
| Extension → network | Any fetch toward Fly is a PHI-path breach on a commerce surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-13 | Information disclosure | PHI fetch from thank-you/order-status | mitigate | D-10 static only; no network_access; contract bans fetch and clinical field names |
| T-6-14 | Elevation of privilege (policy) | Approval-promise copy on thank-you | mitigate | UI-SPEC banned language; ReviewNotice uses locked strings only |
| T-6-15 | Spoofing | Fake clinical status from order id | mitigate | Do not use order id for lookups; Task 2 forbids |
</threat_model>

<verification>
Extension contract green; TOML has both targets; src is static with UI-SPEC copy.
</verification>

<success_criteria>
SHOP-04 extension is buildable on Basic/Grow and safe for checkout editor placement in 06-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-purchase-prerequisites/06-04-SUMMARY.md`
</output>
