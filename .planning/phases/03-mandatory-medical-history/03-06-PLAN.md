---
phase: 03-mandatory-medical-history
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - migrations/003_drop_medical_history_legacy_columns.sql
autonomous: false
requirements: [HIST-05]

user_setup:
  - service: google-cloud-sql
    why: "A named on-demand backup is the restore point for D-01's destructive column drop. Verified during research: this machine's gcloud session is expired and cannot reauthenticate non-interactively."
    dashboard_config:
      - task: "Run `gcloud auth login` interactively, then set the active project to alledrops-quiz"
        location: "Andrew's own terminal — gcloud CLI"
      - task: "Create the on-demand backup and record its ID"
        location: "gcloud sql backups create --instance=alledrops-quiz-data, or Cloud Console → SQL → alledrops-quiz-data → Backups → Create backup"

must_haves:
  truths:
    - "A named on-demand backup of alledrops-quiz-data exists and its ID is recorded before any DDL runs"
    - "The migration file exists, is reviewable on its own, and states its ordering prerequisite inline"
    - "The migration targets alledrops_quiz_dev only"
  artifacts:
    - path: "migrations/003_drop_medical_history_legacy_columns.sql"
      provides: "The DROP COLUMN DDL plus its ordering and backup prerequisites in the header"
      contains: "DROP COLUMN IF EXISTS personal_history_json"
  key_links:
    - from: "migrations/003_drop_medical_history_legacy_columns.sql"
      to: "the recorded backup ID"
      via: "header comment filled in before execution"
      pattern: "Backup ID:"
---

<objective>
Take the pre-migration Cloud SQL backup and author the `DROP COLUMN` migration file as its own
reviewable commit. **Nothing is executed against the database in this plan.** Plan 03-07 runs the DDL,
and only after the application code is confirmed live.

Purpose: D-01 makes three non-negotiable demands — a named on-demand backup taken BEFORE the drop
with its ID recorded, the migration in its own file and its own commit reviewed on its own, and
execution against `alledrops_quiz_dev` only. The table is real: 42 rows / 136 kB measured live during
research, 18 of them carrying data in the two doomed columns. Andrew's stated basis for D-01 is that
these are test rows and no real patient has completed the quiz. The migration is treated as
destructive regardless.

Output: a recorded backup ID and `migrations/003_drop_medical_history_legacy_columns.sql`, committed
alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-mandatory-medical-history/03-CONTEXT.md
@.planning/phases/03-mandatory-medical-history/03-RESEARCH.md
@.planning/phases/03-mandatory-medical-history/03-02-SUMMARY.md

<environment>
| Item | Value |
|---|---|
| GCP project (dev) | `alledrops-quiz` |
| Cloud SQL instance | `alledrops-quiz-data` (Postgres 18, us-east1) |
| Database | `alledrops_quiz_dev` |
| Fly app | `alle-drops-quiz-app` (region iad) |
| Measured table state, 2026-08-09 | 42 rows, 136 kB, 18 rows with a non-null value in either doomed column |

Two facts verified live during research, both load-bearing:
1. `gcloud sql instances describe alledrops-quiz-data --project=alledrops-quiz` FAILED with
   "Reauthentication failed. cannot prompt during non-interactive execution." The active gcloud
   config is `andrew@21adsmedia.com` on project `smart-rope-305817` — not even `alledrops-quiz`. The
   backup step is therefore NOT autonomous under today's credentials.
2. This machine's IP is not on Cloud SQL's authorized-networks list, but the Fly app's is.
   `fly ssh console -a alle-drops-quiz-app` running a `pg` script against `DATABASE_URL` works —
   confirmed live this session, returning `{"total":"42","size":"136 kB","with_history":"18"}`.
   Note that Prisma on the Fly machine is the SQLite session store; a Prisma raw query against
   `submissions` fails with a SQLite parser error. Use the `pg` pool.
</environment>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Interactive gcloud auth and the named pre-migration backup (T-3-02)</name>

  <what-built>
Nothing automated — this step genuinely cannot run unattended. `gcloud auth login` opens a browser
consent flow. Research confirmed live that every `gcloud sql` command against `alledrops-quiz` fails
today with a non-interactive reauthentication error.

Once Andrew has authenticated in his own terminal, a Claude Code session started AFTER that point
can run `gcloud sql backups create` itself. Either route is acceptable; what matters is that the
backup exists and its ID is recorded before any DDL runs.
  </what-built>

  <how-to-verify>
In your own terminal:

    gcloud auth login
    gcloud config set project alledrops-quiz
    gcloud sql instances describe alledrops-quiz-data --project=alledrops-quiz --format="value(name,state)"

The third command must succeed. If it still reports a reauthentication error, stop — the backup
cannot be taken and the phase must not proceed to the DDL.

Then take the backup:

    gcloud sql backups create \
      --instance=alledrops-quiz-data \
      --project=alledrops-quiz \
      --description="pre-phase3-drop-medical-history-legacy-columns"

Read the ID back and confirm it is complete, rather than trusting the create command's output:

    gcloud sql backups list --instance=alledrops-quiz-data --project=alledrops-quiz --limit=3

Record the backup ID and its status. A backup whose status is not `SUCCESSFUL` is not a restore
point. Do not accept "the command exited 0" as evidence — read the listed status.

Cloud Console alternative if the CLI stays blocked: SQL → `alledrops-quiz-data` → Backups → Create
backup, with the same description. Copy the resulting backup ID from the list view.
  </how-to-verify>

  <read_first>
    - `.planning/phases/03-mandatory-medical-history/03-CONTEXT.md` D-01 — the three non-negotiable
      requirements on this migration
    - `.planning/phases/03-mandatory-medical-history/03-RESEARCH.md` § "Pitfall 2" — why this step
      is not autonomous
    - `CLAUDE.md` § "Current dev infra"
  </read_first>

  <acceptance_criteria>
    - A backup ID is recorded, in writing, in this plan's SUMMARY.
    - The backup's status is read back from `gcloud sql backups list` (or the Console list view) and
      recorded as `SUCCESSFUL`. An exit code is not evidence.
    - The backup's description identifies it as the pre-Phase-3 restore point.
    - No PHI value is read, printed, or recorded anywhere during this step.
    - If the backup cannot be taken, STOP the phase here and record why. Plan 03-07 must not run.
  </acceptance_criteria>

  <action>Present the commands above to Andrew and WAIT. Do not attempt `gcloud sql backups create`
from this session first — research verified live that it fails with a non-interactive reauthentication
error under today's credentials. Once Andrew confirms `gcloud auth login` succeeded, either he runs
the backup or a session started after that point may run it. Either way, read the backup ID and its
status back from `gcloud sql backups list` and record both. If no `SUCCESSFUL` backup exists, STOP the
phase — plan 03-07 must not run.</action>

  <done>A backup ID is recorded with a read-back status of `SUCCESSFUL`, or the phase is halted with the blocker documented.</done>

  <resume-signal>Paste the backup ID and its confirmed status, or report the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the migration file and commit it alone</name>

  <read_first>
    - `migrations/001_create_submissions.sql` — the header-comment convention and the original
      column declarations (`personal_history_json JSONB` at :26, `family_history_json JSONB` at :27).
    - `migrations/002_create_submission_access_log.sql` — the shorter header convention. Neither file
      wraps its DDL in an explicit `BEGIN`/`COMMIT`; both rely on a single top-level statement being
      implicitly atomic.
    - `.planning/phases/03-mandatory-medical-history/03-PATTERNS.md` § "migrations/003_..." — the
      suggested shape, following both existing files exactly.
    - `.planning/phases/03-mandatory-medical-history/03-02-SUMMARY.md` — confirmation that
      `insertSubmission` no longer names the two columns.
  </read_first>

  <files>migrations/003_drop_medical_history_legacy_columns.sql</files>

  <action>
Create `migrations/003_drop_medical_history_legacy_columns.sql`. Follow the header convention of the
two existing migration files — a comment block stating purpose, run target, and prerequisites, then
the DDL. No `BEGIN`/`COMMIT` wrapper: a single `ALTER TABLE` is implicitly atomic, matching both
existing files. No `GRANT` statements: `DROP COLUMN` does not change table privileges, and `001`'s
GRANT block is specific to table creation.

The header must state, explicitly:
- What it drops and why — Phase 3 replaces `PART6_MEDICAL_HISTORY` wholesale (D-01), and every new
  medical-history answer now rides `answers_json` keyed by question ID (D-02).
- Run target: `alledrops_quiz_dev` ONLY. Production cutover to AOD's GCP project has not happened;
  there is no second database to migrate yet.
- The recorded backup ID from Task 1, written into the header verbatim on a `Backup ID:` line.
- The ordering prerequisite, stated as a hard requirement rather than a suggestion: the application
  code that stops writing these columns must be DEPLOYED AND CONFIRMED LIVE on Fly before this file
  runs. `insertSubmission` names its columns explicitly, so running this first makes every
  `POST /api/quiz/submit` fail with `column "personal_history_json" of relation "submissions" does
  not exist` for as long as the old release is serving.
- The measured pre-migration state: 42 rows / 136 kB, 18 with data in these columns as of 2026-08-09.
  Re-measure immediately before execution rather than trusting this number.

The DDL itself: a single `ALTER TABLE submissions` with `DROP COLUMN IF EXISTS personal_history_json`
and `DROP COLUMN IF EXISTS family_history_json`. Use `IF EXISTS` on both so a re-run is a no-op
rather than an error.

Commit this file ALONE. D-01 makes that non-negotiable: a column drop must be reviewable on its own,
never bundled with application changes. Use a commit message that names it as a destructive migration
and carries the backup ID.
  </action>

  <verify>
    <automated>test -f migrations/003_drop_medical_history_legacy_columns.sql &amp;&amp; git show --stat --oneline HEAD | tail -n +2</automated>
  </verify>

  <acceptance_criteria>
    - `migrations/003_drop_medical_history_legacy_columns.sql` exists.
    - It contains `DROP COLUMN IF EXISTS personal_history_json` and
      `DROP COLUMN IF EXISTS family_history_json`.
    - It contains a `Backup ID:` line filled in with the real ID from Task 1 — not a placeholder.
      Assert the line does not contain `<fill` or `TODO`.
    - It contains the literal string `alledrops_quiz_dev` and does NOT contain any production
      database name.
    - It contains zero `SELECT` statements naming a PHI column. `COUNT(*)` and column-name checks
      belong in plan 03-07's runbook, not in the DDL file.
    - `git show --stat HEAD` lists exactly ONE changed file: this migration. If any other file
      appears, the commit is wrong — split it.
    - `npm test` still green (this file is not compiled or imported by anything).
  </acceptance_criteria>

  <done>The migration file exists with a real backup ID in its header, states the deploy-first ordering as a hard requirement, and is committed by itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal → Cloud SQL admin API | `gcloud sql backups create` is an authenticated admin-plane action against a PHI-holding instance. |
| migration file → `alledrops_quiz_dev` | Irreversible schema change on a table that holds PHI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure / integrity (irreversible PHI loss) | `ALTER TABLE ... DROP COLUMN` on `submissions` | mitigate | Named on-demand backup taken BEFORE the DDL, its ID read back from `gcloud sql backups list` and confirmed `SUCCESSFUL`, and written into the migration file's own header. Automated backups exist since session 30, but a named pre-migration backup is the restore point that matters. The phase STOPS if the backup cannot be taken. |
| T-3-17 | Repudiation | bundling the DDL with application changes | mitigate | The migration is committed alone. Acceptance criteria assert `git show --stat HEAD` lists exactly one file. D-01 makes this non-negotiable so the drop is reviewable on its own. |
| T-3-18 | Information Disclosure | PHI leaking into migration or verification output | mitigate | The DDL file contains no `SELECT`. Verification in plan 03-07 is `COUNT(*)` and `information_schema` column names only, never a PHI column value — `CLAUDE.md` rules 5 and 6 permit IDs and counts and nothing else. |
| T-3-19 | Denial of Service | running the DDL before the app code is live | mitigate | Nothing is executed in this plan. The ordering is a hard wave boundary: 03-07 depends on 03-06, and 03-07's own acceptance criteria require confirming the Fly release is live on served bytes before the DDL runs. |
| T-3-20 | Spoofing / wrong target | running against the wrong database | mitigate | The header names `alledrops_quiz_dev` explicitly and states that no production database exists yet. Acceptance criteria assert no production database name appears in the file. |
</threat_model>

<verification>
- A backup ID is recorded with a read-back status of `SUCCESSFUL`, not an exit code.
- `migrations/003_drop_medical_history_legacy_columns.sql` exists, carries the real backup ID, names
  `alledrops_quiz_dev`, and contains both `DROP COLUMN IF EXISTS` clauses.
- `git show --stat HEAD` lists exactly one file.
- No PHI value was read, printed, or recorded at any point.
- No DDL has been executed. The database is unchanged after this plan.
</verification>

<success_criteria>
1. A named pre-migration backup exists, its ID is recorded, and its status was read back as
   `SUCCESSFUL`.
2. The migration file exists, is committed alone, and its header carries the backup ID and the
   deploy-first ordering requirement as a hard prerequisite.
3. The file targets `alledrops_quiz_dev` only and contains no PHI-selecting statement.
4. The database is untouched — execution belongs to plan 03-07.
</success_criteria>

<output>
Create `.planning/phases/03-mandatory-medical-history/03-06-SUMMARY.md` when done. Record: the backup
ID, its read-back status, the timestamp it was taken, the migration file path, and the single-file
commit SHA. State explicitly that no DDL has executed and that plan 03-07 must confirm the Fly
release is live before it does.
</output>
</content>
