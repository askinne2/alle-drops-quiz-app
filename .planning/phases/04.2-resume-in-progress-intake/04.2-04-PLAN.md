---
phase: 04.2-resume-in-progress-intake
plan: 04
type: execute
wave: 2
depends_on: [04.2-01, 04.2-02, 04.2-03]
files_modified:
  - app/components/quiz/QuizContainer.tsx
  - tests/quiz-resume-restore-dom.test.ts
autonomous: true
requirements: [RESUME-01, RESUME-04]

must_haves:
  truths:
    - "A patient returning with a valid draft is offered the choice before anything else renders (ROADMAP criterion 1)"
    - "Choosing Resume restores state, answers, and flow position, and shows a one-time identity-free notice"
    - "Choosing Start over from the offer clears the draft and lands on the state gate — the same screen a patient with no draft sees"
    - "When storage is unwritable OR no valid draft exists, the app renders exactly as it does today — same code branch, no message (D-01)"
    - "Restored state can never satisfy the Phase 4.1 mandatory-upload predicate (D-09)"
    - "Consent is never restored as already-checked, and the score is never restored from storage"
  artifacts:
    - path: "app/components/quiz/QuizContainer.tsx"
      provides: "resume_offer FlowStep, lazy draft read on mount, restore handler, offer render, restoration notice, payload wiring"
      contains: "resume_offer"
    - path: "tests/quiz-resume-restore-dom.test.ts"
      provides: "DOM proof of the offer, the restore, the D-01 absent state, and the D-09 landing rule"
      min_lines: 160
  key_links:
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/lib/quiz/draft-store.ts"
      via: "readDraft() in a lazy useState initializer, clearDraft() from the offer's start-over action"
      pattern: "readDraft|clearDraft"
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/lib/quiz/payload.ts"
      via: "buildPayload reduced to a thin wrapper over buildSubmitPayload"
      pattern: "buildSubmitPayload"
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/components/quiz/ResumeOffer.tsx"
      via: "render at step === resume_offer, and RestorationNotice after restore"
      pattern: "ResumeOffer|RestorationNotice"
---

<objective>
Wire the read side of resume into `QuizContainer`: read a draft on mount, offer the choice, restore
on Resume, and route the payload builder through the extracted pure function.

Purpose: this is ROADMAP criterion 1. It is also where D-09's "the 04.1 upload wall must survive
resume" is enforced, and where the two easiest silent failures live — restoring a pre-checked
consent, or restoring a stored score.

Output: a modified `QuizContainer.tsx` plus its DOM proof. The write side (D-07 gate, clear-on-submit,
in-flow control) is plan 04.2-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-resume-in-progress-intake/04.2-CONTEXT.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-UI-SPEC.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-PATTERNS.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-01-SUMMARY.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-02-SUMMARY.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Everything this plan consumes already exists after waves 1. Do not re-derive these. -->

From app/lib/quiz/draft-store.ts (plan 04.2-01):
  readDraft(fingerprint?: string): QuizDraft | null      // null for EVERY failure reason
  clearDraft(): void
  interface QuizDraft { schemaFingerprint; savedAt; step: "quiz_parts" | "consent";
                        patientState; patientInfo; symptomProfileId; currentPartIndex; answers }

From app/lib/quiz/payload.ts (plan 04.2-02):
  buildSubmitPayload(input: SubmitPayloadInput): SubmitPayload

From app/components/quiz/ResumeOffer.tsx (plan 04.2-03):
  ResumeOffer({ onResume, onStartOver })
  RestorationNotice()

Already in app/components/quiz/QuizContainer.tsx — do NOT change these:
  isPartComplete(items, answers)        // imported from ./QuizPartRenderer, line 12
  itemsForPart(QUIZ_PARTS, index)       // imported from ../../lib/quiz/schema, line 22
  quizFlowProgress(...)                 // line 193-204 ternary. resume_offer falls through to the
                                        // final `: null` branch with ZERO edits. Verified directly
                                        // against source. app/lib/quiz/schema.ts:239-243,267
                                        // (FlowProgressStep, NON_PART_STEPS = 3) also need NO edit.
                                        // Editing the counter would reopen UAT defect #6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route buildPayload through payload.ts and add the resume_offer FlowStep with a lazy draft read</name>
  <read_first>
    - app/components/quiz/QuizContainer.tsx (whole file — 434 lines; the FlowStep union at :32-40, the twelve useState hooks at :68-85, buildPayload at :114-136, the progressInfo ternary at :193-204)
    - app/lib/quiz/payload.ts (plan 04.2-02's output — the exact SubmitPayloadInput shape)
    - app/lib/quiz/draft-store.ts (plan 04.2-01's output — readDraft's null-collapsing contract)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-RESEARCH.md § "Code Examples" (the illustrative lazy-initializer wiring) and § "Finding 1" (the keep/drop field table)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-UI-SPEC.md § "Flow Contract" (why resume_offer carries no progress bar)
  </read_first>
  <files>app/components/quiz/QuizContainer.tsx</files>
  <action>
Three edits, one commit.

1. Reduce `buildPayload` (`:114-136`) to a thin `useCallback` that returns
`buildSubmitPayload({ patientState, patientInfo, symptomProfileId, answers, score, scoreBracket, startTime })`,
keeping the existing dependency array. Delete the now-duplicated body. Import `buildSubmitPayload`
from `../../lib/quiz/payload`. Leave `visibleAnswers`, `calculateTotalScore`, `getScoreBracket`,
`ALL_ITEMS`, `ALL_SCORED_QUESTIONS` imported — `goToConsent` and the Test Mode block still use them;
remove an import only if `npm run typecheck` proves it is genuinely unused.

2. Add `"resume_offer"` to the `FlowStep` union (`:32-40`), placed immediately before
`"state_gate"`. Do NOT touch the `progressInfo` ternary at `:193-204` and do NOT touch
`app/lib/quiz/schema.ts`. `resume_offer` falls through to the existing final `: null` branch exactly
as `ineligible`, `submitting`, `results`, and `error` already do, so no progress bar renders and the
`NON_PART_STEPS = 3` arithmetic is untouched. Add a short comment at the union stating this, and
stating that editing the counter for this step would reopen UAT defect #6.

3. Add the mount-time draft read, ahead of the `step` hook so ordering is legal:
`const [initialDraft] = useState<QuizDraft | null>(() => (typeof window === "undefined" ? null : readDraft()));`
then change the `step` hook to `useState<FlowStep>(initialDraft ? "resume_offer" : "state_gate")`.
The `typeof window === "undefined"` guard mirrors the SSR guard `isTestModeEnabled` already uses at
`:42-46`. There must be exactly ONE branch here: `readDraft()` returns `null` for storage-unavailable,
missing, expired, schema-mismatched, malformed, and empty-answers alike, so the D-01 path and the
"no draft" path are the same line of code, per UI-SPEC § "Absent State". Do not add an
`isStorageAvailable()` call at this site — a second branch would be a regression against that
requirement.

Do not add any `console.` statement. Do not add a `draft` value to any existing log, error message,
or thrown Error.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test && node -e 'const fs=require("fs"); const s=fs.readFileSync("app/components/quiz/QuizContainer.tsx","utf8"); const c=n=>s.split(n).length-1; if (c("resume_offer")===0) throw new Error("resume_offer FlowStep missing"); if (c("buildSubmitPayload")===0) throw new Error("payload not routed through payload.ts"); if (c("readDraft")!==1) throw new Error("readDraft must be called exactly once (one branch, per UI-SPEC Absent State); found "+c("readDraft")+" including the import"); if (c("isStorageAvailable")!==0) throw new Error("second D-01 branch introduced at the mount site"); if (c("console.")!==0) throw new Error("console. added to QuizContainer"); const sc=fs.readFileSync("app/lib/quiz/schema.ts","utf8"); if (sc.split("NON_PART_STEPS = 3").length-1 !== 1) throw new Error("schema.ts counter arithmetic changed"); console.log("Task 1 source gates OK");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm test` is green at >= the pre-plan count
    - `QuizContainer.tsx` imports `buildSubmitPayload` and its `buildPayload` body is a single call to it
    - `"resume_offer"` is a member of the `FlowStep` union
    - `git diff app/components/quiz/QuizContainer.tsx` shows zero changes to lines 193-204 (the `progressInfo` ternary)
    - `git diff --name-only` does not list `app/lib/quiz/schema.ts`
    - `readDraft` appears in exactly one call site plus its import; `isStorageAvailable` appears zero times in `QuizContainer.tsx`
    - Zero `console.` occurrences added
  </acceptance_criteria>
  <done>Payload construction has one implementation, and a returning patient with a valid draft lands on `resume_offer` with no progress bar and no counter change.</done>
</task>

<task type="auto">
  <name>Task 2: Restore handler, offer render, and the one-time restoration notice</name>
  <read_first>
    - app/components/quiz/QuizContainer.tsx (as modified by Task 1 — especially `goToConsent` at :150-157, which is the score/bracket recomputation this task must reuse, and the `quizContainer__questions` render block at :246-376 where `state_gate` and `ineligible` already render)
    - app/components/quiz/ResumeOffer.tsx (plan 04.2-03's output — `ResumeOffer` and `RestorationNotice` signatures)
    - app/components/quiz/QuizPartRenderer.tsx lines 1-20 and the exported `isPartComplete` (the predicate the D-09 landing rule uses)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-CONTEXT.md § D-09 ("The 04.1 wall must survive resume: do not let restored state satisfy the file-required predicate")
    - .planning/phases/04.2-resume-in-progress-intake/04.2-RESEARCH.md § "Finding 1" (consentChecked drop, score/bracket recompute, startTime never persisted)
  </read_first>
  <files>app/components/quiz/QuizContainer.tsx</files>
  <action>
Add two ephemeral state flags next to the existing hooks: `resumedSession` (boolean, default false)
and `showRestorationNotice` (boolean, default false). Neither is ever persisted.

`handleResume()` — reads from `initialDraft` (returning immediately if it is null) and applies, in
this order:
  - `setPatientState(draft.patientState)`
  - `setPatientInfo(draft.patientInfo)`
  - `setSymptomProfileId(draft.symptomProfileId)`
  - `setAnswers(draft.answers)` — the raw restored answers; `readDraft` has already stripped every
    `file_multi` value, so `answers.testing_files` is absent by construction (D-09/D-11)
  - `setResumedSession(true)` and `setShowRestorationNotice(true)`

Then compute the landing step with this rule, and write a comment explaining it is D-09's mechanism:
find the lowest index `i` in `[0, QUIZ_PARTS.length)` for which
`isPartComplete(itemsForPart(QUIZ_PARTS, i), draft.answers)` is `false`. If such an `i` exists, call
`setCurrentPartIndex(i)` and `setStep("quiz_parts")` — regardless of what `draft.step` says. If no
such `i` exists (every part is complete), honour `draft.step`: for `"consent"`, recompute score and
bracket using the exact same three-line chain `goToConsent` uses
(`visibleAnswers(ALL_ITEMS, draft.answers)` -> `calculateTotalScore(ALL_SCORED_QUESTIONS, visible)`
-> `getScoreBracket(s)`), set them, and `setStep("consent")`; for `"quiz_parts"`, set
`setCurrentPartIndex(draft.currentPartIndex)` and `setStep("quiz_parts")`.

Why the landing rule exists, stated in the comment: after Phase 4.1 the mandatory allergy-test upload
is the FIRST part, and `readDraft` strips its token. A `had_testing` patient's restored answers
therefore leave Part 0 incomplete, so this rule always lands them back on the upload screen. Without
it, a draft recorded at `step: "consent"` would drop a resumed patient straight onto Submit with a
missing required upload — breaking both the 04.1 wall (D-09) and payload parity (D-10). This is not a
"review your answers" screen and does not add one (UI-SPEC § Flow Contract explicitly forbids that);
it is the normal part renderer at the normal position.

Never restore `consentChecked` — it stays `false` on every resumed session, so a patient landing on
`consent` must actively re-check the box. Comment the reason: `CONSENT_VERSION` has already been
bumped twice this milestone, and a pre-checked box beside text the patient never re-read is a consent
integrity problem, not a convenience. Never restore `score`/`scoreBracket` from storage — `QuizDraft`
has no such field. Never restore `startTime`; its existing `useState(() => Date.now())` lazy
initializer already gives every mount, resumed or not, a fresh clock, which is what keeps
`completion_time` measuring minutes instead of days.

`handleStartOverFromOffer()` — calls `clearDraft()` then `setStep("state_gate")`. Nothing else needs
resetting here: no draft state has been written into React yet at this point.

Render, inside the existing `quizContainer__questions` div alongside the `state_gate` and
`ineligible` conditionals (NOT as an early return like `error`/`submitting`):
`{step === "resume_offer" && <ResumeOffer onResume={handleResume} onStartOver={handleStartOverFromOffer} />}`

Render the restoration notice as the first child inside `quizContainer__questions` when
`showRestorationNotice` is true and `step` is `"quiz_parts"` or `"consent"`:
`{showRestorationNotice && (step === "quiz_parts" || step === "consent") && <RestorationNotice />}`.
Dismiss it on the next navigation: set `setShowRestorationNotice(false)` in the four existing
navigation handlers that leave the restored screen — the `quiz_parts` Previous button, the
`quiz_parts` Next button, the `quiz_parts` Continue button (which calls `goToConsent`), and consent's
Previous button. Do not add a close affordance; it is a one-time orientation cue, not a banner.

Import `ResumeOffer` and `RestorationNotice` from `./ResumeOffer`, `clearDraft` from
`../../lib/quiz/draft-store`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npm test && node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizContainer.tsx","utf8"); const c=n=>s.split(n).length-1; if (c("setConsentChecked(true)")!==0) throw new Error("consent restored as checked"); if (c("draft.score")!==0||c("initialDraft.score")!==0) throw new Error("score read from draft"); if (c("setStartTime")!==0) throw new Error("startTime mutated"); for (const n of ["handleResume","isPartComplete(itemsForPart","RestorationNotice","clearDraft"]) if (c(n)===0) throw new Error("missing: "+n); if (c("console.")!==0) throw new Error("console. added"); console.log("Task 2 source gates OK");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm test` green
    - `handleResume` sets `patientState`, `patientInfo`, `symptomProfileId`, and `answers` from `initialDraft`
    - `handleResume` computes the landing step from the lowest `i` where `isPartComplete(itemsForPart(QUIZ_PARTS, i), draft.answers)` is false, and only falls back to `draft.step` when every part is complete
    - Zero occurrences of `setConsentChecked(true)` and zero reads of a `score` field off any draft object
    - `startTime` has no setter and is never assigned
    - `ResumeOffer` renders inside `quizContainer__questions`, not as an early return
    - `RestorationNotice` renders only when `showRestorationNotice` is true and `step` is `quiz_parts` or `consent`, and `setShowRestorationNotice(false)` appears in all four navigation handlers
    - Zero `console.` occurrences
  </acceptance_criteria>
  <done>Resuming restores answers and identity, always re-requires the mandatory upload, never pre-checks consent, and never trusts a stored score.</done>
</task>

<task type="auto">
  <name>Task 3: DOM-prove the offer, the restore, the absent state, and the D-09 landing rule</name>
  <read_first>
    - app/components/quiz/QuizContainer.tsx (as modified by Tasks 1-2)
    - tests/quiz-file-upload-dom.test.ts (whole file — the jsdom pragma, `React.createElement`, `vi.stubGlobal("fetch", ...)` with the `jsonResponse` helper, `itemsForPart(QUIZ_PARTS, 0)` as the real Part 7 fixture, `cleanup()` + `vi.unstubAllGlobals()` in afterEach)
    - app/lib/quiz/draft-store.ts (DRAFT_STORAGE_KEY, currentSchemaFingerprint — the test seeds storage directly, so it must write a draft with a valid fingerprint)
    - app/lib/quiz/questions.ts lines 465-610 (real Part 7 and Part 1 question IDs for the seeded fixture)
  </read_first>
  <files>tests/quiz-resume-restore-dom.test.ts</files>
  <action>
Create `tests/quiz-resume-restore-dom.test.ts`. Literal first line `// @vitest-environment jsdom`,
`.test.ts` extension, `React.createElement` only, `cleanup()` and
`window.localStorage.clear()` in `afterEach`. Each `describe` opens with a comment naming the
requirement (`RESUME-01`, `RESUME-04(b)`, `D-01`, `D-09`).

Helper: `seedDraft(partial)` writes a valid draft directly to
`window.localStorage.setItem(DRAFT_STORAGE_KEY, JSON.stringify({ schemaFingerprint: currentSchemaFingerprint(), savedAt: Date.now(), ...defaults, ...partial }))`.
Defaults use real question IDs and a `had_testing` Part 7 answer set with `testing_year` /
`testing_location` / `testing_allergens` filled and NO `testing_files` key.

D-01 / absent state — with `localStorage` empty, render `QuizContainer` and assert the state-gate
question ("Are you a resident of Tennessee or Texas?") is on screen and the resume-offer heading is
absent. Then repeat with `window.localStorage.setItem` stubbed to throw a
`DOMException("blocked", "SecurityError")` and assert the identical result, with no error text and no
banner rendered — the two paths must be visually indistinguishable.

D-01 / expired draft — seed a draft with `savedAt: Date.now() - 25 * 60 * 60 * 1000` and assert the
state gate renders, the offer does not, and `window.localStorage.getItem(DRAFT_STORAGE_KEY)` is now
`null` (active cleanup reached through the real component path, not just the unit test).

RESUME-01 / the offer — seed a valid draft, render, assert the offer heading
`You have an unfinished assessment from earlier.` is present and the state-gate question is not.
Assert the rendered document contains none of the seeded PHI values: the seeded name, the seeded
email, the seeded phone digits, and the seeded `dob` string each occur zero times in
`document.body.textContent`. This is the D-06 guarantee proven end-to-end rather than at the
component boundary.

RESUME-01 / restore — click `Resume`. Assert the restoration notice text
`Your previous answers have been restored.` is present, and that a restored answer is visibly
reflected in the rendered part (for example the restored `testing_year` value appears in its text
input's value). Assert the state gate and patient-info steps are NOT rendered — the patient landed
inside the quiz, not back at the start.

D-09 / the upload wall survives resume — with the same `had_testing` seeded draft whose `step` is
`"consent"` and `currentPartIndex` is the last index, click `Resume` and assert the patient lands on
the quiz part containing the file question (Part index 0 after the 04.1 reorder), NOT on the consent
screen. Assert the file dropzone is empty and that the part's forward button is disabled — restored
state did not satisfy the file-required predicate. Also assert `answers.testing_files` never made it
back by checking the rendered file list shows no entries.

D-09 tampering variant — seed a draft that DOES contain `testing_files: ["tok_tampered"]`, click
`Resume`, and assert `document.body.textContent` contains zero occurrences of `tok_tampered` and the
forward button is still disabled.

RESUME-01 / start over from the offer — seed a valid draft, render, click `Start over`, then
`Yes, start over`. Assert `window.localStorage.getItem(DRAFT_STORAGE_KEY)` is `null` and the state
gate is rendered.

Consent integrity — seed a complete-answers draft (every part complete, including a `needs_testing`
Part 7 so no upload is required) with `step: "consent"`, click `Resume`, assert the consent screen
renders, its checkbox is unchecked, and the Submit button is disabled.

Before committing, prove the D-09 test is not vacuous: temporarily change `handleResume`'s landing
rule to honour `draft.step` unconditionally, confirm the D-09 test FAILS, then restore the file and
confirm it passes. Record both observations in the plan summary. A green test on this project is not
a trusted test until it has been observed failing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run tests/quiz-resume-restore-dom.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-resume-restore-dom.test.ts` passes with at least 8 tests
    - Literal first line is `// @vitest-environment jsdom`; no JSX
    - The D-01 absent-state test covers both "no draft" and "setItem throws" and asserts the same rendered output for both
    - The expired-draft test asserts `localStorage.getItem(DRAFT_STORAGE_KEY) === null` after render
    - The offer test asserts the seeded name, email, phone, and dob each occur zero times in `document.body.textContent`
    - The D-09 test seeds `step: "consent"` on a `had_testing` draft and asserts the patient lands on the file part with the forward button disabled
    - A tampering test proves a hand-planted `testing_files` token never reaches the DOM
    - The consent-integrity test asserts the checkbox is unchecked and Submit disabled after resuming onto `consent`
    - The RED proof for the D-09 landing rule is recorded in `04.2-04-SUMMARY.md` with the observed failure message
    - `npm test` green
  </acceptance_criteria>
  <done>Criterion 1 is proven end-to-end, the D-01 absent state is proven indistinguishable from "no draft", and the 04.1 upload wall is proven to survive resume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → React state | A stored draft is untrusted input crossing a deploy-version boundary and a DevTools-edit boundary |
| Restored state → the Phase 4.1 mandatory-upload gate | Restored answers must not be able to satisfy a clinical prerequisite the patient did not re-satisfy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.2-12 | Elevation of Privilege | `consentChecked` restored as true, letting a patient submit under a `CONSENT_VERSION` they never read | mitigate | `consentChecked` is absent from `QuizDraft` and never set on restore; the DOM test asserts an unchecked box and a disabled Submit after resuming onto `consent` |
| T-4.2-13 | Tampering | Restored state satisfying the 04.1 mandatory-upload predicate, producing a submission with no test results | mitigate | `handleResume` lands on the lowest part index where `isPartComplete` is false, regardless of `draft.step`; proven by a DOM test with a documented RED observation |
| T-4.2-14 | Tampering | A hand-edited draft reintroducing a dead GCS staging token | mitigate | `readDraft` strips `file_multi` values a second time (plan 04.2-01); the tampering DOM test asserts the token never reaches the DOM |
| T-4.2-15 | Information Disclosure | The D-01 failure path becoming visually distinguishable from "no draft", telling a bystander a draft exists | mitigate | Exactly one `readDraft()` call site and no `isStorageAvailable()` call in `QuizContainer`; asserted by a source-count gate and by a DOM test comparing both renders |
| T-4.2-16 | Tampering | A persisted score diverging from what current scoring logic computes | mitigate | `QuizDraft` has no score field; `handleResume` recomputes via the same `visibleAnswers`/`calculateTotalScore`/`getScoreBracket` chain `goToConsent` uses; source gate asserts zero `draft.score` reads |
| T-4.2-17 | Information Disclosure | PHI logged while debugging restore | mitigate | Zero `console.` occurrences asserted in `QuizContainer.tsx` on both tasks |
| T-4.2-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this plan |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/quiz-resume-restore-dom.test.ts` green
- `npm test` green
- `git diff --name-only` lists only `app/components/quiz/QuizContainer.tsx` and `tests/quiz-resume-restore-dom.test.ts`
- `app/lib/quiz/schema.ts` and the `progressInfo` ternary are provably unchanged
</verification>

<success_criteria>
A returning patient with a valid draft sees an identity-free offer; Resume restores their answers,
re-requires the allergy-test upload, leaves consent unchecked, and shows a one-time notice; a patient
with no draft or unwritable storage sees exactly today's app.
</success_criteria>

<output>
Create `.planning/phases/04.2-resume-in-progress-intake/04.2-04-SUMMARY.md` when done.
</output>
