---
phase: 04.2-resume-in-progress-intake
plan: 08
type: execute
wave: 6
depends_on: [04.2-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [RESUME-01, RESUME-02, RESUME-03, RESUME-04]

must_haves:
  truths:
    - "Phase 4.1's reorder and Phase 4.2's resume ship together in one three-channel deploy, per Andrew's recorded decision"
    - "The deploy is verified on served bytes, never on an exit code"
    - "The deployed bundle carries BOTH Phase 4.2's resume markers and Phase 4.1's Part-7-first order"
    - "Phase 4.1's owed post-deploy PHI-renderer confirmation is finally closed"
  artifacts:
    - path: ".planning/phases/04.2-resume-in-progress-intake/04.2-08-SUMMARY.md"
      provides: "Deploy record with before/after served-bytes measurements, the Shopify version, the Fly release, and the human verdict"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "RESUME-01..04 marked complete and their traceability rows updated"
      contains: "RESUME-04"
  key_links:
    - from: "phase-4.2-resume-in-progress-intake"
      to: "https://alle-drops-quiz-app.fly.dev/quiz-bundle-js"
      via: "merge to main then fly deploy -a alle-drops-quiz-app"
      pattern: "quiz-bundle-js"
    - from: "extensions/"
      to: "a new Shopify app version"
      via: "shopify app deploy"
      pattern: "shopify app deploy"
---

<objective>
Ship Phase 4.1 and Phase 4.2 together, verified on the bytes the internet actually serves, and close
the two verification items Phase 4.1 deferred when Andrew held the merge.

Purpose: Fly is still on release v51 and Shopify on `alledrops-quiz-production-22`, both carrying the
OLD part order with allergy testing last. Neither the abandonment fix nor resume helps a patient until
it is live, and this project has repeatedly been burned by treating an exit code as evidence.

Output: a merged PR, a new Fly release, a new Shopify app version, a served-bytes verification table,
and updated requirement bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-06-SUMMARY.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-06-SUMMARY.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Deploy topology and the pre-deploy baseline. Do not rediscover. -->

Three independent channels — `shopify app deploy` does NOT deploy the Fly app:
  1. git      -> merge to `main`
  2. Fly      -> `fly deploy -a alle-drops-quiz-app`   (region iad)
  3. Shopify  -> `shopify app deploy`                  (extensions + config only)

Pre-deploy live state, recorded in STATE.md:
  Fly release            : v51
  Shopify app version    : alledrops-quiz-production-22
  Both carry the OLD part order (allergy testing LAST) — Phase 4.1 is unshipped.
  Served /quiz-bundle-js at v51 : 195102 bytes

Open PR: https://github.com/askinne2/alle-drops-quiz-app/pull/22
  `phase-4.1-testing-first-quiz-order` -> `main`, held open by Andrew's decision
  ("merge and deploy with phase 4.2"), a deliberate, recorded override of 04.1's own D-10.

Owed from 04.1-06, never executed:
  Task 2 — three-channel deploy and served-bytes verification
  Task 3 — Andrew's confirmation of the two PHI renderers (`app/lib/pdf.ts`,
           `app/routes/app.quiz-results.tsx`) on the DEPLOYED app (D-05a's blocking criterion)

Verification technique (Phases 1/3/4 discipline): fetch with a `?cb=<nanoseconds>` cache buster and
count occurrences with `split(needle).length - 1`. NEVER `grep -c`.

Standing live exposure, unchanged by this phase and NOT to be silently closed here: the Appointly
app embed loads third-party JS from staq-cdn.com / staqlab.com on `/pages/allergy-quiz`. Needle is
`appointly`, not `apntly`. Owned by Phase 8 / LAUNCH-01.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Obtain authorization for the combined 04.1 + 04.2 merge and three-channel deploy</name>
  <read_first>
    - .planning/phases/04.1-testing-first-quiz-order/04.1-06-SUMMARY.md (Andrew's verbatim "merge and deploy with phase 4.2" decision and the five consequences it recorded)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-07-SUMMARY.md (the human browser verdict this deploy rests on)
    - CLAUDE.md § "Development workflow" (the default is that Andrew reviews and merges; Claude may deploy with authorization)
  </read_first>
  <action>
Before asking anything, run and report as concrete numbers and exit codes: `npm run typecheck`,
`npm test` (test and file counts), `npm run build`, two consecutive `npm run build:theme` runs with
their SHA-256 values, `git status --porcelain` after that rebuild,
`git diff main...HEAD --stat -- package.json package-lock.json` (must produce no output),
`git diff main...HEAD --name-only -- migrations/` (must produce no output), and
`git diff main...HEAD --name-only` filtered to PHI-adjacent paths (`app/routes/api*`,
`app/lib/db.ts`, `app/lib/submissions.ts`, `app/lib/customer-auth.ts`, `app/lib/shopify/`,
`app/lib/pdf.ts`). Then present the three options below along with the standing Phase 8 exposures
named in `<context>`, and record Andrew's selection verbatim.
  </action>
  <decision>How to land Phase 4.1 and Phase 4.2 on `main` and into production.</decision>
  <context>
    Both phases are code-complete and gated green. Fly is on v51 and Shopify on version 22, both
    carrying the OLD part order. PR #22 (04.1 -> main) is open and unmerged by Andrew's choice.
    Phase 4.2's branch descends from 04.1's branch, so 04.2's HEAD already contains everything in
    PR #22 — merging 04.2 to main lands both phases at once.

    Report before asking: `npm run typecheck` exit code, `npm test` count, `npm run build` exit code,
    `npm run build:theme` determinism, whether a fresh theme build leaves the tree clean, the output
    of `git diff main...HEAD --stat -- package.json package-lock.json` (must be empty — zero new
    dependencies this phase), whether any file under `migrations/` changed (must be none — no DDL in
    this phase), and which PHI-adjacent files changed relative to `main`.

    Also state plainly what this deploy does NOT close: the Appointly third-party script still loads
    on the PHI-collecting storefront page, and the storefront still carries no medical disclaimer.
    Both are Phase 8 / LAUNCH-01, both predate this phase, and neither is made worse by it.
  </context>
  <options>
    <option id="combined-pr">
      <name>Open one PR from `phase-4.2-resume-in-progress-intake` to `main` superseding PR #22, and after Andrew merges, Claude runs all three deploy channels</name>
      <pros>One review surface covering both phases; matches Andrew's recorded "ship together" decision; keeps the CLAUDE.md default that Andrew merges</pros>
      <cons>PR #22 must be closed with a note pointing at the superseding PR so the history stays legible</cons>
    </option>
    <option id="claude-merges">
      <name>Claude merges the combined PR and runs all three channels under explicit in-session authorization</name>
      <pros>Fastest; matches the precedent set for PRs #16-19 where Andrew authorized Claude to merge</pros>
      <cons>Overrides CLAUDE.md's default merge rule; must be recorded as a deliberate override, as prior phases did</cons>
    </option>
    <option id="hold-again">
      <name>Hold the merge again and ship later</name>
      <pros>None identified; the abandonment fix has already been unshipped since 2026-08-10</pros>
      <cons>Every additional day keeps the ten-minute-questionnaire-before-abandonment ordering live and leaves the branch exposed to divergence from `main`</cons>
    </option>
  </options>
  <acceptance_criteria>
    - All gate results are reported as concrete numbers and exit codes before the question is asked, not as a claim that they passed
    - `git diff main...HEAD --stat -- package.json package-lock.json` produces no output, and this is stated explicitly
    - No file under `migrations/` differs from `main`, and this is stated explicitly
    - The PHI-adjacent file list relative to `main` is enumerated
    - The two standing Phase 8 exposures are named as out of scope rather than left unaddressed
    - Andrew's selection is recorded verbatim in the summary
  </acceptance_criteria>
  <done>Andrew has chosen how both phases land, with every gate result and every stated exclusion visible to him at the moment he chose.</done>
  <resume-signal>Select: combined-pr, claude-merges, or hold-again.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Three-channel deploy and served-bytes verification</name>
  <read_first>
    - .planning/phases/04.1-testing-first-quiz-order/04.1-06-PLAN.md Task 2 (the verification table format and the deploy sequence this task reproduces for both phases)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-06-SUMMARY.md (the committed bundle's new byte size and SHA-256, and the Phase 4.2 marker needles with their measured counts)
    - .planning/STATE.md (the v51 baseline numbers to measure against)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Record the PRE-deploy baseline first, by fetching the live app, not by copying STATE.md: cache-busted
`GET https://alle-drops-quiz-app.fly.dev/quiz-bundle-js`, its byte length, and the count of each
Phase 4.2 marker needle from plan 04.2-06 plus the `testing_status` Part-7 order evidence. Every
marker must count 0 pre-deploy. Also record the current `fly releases` head and
`shopify app versions list` head.

Then execute the channel actions selected in Task 1: land the branch on `main`, run
`fly deploy -a alle-drops-quiz-app`, and run `shopify app deploy`. If Task 1 selected `combined-pr`,
close PR #22 with a comment naming the superseding PR so the decision history stays legible.

Then verify POST-deploy, on served bytes only:
  - `GET /health` returns 200.
  - Cache-busted `GET /quiz-bundle-js` byte length equals `wc -c public/quiz-bundle.js` exactly, and
    differs from the v51 value of 195102.
  - SHA-256 of the served bundle equals the SHA-256 of the committed artifact recorded in
    04.2-06-SUMMARY.md.
  - Each Phase 4.2 marker needle counts >= 1 in the served bytes, using
    `split(needle).length - 1`. Never `grep -c` — the bundle is one line and `grep -c` would return 1
    for anything present, making the gate vacuous.
  - Phase 4.1's order evidence holds on the SERVED bytes: extract the built `QUIZ_PARTS` element
    identifiers with the same technique `tests/quiz-bundle-freshness.test.ts:330` uses and assert
    element 0 resolves to the `testing_status` head. Report the extracted identifiers.
  - `fly releases` shows a new release number greater than v51, and
    `shopify app versions list` shows a version newer than `alledrops-quiz-production-22`. Record both.

Then update bookkeeping in the same commit:
  - `.planning/REQUIREMENTS.md`: change RESUME-01..04 from `- [ ]` to `- [x]`, and change their four
    traceability rows from `Pending` to `Complete (2026-08-XX, verified live)` matching the format
    already used by DEF-01..04.
  - `.planning/ROADMAP.md`: check the Phase 4.2 entry's plan list items, and check
    `04.1-05-PLAN.md` and `04.1-06-PLAN.md` in the Phase 4.1 plan list, which this deploy finally
    completes.

Do NOT mark TEST-06, LAUNCH-01, or LAUNCH-02 as anything — they are Phase 8 and untouched by this
deploy. Do NOT disable the Appointly app embed as a side effect; it needs its own keep/disable
decision and could break Phase 7's booking dependency.

If any post-deploy check fails, stop and report rather than re-running the deploy. A second deploy on
top of an unexplained failure destroys the evidence.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e 'const{execSync}=require("child_process");const fs=require("fs");const cb=process.hrtime.bigint();const url="https://alle-drops-quiz-app.fly.dev/quiz-bundle-js?cb="+cb;const served=execSync(`curl -s "${url}"`,{maxBuffer:1e9}).toString();const local=fs.readFileSync("public/quiz-bundle.js","utf8");const c=(h,n)=>h.split(n).length-1;if(Buffer.byteLength(served)!==Buffer.byteLength(local))throw new Error("served "+Buffer.byteLength(served)+" != committed "+Buffer.byteLength(local));if(c(served,"resume_offer")<1)throw new Error("resume_offer absent from served bytes");if(c(served,"testing_status")<1)throw new Error("Part 7 marker absent from served bytes");const h=execSync(`curl -s -o /dev/null -w "%{http_code}" https://alle-drops-quiz-app.fly.dev/health`).toString();if(h!=="200")throw new Error("health "+h);console.log("served bytes verified:",Buffer.byteLength(served),"resume_offer=",c(served,"resume_offer"));'</automated>
  </verify>
  <acceptance_criteria>
    - Pre-deploy and post-deploy measurements are both recorded, each fetched live rather than copied from STATE.md
    - Every Phase 4.2 marker counts 0 pre-deploy and >= 1 post-deploy, counted with `split(needle).length - 1`
    - Served bundle byte length equals the committed artifact's and differs from 195102
    - Served bundle SHA-256 equals the committed artifact's SHA-256
    - The built `QUIZ_PARTS` element-0 identifier on the SERVED bytes resolves to the `testing_status` head, with the extracted identifiers reported
    - `/health` returns 200
    - New Fly release number > v51 and a Shopify app version newer than `alledrops-quiz-production-22`, both recorded
    - RESUME-01..04 are `- [x]` with `Complete` traceability rows; the Phase 4.1 and 4.2 plan checkboxes in ROADMAP.md are checked
    - Zero changes to TEST-06 / LAUNCH-01 / LAUNCH-02 and zero changes to the Appointly embed
  </acceptance_criteria>
  <done>Both phases are live and proven live on the bytes the internet serves, with requirement bookkeeping matching reality.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Andrew confirms the two PHI renderers and the resume flow on the deployed app</name>
  <read_first>
    - .planning/phases/04.1-testing-first-quiz-order/04.1-06-PLAN.md Task 3 (the exact D-05a criterion this closes, owed since 2026-08-10)
    - .planning/phases/04.1-testing-first-quiz-order/04.1-02-SUMMARY.md (what the admin Test Results section was supposed to render, and the losslessness claim)
  </read_first>
  <action>
Confirm Task 2's served-bytes verification is green before involving Andrew — a human check against a
failed deploy wastes his time and muddies the evidence. Report the new Fly release number, the new
Shopify app version, and the served bundle byte length, then hand him Parts A and B below. Record a
separate verdict for each of the eight numbered steps and his verbatim overall verdict, and state
explicitly whether Phase 4.1's D-05a criterion is now closed. If Part B step 5 fails, capture the
browser and OS and write the outcome into 04.2-RESEARCH.md's D-03 results table rather than filing it
as a defect.
  </action>
  <what-built>
    Phase 4.1's reorder (allergy testing first, with its Test Results section added to both PHI
    renderers) and Phase 4.2's browser-local resume are now live on Fly and Shopify. Two things are
    owed: Phase 4.1's blocking D-05a confirmation, which could only ever run post-deploy, and a smoke
    of resume on the real storefront rather than a local harness.
  </what-built>
  <how-to-verify>
    Part A — the two PHI renderers (Phase 4.1's owed D-05a check):
      1. Open the embedded admin at `/app/quiz-results` on the deployed app. Open a submission from a
         `had_testing` patient.
      2. Confirm the detail view shows testing status, testing year, testing location, and allergens
         — each appearing EXACTLY ONCE. A duplicate means the Test Results section and the Symptom
         Responses filter disagree.
      3. Download that submission's clinical PDF. Confirm the Part 7 answers appear once, under a
         Test Results heading, and that the uploaded file is embedded.

    Part B — resume on the real storefront:
      4. Open `https://allergist-on-demand.myshopify.com/pages/allergy-quiz` (storefront password
         required). Confirm the allergy-testing part is FIRST — the reorder is finally live.
      5. Answer one question, close the tab, and return to the same page. Confirm the resume offer
         appears and reveals nothing about who you are.
      6. Resume, confirm your answer came back and the file dropzone is empty with the
         "For your security, files aren't kept between visits." copy.
      7. Use the in-flow "Start over" control, confirm the confirmation, and confirm you land on the
         Tennessee/Texas gate.
      8. If the resume offer does NOT appear at step 5, that is the D-03 partitioned-storage case
         showing up in production. It is a silent degradation, not a bug — report it so the row can
         be added to 04.2-RESEARCH.md's table and the deferred App Proxy phase can be weighed.

    Do not use real patient information at any step. Every row in the dev database is test data.
  </how-to-verify>
  <acceptance_criteria>
    - Part A records an explicit per-field verdict for testing status, year, location, and allergens in the admin detail view, each confirmed present exactly once
    - Part A records that the downloaded PDF shows Part 7 answers once under Test Results with the uploaded file embedded, with the byte size noted
    - Part B step 4 confirms allergy testing is the first part on the live storefront — the first live evidence the 04.1 reorder shipped
    - Part B steps 5-7 each record PASS or FAIL with an observation
    - If step 5 fails, the browser and OS are recorded and the outcome is written into 04.2-RESEARCH.md's D-03 results table rather than treated as a defect
    - Andrew's verdict is recorded verbatim, and D-05a is explicitly marked closed or not closed
  </acceptance_criteria>
  <done>Both PHI renderers are confirmed on the deployed app, closing Phase 4.1's owed D-05a criterion, and resume has been exercised once on the real storefront.</done>
  <resume-signal>Type "approved" to close Phase 4.2, or describe what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `main` → the Fly VM and the Shopify app version | Three independent channels; landing one without the others ships a half-state |
| Deploy exit code → what the internet actually serves | This project has shipped invisible no-ops past green exit codes; only served bytes count |
| The storefront page → the BAA chain | Third-party scripts already execute on this PHI-collecting page and are not this phase's to fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.2-33 | Spoofing (of evidence) | A deploy declared successful from an exit code | mitigate | Pre- and post-deploy cache-busted fetches, byte-length and SHA-256 equality against the committed artifact, and marker counts via `split(needle).length - 1` |
| T-4.2-34 | Tampering | The deployed bundle carrying resume but silently losing Phase 4.1's Part-7-first order | mitigate | The built `QUIZ_PARTS` element identifiers are extracted from the SERVED bytes and element 0 asserted to be the `testing_status` head |
| T-4.2-35 | Repudiation | Phase 4.1's D-05a confirmation being quietly dropped because 04.1 was marked done | mitigate | Task 3 Part A carries it forward explicitly and marks it closed or not closed by name |
| T-4.2-36 | Information Disclosure | Third-party JS from staq-cdn.com / staqlab.com executing on the PHI-collecting storefront page | transfer | Pre-existing and owned by Phase 8 / LAUNCH-01. Named in Task 1's context so it is a stated exclusion rather than an omission. Not disabled here because Phase 7's booking path may depend on it and it needs its own keep/disable decision. Correct needle is `appointly`, not `apntly` |
| T-4.2-37 | Information Disclosure | Real patient PHI entered during a production smoke test | mitigate | Task 3 instructs synthetic values only; the in-flow "Start over" at step 7 clears the draft from the tester's browser |
| T-4.2-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs across the whole phase; Task 1 asserts `git diff main...HEAD -- package.json package-lock.json` is empty before authorization is requested |
</threat_model>

<verification>
- Pre- and post-deploy served-bytes tables recorded with concrete numbers
- Served bundle byte length and SHA-256 both equal the committed artifact's
- Every Phase 4.2 marker 0-before / >=1-after on served bytes
- Served `QUIZ_PARTS` element 0 resolves to the `testing_status` head
- New Fly release and Shopify app version recorded
- Andrew's verdicts recorded verbatim for both checkpoints
</verification>

<success_criteria>
Phase 4.1's reorder and Phase 4.2's resume are live, proven on served bytes; Phase 4.1's owed D-05a
confirmation is closed; RESUME-01..04 are marked complete; and no Phase 8 exposure was silently
changed.
</success_criteria>

<output>
Create `.planning/phases/04.2-resume-in-progress-intake/04.2-08-SUMMARY.md` when done.
</output>
