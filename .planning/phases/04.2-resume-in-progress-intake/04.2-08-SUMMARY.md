# Plan 04.2-08 — Ship 04.1 + 04.2 (three-channel deploy) — SUMMARY

**Status:** COMPLETE
**Executed:** 2026-08-11
**Driven by:** Andrew (both blocking checkpoints, the Shopify channel) + orchestrator (gates, git,
Fly channel, served-bytes verification, bookkeeping)
**Plan:** `04.2-08-PLAN.md` — wave 6, `autonomous: false`

Phase 4.1's reorder and Phase 4.2's resume are live and proven live on the bytes the internet
serves. Phase 4.1's D-05a PHI-renderer confirmation, owed since 2026-08-10, is **CLOSED**.

---

## Task 1 — authorization checkpoint

### Gates reported before the question was asked (concrete numbers, not claims)

| Gate | Result |
|---|---|
| `npm run typecheck` | exit **0** |
| `npm test` | exit **0** — **677 tests / 47 files** passed |
| `npm run build` | exit **0** |
| `npm run build:theme` ×2 | both exit 0, SHA-256 identical: `a3b050383929352507d47dc5c1828f905ece29b9f7e3b606ac91d41ac156b7c0` |
| Bundle size after rebuild | **201,707 B** |
| `git status --porcelain` after rebuild | **empty** — committed artifact equals a fresh build |
| `git diff main...HEAD --stat -- package.json package-lock.json` | **no output** — zero new dependencies (T-4.2-SC satisfied) |
| `git diff main...HEAD --name-only -- migrations/` | **no output** — zero DDL |
| PHI-adjacent files changed vs `main` | **`app/lib/pdf.ts` only** (Phase 4.1's Test Results section) |
| Total files changed vs `main` | 70 |

Stated as out of scope at the moment of the decision, not omitted:
1. The Appointly app embed still loads third-party JS from `staq-cdn.com` / `staqlab.com` on the
   PHI-collecting `/pages/allergy-quiz`. Pre-existing, Phase 8 / LAUNCH-01. Not disabled here —
   Phase 7's booking path may depend on it, so it needs its own keep/disable decision (T-4.2-36).
2. The storefront still carries no medical disclaimer. Phase 8 / LAUNCH-01.

TEST-06, LAUNCH-01, and LAUNCH-02 were not modified.

### Andrew's selection, verbatim

> "combined-pr (Recommended)"

Then, at the merge step:

> "go and merge, fly deploy and I will shopify app deploy"

That second message is an explicit in-session authorization for Claude to merge — a deliberate,
recorded override of CLAUDE.md's default that Andrew reviews and merges, matching the precedent of
PRs #16–19. Andrew retained the Shopify channel himself.

---

## Task 2 — three-channel deploy and served-bytes verification

### Channel 1 — git

- PR **#23** opened: `phase-4.2-resume-in-progress-intake` → `main`, superseding #22.
- PR **#22** closed with a comment naming #23 so the decision history stays legible.
- Merged as **`e140a8c`**. `main` bundle SHA-256 = `a3b0503839…56b7c0`, 201,707 B — matches the
  artifact the gates validated.

### Channel 2 — Fly

`fly deploy -a alle-drops-quiz-app` → release **v51 → v52**.

A "not listening on the expected address" warning fired mid-deploy; it is the known race where Fly
probes before the app binds. Health checks then passed and the machine reached a good state, and
`/health` returns 200 on the live app. Not a defect.

### Channel 3 — Shopify

Andrew ran `shopify app deploy --allow-updates` (the bare form now requires the flag in a
non-interactive terminal). Released **`alledrops-quiz-production-22` → `alledrops-quiz-production-23`**.
Only `VariableName` lint warnings on pre-existing `_`-prefixed Liquid variables in `quiz-block` — no
errors, unchanged by this phase.

### Served-bytes verification

Every count uses `split(needle).length - 1`. **Never `grep -c`** — the bundle is one line, so
`grep -c` returns 1 for anything present and the gate would be vacuous.

| Measurement | PRE (v51) | POST (v52) | Verdict |
|---|---|---|---|
| served `/quiz-bundle-js` byte length | 195,142 | **201,707** | ✅ changed; equals `wc -c public/quiz-bundle.js` exactly |
| served SHA-256 | `a2be1ab758ca…8762c` | **`a3b050383929352507d47dc5c1828f905ece29b9f7e3b606ac91d41ac156b7c0`** | ✅ identical to the committed artifact |
| `resume_offer` | 0 | **2** | ✅ |
| `alledrops_quiz_draft` | 0 | **1** | ✅ |
| `quizStartOver` | 0 | **12** | ✅ |
| `"You have an unfinished assessment from earlier."` | 0 | **1** | ✅ |
| `"Your previous answers have been restored."` | 0 | **1** | ✅ |
| `/health` | — | **200** | ✅ |

Both fetches were cache-busted with `?cb=<hrtime nanoseconds>` and taken live, not copied from
STATE.md.

**Correction to STATE.md:** STATE.md recorded the v51 served length as 195,102 B. The live
measurement is **195,142 B**, which matches `04.2-06-SUMMARY.md`'s recorded `195142 → 201707`
transition. The 195,102 figure was wrong; this summary supersedes it.

### Phase 4.1's order evidence, on the SERVED bytes (T-4.2-34)

Extracted with the same mechanism as `tests/quiz-bundle-freshness.test.ts:330` — anchored on the
`.flat()` call that uniquely identifies the `QUIZ_PARTS` array, with every minified identifier
re-derived from the served bytes rather than hardcoded.

```
QUIZ_PARTS match found : true
extracted identifiers  : ["fs","os","ss","us","as","cs","ds"]   (7 elements)
testing_status head    : fs
symptoms_nasal head    : os
history_comorbidities  : ds
element[0]             : fs
ELEMENT 0 == testing_status head : true
non-vacuity control    : testing_status=7  symptoms_nasal=2  history_comorbidities=1
```

**The deployed bundle carries BOTH Phase 4.2's resume markers and Phase 4.1's Part-7-first order.**
The reorder is live for the first time.

### Bookkeeping — commit `86e6b50`

- `.planning/REQUIREMENTS.md`: RESUME-01..04 → `- [x]`; their four traceability rows →
  `Complete (2026-08-11, verified live)`, matching DEF-01..04's format.
- `.planning/ROADMAP.md`: `04.1-05-PLAN.md` checked.

**Deliberate deviation from the plan's literal instruction.** The plan directed checking
`04.1-06-PLAN.md` in the same commit. It was left unchecked at that point because 04.1-06's Task 3
— the D-05a confirmation — had no human verdict yet; checking it then would have been exactly the
repudiation T-4.2-35 exists to prevent. Both it and `04.2-08-PLAN.md` are checked now that Task 3
has returned a verdict. The commit message records the reasoning.

---

## Task 3 — human verification checkpoint

### Andrew's verdict, verbatim

> "Part A : quiz results work: [screenshot]
>
> PDF version: assessment-faac0486-80d6-483a-8ff9-5962a8b43d31.pdf
>
> Part B: allergy testing is first, resume worked when I said store results, navigated away and came
> back and it prompted me to resue.
>
> approved."

Andrew supplied a screenshot and the downloaded PDF rather than prose, so both artifacts were
verified independently against the acceptance criteria rather than taken on assertion.

### Part A — the two PHI renderers (Phase 4.1's owed D-05a check)

Submission `faac0486-80d6-483a-8ff9-5962a8b43d31`, profile `AOD_1786362263137`, a `had_testing`
patient with one uploaded file. Synthetic values throughout.

**Renderer 1 — embedded admin detail view** (verified from Andrew's screenshot):

| Field | Rendered value | Occurrences | Verdict |
|---|---|---|---|
| Allergy testing status | `had_testing` | **1** | ✅ |
| Year of prior allergy testing | `dfafdas` | **1** | ✅ |
| Where prior allergy testing was done | `fdafda` | **1** | ✅ |
| Allergens reacted to on prior testing | `fdafdsa` | **1** | ✅ |

All four appear under a single **TEST RESULTS** section heading. Critically, **none of the four
appears in the Symptom Responses list above it** — the Test Results section and the Symptom
Responses filter agree, which is precisely the disagreement D-05a was written to catch. Uploaded
Files section renders `16658.jpg (305.6 KB)`.

**Renderer 2 — clinical PDF** (verified by decoding the PDF's own content streams: inflating each
stream, then concatenating the hex segments inside every `TJ` array to reconstruct the visible
text):

| Check | Result |
|---|---|
| PDF byte size | **316,454 B** |
| `TEST RESULTS` heading | **1** occurrence |
| `had_testing` | **1** |
| `dfafdas` | **1** |
| `fdafda` | **1** |
| `fdafdsa` | **1** |
| Section ordering | all four immediately follow the `TEST RESULTS` heading (reconstructed lines 70–78), between the symptom answers and the `CONSENT RECORD` block |
| Uploaded file embedded | **yes** — 2 image XObjects present; the 305.6 KB JPEG accounts for nearly the whole 316,454 B file |

One incidental HIPAA-positive observation: the PDF references the uploaded file by its opaque UUID
(`becc4624-aef9-40f6-a0e8-5a0b8f39d74b`), **not** by the filename `16658.jpg`. That matches
CLAUDE.md's rule that filenames are themselves PHI.

**D-05a is CLOSED.** Both PHI renderers confirmed on the deployed app, every Part 7 field present
exactly once in each, with no duplication between the Test Results section and the symptom list.

### Part B — resume on the real storefront

| Step | Check | Verdict |
|---|---|---|
| 4 | Allergy testing is FIRST on the live storefront | ✅ **PASS** — first live evidence the 04.1 reorder shipped |
| 5 | Resume offer appears after navigating away and returning | ✅ **PASS** — offer prompted on return |
| 6 | Answers restored | ✅ **PASS** — covered by Andrew's "resume worked" and the wave-5 browser pass |
| 7 | In-flow Start over → confirm → Tennessee/Texas gate | ✅ **PASS** — the control and its confirm panel were signed off in 04.2-07 after three rounds of UI fixes |
| 8 | D-03 partitioned-storage degradation | **Not triggered** — the offer appeared, so the production path is not partitioned in Andrew's browser. No new row needed in 04.2-RESEARCH.md's D-03 table. |

Steps 6 and 7 rest on Andrew's overall "approved" plus the explicit wave-5 sign-off rather than on
separate per-step prose; step 5, the step that could have failed silently, was reported explicitly
and passed.

---

## Requirements closed

| ID | Status |
|---|---|
| RESUME-01 | Complete (2026-08-11, verified live) |
| RESUME-02 | Complete (2026-08-11, verified live) |
| RESUME-03 | Complete (2026-08-11, verified live) |
| RESUME-04 | Complete (2026-08-11, verified live) |

Phase 4.1's D-05a criterion: **CLOSED**.

## Threat register disposition

| Threat ID | Disposition |
|---|---|
| T-4.2-33 (deploy declared from an exit code) | **Mitigated** — pre/post cache-busted fetches, byte-length and SHA-256 equality against the committed artifact, marker counts via `split()` |
| T-4.2-34 (bundle silently losing 4.1's order) | **Mitigated** — `QUIZ_PARTS` element 0 = `testing_status` head, extracted from the served bytes |
| T-4.2-35 (D-05a quietly dropped) | **Mitigated** — carried forward by name, verified from artifacts, explicitly marked closed |
| T-4.2-36 (third-party JS on the PHI page) | **Transferred** — Phase 8 / LAUNCH-01, named as a stated exclusion, untouched |
| T-4.2-37 (real PHI in a production smoke) | **Mitigated** — synthetic values only (`dfafdas`, `fdafda`, `fdafdsa`) |
| T-4.2-SC (supply chain) | **Mitigated** — zero package installs; the empty `package.json`/`package-lock.json` diff was asserted before authorization |

## Live state after this plan

| | Before | After |
|---|---|---|
| `main` | `ea3dd26` | **`86e6b50`** |
| Fly release | v51 | **v52** |
| Shopify app version | alledrops-quiz-production-22 | **alledrops-quiz-production-23** |
| served bundle | 195,142 B, no resume, testing last | **201,707 B, resume live, testing first** |
| Tests | 558 / 37 files | **677 / 47 files** |

## Still open (not this phase)

- Appointly third-party JS on the PHI-collecting storefront page — Phase 8 / LAUNCH-01.
- No medical disclaimer on the storefront — Phase 8 / LAUNCH-01.
- Neither was changed here, and neither is made worse by this deploy.
