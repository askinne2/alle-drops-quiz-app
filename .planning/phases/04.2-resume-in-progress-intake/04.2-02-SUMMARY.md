---
phase: 04.2-resume-in-progress-intake
plan: 02
subsystem: quiz
tags: [vitest, typescript, pure-function-extraction, hipaa]

# Dependency graph
requires: []
provides:
  - "app/lib/quiz/payload.ts — pure buildSubmitPayload(input) plus RESUME_PARITY_EXCLUDED_FIELDS (exactly 3 members)"
  - "app/lib/quiz/payload.test.ts — unit-level D-10 parity proof, 8 tests"
affects: [04.2-04, 04.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure module extraction from a React useCallback (no React import, named exports, JSDoc per export) — same shape as scoring.ts"
    - "vi.useFakeTimers()/vi.setSystemTime() for proving time-derived-field exclusion in a parity test"

key-files:
  created:
    - app/lib/quiz/payload.ts
    - app/lib/quiz/payload.test.ts
  modified: []

key-decisions:
  - "buildSubmitPayload takes RAW answers and applies visibleAnswers(ALL_ITEMS, answers) internally — the D-03 filter lives on exactly one side of the function boundary so a future resumed-path caller cannot apply it differently"
  - "RESUME_PARITY_EXCLUDED_FIELDS declared as a readonly 3-tuple (as const) and asserted by test to have exactly 3 members in the exact order — closes the list against silent widening"

patterns-established:
  - "Pure-module unit test header comment states which ROADMAP criterion/decision ID it pins (house style from scoring.test.ts)"

requirements-completed: [RESUME-04]

# Metrics
duration: ~20min
completed: 2026-08-11
---

# Phase 04.2 Plan 02: Extract buildSubmitPayload Summary

**Extracted `QuizContainer.tsx`'s inline `buildPayload` into a pure, unit-testable `app/lib/quiz/payload.ts`, and proved D-10 parity (identical payload/score across differing time inputs) with 8 unit tests, locking the resume-exclusion list to exactly 3 named fields.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-11 (session start)
- **Completed:** 2026-08-11T09:10:33Z
- **Tasks:** 2 completed
- **Files modified:** 2 (both new)

## Accomplishments
- `app/lib/quiz/payload.ts` — pure `buildSubmitPayload(input)` reproducing `QuizContainer.tsx:114-136`'s behavior verbatim (same throw guard, same `visibleAnswers` filter, same `??` score/bracket fallback, same twelve output keys), plus the documented, closed `RESUME_PARITY_EXCLUDED_FIELDS` constant
- `app/lib/quiz/payload.test.ts` — 8 tests pinning ROADMAP success criterion 5 / D-10 at the unit level: parity across differing time inputs, non-vacuity of the exclusion, score-recomputed-never-trusted (including a documented pass-through case for a deliberately wrong pre-set score), hidden-answer filtering across an open and a closed `showIf` gate, the exclusion-list-cannot-grow guard, and the missing-patient-context throw for both `patientState` and `symptomProfileId`
- `QuizContainer.tsx` left completely untouched, as required — verified with `git diff` against the plan's base commit

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract buildSubmitPayload into a pure module** - `eef4092` (feat)
2. **Task 2: Unit-prove D-10 parity and lock the exclusion list** - `c4f1ce9` (test)

**Plan metadata:** (this commit, see below)

## Files Created/Modified
- `app/lib/quiz/payload.ts` - Pure `buildSubmitPayload`, `SubmitPayload`/`SubmitPayloadInput` types, `RESUME_PARITY_EXCLUDED_FIELDS`
- `app/lib/quiz/payload.test.ts` - 8 unit tests proving D-10 parity, score recomputation, hidden-answer filtering, and the closed exclusion list

## Decisions Made
- `buildSubmitPayload` takes raw `answers` (not pre-filtered) and applies `visibleAnswers(ALL_ITEMS, ...)` internally, matching the plan's interface contract exactly — this is the single construction site both the one-sitting and (future) resumed paths will call, so no second implementation can diverge (mitigates T-4.2-05).
- Test fixture uses real question IDs read from `questions.ts`: `symptoms_nasal` (scored checkbox_multi), `severity_nasal_congestion` (scored severity_0_3), `med_list` (showIf-gated, closed — `taking_meds: "no"`), and the Part 7 `testing_status`/`testing_year` pair (showIf-gated, open — `testing_status: "had_testing"`), per the plan's requirement to avoid a vacuous filtering test.

## Deviations from Plan

None — plan executed exactly as written. One in-flight fix during Task 2 authoring: an initial `import * as vi from "vitest"` plus a stray `vi.vi.*` typo (introduced while drafting, never committed) was corrected to the standard `import { vi } from "vitest"` before the typecheck/test verification gates ran; this was pre-commit drafting cleanup, not a deviation from the plan's design.

## Issues Encountered
- `npm run typecheck`'s PostToolUse hook initially flagged a type error on `stripExcluded(payload: Record<string, unknown>)` receiving a `SubmitPayload` argument (no index signature). Fixed by typing the helper's parameter as `SubmitPayload` and copying into a `Record<string, unknown>` internally before deleting excluded keys. Verified clean on the next `npm run typecheck` run before proceeding to test execution.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `app/lib/quiz/payload.ts` exports the exact interface (`buildSubmitPayload`, `RESUME_PARITY_EXCLUDED_FIELDS`, `SubmitPayload`, `SubmitPayloadInput`) that plan 04.2-04 needs to rewire `QuizContainer.tsx`'s `buildPayload` `useCallback` into a thin wrapper, and that plan 04.2-06 needs for its DOM-level parity test.
- `npm test`: 610/610 passing (40 files, up from the 602/39 baseline — +8 tests, +1 file). `npm run typecheck`: clean.
- No blockers. `QuizContainer.tsx` remains completely unmodified by this plan, confirmed via `git diff` against the plan's base commit (`ba0c78b`).

---
*Phase: 04.2-resume-in-progress-intake*
*Completed: 2026-08-11*
