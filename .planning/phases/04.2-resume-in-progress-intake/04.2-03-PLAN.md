---
phase: 04.2-resume-in-progress-intake
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/quiz/ResumeOffer.tsx
  - app/styles/quiz.module.css
  - tests/quiz-resume-offer-dom.test.ts
autonomous: true
requirements: [RESUME-01, RESUME-03]

must_haves:
  truths:
    - "The resume offer screen renders zero identity — no name, DOB, email, phone, initials, avatar, or elapsed time (D-06)"
    - "Clicking Start over never clears anything directly; it opens a confirmation whose safe option is rendered first and holds focus (D-08)"
    - "The control is called 'Start over' verbatim in both the offer and the in-flow control"
    - "A patient who resumes sees a one-line, identity-free notice explaining their answers were restored"
  artifacts:
    - path: "app/components/quiz/ResumeOffer.tsx"
      provides: "ResumeOffer, StartOverControl, RestorationNotice — every new patient-facing surface in this phase"
      min_lines: 120
      exports: ["ResumeOffer", "StartOverControl", "RestorationNotice"]
    - path: "app/styles/quiz.module.css"
      provides: "quizStartOverRow / quizStartOver / quizStartOver__confirm / quizStartOver__confirmButton"
      contains: "quizStartOver"
    - path: "tests/quiz-resume-offer-dom.test.ts"
      provides: "DOM proof of zero-identity copy, the confirm gate, focus placement, and the locked terminology"
      min_lines: 130
  key_links:
    - from: "app/components/quiz/ResumeOffer.tsx"
      to: "app/styles/quiz.module.css"
      via: "CSS Modules class reuse — questionCard, questionCategory__title, questionCard__subtitle, questionCard__optionsVertical, quizNavigation__buttonNext/Prev, infoBlockCard family"
      pattern: "styles\\.questionCard"
---

<objective>
Build every new patient-facing surface this phase adds, as presentational components with no
knowledge of storage: the resume offer, the persistent in-flow "Start over" control, the shared
two-step confirmation, and the post-resume restoration notice.

Purpose: these components carry the D-06 (zero identity) and D-08 (confirmed destructive action)
mitigations that make D-04's full-PHI draft defensible. Keeping them free of any draft-store import
means their guarantees can be proven by rendering alone.

Output: `ResumeOffer.tsx`, four new CSS classes, and a DOM test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-resume-in-progress-intake/04.2-UI-SPEC.md
@.planning/phases/04.2-resume-in-progress-intake/04.2-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan produces. Plans 04.2-04 and 04.2-05 consume these exact names. -->

app/components/quiz/ResumeOffer.tsx exports:

  interface ResumeOfferProps { onResume: () => void; onStartOver: () => void }
  function ResumeOffer(props: ResumeOfferProps): JSX.Element

  interface StartOverControlProps { onStartOver: () => void }
  function StartOverControl(props: StartOverControlProps): JSX.Element

  function RestorationNotice(): JSX.Element

NOTE: none of these props carries identity. That absence is the D-06 mechanism — do not add a
`patientName`, `savedAt`, `answeredCount`, or any similar prop, even "for later".

Existing CSS Modules classes reused verbatim (all already declared in app/styles/quiz.module.css):
  .questionCard, .questionCategory__title, .questionCard__subtitle,
  .questionCard__optionsVertical,
  .quizNavigation__button, .quizNavigation__buttonNext, .quizNavigation__buttonPrev,
  .infoBlockCard, .infoBlockCard__icon, .infoBlockCard__paragraph,
  .fileUpload__remove  (the 44px-tap-target formula to copy for .quizStartOver)

New CSS Modules classes this plan adds:
  .quizStartOverRow, .quizStartOver, .quizStartOver__confirm, .quizStartOver__confirmButton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ResumeOffer, StartOverControl, RestorationNotice and the shared confirm panel</name>
  <read_first>
    - app/components/quiz/StateGate.tsx (whole file — the exact structural analog: .questionCard wrapper, h2 with .questionCategory__title, p with .questionCard__subtitle, .questionCard__optionsVertical stack of `<button type="button">` children)
    - app/components/quiz/QuizPartRenderer.tsx lines 245-300 (the internal `InfoBlockCard` component: `role="note"`, the inline info-icon SVG, `.infoBlockCard__paragraph` — RestorationNotice must reproduce this visual family; InfoBlockCard is NOT exported, so the markup is reproduced, not imported)
    - app/components/quiz/QuizContainer.tsx lines 258-330 (how .quizNavigation__buttonNext / .quizNavigation__buttonPrev are actually applied together with .quizNavigation__button)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-UI-SPEC.md § "Copywriting Contract" and § "Component Inventory" §1, §2, §3, §5 (the approved contract — copy strings are taken from here verbatim)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-PATTERNS.md § "app/components/quiz/ResumeOffer.tsx"
  </read_first>
  <files>app/components/quiz/ResumeOffer.tsx, app/styles/quiz.module.css</files>
  <action>
Create `app/components/quiz/ResumeOffer.tsx`. It imports only `useState`, `useRef`, `useEffect` from
react and `styles` from `../../styles/quiz.module.css`. It must NOT import `draft-store`,
`questions`, `schema`, or anything from `app/lib/quiz/` — these are presentational only.

Shared internal component `StartOverConfirmPanel({ heading, body, dismissLabel, onConfirm, onDismiss })`:
- Container `<div aria-live="polite">`. Explicitly do NOT set `role="dialog"` or `role="alertdialog"`
  anywhere — UI-SPEC §3 rejects those roles because this inline pattern has no focus trap and no
  Escape handling, and claiming the role without the behavior is worse than omitting it.
- Heading rendered with `styles.questionCategory__title`, body with `styles.questionCard__subtitle`.
- Two `<button type="button">` children in this order: the dismiss button FIRST, using
  `${styles.quizNavigation__button} ${styles.quizNavigation__buttonPrev}`, labelled by the
  `dismissLabel` prop; then the confirm button, using
  `${styles.quizNavigation__button} ${styles.quizStartOver__confirmButton}`, labelled exactly
  `Yes, start over`.
- The dismiss button receives programmatic focus when the panel mounts: a `useRef<HTMLButtonElement>`
  plus a `useEffect(() => { ref.current?.focus(); }, [])`. This is a blocking checker requirement,
  not polish — a patient who reflexively presses Enter again after tapping "Start over" must land on
  the safe option.

`ResumeOffer({ onResume, onStartOver })` — UI-SPEC Component Inventory §1 and §3 (resume-offer
variant). A `<div className={styles.questionCard}>` containing an `<h2 className={styles.questionCategory__title}>`
reading exactly `You have an unfinished assessment from earlier.` and a
`<p className={styles.questionCard__subtitle}>` reading exactly `Resume where you left off, or start over?`.
Those two lines stay visible in both states. Below them, a local `showConfirm` boolean selects
between:
  - the resting state: `<div className={styles.questionCard__optionsVertical}>` holding two
    `<button type="button">` children — first `Resume` with
    `${styles.quizNavigation__button} ${styles.quizNavigation__buttonNext}` (primary/accent, because
    the whole point of the phase is that most patients who see this screen intend to finish), then
    `Start over` with `${styles.quizNavigation__button} ${styles.quizNavigation__buttonPrev}`.
    `Resume` calls `onResume`; `Start over` sets `showConfirm` true and calls nothing else.
  - the confirm state: `StartOverConfirmPanel` with heading `Start over and lose your saved answers?`,
    body `This clears everything you entered earlier. This can't be undone.`, dismiss label
    `Keep my answers`, `onConfirm={onStartOver}`, and `onDismiss` setting `showConfirm` false.
No progress bar, no spinner, no skeleton, no third option on this screen.

`StartOverControl({ onStartOver })` — UI-SPEC §2 and §3 (in-flow variant). A
`<div className={styles.quizStartOverRow}>` containing a `<button type="button" className={styles.quizStartOver}>`
whose visible label is exactly `Start over`, preceded by a 16x16 inline restart/circular-arrow
`<svg aria-hidden="true">` authored in-repo in the same style as the existing info-block and
file-upload icons (no icon font, no sprite, no remote asset — this is a PHI-collecting page). Clicking
it opens `StartOverConfirmPanel` rendered directly below the row inside
`<div className={styles.quizStartOver__confirm}>`, with heading `Start over and lose your progress?`,
body `This clears everything you've entered in this assessment so far. This can't be undone.`,
dismiss label `Keep my progress`, `onConfirm={onStartOver}`.

`RestorationNotice()` — UI-SPEC §5. Reproduce `InfoBlockCard`'s markup family: a
`<div className={styles.infoBlockCard} role="note">` with the same inline info-icon SVG wrapped in
`<div className={styles.infoBlockCard__icon} aria-hidden="true">`, and one
`<p className={styles.infoBlockCard__paragraph}>` reading exactly
`Your previous answers have been restored.` No heading, no bullets, no count, no timestamp, no name.
Takes no props.

The literal string `Start over` is locked terminology (D-08 / ROADMAP criterion 3) and must read
identically on the offer button and the in-flow control. Do not substitute Reset, Clear, Restart, or
Start fresh. The dismiss labels are `Keep my answers` / `Keep my progress`; the literal `Cancel` must
not appear anywhere in this file.

Then append four rules to `app/styles/quiz.module.css`, placed after the existing
`.quizNavigation__*` block:
- `.quizStartOverRow` — `display: flex; justify-content: flex-end; margin-top: var(--quiz-spacing-xs);`
- `.quizStartOver` — copy `.fileUpload__remove`'s formula: transparent background, no border,
  `min-height: 44px`, padding using `var(--quiz-spacing-xs)` / `var(--quiz-spacing-sm)`,
  `display: inline-flex; align-items: center; gap: var(--quiz-spacing-xs);`, `cursor: pointer`,
  `border-radius: var(--quiz-border-radius)`, resting color `rgba(var(--color-foreground, 32, 34, 35), 0.6)`,
  and on `:hover` / `:focus-visible` full-opacity `rgb(var(--color-foreground, 32, 34, 35))` plus
  `text-decoration: underline`. Give `:focus-visible` the same `outline: 2px solid #2563eb; outline-offset: 2px;`
  `.fileUpload__remove` already uses.
- `.quizStartOver__confirm` — a width-constrained inline panel: `.questionCard`-style
  `padding: var(--quiz-spacing-md)`, `border: 1px solid rgba(var(--color-foreground, 32, 34, 35), 0.15)`,
  `border-radius: var(--quiz-border-radius)`, `margin-top: var(--quiz-spacing-sm)`.
- `.quizStartOver__confirmButton` — copy `.quizNavigation__buttonPrev`'s box model verbatim
  (flex, same padding, same font-size/weight/family, same radius, `min-height: 44px`) and change only
  two properties: `border-color: var(--quiz-color-error)` and `color: var(--quiz-color-error)`, with
  `background-color: transparent`. Do not fill it — a filled red button overstates the severity of a
  patient-initiated, fully intentional action.
Use only existing spacing tokens; declare no new custom property and no new color.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && node -e 'const fs=require("fs"); const s=fs.readFileSync("app/components/quiz/ResumeOffer.tsx","utf8"); const css=fs.readFileSync("app/styles/quiz.module.css","utf8"); const c=(h,n)=>h.split(n).length-1; const must=["You have an unfinished assessment from earlier.","Resume where you left off, or start over?","Start over and lose your saved answers?","This clears everything you entered earlier. This can't be undone.","Start over and lose your progress?","Keep my answers","Keep my progress","Yes, start over","Your previous answers have been restored."]; for (const m of must) if (c(s,m)===0) throw new Error("missing locked copy: "+m); if (c(s,"Cancel")!==0) throw new Error("forbidden label Cancel present"); if (c(s,"role=\"dialog\"")!==0||c(s,"alertdialog")!==0) throw new Error("dialog role present without a focus trap"); if (c(s,"aria-live=\"polite\"")===0) throw new Error("confirm panel missing aria-live"); if (c(s,"lib/quiz")!==0) throw new Error("presentational component imports quiz lib"); for (const k of [".quizStartOverRow",".quizStartOver ",".quizStartOver__confirm",".quizStartOver__confirmButton"]) if (c(css,k)===0) throw new Error("missing css rule: "+k); console.log("ResumeOffer source gates OK");'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `ResumeOffer.tsx` exports `ResumeOffer`, `StartOverControl`, and `RestorationNotice`
    - All nine locked copy strings from UI-SPEC's Copywriting Contract appear verbatim
    - Zero occurrences of `Cancel`, `Reset`, `Restart`, `Start fresh` in the file
    - Zero occurrences of `role="dialog"` or `alertdialog`; the confirm panel container carries `aria-live="polite"`
    - The dismiss button is rendered before the confirm button in the JSX of `StartOverConfirmPanel`, and a `useEffect` focuses it on mount
    - `ResumeOfferProps` and `StartOverControlProps` contain only callback props — no identity or timestamp field
    - The file imports nothing from `app/lib/quiz/`
    - `app/styles/quiz.module.css` gains exactly the four new class rules and declares no new custom property or literal color other than reuse of `var(--quiz-color-error)` and the existing `#2563eb` focus outline
  </acceptance_criteria>
  <done>Every new patient-facing surface exists as a presentational component with the approved copy, the confirm gate, and no identity props.</done>
</task>

<task type="auto">
  <name>Task 2: DOM-prove zero identity, the confirm gate, and focus placement</name>
  <read_first>
    - app/components/quiz/ResumeOffer.tsx (Task 1's output)
    - tests/quiz-part-renderer-dom.test.ts lines 1-80 (the jsdom pragma, `React.createElement` construction, `cleanup()` in afterEach, and the `describe`-opens-with-a-requirement-ID comment house style)
    - tests/quiz-file-upload-dom.test.ts lines 1-120 (the same conventions plus `within`/`waitFor` usage)
    - .planning/phases/04.2-resume-in-progress-intake/04.2-UI-SPEC.md § "Interaction Contract Summary"
  </read_first>
  <files>tests/quiz-resume-offer-dom.test.ts</files>
  <action>
Create `tests/quiz-resume-offer-dom.test.ts`. Literal first line `// @vitest-environment jsdom`.
Extension `.test.ts`, never `.test.tsx`. Build all elements with `React.createElement`, never JSX —
vitest's include glob does not match `.tsx` and this has cost this project time before. `cleanup()`
in `afterEach`. Each `describe` opens with a one-line comment naming the requirement it proves
(`RESUME-01`, `RESUME-03`, `D-06`, `D-08`), matching the house style in the two files above.

RESUME-01 / D-06 — zero identity. Render `ResumeOffer` with `vi.fn()` callbacks. Assert the
container's `textContent`, after collapsing whitespace, is exactly the concatenation of the four
locked strings (`You have an unfinished assessment from earlier.`, `Resume where you left off, or
start over?`, `Resume`, `Start over`) and nothing else. This closed-world assertion is stronger than
a list of absences, but ALSO add the pattern absences so the intent is legible to a future reader:
no `@` character, no `/\d{4}-\d{2}-\d{2}/` match, no `/\d+\s*(second|minute|hour|day)/i` match, and
no `<input>` element in the tree.

RESUME-01 — the two actions. `Resume` calls `onResume` once and never calls `onStartOver`.

RESUME-03 / D-08 — the confirm gate. Clicking `Start over` must NOT call `onStartOver`. Assert the
panel then shows the exact confirm heading and body for the resume-offer variant, that a button
labelled `Keep my answers` exists, that a button labelled `Yes, start over` exists, and that the
document contains zero elements whose accessible name is `Cancel`.

RESUME-03 / D-08 — focus. Immediately after the panel opens, `document.activeElement` is the
`Keep my answers` button, not `Yes, start over`.

RESUME-03 — dismiss is inert. Clicking `Keep my answers` collapses the panel back to the two-button
row (`Resume` visible again), and `onStartOver` has still never been called.

RESUME-03 — confirm fires once. Re-open the panel and click `Yes, start over`; `onStartOver` is
called exactly once.

RESUME-03 — in-flow variant. Render `StartOverControl`, assert the visible control label is exactly
`Start over` (the locked term, identical to the offer's secondary button), that its icon `svg`
carries `aria-hidden="true"`, that clicking it does not call `onStartOver`, that the panel's heading
and body are the in-flow strings, and that its dismiss label is `Keep my progress`.

Accessibility contract — assert the rendered tree contains zero elements with
`role="dialog"`/`role="alertdialog"` and that the confirm panel's container carries
`aria-live="polite"`.

RestorationNotice — render it and assert `textContent` is exactly
`Your previous answers have been restored.`, that the wrapper carries `role="note"`, and that it
contains no `@`, no date pattern, and no digit sequence at all (a count of restored answers would be
a D-06 leak by degrees).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run tests/quiz-resume-offer-dom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-resume-offer-dom.test.ts` passes with at least 9 tests
    - Literal first line is `// @vitest-environment jsdom`; no JSX anywhere in the file
    - The zero-identity test asserts an exact whitespace-collapsed `textContent` equality, not only absences
    - A test asserts `document.activeElement` is the `Keep my answers` button right after the panel opens
    - A test asserts `onStartOver` is not called by the first `Start over` click and is called exactly once by `Yes, start over`
    - A test asserts zero `role="dialog"` / `role="alertdialog"` elements and one `aria-live="polite"` container
    - The in-flow variant's distinct copy (`Start over and lose your progress?`, `Keep my progress`) is asserted separately from the offer variant's
    - `npm test` green
  </acceptance_criteria>
  <done>D-06's zero-identity promise and D-08's confirm gate are proven by rendering, not by reading the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored draft → pixels on a shared device | The resume offer renders before anyone has proven they are the patient. Whatever it shows is visible to the next person who opens that browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.2-08 | Information Disclosure | The resume-offer screen on a shared or family device | mitigate | `ResumeOfferProps` carries no identity field, so there is nothing to leak by construction; the DOM test asserts the rendered `textContent` is exactly the four locked strings and matches no email, date, or elapsed-time pattern (D-06) |
| T-4.2-09 | Denial of Service (patient data) | A mis-tap destroying a half-finished ten-minute clinical questionnaire | mitigate | Two-step confirmation; the dismiss action is rendered first and receives programmatic focus on open, so a reflexive second Enter lands on the safe option (D-08, UI-SPEC §3) |
| T-4.2-10 | Spoofing | An inline panel claiming a dialog contract it does not fulfil | mitigate | No `role="dialog"` / `role="alertdialog"`; `aria-live="polite"` plus programmatic focus is the announced-and-operable pattern actually implemented, asserted by test |
| T-4.2-11 | Information Disclosure | Third-party assets on a PHI-collecting page | mitigate | The one new icon is an inline in-repo SVG. No icon font, no sprite sheet, no remote font, no remote CSS, no new dependency — CLAUDE.md rule 4 |
| T-4.2-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this plan |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npx vitest run tests/quiz-resume-offer-dom.test.ts` green
- `npm test` green
- `git diff --name-only` lists only the three files in `files_modified`
</verification>

<success_criteria>
`ResumeOffer.tsx` renders the approved copy exactly, leaks zero identity, gates the destructive
action behind a confirmation focused on the safe option, and does so with zero new colors, zero new
dependencies, and zero remote assets.
</success_criteria>

<output>
Create `.planning/phases/04.2-resume-in-progress-intake/04.2-03-SUMMARY.md` when done.
</output>
