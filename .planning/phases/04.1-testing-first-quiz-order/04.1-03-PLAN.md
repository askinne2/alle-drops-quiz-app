---
phase: 04.1-testing-first-quiz-order
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - docs/gcs-lifecycle-and-retention.md
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "D-01: DEC-medical-history-before-testing-split is amended in place with its clinical rationale intact; D-03: PENDING_OLM_AGE_DAYS stays 2 and only the analysis is written; D-04: any future value change is applied AND verified by gcloud read-back against real probe objects"
    - "Anyone who read DEC-medical-history-before-testing-split sees that its position clause changed, why, and that its clinical rationale survives unchanged"
    - "The original decision text is still visible — nothing was deleted"
    - "The retention doc records that abandonment now stages an orphaned PHI object as the common case, not the rare one"
    - "PENDING_OLM_AGE_DAYS is still 2 and no live GCS lifecycle rule was touched by this phase"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "DEC-medical-history-before-testing-split amended in place, dated, sourced to 04.1-CONTEXT.md D-01"
      contains: "AMENDED 2026-08-10"
    - path: "docs/gcs-lifecycle-and-retention.md"
      provides: "Phase 4.1 orphan-volume analysis, deferring the value decision to Phase 4.2"
      contains: "PENDING_OLM_AGE_DAYS"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md"
      via: "Source: line citing D-01"
      pattern: "04\\.1-CONTEXT\\.md"
    - from: "docs/gcs-lifecycle-and-retention.md"
      to: ".planning/ROADMAP.md Phase 04.2"
      via: "explicit deferral of the value decision"
      pattern: "Phase 4\\.2"
---

<objective>
Two documentation obligations this phase owes, neither of which changes code or live infrastructure:
amend `DEC-medical-history-before-testing-split` in place so its superseded position clause is
visible as superseded (D-01), and record in the retention doc why orphaned staged PHI becomes the
common case after the reorder — while explicitly NOT changing `PENDING_OLM_AGE_DAYS` (D-03).

Purpose: the amendment keeps the decision record honest for anyone who read the original; the
retention analysis hands Phase 4.2 the reasoning it needs to pick the right window without repeating
the measurement.

Output: two edited documents. Zero code, zero DDL, zero `gcloud` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-PATTERNS.md
@docs/gcs-lifecycle-and-retention.md

<interfaces>
<!-- Exact source locations, measured 2026-08-10. -->

`.planning/PROJECT.md` `:240-249` — the block to amend. Opening tag today:
`id="DEC-medical-history-before-testing-split"`, `status="LOCKED"`, `date="2026-07-29"`,
`source="docs/REQUIREMENTS-AND-GAPS-2026-07-29.md (R3, CALL OVERRIDE — position)"`.
Its body contains, in order: the position clause ("The medical history section moves before the
allergy-testing split, into the main `QUIZ_PARTS` array."), the every-patient clause, the
must-not-affect-score clause, the `Rationale:` sentence ("even a patient who books a consult
directly still needs history on file for Dr. Sullivan."), and a `Hard sequencing consequence:`
paragraph about `setStep("medical_history")` (`QuizContainer.tsx:243`) being reachable only through
the proceed-without-testing flow.

`.planning/PROJECT.md` `:251-269` — `DEC-testing-results-by-email-not-upload`, the convention to
copy. Structure, in order: (1) original body wrapped in `~~` strikethrough markers, (2) a bolded
dated lead-in `**RETRACTED 2026-08-09.**`, (3) plain-language what-and-why, (4) a `Source:` line
citing the superseding CONTEXT.md and its decision ID. Its opening tag carries
`status="RETRACTED"` plus `retracted="2026-08-09"` and `retracted_by="04-CONTEXT.md D-01"`.

`docs/gcs-lifecycle-and-retention.md` — measured anchors:
- `:13`      `- Orphan-cleanup age: PENDING_OLM_AGE_DAYS = 2 (04-UPLOAD-DECISIONS.md §Ratified)`
- `:75-86`   `### Timing caveat — OLM evaluation is not real-time` (roughly daily, not continuous;
             a same-session write-then-check cannot observe deletion)
- `:114-134` `## Orphan sources this rule cleans up` — the numbered "distinct patient paths ->
             shared consequence" structure to mirror. Case 1 is abandonment before submit; case 2
             is a `had_testing` -> `needs_testing` flip before submit.
- `:168-181` `## Cutover note — AOD's own GCP project`, which states the exact rule shape
             `condition: { age: PENDING_OLM_AGE_DAYS, matchesPrefix: ["pending/"] }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend DEC-medical-history-before-testing-split in place (D-01)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 236-272 — the target decision block AND the
      `DEC-testing-results-by-email-not-upload` block immediately below it, whose formatting is the
      convention being copied
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-01 in full
    - `.planning/phases/03-mandatory-medical-history/03-CONTEXT.md` D-11 and D-13 — the two facts
      that make the amendment defensible; cite them by ID
  </read_first>
  <action>
    Amend `DEC-medical-history-before-testing-split` in place. Do not delete the original text. This
    is an AMENDMENT, not a retraction: only the position clause is superseded, and the clinical
    rationale survives verbatim.

    Opening tag: keep `id`, `date`, and `source` unchanged. Change `status="LOCKED"` to
    `status="AMENDED"` and add `amended="2026-08-10"` and `amended_by="04.1-CONTEXT.md D-01"`,
    mirroring the `retracted` / `retracted_by` attribute pattern on the sibling block.

    Body: wrap in `~~` strikethrough ONLY the superseded material — the position clause and the
    entire `Hard sequencing consequence:` paragraph. Leave the every-patient clause, the
    must-not-affect-score clause, and the `Rationale:` sentence unstruck and visually intact,
    because they still hold.

    Append a dated amendment paragraph in the sibling block's voice, opening with a bolded
    `**AMENDED 2026-08-10.**` lead-in — the word AMENDED, not RETRACTED. It must state plainly:
    (a) Phase 4.1 moves the allergy-testing split to the front of `QUIZ_PARTS`, so medical history
    now sits last, immediately before consent, superseding the position clause; (b) the clinical
    rationale survives unchanged, because "even a patient who books a consult directly still needs
    history on file for Dr. Sullivan" is a statement about every patient REACHING medical history,
    and Part 6 is still in `QUIZ_PARTS` and still reached by 100% of patients regardless of where
    the testing split sits — cite `03-CONTEXT.md D-13`; (c) the struck `Hard sequencing
    consequence:` paragraph is obsolete because Phase 3's D-11 deleted the proceed-without-testing
    bypass that made `setStep("medical_history")` the only entry point, so it now protects against a
    failure mode that no longer exists; (d) why the reorder happened at all, in one sentence — a
    patient who cannot supply allergy test results should hit that wall in seconds, not after a
    completed ten-minute clinical intake. Close with a `Source:` line citing
    `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-01.

    Touch no other decision block. Do not edit `.planning/STATE.md` or `.planning/ROADMAP.md` — the
    orchestrator owns those.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf-8');const c=(n)=>s.split(n).length-1;const need=['AMENDED 2026-08-10','04.1-CONTEXT.md','status=\"AMENDED\"','amended_by=','still needs history on file for Dr. Sullivan','03-CONTEXT.md D-13','D-11'];let ok=true;for(const n of need){const g=c(n);console.log(g>=1?'ok':'FAIL',n,g);if(g<1)ok=false}process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 — every listed needle occurs at least once in `.planning/PROJECT.md`
    - The decision's opening tag contains `status="AMENDED"`, `amended="2026-08-10"`, and
      `amended_by="04.1-CONTEXT.md D-01"`; `id`, `date`, and `source` are byte-identical to HEAD
    - The literal sentence `even a patient who books a consult directly still needs history on file
      for Dr. Sullivan.` is present and NOT inside `~~` markers
    - The literal phrase `The medical history section moves before the allergy-testing split` is
      still present in the file (nothing deleted) and IS inside `~~` markers
    - The word `RETRACTED` does not appear anywhere inside the
      `DEC-medical-history-before-testing-split` block
    - `git diff --stat .planning/PROJECT.md` shows exactly one file changed and the diff hunk is
      confined to that decision block — no other `<decision>` id appears in the diff
      (`git diff .planning/PROJECT.md | grep -c 'decision id=' ` outputs `1`)
  </acceptance_criteria>
  <done>The decision is amended in place, dated, sourced, with the original visible and the rationale unstruck.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Phase 4.1 orphan-volume analysis without changing PENDING_OLM_AGE_DAYS (D-03)</name>
  <files>docs/gcs-lifecycle-and-retention.md</files>
  <read_first>
    - `docs/gcs-lifecycle-and-retention.md` lines 1-20, 70-90, 110-140, and 168-181
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-03 and D-04
      in full, including the "Constraint the planner must not lose" box
    - `.planning/ROADMAP.md` § "Phase 04.2: Resume In-Progress Intake" — the paragraph beginning
      "Interaction with Phase 4.1", which states a resuming patient on day 3 finds their upload gone
  </read_first>
  <action>
    Add one new `##` section to `docs/gcs-lifecycle-and-retention.md`, placed immediately after the
    existing `## Orphan sources this rule cleans up` section (which ends around `:134`, before its
    `---` separator and the reconciliation-query section). Title it to name the change plainly, e.g.
    `## Phase 4.1 impact — abandonment becomes the common case, not the rare one`. Mirror that
    section's voice: state the mechanism, then its consequence, in the same measured register.

    Content, all four points required:

    1. **The mechanism.** Phase 4.1 moves `PART7_ALLERGY_TESTING` to index 0 of `QUIZ_PARTS`. Before
       4.1, a patient had to complete six of seven parts before reaching the upload, so only a
       late-stage abandoner could leave a staged `pending/` object. After 4.1 the upload is the very
       first quiz part, so case 1 above ("Abandonment before submit") is reachable from *any*
       abandonment point. Orphaned PHI at rest under `pending/` becomes the common case rather than
       the rare one. Case 2 (the `had_testing` -> `needs_testing` flip) is unchanged in kind but is
       now also reachable earlier.

    2. **What is NOT changing.** State explicitly that `PENDING_OLM_AGE_DAYS` remains `2`, that the
       live rule on the dev bucket is untouched by Phase 4.1, and that no `gcloud` mutation was run.
       Cross-reference `:13` (the current value) and the `## Cutover note` section's statement of the
       exact rule shape.

    3. **Why the value decision is deferred, and to whom.** Phase 4.2 (Resume In-Progress Intake)
       owns it, because the correct window depends on how long a resuming patient must still be able
       to find their staged file — a shorter window buys strictly less PHI at rest but breaks resume
       for a patient who returns on day 3. The two phases set this value together; do not tune it in
       isolation. Per D-04, whichever phase changes the value must APPLY the rule and VERIFY it using
       plan 04-17's pattern — apply, read the rule back with `gcloud`, then prove the prefix scoping
       against real probe objects. Never a doc-only change, never deferred to a human to run later,
       never asserted from an exit code.

    4. **The GCS mechanism constraint any 4.2 proposal must respect.** OLM expresses `age` in whole
       integer days and evaluates asynchronously — roughly once per day, with up to about 24 hours of
       latency after a condition is met. `age: 1` therefore means "deleted somewhere between 24 and
       48 hours," and sub-day windows are not expressible through OLM at all. An hours-scale window
       needs a different mechanism entirely — a scheduled deletion job or signed-URL expiry — which
       is a new design, not a config tweak. Cross-reference the existing
       `### Timing caveat — OLM evaluation is not real-time` section rather than restating it in full.

    Change no existing line in this file. Add no new rule, run no `gcloud` command, and do not edit
    the `PENDING_OLM_AGE_DAYS` value at `:13` or anywhere else.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/gcs-lifecycle-and-retention.md','utf-8');const c=(n)=>s.split(n).length-1;const need=['Phase 4.1','Phase 4.2','PENDING_OLM_AGE_DAYS','whole','asynchron'];let ok=true;for(const n of need){const g=c(n);console.log(g>=1?'ok':'FAIL',n,g);if(g<1)ok=false}const v=c('PENDING_OLM_AGE_DAYS = 2');console.log('value-still-2 occurrences',v);if(v<1)ok=false;process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0
    - `git diff docs/gcs-lifecycle-and-retention.md` shows ADDED lines only — zero removed lines
      (`git diff docs/gcs-lifecycle-and-retention.md | grep -c '^-[^-]'` outputs `0`)
    - The literal string `PENDING_OLM_AGE_DAYS = 2` is still present at the file's bullet list
    - The new section names Phase 4.2 as the owner of the value decision and cites D-04's
      apply-then-verify requirement
    - The new section states that OLM `age` is whole days and evaluation is asynchronous, and that
      sub-day windows are not expressible through OLM
    - No `gcloud` command was executed during this task — the SUMMARY must state this explicitly.
      `git status --porcelain` shows no file outside `files_modified` changed.
  </acceptance_criteria>
  <done>The analysis is recorded, the value is unchanged, the decision is explicitly handed to Phase 4.2 with D-04's verification requirement attached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> future implementer | A decision record that silently contradicts shipped behavior misleads whoever reads it next. This is the asset being protected. |
| `pending/` prefix in GCS | Staged PHI at rest. This plan describes its changing volume but does not touch it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-11 | Repudiation | `.planning/PROJECT.md` decision record | mitigate | The amendment is dated, attributed to `04.1-CONTEXT.md D-01`, and preserves the original text under `~~` markers. Task 1's acceptance criteria assert the original phrase is still present, so a silent rewrite fails the gate. |
| T-4.1-05 | Information disclosure | Orphaned staged PHI under `gs://…/pending/` | accept, documented | Volume increases materially after the reorder; the window stays at 2 days for this phase by D-03. The exposure is recorded in the retention doc with the reasoning Phase 4.2 needs. Accepted here because the dev bucket carries no real patient PHI (Blockers 2 and 3 still open) and because shortening the window without the resume design would break Phase 4.2. |
| T-4.1-12 | Tampering | Live GCS lifecycle configuration | mitigate | This plan runs zero `gcloud` mutations. Task 2's acceptance criteria require a diff of added-lines-only and an explicit SUMMARY statement that no `gcloud` command was executed. D-04 binds the phase that eventually changes the value to apply-and-verify, not doc-only. |
| T-4.1-SC | Tampering | npm/pip/cargo installs | n/a | **This plan installs zero packages** and touches no code. No legitimacy gate is owed. |
</threat_model>

<verification>
- Both verify commands exit 0
- `git status --porcelain` lists only `.planning/PROJECT.md` and `docs/gcs-lifecycle-and-retention.md`
- Zero lines removed from `docs/gcs-lifecycle-and-retention.md`
- No `gcloud` command executed anywhere in this plan
- `npm test` and `npm run typecheck` still exit 0 (no code was touched, so this is a no-op control
  proving nothing leaked into code)
</verification>

<success_criteria>
The amended decision shows what changed, why, and what survived, with the original still readable.
The retention doc explains the new orphan volume, keeps `PENDING_OLM_AGE_DAYS` at 2, hands the value
decision to Phase 4.2, and records the whole-days/asynchronous OLM constraint that rules out an
hours-scale window.
</success_criteria>

<output>
Create `.planning/phases/04.1-testing-first-quiz-order/04.1-03-SUMMARY.md` when done. It MUST state
explicitly that no `gcloud` command was executed and that `PENDING_OLM_AGE_DAYS` is still `2`.
</output>
</content>
