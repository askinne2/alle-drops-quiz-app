---
phase: 04.1-testing-first-quiz-order
plan: 06
type: execute
wave: 4
depends_on: [04.1-05]
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-04]

must_haves:
  truths:
    - "D-10: the full three-channel deploy (merge, fly deploy, shopify app deploy) is verified on served bytes not exit codes; D-05a: the admin presence-exactly-once check runs against the deployed surface"
    - "The reorder is live on the deployed Fly app, proven on served bytes rather than a deploy exit code"
    - "The served bundle is byte- and SHA-identical to the committed artifact, and different from the pre-deploy bundle"
    - "The deployed quiz serves Part 7 first, proven by the same order extraction used on the committed artifact"
    - "A provider looking at the deployed embedded admin still sees testing status, year, location, and allergens — exactly once each"
    - "The clinical PDF downloaded from the deployed app shows Part 7 answers once, under Test Results"
  artifacts:
    - path: ".planning/phases/04.1-testing-first-quiz-order/04.1-06-SUMMARY.md"
      provides: "Deploy record with before/after served-bytes measurements and the human admin verdict"
      min_lines: 40
  key_links:
    - from: "main"
      to: "https://alle-drops-quiz-app.fly.dev/quiz-bundle-js"
      via: "fly deploy -a alle-drops-quiz-app"
      pattern: "quiz-bundle-js"
    - from: "extensions/"
      to: "Shopify app version"
      via: "shopify app deploy"
      pattern: "shopify app deploy"
---

<objective>
Ship the reorder: merge, deploy all three channels, and verify on served bytes (D-10). Then have a
human confirm the two PHI renderer surfaces on the deployed app.

Purpose: the reorder only helps patients once it is live, and this project has been burned
repeatedly by treating an exit code as evidence. Every claim in this plan is measured against bytes
the internet actually served.

Output: a merged PR, a new Fly release, a new Shopify app version, and a recorded verification
table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-04-SUMMARY.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Deploy topology and the pre-deploy baseline, measured 2026-08-10. -->

Two separate deploy systems. `shopify app deploy` ships extensions and app config to Shopify and
does NOT deploy the Fly app. `fly deploy -a alle-drops-quiz-app` ships the web app. Both are needed.

Current live state before this plan runs:
- Fly app `alle-drops-quiz-app`, region `iad`, release **v51**
- Shopify app version **alledrops-quiz-production-22**
- Served `https://alle-drops-quiz-app.fly.dev/quiz-bundle-js` -> 200, **195142 bytes**,
  SHA-256 `a2be1ab758cab05aa1f1f81890412636ba8076aa52afd286e221bf64fde8762c` — byte-identical to the
  pre-4.1 committed artifact. (STATE.md's Phase 4 table records 195,102 for v51; the measured value
  is 195,142. Carry the correction, do not propagate the old number.)

Branch: `phase-4.1-testing-first-quiz-order`, already created and checked out. **Do not create,
rename, or switch branches.**

Scope of this change: zero migrations, zero DDL, zero schema change, zero new environment variable,
zero new Fly secret, zero GCS lifecycle change, zero package install. If any of those appears to be
needed, stop — it means something outside this phase's scope was introduced.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Open the PR and obtain merge + three-channel deploy authorization</name>
  <read_first>
    - All five prior SUMMARYs in `.planning/phases/04.1-testing-first-quiz-order/`
    - `CLAUDE.md` § "Development workflow" and § "Self-review checklist for PHI-handling changes"
  </read_first>
  <files></files>
  <action>
    Push `phase-4.1-testing-first-quiz-order`, open the PR with the description content described in
    `<context>` below, run and report the four gate commands with exact numbers, then present the
    three options and HALT. Do not merge and do not run any deploy command until Andrew names an
    option.
  </action>
  <decision>Authorize merging `phase-4.1-testing-first-quiz-order` to `main` and running the full three-channel deploy (Fly + Shopify + served-bytes verification).</decision>
  <context>
    CLAUDE.md's default is that Andrew reviews and merges, not Claude. That default has been
    overridden by explicit in-session authorization for PRs #16 through #20. This checkpoint asks
    for it again rather than assuming it.

    Before presenting, push the branch and open a PR whose description calls out, per CLAUDE.md's
    PR requirements: that two PHI-facing renderers changed (`app/lib/pdf.ts`,
    `app/routes/app.quiz-results.tsx`); that the admin change ADDS a Test Results section before
    filtering Symptom Responses, so no clinical field is dropped; that no route, auth check,
    ownership predicate, query, or logging statement changed; that no new dependency was added; and
    that there is no migration and no DDL. Run and report `npm run typecheck`, `npm test` (with the
    exact totals), `npm run build`, and `npm run build:theme` determinism.

    Also state plainly what is NOT covered pre-deploy: the admin detail modal has no rendered-DOM
    test, only a losslessness unit test on `partitionAnswers` plus source-text guards. Task 3 of
    this plan is the human check that closes it.
  </context>
  <options>
    <option id="merge-and-deploy">
      <name>Merge and run the full three-channel deploy now</name>
      <pros>Ships the abandonment fix immediately; matches D-10; the change carries no migration, no DDL, and no rollback complexity beyond a redeploy of the prior release</pros>
      <cons>Requires trusting the pre-deploy suite for the admin modal until Task 3's human check runs</cons>
    </option>
    <option id="review-first">
      <name>Andrew reviews and merges the PR himself, then Claude deploys</name>
      <pros>Matches CLAUDE.md's written default exactly</pros>
      <cons>Adds a round trip</cons>
    </option>
    <option id="hold">
      <name>Hold the merge and ship together with Phase 4.2</name>
      <pros>One deploy instead of two</pros>
      <cons>Rejected by D-10 — delays the abandonment fix by 1-2 days for no technical benefit</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A PR exists against `main` from `phase-4.1-testing-first-quiz-order` with the description
      content listed above; the PR URL is recorded in the SUMMARY
    - `npm run typecheck`, `npm test`, and `npm run build` results are reported with exact numbers,
      not characterized as "passing"
    - `git diff main...HEAD --stat -- package.json package-lock.json` produces no output
    - `git diff main...HEAD --name-only` contains no file under `migrations/`
    - Andrew's chosen option is recorded verbatim before any merge or deploy command runs
  </acceptance_criteria>
  <done>PR opened, gate results reported with exact numbers, and Andrew's chosen option recorded verbatim.</done>
  <verify><human-check>Andrew names one of merge-and-deploy, review-first, or hold.</human-check></verify>
  <resume-signal>Select: merge-and-deploy, review-first, or hold.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Three-channel deploy and served-bytes verification</name>
  <files></files>
  <read_first>
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-04-SUMMARY.md` (the committed artifact's
      post-rebuild byte size and SHA-256 — the values the served bytes must match)
    - `.planning/STATE.md` § "Phase 4 deploy verification" (the measurement table format to mirror)
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-09 and D-10
  </read_first>
  <action>
    Only after Task 1's authorization. Execute in this order and record the result of each step
    before starting the next.

    1. Merge the PR to `main` (or confirm Andrew merged it, per his chosen option). Record the merge
       commit SHA.
    2. `fly deploy -a alle-drops-quiz-app`. Record the new release number and confirm it is greater
       than v51.
    3. `shopify app deploy`. Record the new app version and confirm it is greater than
       `alledrops-quiz-production-22`.
    4. Verify on served bytes, never on exit codes. Fetch with a cache-busting query parameter on
       every request; a single stale fetch is not evidence, and this project has recorded a case
       where a fetch two minutes early showed the old state.

    The verification set, all recorded as a before/after table in the SUMMARY:
    - `GET /health` returns 200
    - `GET /quiz-bundle-js?cb={timestamp}` returns 200; record the byte length and the SHA-256 of
      the served bytes
    - The served SHA-256 EQUALS the committed `public/quiz-bundle.js` SHA-256 from 04.1-04-SUMMARY
    - The served SHA-256 DIFFERS from the pre-deploy value
      `a2be1ab758cab05aa1f1f81890412636ba8076aa52afd286e221bf64fde8762c` — this is the
      "the deploy actually changed what is served" control
    - Apply the `QUIZ_PARTS` element-order extraction from plan 04.1-04 Task 2 to the SERVED bytes:
      element 0 is the `testing_status` head, element 1 the `symptoms_nasal` head, element 6 the
      `history_comorbidities` head. Run the non-vacuity control in the SAME fetch first — all three
      question ids must resolve to non-empty minified identifiers, and the seven extracted
      identifiers must be distinct — before making any ordering claim.
    - Count any occurrence with `split(needle).length - 1`. **Never `grep -c`** — it counts matching
      lines, and a minified single-line bundle collapses every nonzero count to 1. This project has
      shipped two separate wrong-count errors from exactly this, and D-09 restates the rule as
      non-negotiable.

    Do not run any migration. Do not execute DDL. Do not change a Fly secret. Do not touch the GCS
    lifecycle rule — `PENDING_OLM_AGE_DAYS` stays at 2 for this phase per D-03, and Phase 4.2 owns
    the value under D-04's apply-and-verify requirement.

    If any assertion fails, stop and report before touching anything else. Do not retry a fetch until
    it says what you want.
  </action>
  <verify>
    <automated>node -e "const crypto=require('crypto');const url='https://alle-drops-quiz-app.fly.dev/quiz-bundle-js?cb='+Date.now();fetch(url).then(r=>{if(r.status!==200)throw new Error('status '+r.status);return r.arrayBuffer()}).then(b=>{const buf=Buffer.from(b);const s=buf.toString('utf-8');const sha=crypto.createHash('sha256').update(buf).digest('hex');const m=s.match(/([A-Za-z_\$][\w\$]*)=\[((?:[A-Za-z_\$][\w\$]*,){6}[A-Za-z_\$][\w\$]*)\],[A-Za-z_\$][\w\$]*=\1\.flat\(\)/);if(!m)throw new Error('QUIZ_PARTS array not found in served bytes');const parts=m[2].split(',');const head=(q)=>{const r=s.match(new RegExp('([A-Za-z_\$][\\\\w\\\\\$]*)=\\\\[\\\\{kind:\"question\",id:\"'+q+'\"'));return r&&r[1]};const t=head('testing_status'),n=head('symptoms_nasal'),h=head('history_comorbidities');console.log('bytes',buf.length,'sha',sha);console.log('nonvacuity',{t,n,h},'distinct',new Set(parts).size===7);console.log('order',parts);if(!t||!n||!h)throw new Error('non-vacuity control failed');if(parts[0]!==t||parts[1]!==n||parts[6]!==h)throw new Error('served bundle part order is wrong: '+parts.join(','));console.log('SERVED ORDER OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `SERVED ORDER OK`
    - `GET /health` returns 200
    - Served byte length equals the committed artifact byte length from 04.1-04-SUMMARY
    - Served SHA-256 equals the committed artifact SHA-256 AND differs from
      `a2be1ab758cab05aa1f1f81890412636ba8076aa52afd286e221bf64fde8762c`
    - `fly releases -a alle-drops-quiz-app` shows a release number greater than 51
    - The Shopify app version is greater than `alledrops-quiz-production-22`
    - The SUMMARY contains a before/after table in the format of STATE.md's Phase 4 deploy table
    - No migration ran, no DDL executed, no Fly secret changed, no `gcloud` mutation performed —
      stated explicitly in the SUMMARY
    - No `grep -c` was used for any occurrence count anywhere in this task
  </acceptance_criteria>
  <done>The reorder is live on Fly and Shopify, proven by served bytes matching the committed artifact and differing from the pre-deploy artifact, with the part order extracted from the served payload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Andrew confirms both PHI renderers on the deployed app (D-05a's blocking criterion)</name>
  <read_first>
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-02-SUMMARY.md` (what changed in each
      renderer and which commit carried the section versus the filter)
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-05a
  </read_first>
  <files></files>
  <action>
    Present the seven checks below to Andrew against the DEPLOYED embedded admin, then HALT. Confirm
    first that Task 2's served-bytes verification passed, so Andrew is looking at the new code. This
    is a human-owned verification; Claude presents, waits, and records presence/count/label only —
    never a PHI value — into the SUMMARY.
  </action>
  <what-built>
    Both PHI answer renderers now show Part 7 testing answers only under a "Test Results" section.
    In the clinical PDF this closes a pre-existing Phase 4 defect where those four answers printed
    twice. In the embedded admin detail modal a "Test Results" section was ADDED — it did not exist
    before — and only then were the testing keys removed from "Symptom Responses", so nothing was
    dropped. The suite proves the partition is lossless and proves the section exists in source, but
    no automated test renders that modal. This check is the one that confirms it on a real screen.
  </what-built>
  <how-to-verify>
    In the Shopify admin, open the AlleDrops app and go to **Quiz Results** (`/app/quiz-results`).
    Pick a submission that has Part 7 answers — the UAT submission from Phase 4 has them, including
    an uploaded file. Open its detail modal and answer:

    1. **Test Results section exists.** There is a "Test Results" section in the modal.
    2. **Nothing was lost.** Testing status, testing year, testing location, and allergens are all
       four visible under it, with their clinical labels and their real values.
    3. **No duplication.** None of those four appears a second time anywhere in the modal —
       specifically, they are gone from "Symptom Responses".
    4. **Symptoms still open the symptom section.** "Symptom Responses" now begins with an actual
       symptom answer, not with testing status.
    5. **Neighbouring sections intact.** "Patient Information" and "Uploaded Files" render as
       before, and the uploaded file still downloads.
    6. **The list table is untouched.** Back on the submissions table, the testing-status column
       still shows "Had testing" / "Needs testing" as it did before.
    7. **Clinical PDF.** Click Download PDF for that same submission and open it. Testing status,
       year, location, and allergens appear exactly once, under a "Test Results" heading, and
       "Symptom Responses" opens with a symptom rather than testing status. The uploaded file is
       still embedded in the document.

    If any of checks 2, 3, or 7 fails, that is a clinical-record data-loss or duplication defect —
    say so immediately and do not close the phase.
  </how-to-verify>
  <acceptance_criteria>
    - All seven checks explicitly answered and recorded verbatim in `04.1-06-SUMMARY.md`
    - Check 2 is answered as presence-exactly-once for each of the four keys individually, not as a
      general "looks right" — D-05a's blocking criterion is presence, not absence
    - If any check fails, a gap-closure plan is created and the phase does not close
    - This checkpoint is never auto-approved. `workflow.auto_advance` does not apply.
    - No PHI value is transcribed into the SUMMARY. Record presence, count, and label only — never a
      patient name, DOB, email, phone, allergen list, or testing location string. Per CLAUDE.md,
      IDs and counts only.
  </acceptance_criteria>
  <done>All seven checks answered by Andrew and recorded verbatim, with no PHI value transcribed.</done>
  <verify><human-check>Andrew's explicit in-session response to all seven checks.</human-check></verify>
  <resume-signal>Type "approved" to close the phase, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `main` -> deployed Fly release | Two independent deploy systems; a green `shopify app deploy` says nothing about the Fly app and vice versa. |
| deployed release -> served bytes | A successful deploy is not proof that the intended bytes are what the internet receives. |
| Cloud SQL PHI -> provider's browser and downloaded PDF | The clinical record's integrity is the asset being confirmed by the human check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-03 | Repudiation / verification-by-assumption | `fly deploy` and `shopify app deploy` exit codes | mitigate | Task 2 asserts served byte length, served SHA-256 equal to the committed artifact, served SHA-256 different from the pre-deploy value, and the part order extracted from the served payload — all with cache-busted fetches and a non-vacuity control in the same fetch. |
| T-4.1-19 | Tampering | Occurrence counting with `grep -c` | mitigate | Explicitly forbidden in Task 2's action and acceptance criteria; `split(needle).length - 1` is mandated. Two prior wrong-count errors on this project are the precedent. |
| T-4.1-01 | Tampering (integrity of the clinical record) | Admin detail modal and clinical PDF | mitigate | Task 3 checks 2, 3, and 7 confirm presence-exactly-once for each of the four testing keys on both rendered surfaces. This is the check that catches a silent deletion the source guards cannot see. |
| T-4.1-20 | Information disclosure | PHI transcribed into planning artifacts during the human check | mitigate | Task 3's acceptance criteria forbid recording any PHI value in the SUMMARY — presence, count, and label only, per CLAUDE.md's "log IDs and counts only" rule. |
| T-4.1-21 | Elevation of privilege | Deploying scope creep — a migration, DDL, secret, or lifecycle change smuggled into a presentation-order deploy | mitigate | Task 1's acceptance criteria assert an empty `package.json`/`package-lock.json` diff and no file under `migrations/`; Task 2's forbid running any migration, DDL, secret change, or `gcloud` mutation and require an explicit statement to that effect in the SUMMARY. |
| T-4.1-SC | Tampering | npm/pip/cargo installs | n/a | **This plan installs zero packages.** The PR diff is asserted to contain no `package.json` or `package-lock.json` change. No legitimacy gate is owed. |
</threat_model>

<verification>
- Served `/quiz-bundle-js` SHA-256 equals the committed artifact's and differs from the pre-deploy value
- The served-bytes part-order extraction passes with its non-vacuity and distinctness controls
- `/health` returns 200
- Fly release > v51 and Shopify app version > alledrops-quiz-production-22
- All seven human checks answered
- No migration, DDL, secret change, or `gcloud` mutation performed
</verification>

<success_criteria>
The reorder is live and proven live on served bytes; a provider's view of the clinical record shows
each Part 7 answer exactly once on both the admin modal and the clinical PDF; and Phase 4.1 closes
with a recorded verification table rather than an assertion.
</success_criteria>

<output>
Create `.planning/phases/04.1-testing-first-quiz-order/04.1-06-SUMMARY.md` when done. It MUST
contain: the PR URL and merge SHA, the new Fly release and Shopify app version, a before/after
served-bytes table (byte length + SHA-256 + extracted part order), Andrew's verbatim answers to all
seven checks, and an explicit statement that no migration, DDL, secret change, or `gcloud` mutation
was performed.
</output>
</content>
