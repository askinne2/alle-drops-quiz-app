---
phase: 04.1-testing-first-quiz-order
plan: 03
subsystem: docs
tags: [decision-record, retention-policy, gcs, phi, hipaa]

# Dependency graph
requires:
  - phase: 03-mandatory-medical-history
    provides: "D-11 (proceed-without-testing bypass deleted) and D-13 (Part 6 reached by 100% of patients) — the two facts that make the D-01 amendment defensible"
  - phase: 04-mandatory-allergy-testing
    provides: "PENDING_OLM_AGE_DAYS = 2 lifecycle rule (plan 04-17) and the apply-then-verify gcloud pattern this plan cites but does not run"
provides:
  - "DEC-medical-history-before-testing-split amended in place — status AMENDED, original text preserved under strikethrough, clinical rationale marked as surviving"
  - "Phase 4.1 orphan-volume analysis in docs/gcs-lifecycle-and-retention.md, handing the PENDING_OLM_AGE_DAYS value decision to Phase 4.2 with the D-04 apply-and-verify requirement attached"
affects: [04.1-testing-first-quiz-order (remaining plans), 04.2-resume-in-progress-intake]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - .planning/PROJECT.md
    - docs/gcs-lifecycle-and-retention.md

key-decisions:
  - "D-01 amendment: only the position clause and the Hard sequencing consequence paragraph are struck; the every-patient clause, must-not-affect-score clause, and Rationale sentence remain unstruck because they still hold"
  - "D-03/D-04: PENDING_OLM_AGE_DAYS stays 2; the value decision is explicitly deferred to Phase 4.2, which must apply-and-verify via gcloud read-back per D-04, never doc-only"

patterns-established:
  - "AMENDED-in-place decision convention (distinct from RETRACTED): strike only the superseded clause(s), leave surviving clauses unstruck, append a dated **AMENDED <date>.** paragraph stating what changed, why, and what survives, closed with a Source: line"

requirements-completed: [TEST-04]

# Metrics
duration: 5min
completed: 2026-08-10
---

# Phase 04.1 Plan 03: Decision Amendment and Retention Analysis Summary

**Amended DEC-medical-history-before-testing-split in place (status LOCKED to AMENDED, position clause superseded, clinical rationale intact) and recorded the Phase 4.1 orphan-volume analysis in the GCS retention doc — zero code touched, zero `gcloud` mutations, `PENDING_OLM_AGE_DAYS` unchanged at 2.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-10T10:51:24-04:00 (base commit b5fbf29)
- **Completed:** 2026-08-10T10:55:48-04:00
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `DEC-medical-history-before-testing-split` amended in place per D-01: position clause and the obsolete "Hard sequencing consequence" paragraph struck with `~~`, original text fully preserved, clinical rationale ("even a patient who books a consult directly still needs history on file for Dr. Sullivan") left unstruck and explicitly marked as surviving
- `docs/gcs-lifecycle-and-retention.md` gained a new `## Phase 4.1 impact — abandonment becomes the common case, not the rare one` section covering the mechanism, what is NOT changing, why the value decision is deferred to Phase 4.2, and the GCS whole-days/asynchronous OLM constraint
- `PENDING_OLM_AGE_DAYS` verified unchanged at `2` throughout; no `gcloud` command was executed at any point in this plan

## Task Commits

Each task was committed atomically:

1. **Task 1: Amend DEC-medical-history-before-testing-split in place (D-01)** - `88ead74` (docs)
2. **Task 2: Record the Phase 4.1 orphan-volume analysis without changing PENDING_OLM_AGE_DAYS (D-03)** - `98547f6` (docs)

**Plan metadata:** commit pending (this SUMMARY + final metadata commit)

## Files Created/Modified
- `.planning/PROJECT.md` - `DEC-medical-history-before-testing-split` block: `status="LOCKED"` → `status="AMENDED"`, `amended="2026-08-10"` and `amended_by="04.1-CONTEXT.md D-01"` attributes added; position clause and Hard sequencing consequence paragraph wrapped in `~~`; dated `**AMENDED 2026-08-10.**` paragraph appended explaining what changed, why the rationale survives (citing `03-CONTEXT.md D-13`), why the struck paragraph is obsolete (citing Phase 3's D-11), and why the reorder happened at all; closed with a `Source:` line citing `04.1-CONTEXT.md` D-01
- `docs/gcs-lifecycle-and-retention.md` - new `## Phase 4.1 impact` section added after `## Orphan sources this rule cleans up`, before the `## Reconciliation query` section; states the reorder makes abandonment-with-orphan the common case (upload moves from part 7 of 7 to part 1 of 7), confirms `PENDING_OLM_AGE_DAYS = 2` and the live rule are untouched, defers the value decision to Phase 4.2 with the D-04 apply-and-verify requirement, and restates the whole-days/asynchronous OLM constraint (cross-referencing the existing `### Timing caveat` section rather than duplicating it)

## Decisions Made
- Followed the plan's explicit instruction to treat D-01 as an amendment, not a retraction — the word `RETRACTED` does not appear anywhere in the `DEC-medical-history-before-testing-split` block, confirmed by direct text search of the block's contents
- Chose to wrap the entire `PENDING_OLM_AGE_DAYS = 2` phrase in a single pair of backticks (rather than backticking each token separately, as the existing `:13` bullet does) so the plan's required literal substring `PENDING_OLM_AGE_DAYS = 2` is present verbatim and machine-verifiable — a minor formatting divergence from `:13`'s style, made to satisfy the plan's automated verify command without altering `:13` itself

## Deviations from Plan

### Notes (no code/behavior change, informational only)

**1. Task 1 acceptance criterion `git diff .planning/PROJECT.md | grep -c 'decision id=' outputs 1` is unreachable as literally written, given the task's own required opening-tag edit.**
- **Found during:** Task 1 verification
- **Issue:** The task explicitly requires changing `status="LOCKED"` to `status="AMENDED"` and adding `amended=`/`amended_by=` attributes on the decision's opening tag line. Because that line itself changes, `git diff` necessarily shows both a removed (`-`) and an added (`+`) copy of the line — each containing the substring `decision id=` — for the single edited decision. Default diff context (3 lines) additionally surfaces the neighboring `DEC-testing-results-by-email-not-upload` block's unchanged opening tag as pure context (no `+`/`-` prefix). The literal count is `3`, not `1`.
- **Verification performed instead:** Confirmed via `git diff .planning/PROJECT.md | grep 'decision id='` that only the `DEC-medical-history-before-testing-split` line carries a `+`/`-` prefix; the `DEC-testing-results-by-email-not-upload` line is unprefixed context, i.e., genuinely unmodified. This satisfies the acceptance criterion's actual intent — "no other `<decision>` id appears in the diff" as a *modification* — even though the raw grep count is 3.
- **Files modified:** none beyond the planned edit; this is a verification-methodology note, not a code change
- **Committed in:** `88ead74` (Task 1 commit; no separate fix needed)

---

**Total deviations:** 0 auto-fixed; 1 documented verification-methodology note (not a Rule 1-4 deviation — no bug, no missing functionality, no blocker, no architectural question, just an acceptance-criterion whose literal wording conflicts with the task's own explicit instruction)
**Impact on plan:** None on scope or correctness. Both files changed exactly as specified; no other decision block, no other doc section, and no code was touched.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. This plan is docs-only.

## Next Phase Readiness
- `.planning/PROJECT.md` and `docs/gcs-lifecycle-and-retention.md` are ready for Phase 4.2 (Resume In-Progress Intake), which owns the `PENDING_OLM_AGE_DAYS` value decision per D-03/D-04 and will find the reasoning and constraint already recorded here
- `npm run typecheck` exits 0; `npm test` reports 558 tests / 37 files passing — identical to the pre-plan baseline, confirming zero code was touched
- `git status --porcelain` is clean after both task commits; `PENDING_OLM_AGE_DAYS` is confirmed still `2` and no `gcloud` command was executed at any point

---
*Phase: 04.1-testing-first-quiz-order*
*Completed: 2026-08-10*
