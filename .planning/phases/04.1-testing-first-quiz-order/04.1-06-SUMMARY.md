# Phase 04.1 Plan 06: Ship the Reorder — PARTIAL (Task 1 only)

**Plan:** `.planning/phases/04.1-testing-first-quiz-order/04.1-06-PLAN.md`
**Wave:** 4
**Depends on:** 04.1-05
**Requirements:** TEST-01, TEST-04
**Date:** 2026-08-10
**Status:** **INCOMPLETE — Task 1 done, Tasks 2 and 3 deliberately deferred by Andrew.**

---

## Task 1: PR opened, gates reported, authorization obtained — DONE

**PR:** https://github.com/askinne2/alle-drops-quiz-app/pull/22
`phase-4.1-testing-first-quiz-order` → `main`, 31 commits, 30 files
(+4541 / −50).

### Gate results (exact numbers, per the plan's acceptance criteria)

| Gate | Result |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm test` | **602 tests / 39 files**, all passed |
| `npm run build` | exit 0 |
| `npm run build:theme` determinism | two consecutive runs → identical SHA-256 for both `.js` and `.css` |
| Fresh rebuild vs committed artifact | reproduces `ba35bdc5c2c148d1d55a6353cd48e768d3667c4d45ea98c7ed94e2717cc9ba18` exactly; working tree clean after |
| `git diff main...HEAD --stat -- package.json package-lock.json` | **no output** — no dependency change |
| `git diff main...HEAD --name-only` under `migrations/` | **no match** — no migration, no DDL |
| Route / auth / ownership / query / metafield files | **none changed** — no match on `app/routes/api*`, `app/lib/db.ts`, `app/lib/submissions.ts`, `app/lib/customer-auth.ts`, `app/lib/shopify/` |

### PR description content

Carries all items CLAUDE.md requires: the two PHI-facing renderers changed (`app/lib/pdf.ts`,
`app/routes/app.quiz-results.tsx`); that the admin change ADDS a Test Results section before
filtering Symptom Responses so no clinical field is dropped (losslessness asserted by
`tests/phi-answer-partition.test.ts`); that no route, auth check, ownership predicate, query, or
logging statement changed; no new dependency; no migration and no DDL. It also states plainly what
is NOT covered pre-deploy — the admin detail modal has no rendered-DOM test, only a losslessness
unit test on `partitionAnswers` plus source-text guards.

---

## Andrew's decision, verbatim

> **"merge and deploy with phase 4.2"**

This selects the plan's `hold` option: **hold the merge and ship Phase 04.1 together with Phase
04.2 (Resume In-Progress Intake)** in a single three-channel deploy.

### Recorded deviation from D-10

The plan's own D-10 specifies shipping the reorder immediately, and the `hold` option was presented
with its stated con — *"Rejected by D-10 — delays the abandonment fix by 1–2 days for no technical
benefit."* Andrew selected it with that con visible. **This is a deliberate, informed override of
D-10, not drift.** D-10 is superseded for this phase; the reorder ships with 04.2.

### Consequences to carry forward

1. **PR #22 stays open and unmerged.** Do not merge it as part of routine phase completion. It
   merges when Phase 04.2 is ready to ship, or as part of a combined PR at that time.
2. **No deploy was run.** Fly remains on release **v51** and Shopify on app version
   **alledrops-quiz-production-22** — both still carry the *old* part order, with allergy testing
   last. The abandonment fix is NOT live.
3. **Phase 04.1 is code-complete but not shipped.** Do not mark TEST-01 or TEST-04 as verified in
   the new position; the plan assigns that to Task 2's served-bytes verification, which has not run.
4. **Task 3 travels with the deploy.** Andrew's confirmation of the two PHI renderers on the
   deployed app (D-05a's blocking criterion) is still owed and can only run post-deploy.
5. **Branch divergence risk.** Phase 04.2 work must branch from a base that includes this branch, or
   be merged with it, or the theme bundle will be rebuilt from a source tree missing the reorder.
   `public/quiz-bundle.js` is a committed build artifact — a 04.2 rebuild off `main` would silently
   revert the reorder in the shipped bundle. **This is the single highest-risk consequence of
   holding.**

---

## Tasks NOT executed

| Task | Status | Why |
|---|---|---|
| Task 2: Three-channel deploy and served-bytes verification | **NOT RUN** | Deferred to the joint 04.1 + 04.2 ship |
| Task 3: Andrew confirms both PHI renderers on the deployed app (D-05a) | **NOT RUN** | Requires a deployed surface; blocked by Task 2 |

No merge command, no `fly deploy`, and no `shopify app deploy` was run. The plan's Task 1 explicitly
forbids running any of them before Andrew names an option, and the option he named defers all three.

---

## Self-Check: PASSED (for the scope executed)

- Task 1 acceptance criteria: all met — PR exists with the required description content, PR URL
  recorded, all four gate commands reported with exact numbers, dependency and migration diffs both
  empty, and Andrew's chosen option recorded verbatim before any merge or deploy command ran
- Tasks 2 and 3: correctly left unexecuted, with the reason recorded rather than silently skipped
- The D-10 deviation is recorded as an explicit override with the con that was shown at decision
  time, so a future reader can tell an informed choice from an accident
