---
phase: 04.1-testing-first-quiz-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/quiz/questions.ts
  - tests/quiz-part-renderer-dom.test.ts
  - tests/quiz-file-upload-dom.test.ts
  - tests/quiz-medical-history-deletion.test.ts
  - tests/quiz-parts-order.test.ts
autonomous: true
requirements: [TEST-01, TEST-04]

must_haves:
  truths:
    - "D-02: QUIZ_PARTS order is [P7, P1-P5, P6] with exactly one element moved; D-06: the part:/order: fields stay inert and a banner comment records that array position governs flow order; D-07: PART7_ALLERGY_TESTING internal item order is untouched"
    - "PART7_ALLERGY_TESTING is the first element of QUIZ_PARTS, so it is the first quiz part a patient sees after the state gate and patient-info step"
    - "PART6_MEDICAL_HISTORY is the last element of QUIZ_PARTS, so medical history still sits immediately before consent"
    - "The score for a fixed answer set is unchanged by the reorder — ALL_SCORED_QUESTIONS still excludes every Part 7 question"
    - "Consent still sits between the final quiz part and the results screen, on one path for every bracket"
    - "The full suite is green with every coupled index literal repointed, not deleted"
  artifacts:
    - path: "app/lib/quiz/questions.ts"
      provides: "QUIZ_PARTS in [P7, P1, P2, P3, P4, P5, P6] order plus a D-06 banner comment"
      contains: "PART7_ALLERGY_TESTING"
    - path: "tests/quiz-parts-order.test.ts"
      provides: "Source-of-truth order guard, score-invariance guard, and consent-invariant guard"
      min_lines: 60
  key_links:
    - from: "app/lib/quiz/questions.ts"
      to: "ALL_ITEMS"
      via: "QUIZ_PARTS.flat()"
      pattern: "QUIZ_PARTS\\.flat\\(\\)"
    - from: "tests/quiz-parts-order.test.ts"
      to: "app/lib/quiz/questions.ts"
      via: "direct import of QUIZ_PARTS and ALL_SCORED_QUESTIONS"
      pattern: "from \"\\.\\./app/lib/quiz/questions\""
---

<objective>
Move `PART7_ALLERGY_TESTING` from last to first in `QUIZ_PARTS` (D-02), record why array position
rather than the inert `part:` field governs flow order (D-06), repoint the three test files that
hardcode literal part indices, and add a dedicated guard that proves the new order, the unchanged
score, and the surviving consent invariant.

Purpose: a patient who cannot supply allergy test results hits that wall in the first ~30 seconds
instead of after a completed ten-minute clinical intake.

Output: reordered `QUIZ_PARTS`, three repointed test files, one new guard test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md
@.planning/phases/04.1-testing-first-quiz-order/04.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase 2026-08-10. No exploration required. -->

From `app/lib/quiz/questions.ts` (measured line numbers):
- `:562-568`  `export const ALL_SCORED_QUESTIONS: QuizQuestion[]` — explicit spread of
  PART1_SYMPTOM_CHECKLIST, PART2_TIMING_TRIGGERS, PART3_SEVERITY, PART4_IMPACT, PART5_TREATMENT.
  Independent of QUIZ_PARTS. DO NOT TOUCH.
- `:574-578`  `export function getQuestionById(id: string): QuizQuestion | undefined` — resolves by
  `.find`, concatenation order irrelevant. DO NOT TOUCH.
- `:580-583`  existing `/** ... */` banner above QUIZ_PARTS, citing `03-CONTEXT.md D-13` and
  `04-CONTEXT.md D-06/D-07`. This is the voice to match.
- `:584-592`  `export const QUIZ_PARTS: QuizItem[][] = [PART1_SYMPTOM_CHECKLIST,
  PART2_TIMING_TRIGGERS, PART3_SEVERITY, PART4_IMPACT, PART5_TREATMENT, PART6_MEDICAL_HISTORY,
  PART7_ALLERGY_TESTING]` — the single array literal this plan reorders.
- `:594-597`  `export const ALL_ITEMS: QuizItem[] = QUIZ_PARTS.flat()` — feeds `visibleAnswers`.
  Reordering changes key ORDER in the emitted answers object, never key MEMBERSHIP.

From `app/lib/quiz/schema.ts` `:261-284`:
- `export function quizFlowProgress(step: FlowProgressStep | null, totalParts: number)` — derives
  `total = totalParts + 3` and `at(3 + step.index)` for a quiz part; `case "consent": return
  at(total)`. Index-driven, self-adjusting, needs no change.

Known question ids, for identifying parts without relying on indices:
- Part 1 head: `symptoms_nasal`   - Part 6 head: `history_comorbidities`
- Part 7 head: `testing_status`   - Part 7 upload: `testing_files`

Post-reorder index map (this is the whole change surface):
Part 7 -> 0 · Part 1 -> 1 · Part 2 -> 2 · Part 3 -> 3 · Part 4 -> 4 · Part 5 -> 5 · Part 6 -> 6
</interfaces>

<test_repointing_decision>
CONTEXT.md leaves the repointing mechanic to Claude's discretion. **Decision: renumber the literals
in place; do not derive indices from `QUIZ_PARTS`.** Reasons: six one-token edits versus new helper
code, and derived indices would make these three DOM tests silently survive a future accidental
reorder — the loud-failure canary is worth keeping. The dedicated order guard added in Task 3 is the
positive assertion; these three files stay as the negative one. Apply this choice consistently
across all four sites in `quiz-part-renderer-dom.test.ts`, both in `quiz-file-upload-dom.test.ts`,
and the prose in `quiz-medical-history-deletion.test.ts`.
</test_repointing_decision>

<tasks>

<task type="auto">
  <name>Task 1: Reorder QUIZ_PARTS and add the D-06 array-position banner</name>
  <files>app/lib/quiz/questions.ts</files>
  <read_first>
    - `app/lib/quiz/questions.ts` lines 555-600 (ALL_SCORED_QUESTIONS, getQuestionById, the existing
      QUIZ_PARTS banner, the array literal, and the ALL_ITEMS banner below it)
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-CONTEXT.md` §`<decisions>` D-02 and D-06
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-PATTERNS.md`
      §`app/lib/quiz/questions.ts` (the banner-comment convention to match)
  </read_first>
  <action>
    Reorder the `QUIZ_PARTS` array literal to exactly: `PART7_ALLERGY_TESTING`,
    `PART1_SYMPTOM_CHECKLIST`, `PART2_TIMING_TRIGGERS`, `PART3_SEVERITY`, `PART4_IMPACT`,
    `PART5_TREATMENT`, `PART6_MEDICAL_HISTORY`. Exactly one element moves (D-02) — Part 7 to the
    front; Parts 1-6 keep their relative order and Part 6 stays last, immediately before consent.

    Update the existing `/** ... */` banner directly above the array (currently at :580-583) so it
    still explains the `QuizItem[][]` shape and the "Parts 6 and 7 are each reached by 100% of
    patients" fact, and additionally records, in the same voice and with the same
    cite-the-decision-ID convention: (a) that Part 7 now leads the flow per `04.1-CONTEXT.md D-02`,
    with the one-sentence reason — a patient who cannot supply test results should discover the hard
    upload requirement in seconds, not after a completed intake; and (b) per `04.1-CONTEXT.md D-06`,
    that **array position in this literal, not the `part:` field on each question object, determines
    flow order** — the `part:` and `order:` fields are inert, deliberately left unrenumbered, and
    their only repo-wide consumer is `app/lib/quiz/scoring.test.ts:102`'s `part <= 5` assertion on
    scored questions. State plainly that a future reader must not "fix" the resulting mismatch.

    Do NOT touch `ALL_SCORED_QUESTIONS` (:562-568), `getQuestionById` (:574-578), `ALL_ITEMS`
    (:594-597), any `part:` or `order:` field on any question, or the internal item order of
    `PART7_ALLERGY_TESTING` (D-07). Do not add a scoring task of any kind — scoring is structurally
    out of reach of this change and PROJECT.md forbids it.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-schema-type-guarantees.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/quiz/questions.ts` `QUIZ_PARTS` array literal element 0 is `PART7_ALLERGY_TESTING`
      and element 6 is `PART6_MEDICAL_HISTORY`; elements 1-5 are PART1..PART5 in ascending order
    - `node -e 'import("./app/lib/quiz/questions.ts")'` is not required; instead
      `npx vitest run tests/quiz-schema-type-guarantees.test.ts` exits 0 (its
      `QUIZ_PARTS.length === 7` assertion at :88-89 still holds and must not be edited)
    - `npm run typecheck` exits 0
    - The banner above `QUIZ_PARTS` contains the literal substrings `04.1-CONTEXT.md D-02`,
      `04.1-CONTEXT.md D-06`, and `array position`
    - `git diff --stat app/lib/quiz/questions.ts` shows changes confined to the banner block and the
      seven array-literal lines; no `part:` or `order:` field value differs from HEAD
      (`git diff app/lib/quiz/questions.ts | grep -E '^[-+]\s+(part|order):' | wc -l` outputs `0`)
    - `git diff app/lib/quiz/questions.ts` contains no change to any line inside
      `ALL_SCORED_QUESTIONS`, `getQuestionById`, or `PART7_ALLERGY_TESTING`'s item list
  </acceptance_criteria>
  <done>QUIZ_PARTS is `[P7, P1, P2, P3, P4, P5, P6]`, banner records D-02 and D-06, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the three test files that hardcode literal part indices</name>
  <files>tests/quiz-part-renderer-dom.test.ts, tests/quiz-file-upload-dom.test.ts, tests/quiz-medical-history-deletion.test.ts</files>
  <read_first>
    - `tests/quiz-part-renderer-dom.test.ts` lines 45-95 and 240-256
    - `tests/quiz-file-upload-dom.test.ts` lines 28-40 and 84-92
    - `tests/quiz-medical-history-deletion.test.ts` lines 136-145
    - `.planning/phases/04.1-testing-first-quiz-order/04.1-PATTERNS.md` §"Tests coupled to literal
      part indices"
    - This plan's `<test_repointing_decision>` block
  </read_first>
  <action>
    Repoint every hardcoded part index to the post-reorder map (Part 7 -> 0, Part 6 -> 6). Repoint,
    never delete — each of these assertions is live coverage for TEST-01/TEST-03/TEST-04.

    In `tests/quiz-part-renderer-dom.test.ts`: change `PART_6_ITEMS = itemsForPart(QUIZ_PARTS, 5)`
    to index `6`; change `PART_7_ITEMS = itemsForPart(QUIZ_PARTS, 6)` to index `0`; update the two
    `it(...)` description strings that quote those literals (`itemsForPart(QUIZ_PARTS, 5) is
    non-empty and contains the known HIST item IDs` -> index `6`, and `itemsForPart(QUIZ_PARTS, 6)
    is non-empty and contains the known Part 7 item IDs` -> index `0`) so the prose matches the call
    site. Leave the comment block above `PART_6_ITEMS` describing the session-32 seam intact, and
    leave every `expect(...)` body unchanged.

    In `tests/quiz-file-upload-dom.test.ts`: change `PART_7_ITEMS = itemsForPart(QUIZ_PARTS, 6)` to
    index `0`, and update the `it("itemsForPart(QUIZ_PARTS, 6) contains testing_files, gated on
    had_testing", ...)` description string to index `0`.

    In `tests/quiz-medical-history-deletion.test.ts`: this is a source-text guard, not a DOM test.
    Change ONLY the prose inside the `it(...)` description on line 140 — `now six entries, including
    medical history as QUIZ_PARTS[5]` becomes a correct statement of seven entries with medical
    history at `QUIZ_PARTS[6]`. Do not touch line 141's
    `expect(count(QUIZ_CONTAINER_SOURCE, "QUIZ_PARTS")).toBeGreaterThan(0)` assertion — it is a
    substring count on `QuizContainer.tsx` source and is index-independent.

    Do not touch `tests/quiz-schema-type-guarantees.test.ts` (its `QUIZ_PARTS.length === 7`
    assertion is still true) or `tests/quiz-flow-progress.test.ts` (derives TOTAL_PARTS from
    `QUIZ_PARTS.length`, already order-independent).
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-part-renderer-dom.test.ts tests/quiz-file-upload-dom.test.ts tests/quiz-medical-history-deletion.test.ts tests/quiz-flow-progress.test.ts tests/quiz-schema-type-guarantees.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest command above exits 0 with zero failures
    - `grep -n 'itemsForPart(QUIZ_PARTS, 5)' tests/quiz-part-renderer-dom.test.ts` returns no lines
    - `grep -c 'itemsForPart(QUIZ_PARTS, 0)' tests/quiz-file-upload-dom.test.ts` is at least `2`
      (the const and the `it(...)` description)
    - `grep -n 'QUIZ_PARTS\[5\]' tests/quiz-medical-history-deletion.test.ts` returns no lines
    - `git diff tests/quiz-medical-history-deletion.test.ts` shows exactly one changed line, and it
      is the `it(...)` description string — the `expect(count(...))` assertion on the following line
      is byte-identical to HEAD
    - `npm test` exits 0 with `558 passed (558)` and `37 passed (37)` test files (this task adds no
      tests; the count moves only in Task 3)
  </acceptance_criteria>
  <done>All six index sites repointed, all three files green, suite still 558/37.</done>
</task>

<task type="auto">
  <name>Task 3: Add the source-of-truth order, score-invariance, and consent-invariant guard</name>
  <files>tests/quiz-parts-order.test.ts</files>
  <read_first>
    - `app/lib/quiz/questions.ts` lines 555-600 (post-Task-1 state)
    - `app/lib/quiz/schema.ts` lines 240-290 (`quizFlowProgress` and its `FlowProgressStep` input)
    - `app/components/quiz/QuizContainer.tsx` lines 150-200 and 340-380 (the `setStep("consent")`
      call sites and consent's Previous handler that re-enters `quiz_parts` at `quizPartsTotal - 1`)
    - `tests/quiz-medical-history-deletion.test.ts` lines 1-40 (the `count(SOURCE, needle)`
      source-text-guard scaffold to copy for the consent-invariant assertions)
    - `.planning/ROADMAP.md` § "Phase 04.1" success criteria 1, 3, and 4
  </read_first>
  <action>
    Create `tests/quiz-parts-order.test.ts` with three `describe` blocks. Use the repo's
    established `const count = (source: string, needle: string): number =>
    source.split(needle).length - 1` helper for every source-text count — never `grep -c`, per
    D-09's non-negotiable counting rule.

    Block 1 — "QUIZ_PARTS flow order (04.1 D-02)". Import `QUIZ_PARTS` from
    `../app/lib/quiz/questions`. Assert `QUIZ_PARTS.length` is 7. Assert the item ids of
    `QUIZ_PARTS[0]` include `testing_status`, `testing_year`, `testing_location`,
    `testing_allergens`, and `testing_files` — i.e. index 0 IS Part 7. Assert `QUIZ_PARTS[1]` item
    ids include `symptoms_nasal` — i.e. Part 1 immediately follows. Assert `QUIZ_PARTS[6]` item ids
    include `history_comorbidities` and `has_pcp` — i.e. medical history is last, immediately before
    consent. Add an explicit non-vacuity control `it(...)` first, asserting each of those three
    needle ids resolves through `getQuestionById` to a defined question before any positional claim
    is made, so a typo'd id can never make a positional assertion pass by accident.

    Block 2 — "score invariance (ROADMAP success criterion 3)". Import `ALL_SCORED_QUESTIONS` and
    `ALL_ITEMS`. Assert `ALL_SCORED_QUESTIONS` contains zero question whose id starts with
    `testing_` and zero id in the Part 6 set (`history_`, `has_pcp`, `pcp_`,
    `diagnosed_allergic_condition`, `current_medications`). Assert every `ALL_SCORED_QUESTIONS`
    entry has `part <= 5`. Assert that the SET of ids in `ALL_ITEMS` is exactly the union of the
    ids in all seven `QUIZ_PARTS` entries and has no duplicates — this is the payload-boundary
    membership proof (threat T-4.1-02): the reorder changes key ORDER in the submitted answers
    object, never key MEMBERSHIP, so no PHI field enters or leaves the boundary.

    Block 3 — "consent invariant survives the reorder (04-CONTEXT.md D-09, ROADMAP criterion 4)".
    Read `app/components/quiz/QuizContainer.tsx` with `readFileSync` and assert on its source:
    `count(SOURCE, 'setStep("results")')` is exactly 2 (the post-submit transition and the Test Mode
    shortcut) and `count(SOURCE, 'setStep("consent")')` is at least 1; assert
    `count(SOURCE, 'handleConsentSubmit')` is at least 2 (definition plus its single call site);
    assert `count(SOURCE, 'submitPayload(')` is exactly the number of call sites present at HEAD
    (measure it, then hardcode with a comment recording the measured value) so a future edit that
    adds a second submit path fails loudly. Separately, import `quizFlowProgress` and `QUIZ_PARTS`
    and assert that `quizFlowProgress({ kind: "consent" }, QUIZ_PARTS.length)` returns the label for
    the FINAL step (`Step 10 of 10`) and `quizFlowProgress({ kind: "quiz_part", index: 6 },
    QUIZ_PARTS.length)` returns `Step 9 of 10` — proving consent still sits immediately after the
    last quiz part for every bracket, with the counter self-adjusted.

    Every `it(...)` description must name what it protects. Add a file header comment in the voice of
    `tests/quiz-bundle-freshness.test.ts`'s header explaining that this file is the positive canary
    for part order, that the three DOM test files are the negative canary, and that the counting rule
    is `split(needle).length - 1`.

    Log no answer values, patient fields, or PHI-shaped strings anywhere in this file — the only
    strings it handles are question ids and source text.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-parts-order.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-parts-order.test.ts` exits 0
    - `npm test` exits 0, `38 passed (38)` test files, and the test count is `558 + N` where N is the
      number of `it(...)` blocks added by this file — the SUMMARY must state N and the new total
    - `grep -c 'grep -c' tests/quiz-parts-order.test.ts` outputs `0`
    - `grep -c 'split(' tests/quiz-parts-order.test.ts` is at least `1`
    - Proven non-vacuous before being trusted: temporarily revert `QUIZ_PARTS` to its HEAD order,
      confirm `npx vitest run tests/quiz-parts-order.test.ts` FAILS in Block 1, then restore the
      reordered array and confirm it passes. Record both observed outcomes in the SUMMARY.
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Order, score-invariance, and consent-invariant guards exist, are green, and were proven RED against the pre-reorder array.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz client -> `POST /api/quiz/submit` | The `answers` object built by `visibleAnswers(ALL_ITEMS, answers)` crosses here. `ALL_ITEMS` is derived from `QUIZ_PARTS`, so this plan touches the shape of what crosses. |
| source -> committed test suite | The suite is the only automated line of defence between this edit and a shipped flow regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-02 | Tampering / Information disclosure | `ALL_ITEMS = QUIZ_PARTS.flat()` -> `visibleAnswers` -> `answers_json` | mitigate | Task 3 Block 2 asserts the id SET of `ALL_ITEMS` equals the union of all seven parts with no duplicates. Proves the reorder changes key ORDER only, never key MEMBERSHIP — no PHI field enters or leaves the payload boundary. |
| T-4.1-06 | Tampering | `QUIZ_PARTS` reorder silently breaking the D-09 consent invariant | mitigate | Task 3 Block 3 asserts `quizFlowProgress` places consent at the final step for `QUIZ_PARTS.length`, and source-counts the `setStep("results")` / `handleConsentSubmit` / `submitPayload(` call sites so a second submit path fails loudly. |
| T-4.1-07 | Denial of service (clinical) | Deleting rather than repointing the three coupled index tests | mitigate | Task 2 acceptance criteria require the suite to stay at exactly 558/37 after repointing — a deletion would drop the count and fail the gate. |
| T-4.1-08 | Information disclosure | PHI printed while debugging the reorder | mitigate | Task 3 handles question ids and source text only; CLAUDE.md's "log IDs and counts only" rule applies to any scratch debugging during this plan. |
| T-4.1-SC | Tampering | npm/pip/cargo installs | n/a | **This plan installs zero packages.** No `npm install`, no new dependency, no lockfile change. `git diff package.json package-lock.json` must be empty at plan end. No legitimacy gate is owed. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm test` exits 0, 38 files, 558 + N tests (N stated in the SUMMARY)
- `npm run build` exits 0
- `git diff package.json package-lock.json` is empty
- The new order guard was observed RED against the pre-reorder array before being trusted
- `public/quiz-bundle.js` is NOT rebuilt in this plan — the rebuild is plan 04.1-04's scope, so the
  freshness guard `tests/quiz-bundle-freshness.test.ts` must still pass unchanged here
</verification>

<success_criteria>
QUIZ_PARTS is `[P7, P1, P2, P3, P4, P5, P6]`; the banner records D-02 and D-06; all six coupled
index literals are repointed and green; a new guard proves the order, the unchanged score, the
unchanged payload-boundary membership, and the surviving consent invariant; typecheck and both
builds clean.
</success_criteria>

<output>
Create `.planning/phases/04.1-testing-first-quiz-order/04.1-01-SUMMARY.md` when done. It MUST state:
the new test count and file count, the number of `it(...)` blocks added, the measured
`submitPayload(` call-site count hardcoded into Block 3, and the observed RED-then-GREEN result of
the order guard's non-vacuity check.
</output>
</content>
</invoke>
