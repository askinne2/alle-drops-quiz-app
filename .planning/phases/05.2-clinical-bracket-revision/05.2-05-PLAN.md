---
phase: 05.2-clinical-bracket-revision
plan: 05
type: execute
wave: 3
depends_on: ["05.2-01", "05.2-02", "05.2-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [SCORE-04, SCORE-05, SCORE-06]

must_haves:
  truths:
    - "The live /quiz-bundle-js served by Fly carries the new recommendation copy and none of the retired copy, counted on served bytes"
    - "A live POST carrying score_bracket 9+ succeeds against the deployed app and the widened constraint"
    - "A live POST carrying score_bracket 7+ is rejected by the deployed validation"
    - "A human has seen all three brackets render on the deployed page at 375px and at desktop width"
    - "The longest new headline wraps fully at 375px with no clipping and no horizontal scroll"
    - "A patient scoring 8 is observed as Moderate and one scoring 9 as High on the deployed page"
    - "SCORE-04, SCORE-05 and SCORE-06 are marked complete only after the human pass"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCORE-04/05/06 marked complete with the phase that closed them"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5.2 checked off with its plan list"
    - path: ".planning/STATE.md"
      provides: "Deploy verification table on served bytes and the Phase 6 threshold note"
  key_links:
    - from: "https://alle-drops-quiz-app.fly.dev/quiz-bundle-js"
      to: "public/quiz-bundle.js"
      via: "served bytes SHA-256 identical to the committed artifact"
      pattern: "Prior to Starting Treatment"
---

<objective>
Merge, deploy, and verify Phase 5.2 on served bytes rather than exit codes; put the three new
brackets in front of a human at 375px and at desktop width; and only then close SCORE-04, SCORE-05
and SCORE-06.

Purpose: six shipped defects on this project were found by a human clicking and missed by a fully
green suite. This is a patient-facing clinical copy change with a moved clinical threshold — a green
build proves nothing shipped, and a green suite proves nothing rendered.
Output: a verified Fly release, a human sign-off, and closed requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-clinical-bracket-revision/05.2-SOURCE-william-2026-08-13.md
@.planning/phases/05.2-clinical-bracket-revision/05.2-01-SUMMARY.md
@.planning/phases/05.2-clinical-bracket-revision/05.2-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Environment facts. Do not re-derive these. -->

Fly app `alle-drops-quiz-app`, region `iad`. Deploy runs from `main` after merge —
`fly deploy -a alle-drops-quiz-app`. `shopify app deploy` is a SEPARATE system that ships only
extensions and app config; Phase 5.2 changes no file under `extensions/`, so it is NOT required and
must not be run.

Baseline at last ship (STATE.md, 2026-08-12): served `/quiz-bundle-js` = 203,797 B, byte-identical
to the committed artifact. 734 tests / 49 files green. `/health` returns 200. Fly's rollout printed
a false "not listening on the expected address" warning on both prior deploys — health and every
endpoint returned 200 both times.

Deployed preview surface for a synthetic score:
`https://alle-drops-quiz-app.fly.dev/quiz-embed?test=1` — the same surface Phase 5 used to confirm
the scale bar at a synthetic score of 30.

Occurrence counting on served bytes uses `split(needle).length - 1` inside a `node -e` script over
the fetched body. NEVER `grep -c`: it counts matching lines, and the bundle is one ~200KB line, so
every needle present anywhere collapses to a count of 1 and every "at least one" gate passes
vacuously. Four agents on this project have already been burned by this.
</interfaces>

<branch_discipline>
**This is a phase-level precondition on the operator, not a per-task assertion on the executor.**

Before `/gsd:execute-phase 5.2` is invoked, the orchestrator's working tree must already be on
`phase-5.2-clinical-bracket-revision`, cut from `main` — not `main`, and not
`thread-phase-6-purchase-prerequisites`, which carries in-flight Phase 6 Wave 1 work. This repo uses
the manual branching convention: `.planning/config.json` sets no `git.branching_strategy`, so
`/gsd:execute-phase` neither creates nor switches branches. Andrew cuts the branch by hand, exactly
as phases 1 through 6 were run.

**Do not assert on `git branch --show-current` inside a task, and never check out a branch to
"correct" it.** `workflow.use_worktrees` defaults to true, so an autonomous plan runs in an isolated
worktree whose HEAD is `worktree-agent-<id>`, and the executor harness FATALs on a commit from any
other branch name in that mode. Committing on `worktree-agent-*` is correct and expected here; the
work reaches `phase-5.2-clinical-bracket-revision` through the harness, not through a checkout.
Nobody should "fix" this later.

Why the precondition exists at all: plan 05.2-05 opens its PR from
`phase-5.2-clinical-bracket-revision`. If any part of this phase — `migrations/005_widen_score_bracket_check.sql`
above all — lands on a different base, it is absent from the branch that actually ships, and the
deploy puts code writing `9+` in front of a database whose CHECK was never widened. That is
T-5.2-01's data-loss mode arriving through the back door.

This plan opens the PR and performs the merge, so it is where the precondition is finally observable:
Task 1's checkpoint requires confirming the PR's base branch before authorization.

An agent never merges unprompted. Andrew reviews and merges; an agent merges only under explicit
in-session authorization, matching the PR #16-19 precedent recorded in STATE.md.
</branch_discipline>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review the PR and authorize merge and deploy</name>

  <action>
    Push the feature branch and open a PR against `main` whose description explicitly calls out the
    PHI-relevant surfaces this phase touches (`app/lib/quiz-validation.ts` on the submit path,
    `app/lib/pdf.ts` and `app/routes/app.quiz-results.tsx` as PHI renderers, and
    `migrations/005_widen_score_bracket_check.sql`), states that zero dependencies were added, and
    links the SOURCE doc. Then STOP and wait. Do not merge and do not deploy until Andrew's
    authorization string is received.
  </action>

  <what-built>
    Plans 05.2-01 through 05.2-04 are complete: the clinical brackets moved to `0–2 / 3–8 / 9+` in
    `scoring.ts` with the `ScoreBracket` union and `types.ts` following; the `isProvisional` marking
    is cleared; all three recommendation blocks carry William Miller's verbatim 2026-08-13 copy; the
    `/ 60` denominator is gone from the patient view while the derived ceiling still drives the bar's
    geometry; API validation, the clinical PDF, the embedded admin table, the dashboard counts, the
    E2E script and every test fixture follow; the theme bundle was rebuilt in the same commits as the
    source it reflects and is guarded by freshness markers each measured 0-before / at-least-1-after;
    and migration 005 has already been executed against `alledrops_quiz_dev` behind a verified
    backup, so the database accepts `9+` today.
  </what-built>

  <how-to-verify>
    1. Open the PR. Confirm the branch is NOT `main` and NOT `thread-phase-6-purchase-prerequisites`.
    2. Confirm the PR description calls out that this touches the PHI write path — `score_bracket` is
       a PHI column, `quiz-validation.ts` sits on `POST /api/quiz/submit`, and `pdf.ts` plus
       `app.quiz-results.tsx` are PHI renderers.
    3. Read the three replaced recommendation blocks in the `ResultsDisplay.tsx` diff side by side
       with `.planning/phases/05.2-clinical-bracket-revision/05.2-SOURCE-william-2026-08-13.md`. This
       is the only human check that catches a transcription error before it reaches a patient.
       Confirm the stray trailing `no` on the source's `0-2` line was NOT transcribed.
    4. Confirm no commit touches `package.json` or `package-lock.json` — zero new dependencies.
    5. Confirm no commit adds a third-party script, analytics tag, or tracking pixel to the quiz
       embed page.
    6. Confirm `npm run typecheck` and `npm test` are green on the branch head.
  </how-to-verify>

  <resume-signal>Type "merge and deploy" to authorize, or describe what must change first.</resume-signal>

  <acceptance_criteria>
    - The PR URL and the branch name are recorded in the SUMMARY
    - Andrew's verbatim authorization string is recorded in the SUMMARY
    - `git log --stat` on the branch shows zero changes to `package.json` and `package-lock.json`, confirmed and stated in the SUMMARY
  </acceptance_criteria>

  <done>Explicit authorization to merge to `main` and deploy to Fly is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy and verify on served bytes</name>

  <files>(no repository files modified)</files>

  <read_first>
    - `.planning/phases/05.2-clinical-bracket-revision/05.2-01-SUMMARY.md` — the committed bundle byte length and the exact marker needles measured during the rebuild; the served-bytes check must reuse those needles
    - `.planning/STATE.md` — the Phase 4 and Phase 5 deploy verification tables, the format this task reproduces, and the note that Fly's "not listening on the expected address" warning has been a false alarm twice
    - `CLAUDE.md` — the deploy section (`shopify app deploy` does not deploy the Fly app) and the manual smoke-test curl with its cleanup DELETE
    - `.planning/phases/05.2-clinical-bracket-revision/05.2-04-SUMMARY.md` — confirmation that migration 005 is already live, which is what makes a `9+` POST expected to succeed
  </read_first>

  <action>
    Merge to `main` and run `fly deploy -a alle-drops-quiz-app`. Do NOT run `shopify app deploy` —
    Phase 5.2 changes no file under `extensions/`, so it has nothing to ship and would issue a new
    Shopify app version for no reason.

    Confirm `GET https://alle-drops-quiz-app.fly.dev/health` returns 200 before believing the
    rollout, regardless of what the rollout printed.

    Fetch `https://alle-drops-quiz-app.fly.dev/quiz-bundle-js` with a cache-buster query parameter
    and verify on the SERVED bytes using a `node -e` script whose only counting primitive is
    `body.split(needle).length - 1`. Presence needles, each expected at least 1: `Prior to Starting
    Treatment`, `May Significantly Help Manage Your Symptoms`, `board-certified allergist`. Absence
    needles, each expected exactly 0: `May Significantly Help You`, `or scheduling an appointment
    with an allergist`, and the aria-label fragment that begins with a space and reads `on a 0 to`
    followed by a space.

    Do NOT use `3-6` as a needle in either direction: `ConsentStep.tsx` ships the clinical phrase
    `3-6 months` into the same bundle, so that count is permanently non-zero and proves nothing.

    Record the served byte length against the 203,797 B baseline, and compute SHA-256 of both the
    served body and the committed `public/quiz-bundle.js`. They must be IDENTICAL, as in Phase 4 and
    Phase 5 — a similar length is not evidence.

    Prove the write path end to end against the deployed app and the widened constraint. Preferred:
    run `npx tsx scripts/e2e-test.ts` if a Cloud SQL proxy on port 5433 and a populated `.env` are
    available. If they are not, fall back to the manual smoke test documented in `CLAUDE.md`: POST to
    `https://alle-drops-quiz-app.fly.dev/api/quiz/submit` with `score_bracket` of `9+`, `quiz_score`
    of 9, an `Origin` header, and obviously synthetic identity values including the email
    `phase52-deploy-probe@example.com` and a `symptom_profile_id` prefixed `AOD_PHASE52_PROBE_`. It
    must return a success status — the first live proof that the deployed code and the widened
    constraint agree.

    Then POST a second probe identical except `score_bracket` of `7+`, and confirm it is REJECTED
    with a 400 carrying the fixed validation message. This proves the narrowed API allowlist is live,
    not merely committed.

    Clean up: delete the successful probe row by `patient_email`, then confirm the deletion by
    re-reading `SELECT COUNT(*) FROM submissions` and comparing against the count recorded in
    05.2-04's SUMMARY. Never trust the DELETE's exit code. Confirm the rejected probe created no row
    by the same count. Print no column of any other row.
  </action>

  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}\n" https://alle-drops-quiz-app.fly.dev/health</automated>
  </verify>

  <acceptance_criteria>
    - CLI: `GET /health` returns 200
    - CLI: a `node -e` served-bytes counter using `split(needle).length - 1` prints at least 1 for each of the three presence needles and exactly 0 for each of the three absence needles; the full output table is pasted into the SUMMARY
    - CLI: SHA-256 of the served `/quiz-bundle-js` body equals SHA-256 of the committed `public/quiz-bundle.js`; both digests are pasted into the SUMMARY alongside the served byte length and the 203,797 B baseline
    - Behavior: a live POST with `score_bracket` of `9+` returns a success status; the status code and the response body shape (without any PHI value) are recorded
    - Behavior: a live POST with `score_bracket` of `7+` returns 400 and the validation message; the message text is quoted
    - CLI: post-cleanup `SELECT COUNT(*) FROM submissions` equals the figure recorded in 05.2-04's SUMMARY; both numbers stated
    - CLI: `shopify app deploy` was NOT run — confirm explicitly in the SUMMARY
    - PHI self-review (CLAUDE.md): both probes used synthetic identity values, the successful probe row was deleted and the deletion confirmed by count, and no existing row's column value was printed or logged — confirm each explicitly
  </acceptance_criteria>

  <done>The bytes Fly serves are the bytes committed, they carry the new copy and none of the retired copy, and the live write path accepts `9+` while rejecting `7+`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser pass on the deployed page at 375px and desktop</name>

  <action>
    Present the ten-item checklist below verbatim, with the deployed preview URL
    `https://alle-drops-quiz-app.fly.dev/quiz-embed?test=1` and the three synthetic scores (1, 8, 9)
    Andrew needs to reach each bracket. Then STOP and wait for a per-item pass/fail result. Do not
    self-approve, do not infer a pass from a green suite, and do not fix any reported failure inside
    this checkpoint — file it as a gap for `/gsd:plan-phase 5.2 --gaps` instead.
  </action>

  <what-built>
    The deployed `/quiz-embed` renders three revised brackets. `0–2` keeps its headline and gains a
    rewritten body; `3–8` gains the headline "You May Benefit From Seeing an Allergist Prior to
    Starting Treatment", materially longer than anything that shipped before; `9+` gains the headline
    "Sublingual Immunotherapy May Significantly Help Manage Your Symptoms". The score readout now
    reads "Score: N" with no denominator, and the bar's aria-label no longer names a ceiling. The
    colour bar itself is unchanged in construction — three equal-width bands tracking the clinical
    brackets, which is exactly what William confirmed.
  </what-built>

  <how-to-verify>
    Use `https://alle-drops-quiz-app.fly.dev/quiz-embed?test=1` and the synthetic-score path Phase 5
    used. Check every item; report each as pass or fail rather than approving in bulk.

    At a 375px-wide viewport (device toolbar, iPhone SE width):
      1. Score 1: headline reads "Your Symptoms Appear Mild and Well-Controlled", the marker sits in
         the green third, the legend bolds "Low", and the body ends with "...if you would like to
         discuss this further."
      2. Score 8 — the case this whole phase exists for: the marker sits in the ORANGE third, the
         legend bolds "Moderate", the caption reads "Moderate symptom burden", and the headline reads
         "You May Benefit From Seeing an Allergist Prior to Starting Treatment". If score 8 renders
         as High, STOP — the boundary did not move.
      3. Score 9: the marker sits in the RED third, the legend bolds "High", and the headline reads
         "Sublingual Immunotherapy May Significantly Help Manage Your Symptoms".
      4. The longest headline (score 8) wraps onto multiple lines and is fully readable — no
         truncation, no ellipsis, no clipped descender, and no horizontal page scroll. This is the
         one open UI question in the phase (plan 05.2-01, D-52-02).
      5. The score readout above the bar reads "Score: 8" — no "of 60", no denominator anywhere on
         the page.
      6. The bridge sentence "The bar shows how many symptoms you reported. Below is what that
         usually means for care." is still present. It is D-06's only remaining two-axis label; if it
         is gone, something must replace it.

    At desktop width (>= 750px):
      7. Repeat items 2, 3, 4 and 5.
      8. Confirm the three colour bands are still EQUAL width. If red occupies most of the bar,
         someone reverted the equal-share rendering to span-proportional — that is the one
         arrangement nobody chose.

    Both widths:
      9. Confirm no cookie banner, chat widget, analytics tag, or any third-party script appears on
         the embed page.
     10. Note that scores 8 and 9 land within a few pixels of each other on the orange/red seam. This
         is expected, measured, and accepted — colour, the bolded legend word, and the recommendation
         copy carry the threshold, not marker position. Do not report it as a defect.
  </how-to-verify>

  <resume-signal>Type "approved" with the pass/fail result for each of the ten items, or describe the failures.</resume-signal>

  <acceptance_criteria>
    - All ten checks were performed at the stated widths and each result is recorded individually in the SUMMARY, not collapsed into a single approval
    - Item 2 (score 8 renders Moderate) explicitly passed — this is the phase's central claim
    - Item 4 (375px wrapping of the longest headline) explicitly passed, closing D-52-02
    - Item 5 (no denominator) explicitly passed, closing SCORE-06 on the rendered page
    - Item 8 (equal-width bands) explicitly passed
    - Any failure is filed as a gap for `/gsd:plan-phase 5.2 --gaps` rather than fixed inside this checkpoint
  </acceptance_criteria>

  <done>A human has confirmed all three brackets, the moved threshold, the denominator removal, and the 375px headline wrap on the deployed page.</done>
</task>

<task type="auto">
  <name>Task 4: Close the requirements and record the phase</name>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the file being modified; the "Clinical Bracket Revision" section (SCORE-04/05/06) and the coverage table rows for those three IDs near line 363
    - `.planning/ROADMAP.md` — the file being modified; the Phase 5.2 block (line 577 onward), its `**Plans**: TBD` line, the phase list entry at line 30, and Sequencing Constraint 7 at line 69
    - `.planning/STATE.md` — the file being modified; read the warning about `gsd-sdk query state.*` handlers rewriting the whole `progress:` frontmatter block, and the Phase 5 deploy verification table whose format this task copies
    - `.planning/phases/05.2-clinical-bracket-revision/05.2-04-SUMMARY.md` and `05.2-05-SUMMARY.md` — for the backup ID, row counts, served byte length, SHA-256 digests, and marker counts to transcribe
  </read_first>

  <action>
    Mark SCORE-04, SCORE-05 and SCORE-06 complete in `.planning/REQUIREMENTS.md` — change each
    checkbox to `[x]` and update the three coverage-table rows near line 363 from "Not started" to a
    completion note naming Phase 5.2 and the deploy date. Do not delete the amendment notes above the
    section; this project records retractions in place.

    In `.planning/ROADMAP.md`: check the Phase 5.2 entry in the phase list at line 30; replace the
    Phase 5.2 block's `**Plans**: TBD — run /gsd:plan-phase 5.2` with the five-plan list, each marked
    complete with a one-line objective; and annotate Sequencing Constraint 7 as SATISFIED with the
    date, since Phase 5.2 has now landed and Phase 6 Wave 2 is unblocked.

    In `.planning/STATE.md`: add a Phase 5.2 deploy verification table in the same before/after
    format as the Phase 4 and Phase 5 tables, carrying the served `/quiz-bundle-js` byte length, the
    SHA-256 match, and each presence and absence marker count. Add a decision entry recording
    D-52-01 (the score line reads "Score: N"), D-52-02 (no CSS change; the h3 wraps), D-52-03 (strict
    API allowlist versus permissive DB CHECK), D-52-04 (provider surfaces read five labels), and
    D-52-05 (the denominator stays on provider surfaces). Add the migration 005 execution record with
    its backup ID. Update the Current Position block to point at Phase 6.

    Add a forward note — as a note, NOT as an edit to any Phase 6 file: `06-03` and `06-05` are the
    first Phase 6 artifacts that name a bracket threshold, and it has moved from 7 to 9. Record that
    this must be picked up by `/gsd:plan-phase 6` when those plans are written. Do not modify any
    file under `.planning/phases/06-purchase-prerequisites/`.

    Treat every `gsd-sdk query state.*` call as capable of rewriting the entire `progress:`
    frontmatter block, not just the field it advertises. Six separate handlers have corrupted
    `completed_phases`, `completed_plans` and `percent` on this project. Diff the frontmatter after
    any such call and restore by hand if it moved. Prefer editing STATE.md directly.

    Commit the three planning files together with a `docs(05.2):` message.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');for(const id of ['SCORE-04','SCORE-05','SCORE-06']){const i=r.indexOf('**'+id+'**');if(i<0||r.slice(Math.max(0,i-8),i).indexOf('[x]')<0){console.error('not marked complete:',id);process.exit(1)}}console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Source: `.planning/REQUIREMENTS.md` shows `[x]` for SCORE-04, SCORE-05 and SCORE-06, and all three coverage-table rows name Phase 5.2 and the deploy date
    - Source: `.planning/ROADMAP.md` line-30 phase entry is checked, the Phase 5.2 block lists all five plans as complete with one-line objectives, and Sequencing Constraint 7 is annotated SATISFIED with a date
    - Source: `.planning/STATE.md` contains a Phase 5.2 deploy verification table with the served byte length, the SHA-256 match, and six marker counts
    - Source: `.planning/STATE.md` contains decision entries for D-52-01 through D-52-05 and the migration 005 execution record with its backup ID
    - Source: `.planning/STATE.md` contains the Phase 6 threshold forward note, and `git diff --name-only` for this commit lists no file under `.planning/phases/06-purchase-prerequisites/`
    - CLI: after any `gsd-sdk query state.*` call, `git diff .planning/STATE.md` was inspected and the `progress:` frontmatter confirmed unchanged except where intended; the check is stated in the SUMMARY
    - CLI: `git show --stat HEAD` lists exactly the three planning files
  </acceptance_criteria>

  <done>SCORE-04, SCORE-05 and SCORE-06 are closed, Phase 5.2 is recorded with its deploy evidence, Sequencing Constraint 7 is satisfied, and Phase 6's moved threshold is noted without touching a Phase 6 file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `main` → Fly production release | A merge and deploy puts new clinical copy in front of patients with no further gate |
| public internet → deployed `/quiz-embed` | The verification probes POST to a live PHI endpoint |
| deployed code → widened CHECK | The first moment the two halves of this phase meet is in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.2-23 | Denial of Service | deploy-before-migration ordering | mitigate | Migration 005 executed in plan 05.2-04, which this plan depends on. The deploy therefore lands into a database that already accepts `9+`. Task 2 proves it with a live POST rather than assuming it. |
| T-5.2-24 | Tampering | stale committed bundle shipped | mitigate | Task 2 compares SHA-256 of the served body against the committed artifact. A length match is explicitly rejected as evidence. |
| T-5.2-25 | Information Disclosure | verification probes writing PHI | mitigate | Both probes use synthetic identity values and a `phase52-deploy-probe@example.com` email. The successful row is deleted and the deletion confirmed by `COUNT(*)`, never by exit code — the 01-06 precedent. No other row's column value is printed. |
| T-5.2-26 | Information Disclosure | third-party scripts on the PHI-collecting page | mitigate | Human browser pass item 9 explicitly checks for analytics, chat, and tracking scripts. Note that LAUNCH-01 in STATE.md records a live Klaviyo pixel on the storefront quiz page that a DOM scan will NOT find — a clean item 9 does not close LAUNCH-01 and must not be read as closing it. |
| T-5.2-27 | Spoofing | wrong branch merged | mitigate | Task 1's checkpoint requires confirming the branch is neither `main` nor `thread-phase-6-purchase-prerequisites`, which carries unrelated in-flight Phase 6 work. |
| T-5.2-28 | Repudiation | planning-state corruption | mitigate | Task 4 requires diffing STATE.md's `progress:` frontmatter after any `gsd-sdk query state.*` call, because six separate handlers have silently rewritten it on this project. |
| T-5.2-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. Task 1's checkpoint asserts `package.json` and `package-lock.json` are unchanged across the whole phase. |
</threat_model>

<verification>
- `GET /health` returns 200 after the deploy
- Served `/quiz-bundle-js` SHA-256 equals the committed `public/quiz-bundle.js` SHA-256
- Three presence markers at least 1, three absence markers exactly 0, all counted with `split(needle).length - 1` on served bytes
- A live `9+` POST succeeds; a live `7+` POST returns 400
- Post-cleanup `SELECT COUNT(*) FROM submissions` matches 05.2-04's recorded figure
- All ten human browser checks recorded individually, with items 2, 4, 5 and 8 explicitly passing
- SCORE-04, SCORE-05, SCORE-06 marked `[x]` in REQUIREMENTS.md
- No file under `.planning/phases/06-purchase-prerequisites/` modified
</verification>

<success_criteria>
The bracket revision is live, proven on served bytes and on a live write, seen by a human at both
widths, and recorded — and Phase 6 Wave 2 is unblocked with its moved threshold written down.
</success_criteria>

<output>
Create `.planning/phases/05.2-clinical-bracket-revision/05.2-05-SUMMARY.md` when done. It MUST
contain the served-bytes marker table, both SHA-256 digests, both live POST outcomes with status
codes, the post-cleanup row count, and the ten individual human browser-check results.
</output>
