---
phase: 05.2-clinical-bracket-revision
plan: 03
subsystem: database
tags: [postgres, migration, check-constraint, cloud-sql, vitest]

requires:
  - phase: 04-mandatory-allergy-testing
    provides: "004_create_submission_files.sql's precedent for additive CHECK widening (DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT pair) and the SET ROLE alledrops_app execution pattern from the 04-19 execution record"
provides:
  - "migrations/005_widen_score_bracket_check.sql — unexecuted, reviewable migration widening submissions.score_bracket's CHECK to the five-value union of retired and current clinical bracket labels"
  - "tests/migration-005-contract.test.ts — automated, non-vacuous guard proving migration 005 is additive, single-table, and carries its execution preconditions in writing"
  - "a comment pointing readers of migrations/001's original three-value CHECK at migration 005"
affects: ["05.2-04 (executes this migration against alledrops_quiz_dev)", "05.2-05 (opens the PR that ships this file on phase-5.2-clinical-bracket-revision)"]

tech-stack:
  added: []
  patterns:
    - "Comment-stripped source-text guard test (strip lines starting with `--`, then split(needle).length-1 counting) for asserting properties of an unexecuted SQL migration file without a DB connection"

key-files:
  created:
    - migrations/005_widen_score_bracket_check.sql
    - tests/migration-005-contract.test.ts
  modified:
    - migrations/001_create_submissions.sql

key-decisions:
  - "CHECK list is the five-value UNION ('0-2', '3-6', '3-8', '7+', '9+'), not a replacement — no historical row is relabelled, per the SOURCE doc's 'Owed back to William' assumption 2"
  - "Migration file contains no BEGIN/COMMIT/SET ROLE statements itself — those are execution-time instructions recorded in the header for plan 05.2-04's operator to perform by hand, matching 004's split between file body and header prose"
  - "Left an empty EXECUTION RECORD block at the bottom of the migration's header for plan 05.2-04 to fill in (backup ID, pre/post row counts, confirmed constraint name), matching 004's own post-execution record shape"

requirements-completed: [SCORE-04]

duration: ~15min
completed: 2026-08-13
---

# Phase 05.2 Plan 03: Widen score_bracket CHECK constraint Summary

**Authored `migrations/005_widen_score_bracket_check.sql`, an unexecuted, additive DROP/ADD CONSTRAINT pair widening `submissions.score_bracket` to the five-value union of retired and current clinical bracket labels, guarded by a non-vacuous source-text contract test — zero DDL executed against Cloud SQL.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-08-13T10:04:00Z (approx)
- **Completed:** 2026-08-13T10:06:25Z
- **Tasks:** 2/2 completed
- **Files modified:** 3 (1 created migration, 1 comment-only edit, 1 created test)

## Accomplishments
- `migrations/005_widen_score_bracket_check.sql` written: widens `submissions.score_bracket`'s CHECK from `('0-2', '3-6', '7+')` to `('0-2', '3-6', '3-8', '7+', '9+')` via `DROP CONSTRAINT IF EXISTS` / `ADD CONSTRAINT`, matching migration 004's precedent shape exactly. Header records every precondition plan 05.2-04 must satisfy before running it: named on-demand Cloud SQL backup read back via `gcloud sql backups list`/`describe`, `pg_constraint` confirmation of the live auto-generated constraint name, run-before-deploy ordering (with rationale for why this migration's additive direction reverses Phase 3's DROP-before-code rule), a pre/post row-count check, and the `SET ROLE alledrops_app` / `RESET ROLE` ownership pattern from STATE.md's 04-19 entry. An empty "EXECUTION RECORD" block is left at the bottom for plan 05.2-04 to fill in.
- `migrations/001_create_submissions.sql` received a 3-line comment-only edit above the `score_bracket` column pointing readers at migration 005; the executed DDL itself is untouched.
- `tests/migration-005-contract.test.ts` written as a pure source-text guard (no DB connection, no `app/lib/db.ts` import) asserting: exactly one `DROP CONSTRAINT IF EXISTS`, exactly one `ADD CONSTRAINT ... CHECK (score_bracket IN`, the exact five-value ordered list, zero destructive statements (`UPDATE `, `DELETE `, `TRUNCATE`, `DROP TABLE`, `DROP COLUMN`, `ALTER COLUMN`) in a comment-stripped view, zero references to `submission_access_log` or `submission_files` (the 04-19 ownership trap), and presence of `gcloud sql backups`, `pg_constraint`, and `SET ROLE alledrops_app` in the full file.
- Proved the destructive-statement gate is non-vacuous: temporarily appended `UPDATE submissions SET score_bracket = '3-8';` to the migration file, ran the test, observed the destructive-statement assertion fail RED (`expected 0, got 1`), removed the line, reran green, and confirmed via `git diff`/`git status` that the migration file was byte-identical to its committed state afterward.
- Full suite (`npm test`, 50 files / 740 tests) and `npm run typecheck` both pass.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author migration 005 — widen the score_bracket CHECK, execute nothing** - `cf34fc5` (feat)
2. **Task 2: Add an automated contract test for migration 005** - `5e8f0f6` (test)

_No plan-metadata commit made in this plan — the orchestrator owns STATE.md/ROADMAP.md writes after all worktree agents in the wave complete, per this plan's instructions._

## Files Created/Modified
- `migrations/005_widen_score_bracket_check.sql` - New, unexecuted migration widening `submissions.score_bracket`'s CHECK to the five-value union; carries every precondition plan 05.2-04 needs and an empty execution-record block
- `migrations/001_create_submissions.sql` - Comment-only edit above `score_bracket` pointing readers at migration 005
- `tests/migration-005-contract.test.ts` - New source-text contract test, proven non-vacuous by an observed RED/GREEN cycle

## Decisions Made
- Kept the migration file to exactly the two `ALTER TABLE` statements the plan specified (no `BEGIN`/`COMMIT`/`SET ROLE` wrapping in the file itself) — those transactional/role details are execution-time instructions for the human operator running it by hand in Cloud SQL Studio/`psql`, recorded in the header, matching how migration 004 separates file body from header prose.
- Followed the plan's exact five-value CHECK ordering (`'0-2', '3-6', '3-8', '7+', '9+'`) rather than reordering old-then-new or alphabetically, since 05.2-04's contract test and this plan's own test both assert on that literal string.

## Deviations from Plan

None - plan executed exactly as written. One environment-level note (not a plan deviation): this worktree's branch (`worktree-agent-ac8d3f37c7d44aa75`) was created from an earlier point on `main` than `phase-5.2-clinical-bracket-revision`'s tip, so the plan file, `05.2-SOURCE-william-2026-08-13.md`, and the sibling plan files were not present on disk at task start. All were read via `git show phase-5.2-clinical-bracket-revision:<path>` (read-only, no checkout of that branch, no HEAD change) rather than being missing from the repository. Everything the plan's `<read_first>` and `<context>` sections named was available either directly on disk (`migrations/001` through `004`, `STATE.md`'s 04-19 entry, `tests/quiz-bundle-freshness.test.ts`) or via that read-only `git show`.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required. Plan 05.2-04 (not this plan) is the human-gated task that takes a Cloud SQL backup and executes this migration.

## Next Phase Readiness
- Migration 005 is authored, reviewable, and provably additive/non-destructive — ready for plan 05.2-04 to execute against `alledrops_quiz_dev` after taking and reading back a named backup and confirming the live constraint name via `pg_constraint`.
- Zero DDL was executed as part of this plan. No `gcloud`, `psql`, or `pg`-script invocation occurred at any point.
- `git log --oneline -2` on this branch shows two commits: the first (`cf34fc5`) touching only `migrations/`, the second (`5e8f0f6`) touching only `tests/migration-005-contract.test.ts`.

---
*Phase: 05.2-clinical-bracket-revision*
*Completed: 2026-08-13*

## Self-Check: PASSED

- FOUND: migrations/005_widen_score_bracket_check.sql
- FOUND: migrations/001_create_submissions.sql
- FOUND: tests/migration-005-contract.test.ts
- FOUND: .planning/phases/05.2-clinical-bracket-revision/05.2-03-SUMMARY.md
- FOUND: commit cf34fc5 (git log --oneline -3)
- FOUND: commit 5e8f0f6 (git log --oneline -3)
