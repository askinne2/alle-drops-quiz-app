---
phase: 05.2-clinical-bracket-revision
plan: 04
type: execute
wave: 2
depends_on: ["05.2-03"]
files_modified:
  - migrations/005_widen_score_bracket_check.sql
autonomous: false
requirements: [SCORE-04]

must_haves:
  truths:
    - "A named on-demand Cloud SQL backup exists and its SUCCESSFUL status was read back from gcloud, not inferred from an exit code"
    - "The live constraint on submissions.score_bracket accepts all five labels, confirmed by reading its definition back from pg_constraint"
    - "The submissions row count is identical before and after the DDL"
    - "An INSERT carrying score_bracket 9+ succeeds against alledrops_quiz_dev, proven inside a transaction that is rolled back"
    - "No historical row's score_bracket value changed"
  artifacts:
    - path: "migrations/005_widen_score_bracket_check.sql"
      provides: "Post-execution record — backup ID, confirmed constraint name, row counts, verification query output"
      contains: "EXECUTED"
  key_links:
    - from: "migrations/005_widen_score_bracket_check.sql"
      to: "alledrops_quiz_dev.submissions"
      via: "hand-executed ALTER TABLE in Cloud SQL Studio or psql"
      pattern: "ADD CONSTRAINT submissions_score_bracket_check"
---

<objective>
Take a named, verified Cloud SQL backup, then execute `migrations/005_widen_score_bracket_check.sql`
by hand against `alledrops_quiz_dev`, and prove on query output — never on an exit code — that the
widened constraint is live, that no row changed, and that a `9+` INSERT now succeeds.

Purpose: this is the gate that lets Phase 5.2's application code be deployed at all. It runs BEFORE
the deploy, deliberately: the widened constraint keeps accepting what the currently-live code writes,
while the current narrow constraint would reject everything the new code writes.
Output: a verified backup ID, a live five-value CHECK, and an execution record written into 005's
header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-clinical-bracket-revision/05.2-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Environment facts. Do not re-derive these. -->

GCP project: `alledrops-quiz`. Cloud SQL instance: `alledrops-quiz-data` (Postgres 18, us-east1).
Database: `alledrops_quiz_dev`. App user: `alledrops_app`. Superuser: `postgres`.
`submissions` is owned by `alledrops_app`; `submission_access_log` is owned by `postgres`.
Migration 005 touches `submissions` only, so a single role suffices.

Backup precedent (STATE.md): 1786306233540 (Phase 3, `03-06`) and 1786361850289 (Phase 4, `04-19`),
both ON_DEMAND / SUCCESSFUL and both read back via `gcloud sql backups list` / `describe` rather
than trusted from the create command's exit code.

Execution surfaces, in order of preference:
  1. Cloud SQL Studio against `alledrops_quiz_dev`, as `postgres`, with `SET ROLE alledrops_app;`
     before the ALTERs and `RESET ROLE;` after — the 04-19 precedent.
  2. `psql` as `alledrops_app` (which owns `submissions` outright, so no role switch is needed).
This machine's IP is NOT on the Cloud SQL authorized-networks list (only Fly's `216.246.40.114/32`
is). A working non-Studio route documented in STATE.md is `fly ssh console -a alle-drops-quiz-app`
running a `pg` script against the Fly-held `DATABASE_URL`. Note that Prisma on that machine is the
SQLite session store — a Prisma raw query against `submissions` fails with a SQLite parser error.
There is no ORM push command for this database.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Take and verify the pre-DDL backup, and gather every precondition</name>

  <files>(no repository files modified)</files>

  <read_first>
    - `migrations/005_widen_score_bracket_check.sql` — the file about to be executed; read its full precondition header, which enumerates exactly what this task must satisfy
    - `.planning/STATE.md` — the `03-06` and `04-19` decision entries recording how prior backups were taken and read back
    - `CLAUDE.md` — the PHI self-review checklist; every query in this task must select counts and metadata only, never a PHI column value
  </read_first>

  <action>
    Take a named on-demand backup with
    `gcloud sql backups create --instance=alledrops-quiz-data --project=alledrops-quiz
    --description="pre-phase52-widen-score-bracket-check"`.

    Then READ IT BACK. Run `gcloud sql backups list --instance=alledrops-quiz-data
    --project=alledrops-quiz --limit=5` and `gcloud sql backups describe <ID>
    --instance=alledrops-quiz-data --project=alledrops-quiz`, and capture the ID, the
    `type: ON_DEMAND`, the `status: SUCCESSFUL`, the `windowStartTime`, and the description. A
    SUCCESSFUL status printed by `describe` is the evidence; the create command's exit code is not.
    If status is anything other than SUCCESSFUL, STOP and escalate.

    Gather the remaining preconditions, all as query output pasted verbatim into the SUMMARY:
      - The live constraint name and definition:
        `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid =
        'submissions'::regclass AND contype = 'c';`
        Confirm whether the name is literally `submissions_score_bracket_check`. If it is not, the
        migration file's two statements MUST be edited to the real name before execution — a
        `DROP CONSTRAINT IF EXISTS` against a wrong name succeeds silently and leaves the narrow
        constraint in force.
      - A fresh `SELECT COUNT(*) FROM submissions;` read in THIS session, not carried from an
        earlier one.
      - The current distribution: `SELECT score_bracket, COUNT(*) FROM submissions GROUP BY 1 ORDER
        BY 1;`. This is the baseline that must be byte-identical after the DDL. `score_bracket` is a
        PHI column, but a GROUP BY count of it discloses no individual — do not widen this query to
        select any other column, and do not join to anything.
      - Confirmation that `submissions` is still test data only. STATE.md records Andrew's standing
        "everything is test data until I say otherwise"; restate it in the checkpoint rather than
        assuming it silently.

    Run NO `ALTER`, `UPDATE`, `DELETE`, or `INSERT` in this task. Reads and the backup only.

    Every query in this task selects counts, names, and definitions. No PHI field value may be
    printed, logged, or written into the SUMMARY.
  </action>

  <verify>
    <automated>gcloud sql backups list --instance=alledrops-quiz-data --project=alledrops-quiz --limit=3 --format="table(id,type,status,description)"</automated>
  </verify>

  <acceptance_criteria>
    - CLI: `gcloud sql backups describe <ID>` prints `type: ON_DEMAND` and `status: SUCCESSFUL`, and both lines are pasted verbatim into the SUMMARY alongside the backup ID and timestamp
    - CLI: the `pg_constraint` query output is pasted verbatim into the SUMMARY, and the SUMMARY states explicitly whether the live constraint name matches `submissions_score_bracket_check`
    - CLI: a fresh `SELECT COUNT(*) FROM submissions;` result is recorded, with a note that it was read in this session
    - CLI: the `GROUP BY score_bracket` distribution is recorded as the pre-DDL baseline
    - Behavior: shell history for this task contains zero `ALTER`, `UPDATE`, `DELETE`, `INSERT`, `DROP`, and `TRUNCATE` statements
    - PHI self-review (CLAUDE.md): every query selected only `COUNT(*)`, `conname`, or
      `pg_get_constraintdef`; no patient value appears in the SUMMARY or in any log — confirm explicitly
  </acceptance_criteria>

  <done>A SUCCESSFUL named backup exists and was read back from gcloud, the real constraint name is known, and the pre-DDL row count and bracket distribution are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authorize DDL execution against alledrops_quiz_dev</name>

  <action>
    Present, in full and unsummarised, the four evidence items gathered in Task 1 — the
    `gcloud sql backups describe` output with its ID / ON_DEMAND / SUCCESSFUL lines, the
    `pg_constraint` name and definition, the fresh `SELECT COUNT(*) FROM submissions` result, and the
    `GROUP BY score_bracket` distribution — then STOP and wait. Execute no DDL until Andrew's
    authorization string is received. If he reports a constraint-name mismatch, edit and re-commit
    `migrations/005_widen_score_bracket_check.sql` with the real name before resuming, and re-present
    the evidence.
  </action>

  <what-built>
    Migration 005 is authored, committed alone, and guarded by an automated contract test proving it
    contains no UPDATE, DELETE, TRUNCATE, DROP TABLE, DROP COLUMN, or ALTER COLUMN and touches only
    the `submissions` table. A named on-demand Cloud SQL backup has been taken and its SUCCESSFUL
    status read back from `gcloud sql backups describe`. The live constraint name, the current row
    count, and the current bracket distribution have all been read in this session.
  </what-built>

  <how-to-verify>
    1. Read the four evidence items pasted above this checkpoint: the backup ID with its
       `type: ON_DEMAND` / `status: SUCCESSFUL` lines, the `pg_constraint` name and definition, the
       fresh `SELECT COUNT(*) FROM submissions` result, and the `GROUP BY score_bracket` distribution.
    2. Confirm the backup ID is new (not 1786306233540 from Phase 3 or 1786361850289 from Phase 4).
    3. Confirm the constraint name printed by `pg_constraint` matches the name written into the two
       ALTER statements in `migrations/005_widen_score_bracket_check.sql`. If it does not, say so —
       the file must be edited before anything runs.
    4. Confirm the standing "everything in `submissions` is test data until I say otherwise" still
       holds. If any real patient has completed the quiz, STOP — the never-relabel principle stops
       being a correctness nicety and becomes a live clinical-record question.
    5. Confirm you accept that this DDL runs BEFORE the Fly deploy of the Phase 5.2 code, which is
       the deliberate inverse of Phase 3's ordering rule. Rationale: the widened constraint still
       accepts everything the currently-live code writes (`3-6`, `7+`), so running ahead is safe;
       running behind would mean deployed code writing `9+` into a database that rejects it, losing
       a patient's completed intake at INSERT. Migration 004 deviated identically (STATE.md 04-19).
  </how-to-verify>

  <resume-signal>Type "execute ddl" to authorize, or describe what must change first.</resume-signal>

  <acceptance_criteria>
    - The four evidence items were presented in full before the prompt, not summarised
    - Andrew's explicit authorization string is recorded verbatim in the SUMMARY
    - If the constraint name did not match, the migration file was edited and re-committed BEFORE execution, and that commit is referenced in the SUMMARY
  </acceptance_criteria>

  <done>Explicit human authorization to run DDL against `alledrops_quiz_dev` is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Execute migration 005 and prove the widened constraint on query output</name>

  <files>migrations/005_widen_score_bracket_check.sql</files>

  <read_first>
    - `migrations/005_widen_score_bracket_check.sql` — the file being executed and then modified (execution record only); re-read the header immediately before running, since Task 2 may have edited the constraint name
    - `migrations/004_create_submission_files.sql` lines 35-67 — the execution-record format this task must reproduce, including the "verified by query result rather than exit code" phrasing and the rolled-back INSERT proof
    - `.planning/phases/05.2-clinical-bracket-revision/05.2-04-SUMMARY.md` if partially written, otherwise the in-session record of Task 1's backup ID and counts
  </read_first>

  <action>
    Execute the two statements from `migrations/005_widen_score_bracket_check.sql` against
    `alledrops_quiz_dev`, in ONE transaction, on one of the surfaces named in this plan's
    `<interfaces>` block. If running as `postgres`, wrap the ALTERs in `SET ROLE alledrops_app;` /
    `RESET ROLE;`. If running as `alledrops_app`, no role switch is needed — that role owns
    `submissions` outright. Record which surface and which role were actually used.

    Verify on query output, never on an exit code:
      1. Re-run `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid =
         'submissions'::regclass AND contype = 'c';` and confirm exactly ONE check constraint on
         `score_bracket` exists and its definition lists all five labels. Two constraints on the
         column means the DROP silently no-op'd against a wrong name — if that happens, STOP,
         restore intent by dropping the stale one under a fresh authorization, and record it.
      2. Re-run `SELECT COUNT(*) FROM submissions;` and confirm it equals Task 1's figure exactly.
      3. Re-run `SELECT score_bracket, COUNT(*) FROM submissions GROUP BY 1 ORDER BY 1;` and confirm
         the distribution is identical to Task 1's baseline — no row was relabelled.
      4. Prove the write path: open a transaction, `INSERT` one synthetic row with
         `score_bracket = '9+'` and obviously non-clinical placeholder identity values, confirm the
         INSERT returns without a constraint violation, then `ROLLBACK`. Re-run the count and confirm
         it is unchanged. Do not COMMIT this row.
      5. Prove the constraint still bites: in a second transaction, attempt an INSERT with
         `score_bracket = 'not-a-bracket'`, confirm it is REJECTED with a check-constraint violation,
         then `ROLLBACK`. A constraint that accepts everything is not a widened constraint, it is a
         dropped one.

    Then write the execution record into `migrations/005_widen_score_bracket_check.sql`'s reserved
    header section: the date, the surface and role used, the backup ID with its ON_DEMAND/SUCCESSFUL
    status, the confirmed constraint name, the pre and post row counts, the pre and post bracket
    distributions, the rolled-back `9+` INSERT result, and the rejected `not-a-bracket` INSERT
    result. Mirror 004's wording that verification was by query result rather than exit code. Commit
    this file alone.

    The synthetic INSERT must carry no realistic patient identity. Use an obviously synthetic name,
    a `phase52-ddl-probe@example.com` email, and a `symptom_profile_id` prefixed `AOD_PHASE52_PROBE_`.
    It is rolled back, so it never persists — but never write a plausible-looking patient into a PHI
    table even transiently. Do not print any column of any existing row.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('migrations/005_widen_score_bracket_check.sql','utf8');for(const n of ['EXECUTED','ON_DEMAND','SUCCESSFUL','pg_get_constraintdef','ROLLBACK'])if(s.split(n).length-1<1){console.error('missing execution-record element:',n);process.exit(1)}console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - CLI: the post-DDL `pg_constraint` output is pasted verbatim into the SUMMARY and shows exactly one check constraint on `score_bracket`, whose definition contains all five of `'0-2'`, `'3-6'`, `'3-8'`, `'7+'`, `'9+'`
    - CLI: post-DDL `SELECT COUNT(*) FROM submissions` equals the Task 1 figure exactly, both numbers stated in the SUMMARY
    - CLI: the post-DDL `GROUP BY score_bracket` distribution is byte-identical to Task 1's baseline, both pasted into the SUMMARY
    - Behavior: an INSERT with `score_bracket = '9+'` completed without a constraint violation and was ROLLED BACK; the row count after rollback equals the pre-INSERT count
    - Behavior: an INSERT with `score_bracket = 'not-a-bracket'` was REJECTED with a check-constraint violation, and the error text is quoted in the SUMMARY
    - Source: `migrations/005_widen_score_bracket_check.sql` now contains an `EXECUTED` record naming the date, the surface, the role, the backup ID, both row counts, and both INSERT probe outcomes
    - CLI: `git show --stat HEAD` for this commit lists exactly `migrations/005_widen_score_bracket_check.sql`
    - PHI self-review (CLAUDE.md): the synthetic probe row used placeholder identity values and was rolled back; no existing row's column value was printed, logged, or written to the SUMMARY — confirm explicitly
  </acceptance_criteria>

  <done>`submissions.score_bracket` accepts all five labels on the live dev database, every historical row is untouched, and the constraint still rejects an unknown value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → `alledrops_quiz_dev` | Hand-run DDL with table-owner privileges against the PHI table; there is no code review between the keystroke and the database |
| widened CHECK → future INSERTs | The constraint is the last structural guard on what may be stored in a PHI column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.2-17 | Denial of Service | data loss during DDL | mitigate | A named on-demand backup is taken and its SUCCESSFUL status read back from `gcloud sql backups describe` before any ALTER runs. Precedent: 1786306233540, 1786361850289. |
| T-5.2-18 | Tampering | silent no-op from a wrong constraint name | mitigate | The real name is read from `pg_constraint` before execution and the definition is read back after. The post-DDL check asserts exactly ONE check constraint on the column — two means the DROP no-op'd. |
| T-5.2-19 | Tampering | over-permissive constraint | mitigate | A negative probe INSERTs `not-a-bracket` and must be REJECTED. Without it, an accidentally-dropped constraint would look identical to a successfully widened one. |
| T-5.2-20 | Repudiation | historical rows silently relabelled | mitigate | The `GROUP BY score_bracket` distribution is captured before and after and must be byte-identical. The migration file itself contains zero DML, guarded by plan 05.2-03's contract test. |
| T-5.2-21 | Information Disclosure | PHI in query output or the SUMMARY | mitigate | Every read is `COUNT(*)`, `conname`, or `pg_get_constraintdef`. The synthetic probe uses placeholder identity values and is rolled back. Acceptance criteria forbid printing any existing row's column value — the 01-06 precedent. |
| T-5.2-22 | Elevation of Privilege | wrong role for ALTER | mitigate | `submissions` is owned by `alledrops_app`; execution is either as that role directly or as `postgres` with `SET ROLE alledrops_app`. The role actually used is recorded in the execution block. |
| T-5.2-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. |
</threat_model>

<verification>
- `gcloud sql backups describe <ID>` prints ON_DEMAND / SUCCESSFUL, quoted in the SUMMARY
- Exactly one check constraint on `score_bracket`, definition containing all five labels
- Pre and post `COUNT(*)` identical; pre and post `GROUP BY score_bracket` distributions identical
- A `9+` INSERT succeeds and is rolled back; a `not-a-bracket` INSERT is rejected
- `migrations/005_widen_score_bracket_check.sql` carries the execution record and is the only file in the final commit
</verification>

<success_criteria>
The database accepts the labels the Phase 5.2 code produces, still accepts the labels history holds,
still rejects anything else, and holds exactly the rows it held before.
</success_criteria>

<output>
Create `.planning/phases/05.2-clinical-bracket-revision/05.2-04-SUMMARY.md` when done. It MUST
contain the verbatim `gcloud sql backups describe` output, the pre and post `pg_constraint` output,
both row counts, both bracket distributions, both INSERT probe outcomes, and Andrew's verbatim
authorization string.
</output>
