---
phase: 02-quiz-schema-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/lib/quiz/schema.test.ts
  - app/lib/quiz/schema.ts
autonomous: true
requirements: [SCH-01]

must_haves:
  truths:
    - "One shared isAnswered predicate serves both the required check and the showIf isAnswered operator, so they cannot drift (D-07)"
    - "An empty selection [] does not count as answered (D-06)"
    - "A whitespace-only text answer does not count as answered (D-08)"
    - "showIf supports exactly equals, isAnswered, and includes — no fourth operator, no composition (D-02)"
    - "A showIf whose questionId does not resolve renders the item anyway; it never silently omits a clinical question (D-04, fail-open)"
    - "A test detects a dangling showIf reference before it can ship, and that test is proven able to fail (D-04)"
    - "visibleAnswers strips only answers whose question is KNOWN and HIDDEN; keys it does not know are passed through untouched, so history_personal / history_family can never be silently dropped (D-03, DIR-02)"
    - "visibleAnswers does not mutate the answers object it is given (D-03 — typed text survives a parent toggle)"
    - "Clicking an already-selected exclusive option deselects to [] (D-16), and exclusivity works regardless of how the option value is spelled (D-15)"
    - "Info blocks are skipped by visibleAnswers and never acquire an answers key (D-11, D-12)"
    - "Nothing in app/lib/quiz/ throws — invalid input degrades to a safe default (project convention, D-04)"
  artifacts:
    - path: "app/lib/quiz/schema.ts"
      provides: "isQuestion, selectedValues, isAnswered, evaluateShowIf, visibleItems, visibleAnswers, toggleOption, isOptionDisabledByExclusive"
      exports: ["isQuestion", "isAnswered", "evaluateShowIf", "visibleItems", "visibleAnswers", "toggleOption", "isOptionDisabledByExclusive", "selectedValues"]
      min_lines: 90
    - path: "app/lib/quiz/schema.test.ts"
      provides: "Behavior spec for every export plus the D-04 integrity guards"
  key_links:
    - from: "app/lib/quiz/schema.ts"
      to: "app/lib/quiz/questions.ts"
      via: "getQuestionById lookup inside evaluateShowIf"
      pattern: "getQuestionById"
    - from: "app/lib/quiz/schema.ts"
      to: "app/lib/quiz/types.ts"
      via: "QuizItem / ShowIfCondition type imports"
      pattern: "from \\\"./types\\\""
---

<objective>
Build the pure evaluator module that every question-ID literal in the renderer will be replaced by.

Purpose: DIR-01 rules out DOM tests, and this repo will not gain a DOM test dependency. The
response is not to test less — it is to move MORE logic out of the renderer, where it becomes
directly testable. Option toggling and item visibility both leave `QuizPartRenderer.tsx` and land
here as pure functions. Every decision that leaves the renderer is a decision that can no longer
hold a hardcoded literal.

Output: `app/lib/quiz/schema.ts` and its test file, both green, with the D-04 integrity guards
proven able to fail.

TDD order is mandatory in this plan: the test file lands first and is proven RED, then the module
makes it GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-quiz-schema-foundation/02-CONTEXT.md
@.planning/phases/02-quiz-schema-foundation/02-PATTERNS.md
@.planning/phases/02-quiz-schema-foundation/02-VALIDATION.md
@.planning/phases/02-quiz-schema-foundation/02-01-SUMMARY.md

<interfaces>
<!-- The exact public API this plan must produce. Do not invent alternatives; Plans 02-03 and 02-04
     are written against these signatures. -->

app/lib/quiz/schema.ts (NEW):

  isQuestion(item: QuizItem): item is QuizQuestion
  selectedValues(value: string | string[] | number | undefined): string[]
  isAnswered(question: QuizQuestion, value: string | string[] | number | undefined): boolean
  evaluateShowIf(condition: ShowIfCondition | undefined, answers: QuizAnswers): boolean
  visibleItems(items: QuizItem[], answers: QuizAnswers): QuizItem[]
  visibleAnswers(items: QuizItem[], answers: QuizAnswers): QuizAnswers
  toggleOption(question: QuizQuestion, current: string[], clickedValue: string): string[]
  isOptionDisabledByExclusive(question: QuizQuestion, current: string[], option: QuizOption): boolean

Available from Plan 02-01:
  types.ts:     QuizItem, QuizQuestion, QuizInfoBlock, QuizOption, QuizAnswers, ShowIfCondition
                ShowIfCondition = { questionId, equals } | { questionId, includes } | { questionId, isAnswered: true }
  questions.ts: ALL_ITEMS: QuizItem[], ALL_SCORED_QUESTIONS: QuizQuestion[],
                PART1_SYMPTOM_CHECKLIST, PART2_TIMING_TRIGGERS, PART5_TREATMENT,
                PART6_MEDICAL_HISTORY, QUIZ_PARTS, getQuestionById(id): QuizQuestion | undefined

Module conventions this file must follow (from app/lib/quiz/navigation.ts and redirects.ts):
  - named exports only, never `export default`
  - a module-header comment explaining WHY the module exists and which decision drives it
  - a substantial /** */ block above each export citing the D-0x it implements
  - NEVER throws — `navigation.ts:78` states the rule for this directory explicitly
  - no React import, no `window` read, no reach for component state; all inputs are arguments
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write app/lib/quiz/schema.test.ts against the API spec and prove it RED</name>
  <files>app/lib/quiz/schema.test.ts</files>
  <read_first>
    - app/lib/quiz/navigation.test.ts (the exact structure to copy: named ACCEPT/REJECT fixture
      tables at the top with a why-comment per row, describe blocks named after the exported
      function, table-driven `for (const x of TABLE) { it(...) }` loops, explicit
      null/undefined/number/object narrowing cases as individual `it`s, and double-assertion rows
      for correctness-critical claims)
    - app/lib/quiz/types.ts and app/lib/quiz/questions.ts (as delivered by Plan 02-01)
    - app/components/quiz/QuizPartRenderer.tsx lines 21-79 and 272-310 (the behavior these pure
      functions must reproduce exactly — read this before writing a single expectation)
    - .planning/phases/02-quiz-schema-foundation/02-CONTEXT.md (D-02, D-03, D-04, D-06, D-07,
      D-08, D-11, D-12, D-15, D-16)
  </read_first>
  <behavior>
Cover every export. Named `it` strings matter — VALIDATION.md's per-task map targets `required`,
`equals`, `isAnswered`, `includes`, `fails open`, `visibleAnswers`, `info block`, `empty array`,
and `toggleOption` via `vitest run -t`. Use those words in the corresponding describe/it names.

`isAnswered` (D-06, D-07, D-08) — drive off the real question constants, not invented fixtures:
  - checkbox_multi / radio_multi: `["sneezing"]` -> true; `[]` -> false (D-06, the deliberate
    behavior change); `undefined` -> false; a bare string -> false; a number -> false
  - text_input: `"Cetirizine 10mg"` -> true; `""` -> false; `"   "` -> false (D-08, matching the
    two existing med_list assertions); `undefined` -> false
  - severity_0_3 / frequency_0_4 / bother_0_4: `0` -> TRUE (zero is a meaningful answer — "None"
    severity is still an answer, and a test that asserts otherwise would break Part 3); `3` -> true;
    `"0"` -> false; `undefined` -> false
  - yesno: `"yes"` -> true; `"no"` -> true; `"maybe"` -> false; `""` -> false; `undefined` -> false
  - control_0_3: `"well"` -> true; `""` -> false; `undefined` -> false

`evaluateShowIf` (D-01, D-02, D-04):
  - `undefined` condition -> true (no condition = always visible)
  - equals: matching value -> true; non-matching -> false; missing answer -> false
  - includes: array containing the value -> true; array without it -> false; non-array answer ->
    false; missing -> false
  - isAnswered: delegates to `isAnswered` against the RESOLVED target question's type — assert the
    delegation is real by using a checkbox question and `[]`, which must yield false while a
    non-empty array yields true (D-07: the required check and the reveal trigger must fire under
    identical conditions)
  - `fails open`: a condition whose questionId is `does_not_exist_anywhere` -> TRUE. Add the
    inverted-safety comment: for navigation Phase 1 chose fail-closed; for a clinical question
    fail-closed silently skips both the question and its required check, so a patient completes an
    intake missing data nobody knows is missing (D-04). A reviewer pattern-matching on Phase 1
    would introduce that bug.
  - never throws: null / undefined / a number / a plain object passed where a condition is
    expected each return a boolean rather than raising

`visibleItems` (D-12):
  - an item with no showIf is always returned
  - a question whose showIf is unsatisfied is omitted
  - an INFO BLOCK whose showIf is unsatisfied is omitted, and one whose showIf is satisfied is
    returned — info blocks compose with showIf, they are not decoration
  - order is preserved and the input array is not mutated

`visibleAnswers` (D-03, DIR-02) — this is the highest-stakes group in the file:
  - a hidden question's answer is stripped from the RESULT
  - the SOURCE answers object still holds that value afterward (proves no mutation, and proves
    D-03's "a patient who toggles a parent answer back and forth does not lose typed text")
  - a visible question's answer survives
  - `history_personal` and `history_family` survive when the real `ALL_ITEMS` is passed
  - **the DIR-02 row:** `history_personal` ALSO survives when the item list is deliberately
    Part-6-blind. Call `visibleAnswers(ALL_SCORED_QUESTIONS, { taking_meds: "no", med_list: "X",
    history_personal: ["asthma"] })` and assert the result CONTAINS `history_personal` and does NOT
    contain `med_list`. A keep-known-and-visible whitelist passes the `ALL_ITEMS` row above and
    FAILS this one — which is precisely why this row exists. Name the `it` so it reads as the
    DIR-02 proof, e.g. "passes an unknown key through untouched even when the item list omits
    Part 6". Comment it: the failure being guarded is a clinical record losing `history_personal`
    with no error and no failing test.
  - an answers key belonging to NO known item (use a made-up id such as `future_phase_field`)
    passes through untouched — strip only KNOWN and HIDDEN, never unknown
  - an info block's id never appears in the result, even if an answers entry is fabricated for it
    (D-11)
  - realistic end-to-end row: `{ taking_meds: "no", med_list: "Cetirizine 10mg" }` against
    `ALL_ITEMS` yields a result WITHOUT `med_list` — this is the orphan-answer bug D-03 fixes, where
    a contradicted medication list currently reaches the record Dr. Sullivan reads

`toggleOption` (D-13, D-15, D-16) — reproduce today's `onChange` at QuizPartRenderer.tsx:68-79:
  - non-exclusive option not currently selected -> appended
  - non-exclusive option currently selected -> removed
  - selecting a non-exclusive option while an exclusive one is selected -> the exclusive value is
    dropped and the new value is present
  - clicking an UNSELECTED exclusive option -> result is exactly `[that value]`, everything else
    cleared
  - clicking an ALREADY-SELECTED exclusive option -> result is `[]` (D-16), and feeding that `[]`
    into `isAnswered` for the same question returns false, so Next correctly disables. Assert both
    halves in the same test — that pair is the whole of D-06 + D-16 as a patient experiences it.
  - D-15 spelling independence: build a test-local question whose exclusive option value is
    `none_of_the_above` (NOT `none`) and confirm the same exclusive behavior. Today's code would
    silently do nothing for that spelling; this row is the regression guard for the Phase 3 trap
    D-15 names.
  - a question with no exclusive option behaves as plain multi-select throughout

`isOptionDisabledByExclusive` (D-13):
  - with an exclusive value selected, a non-exclusive option -> true
  - with an exclusive value selected, the exclusive option itself -> false (it must stay clickable
    so D-16's deselect is reachable)
  - with nothing selected -> false for every option
  - a question with no exclusive option -> false for every option
  </behavior>
  <action>
Create `app/lib/quiz/schema.test.ts` importing the eight functions from `./schema`. The module does
not exist yet — that is the RED state and it is intended.

Import real question constants from `./questions` (`PART1_SYMPTOM_CHECKLIST`,
`PART2_TIMING_TRIGGERS`, `PART3_SEVERITY`, `PART5_TREATMENT`, `PART6_MEDICAL_HISTORY`,
`ALL_SCORED_QUESTIONS`, `ALL_ITEMS`) rather than inventing question fixtures, matching the
convention already used in `app/components/quiz/QuizPartRenderer.test.ts`. Build test-local
fixtures ONLY where the real data cannot express the case: the info-block rows, the D-15
alternate-spelling row, and the dangling reference. Never write a fixture into `questions.ts`.

Follow `navigation.test.ts`'s comment discipline: every edge-case row explains what class of bug it
guards and cites its D-id.

Run the suite and confirm it fails at module resolution. Do not create `schema.ts` in this task.
  </action>
  <verify>
    <automated>npx vitest run app/lib/quiz/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/lib/quiz/schema.test.ts` EXITS NON-ZERO on this task's commit, failing to
      resolve `./schema`. A green run here means the test file is not exercising the intended API.
    - Every one of the eight exports named in the interfaces block is imported by the test file:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/schema.test.ts","utf-8"); for (const f of ["isQuestion","selectedValues","isAnswered","evaluateShowIf","visibleItems","visibleAnswers","toggleOption","isOptionDisabledByExclusive"]) console.log(f, s.split(f).length-1)'`
      — every count must be >= 1.
    - The file contains describe or it names matching each of these substrings, so
      VALIDATION.md's `-t` commands resolve: `equals`, `isAnswered`, `includes`, `fails open`,
      `visibleAnswers`, `info block`, `empty array`, `toggleOption`.
    - The DIR-02 row exists and calls `visibleAnswers` with `ALL_SCORED_QUESTIONS` (the Part-6-blind
      list), not only with `ALL_ITEMS`:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/schema.test.ts","utf-8"); console.log(s.split("visibleAnswers(ALL_SCORED_QUESTIONS").length-1)'`
      must print at least `1`.
    - The alternate-spelling fixture uses a value that is not `none`: the file contains
      `none_of_the_above` at least once (D-15).
    - `questions.ts` and `types.ts` are byte-identical to their Plan 02-01 state for this task's
      commit (`git diff HEAD~1 -- app/lib/quiz/questions.ts app/lib/quiz/types.ts` is empty).
    - No new dependency was added: `git diff HEAD~1 -- package.json package-lock.json` is empty
      (DIR-01).
  </acceptance_criteria>
  <done>The behavior contract for the evaluator module exists as executable expectations and is
  proven RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/lib/quiz/schema.ts until the spec is GREEN</name>
  <files>app/lib/quiz/schema.ts</files>
  <read_first>
    - app/lib/quiz/schema.test.ts (from Task 1 — the contract)
    - app/lib/quiz/navigation.ts (the module-header + JSDoc + never-throw + narrow-unknown-at-the-
      boundary conventions to copy; note line 78's explicit statement that nothing in this
      directory throws)
    - app/lib/quiz/redirects.ts (the pure-resolver-with-explicit-arguments shape and the
      "browser-global wrapper belongs in the calling component" precedent)
    - app/components/quiz/QuizPartRenderer.tsx lines 21-79 and 272-310 (the behavior being ported)
    - app/lib/quiz/types.ts, app/lib/quiz/questions.ts
  </read_first>
  <behavior>
Same expectations as Task 1. This task makes them pass without editing them.
  </behavior>
  <action>
Create `app/lib/quiz/schema.ts`. Named exports only. No React import, no `window` read, no throw
anywhere — invalid input degrades to a safe default, matching every other module in this directory.

Module header must state: this module exists so `QuizPartRenderer.tsx` holds rendering only; every
decision it used to make from a question-ID literal now lives here as a pure function; and it must
stay in sync with `app/lib/quiz/types.ts`'s `ShowIfCondition` union.

Import `getQuestionById` from `./questions` and the types from `./types`. Do not import from
`./scoring` — scoring is untouched this phase.

`isQuestion(item)` — a type predicate returning `item.kind === "question"`, following the
`isSafeRelativePath(p: unknown): p is string` precedent in `navigation.ts`. This is a NEW pattern
for this codebase: PATTERNS.md confirms no discriminated union exists here today and the only
related precedent is a single `x is Y` narrow on a primitive. Say so in the JSDoc — do not claim to
follow an established union pattern, because there isn't one.

`selectedValues(value)` — returns `value` when it is an array, otherwise `[]`. This replaces the
renderer's local `getMultiAnswer` helper, which moves here so the renderer stops owning it.

`isAnswered(question, value)` — one switch on `question.type`, the single shared predicate D-07
requires. Non-empty array for checkbox_multi/radio_multi (D-06). Trimmed non-empty string for
text_input (D-08). `typeof value === "number"` for the three numeric scales, so `0` is answered.
Exactly `"yes"` or `"no"` for yesno. Non-empty string for control_0_3. `default: return false`.

`evaluateShowIf(condition, answers)` — return `true` when `condition` is falsy ("no condition =
always visible", the direct sibling of `redirects.ts`'s "no configured value = use the fallback").
Resolve the target via `getQuestionById(condition.questionId)`; when it does not resolve, return
`true` — D-04's fail-open. Then dispatch on which operator field is present, using the `in`
operator against the union: `equals` compares the raw answer with strict equality; `includes`
requires the answer to be an array containing the value; `isAnswered` delegates to `isAnswered`
with the resolved target question. Guard the body so a malformed condition returns `true` rather
than raising.

Document loudly, in the JSDoc, that this function reads the RAW live answers state and must NEVER
be called with `visibleAnswers()` output — RESEARCH.md Pitfall 2: mixing them breaks chained
conditionals, and the type signatures alone will not catch it.

`visibleItems(items, answers)` — `items.filter((item) => evaluateShowIf(item.showIf, answers))`.
Returns a new array; never mutates the input.

`visibleAnswers(items, answers)` — build a Set of the ids of items that ARE questions AND are NOT
visible, then copy every entry of `answers` whose key is not in that Set into a fresh object.
Strip only KNOWN-and-HIDDEN; pass unknown keys through untouched. State the reason in the JSDoc,
citing DIR-02 and RESEARCH.md Open Question 2: a keep-known-and-visible whitelist would silently
drop `history_personal` / `history_family` whenever it is handed an item list that excludes Part 6
— a clinical record losing data with no error and no failing test. The blacklist form fails open in
the same direction D-04 chose, and it is a deliberate choice, not an accident. Also state the two
call sites it is licensed for (immediately before `calculateTotalScore`, and when constructing the
`POST /api/quiz/submit` payload) and that it must never be called during render.

`toggleOption(question, current, clickedValue)` — find the clicked option in `question.options`.
When it carries `exclusive === true`: return `[]` if it is already in `current` (D-16), otherwise
return `[clickedValue]`. Otherwise: drop from `current` every value whose option carries
`exclusive === true`, then remove or append `clickedValue` depending on whether it is already
selected. Read exclusivity off the option object only — never derive it from `excludeFromScore`
(D-14) and never compare against a value string (D-15). When the clicked value matches no option,
fall through to plain toggle behavior rather than throwing. Returns a new array.

`isOptionDisabledByExclusive(question, current, option)` — true when `option.exclusive !== true`
AND some value in `current` maps to an option carrying `exclusive === true`. The exclusive option
itself is never disabled, or D-16's deselect would be unreachable.
  </action>
  <verify>
    <automated>npx vitest run app/lib/quiz/schema.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/lib/quiz/schema.test.ts` passes with zero skipped tests.
    - `npm run typecheck` exits 0.
    - `app/lib/quiz/schema.test.ts` is byte-identical to its Task 1 commit:
      `git diff HEAD~1 -- app/lib/quiz/schema.test.ts` is empty. The implementation may not move
      the goalposts.
    - `npx vitest run app/lib/quiz/schema.test.ts -t "visibleAnswers"` passes, INCLUDING the DIR-02
      Part-6-blind row. Paste that command's output into the SUMMARY — it is the DIR-02 proof.
    - The module never throws and imports no React:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/schema.ts","utf-8"); console.log("throw",s.split("throw ").length-1,"react",s.split("react").length-1,"window",s.split("window").length-1,"default",s.split("export default").length-1)'`
      must print `throw 0 react 0 window 0 default 0`.
    - Exclusivity is never derived from excludeFromScore:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/schema.ts","utf-8"); console.log(s.split("excludeFromScore").length-1)'`
      must print `0`.
    - Negative control on the DIR-02 semantics, run and then reverted: temporarily rewrite
      `visibleAnswers` as a keep-known-and-visible whitelist, confirm the Part-6-blind row FAILS,
      then revert to the blacklist form and confirm it passes. Record both outcomes in the SUMMARY.
      Without this, the row could be passing for the wrong reason.
    - Full suite shows a net ADD versus the 173 baseline; no test removed, skipped, or weakened.
    - No new dependency: `git diff main -- package.json package-lock.json` is empty (DIR-01).
  </acceptance_criteria>
  <done>Every decision the renderer used to make from a literal now exists as a tested pure
  function.</done>
</task>

<task type="auto">
  <name>Task 3: Add the D-04 reference-integrity and no-chained-showIf guards, each proven able to fail</name>
  <files>app/lib/quiz/schema.test.ts</files>
  <read_first>
    - app/lib/quiz/schema.test.ts (current state — you are APPENDING describe blocks, not editing
      existing ones)
    - app/lib/quiz/questions.ts (the live showIf declarations to validate against)
    - app/lib/quiz/navigation.test.ts lines 140-181 (the double-assertion non-vacuous idiom:
      assert the independent ground truth AND the function's behavior in the same test, so a
      checker that always answers "safe" cannot pass trivially)
    - .planning/phases/02-quiz-schema-foundation/02-RESEARCH.md "Pitfall 4" and "Pitfall 5"
    - .planning/phases/02-quiz-schema-foundation/02-VALIDATION.md "Non-Vacuous Proof Strategy"
  </read_first>
  <action>
Append two describe blocks to `app/lib/quiz/schema.test.ts`. Do not modify anything already in the
file.

**Block 1 — name it so it contains the substring `reference integrity` (D-04).** Write a reusable
local function that takes a `QuizItem[]` and returns the list of `showIf.questionId` values that
`getQuestionById` cannot resolve. Then:
  - assert it returns an empty list for the real `ALL_ITEMS`, so a typo cannot ship;
  - assert, in a separate `it`, that the SAME function returns a non-empty list for a test-local
    item array containing `showIf: { questionId: "does_not_exist", equals: "yes" }`. Without this
    second `it` the first is vacuous — it would pass against a checker that always returns empty.
    Name it "detects a dangling reference in a deliberately broken fixture".
  - assert that no `showIf.questionId` in `ALL_ITEMS` resolves to an info block. With
    `getQuestionById` scoped to `QuizQuestion` only (RESEARCH.md Pitfall 5) this falls out of the
    first assertion; state that in a comment so a future change widening `getQuestionById` is
    understood to break this guarantee.

**Block 2 — `no chained showIf (forward guard)`.** RESEARCH.md Pitfall 4 and Assumption A3 flag
that `evaluateShowIf` has no transitive-visibility awareness: if C depends on B and B is itself
conditionally hidden, C reads B's stale answer and can stay visible after B disappears. This phase
resolves that deliberately rather than by omission: **the rule is NON-TRANSITIVE, and it is safe
today only because no chain exists.** Verified against the locked scope — `med_list` and
`med_control` both depend on `taking_meds`, which carries no `showIf`; and Phase 3's HIST-02 /
HIST-04 and Phase 4's TEST-02 / TEST-03 all branch off unconditional questions, so no chain appears
through Phase 4 either.

Encode that as a guard rather than a comment: for every `showIf` in `ALL_ITEMS`, assert the
resolved target question itself has no `showIf`. Prove it non-vacuously with a second `it` showing
the same check flags a test-local two-link chain. Comment that the day this guard goes red, the
transitive-visibility rule must be decided before the chain ships — which is the point of putting
it here now, while it is cheap, instead of letting Phase 3 discover the ambiguity under schedule
pressure.
  </action>
  <verify>
    <automated>npx vitest run app/lib/quiz/schema.test.ts && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/lib/quiz/schema.test.ts -t "reference integrity"` passes.
    - Both new describe blocks contain a paired broken-fixture assertion. The file contains at
      least two `it` names including one of `broken`, `dangling`, or `chain`, and each such test
      builds a test-local fixture rather than editing `questions.ts`.
    - Negative control 1, run and then reverted: temporarily add
      `showIf: { questionId: "typo_id", equals: "yes" }` to a real question in `questions.ts`,
      confirm `npx vitest run app/lib/quiz/schema.test.ts -t "reference integrity"` FAILS, then
      revert and confirm it passes again. Record both outcomes in the SUMMARY. A guard that has
      never been seen red is not a guard.
    - Negative control 2, run and then reverted: temporarily add
      `showIf: { questionId: "med_list", isAnswered: true }` to a real question — creating a chain,
      since `med_list` itself carries a showIf — confirm the no-chained-showIf guard FAILS, then
      revert. Record both outcomes.
    - `git diff` for this task's commit touches only `app/lib/quiz/schema.test.ts` and shows
      additions only — zero deletions and zero modifications to pre-existing lines.
    - `git diff main -- app/components/quiz/QuizPartRenderer.test.ts` is empty. The 9 existing
      `it` blocks / 12 `expect` assertions stay byte-identical.
    - `npm run typecheck` exits 0 and `npm test` shows a net ADD versus 173 with no skips.
  </acceptance_criteria>
  <done>A dangling showIf reference and an unplanned conditional chain both fail at test time,
  and both guards have been observed failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React answers state -> `visibleAnswers()` -> `POST /api/quiz/submit` body | The only boundary this plan is about. `visibleAnswers` decides what leaves the browser toward Cloud SQL and the clinical PDF. It is client-side shaping of an existing payload, not a route change — no file under `app/routes/` is touched. |
| `questions.ts` `showIf` declarations -> which clinical questions a patient is asked | A typo here silently removes a question from a clinical intake. Guarded at test time by Task 3 and at runtime by D-04's fail-open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Information Disclosure | `visibleAnswers()` dropping Part 6 answers | mitigate | DIR-02. Implemented as a strip-known-and-hidden blacklist rather than a keep-known-and-visible whitelist, so an unknown key can never be lost. Pinned by the Part-6-blind `visibleAnswers` row in Task 1 plus the Task 2 negative control that rewrites it as a whitelist and observes the row fail. |
| T-2-07 | Tampering | Orphan answer contradicting a later answer in the clinical record | mitigate | D-03. `visibleAnswers` strips `med_list` when `taking_meds` is "no". Asserted end to end in Task 1's realistic row. This is a data-integrity fix, not an external-attacker threat — today a patient who answers, goes back, and flips the parent sends a contradicted medication list into the record Dr. Sullivan reads. |
| T-2-08 | Information Disclosure | Dangling `showIf` silently omitting a required clinical question | mitigate | Two halves, both required. Test-time: Task 3's reference-integrity guard, proven able to fail via a real typo injected and reverted. Runtime: `evaluateShowIf` returns `true` for an unresolved reference (D-04 fail-open), bounding the blast radius to over-collection, which is recoverable, rather than silent omission, which is not. |
| T-2-09 | Repudiation | A guard that has never been observed red | mitigate | Every integrity assertion in this plan carries a paired broken-fixture assertion, and Tasks 2 and 3 require three negative controls run against real data and reverted, with every outcome recorded in the SUMMARY. |
| T-2-10 | Tampering | `evaluateShowIf` called with `visibleAnswers()` output | accept | RESEARCH.md Pitfall 2. Both take an answers-shaped argument, so the type system cannot separate them. Mitigated by JSDoc naming the licensed call sites and by Plan 02-03's assertion that `visibleAnswers` is never imported into `QuizPartRenderer.tsx`. Accepted rather than mitigated structurally because a nominal wrapper type would add surface for one call site; the import-location constraint is cheap and checkable. |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed, asserted by an empty `package.json` / `package-lock.json` diff in both task acceptance criteria. DIR-01 forbids adding `jsdom`, `happy-dom`, `@testing-library/react`, or any other test dependency in this phase; the response to the missing DOM harness is pure-function extraction, not new tooling. |
</threat_model>

<verification>
- `npx vitest run app/lib/quiz/schema.test.ts` passes with zero skips.
- `npm run typecheck` exits 0.
- `npm test` reports strictly more than 173 tests, 0 skipped, 0 removed.
- `app/components/quiz/QuizPartRenderer.test.ts` is byte-identical to `main`.
- `tests/quiz-part-renderer-no-literals.test.ts` still FAILS — the renderer has not been touched
  yet and that is the correct state at the end of this wave.
- `git diff main -- package.json package-lock.json` is empty.
</verification>

<success_criteria>
- Every operator in D-02's vocabulary is implemented and tested, and no fourth operator exists.
- D-06, D-07, D-08, D-15, and D-16 each have a positively-asserting test.
- D-03's boundary filter exists as a pure function, does not mutate its input, and cannot drop an
  unknown key (DIR-02), with a negative control proving the distinction was actually tested.
- The two D-04 guards have each been observed failing against a deliberately broken input.
- Zero new dependencies (DIR-01).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-schema-foundation/02-02-SUMMARY.md` when done. Record: the
`vitest -t "visibleAnswers"` output; all three negative-control outcomes; the new full-suite test
count versus the 173 baseline; and an explicit note that the chained-`showIf` rule was resolved as
NON-TRANSITIVE with a forward guard, so Phase 3 planning inherits the decision rather than
rediscovering it.
</output>
</content>
