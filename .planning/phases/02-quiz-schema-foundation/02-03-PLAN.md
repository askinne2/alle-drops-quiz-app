---
phase: 02-quiz-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/components/quiz/QuizPartRenderer.tsx
  - app/components/quiz/QuizPartRenderer.test.ts
  - app/components/quiz/QuizContainer.tsx
autonomous: true
requirements: [SCH-01, SCH-02]

must_haves:
  truths:
    - "Zero question-ID literals and zero \"none\" value literals remain in QuizPartRenderer.tsx, and the test that says so was proven to fail before the refactor (SCH-02, D-13)"
    - "A question marked required blocks step advance until answered, with no per-ID code (SCH-01, D-05)"
    - "A question with a showIf condition appears and disappears based on another answer, with no per-ID code (SCH-01, D-01)"
    - "A static info block can be placed inside a quiz part and renders without collecting an answer (SCH-01, D-09, D-10, D-11)"
    - "An info block is skipped by isPartComplete's required check via the kind narrow, not via a new special case (D-12)"
    - "The existing med_list / med_control conditional behavior is identical, proven by 9 untouched it blocks / 12 untouched expect assertions (Success Criterion 4)"
    - "Info-block content is rendered as React children only — no dangerouslySetInnerHTML, no markdown, no sanitizer (D-10)"
    - "The renderer never imports visibleAnswers; it evaluates visibility against raw live answers state (RESEARCH Pitfall 2)"
  artifacts:
    - path: "app/components/quiz/QuizPartRenderer.tsx"
      provides: "Item renderer with an info-block branch and a schema-driven isPartComplete"
      contains: "visibleItems"
    - path: "app/components/quiz/QuizPartRenderer.test.ts"
      provides: "Original 12 assertions plus D-06, Part 6 no-regression, and info-block coverage"
  key_links:
    - from: "app/components/quiz/QuizPartRenderer.tsx"
      to: "app/lib/quiz/schema.ts"
      via: "named imports of the evaluator functions"
      pattern: "from \\\"../../lib/quiz/schema\\\""
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/components/quiz/QuizPartRenderer.tsx"
      via: "items prop at both render sites"
      pattern: "items=\\{"
---

<objective>
Delete every question-ID literal from the renderer and make it near-dumb.

Purpose: this is SCH-02 itself, and it is the task the whole phase exists to enable — Sequencing
Constraint 1 says building Phases 3 and 4 first would add five more ID-literal special cases across
two files. The literal-inventory test flips from RED to GREEN here, and that flip is the phase's
central piece of evidence.

Output: a renderer that maps items to markup and delegates every decision to `schema.ts`, an
`isPartComplete` with no per-ID branches, and an appended (never edited) regression harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-quiz-schema-foundation/02-CONTEXT.md
@.planning/phases/02-quiz-schema-foundation/02-PATTERNS.md
@.planning/phases/02-quiz-schema-foundation/02-VALIDATION.md
@.planning/phases/02-quiz-schema-foundation/02-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Available from Plan 02-02. Use these directly; do not re-derive them from the codebase. -->

app/lib/quiz/schema.ts:
  isQuestion(item: QuizItem): item is QuizQuestion
  selectedValues(value: string | string[] | number | undefined): string[]
  isAnswered(question: QuizQuestion, value: string | string[] | number | undefined): boolean
  evaluateShowIf(condition: ShowIfCondition | undefined, answers: QuizAnswers): boolean
  visibleItems(items: QuizItem[], answers: QuizAnswers): QuizItem[]
  visibleAnswers(items: QuizItem[], answers: QuizAnswers): QuizAnswers   <-- DO NOT import here
  toggleOption(question: QuizQuestion, current: string[], clickedValue: string): string[]
  isOptionDisabledByExclusive(question: QuizQuestion, current: string[], option: QuizOption): boolean

app/lib/quiz/types.ts:
  QuizItem = QuizQuestion | QuizInfoBlock
  QuizInfoBlock { kind: "info"; id; heading?; paragraphs: string[]; bullets?; order; part; showIf? }
  QuizQuestion  { kind: "question"; id; type; text; subtitle?; options?; excludeFromScore?;
                  order; part; scoreWeight?; required?; showIf? }
  QuizOption    { value; label; score?; exclusive? }

Current renderer surface being changed:
  interface QuizPartRendererProps { questions: QuizQuestion[]; answers; onAnswerChange; disabled? }
    -> becomes                    { items: QuizItem[]; answers; onAnswerChange; disabled? }
  local helper getMultiAnswer(...)          -> DELETE, replaced by schema.selectedValues
  local helper isExclusiveNoneQuestion(...) -> DELETE, replaced by option.exclusive reads
  export function isPartComplete(questions: QuizQuestion[], answers) -> (items: QuizItem[], answers)

QuizContainer.tsx renders QuizPartRenderer at exactly two sites:
  :453-457  questions={currentPartQuestions}   (quiz_parts step)
  :547-551  questions={PART6_MEDICAL_HISTORY}  (medical_history step)
Both need the prop renamed to `items` in this plan so typecheck stays green. The D-03
`visibleAnswers` wiring is Plan 02-04's job — do not add it here.

Existing CSS classes to reuse for the info block (no stylesheet change in this phase):
  styles.questionCard, styles.questionCard__label, styles.questionCard__subtitle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor QuizPartRenderer.tsx to delegate every decision to schema.ts</name>
  <files>app/components/quiz/QuizPartRenderer.tsx, app/components/quiz/QuizContainer.tsx</files>
  <read_first>
    - app/components/quiz/QuizPartRenderer.tsx (full file — you are rewriting its guards, keeping
      its six per-type JSX cases intact)
    - app/lib/quiz/schema.ts (the functions being delegated to)
    - app/lib/quiz/types.ts, app/lib/quiz/questions.ts
    - app/components/quiz/QuizContainer.tsx lines 451-457 and 544-551 (the two render sites)
    - app/lib/quiz/scoring.ts lines 38-65 (the exhaustive-switch-on-a-discriminant idiom this file
      already mirrors)
    - tests/quiz-part-renderer-no-literals.test.ts (the forbidden-literal list this refactor must
      drive to zero)
  </read_first>
  <action>
**`app/components/quiz/QuizPartRenderer.tsx`.**

Rename the prop `questions: QuizQuestion[]` to `items: QuizItem[]` and update the destructure and
every use inside the component.

Delete `getMultiAnswer` and `isExclusiveNoneQuestion` outright. Import from
`../../lib/quiz/schema`: `isAnswered`, `isOptionDisabledByExclusive`, `selectedValues`,
`toggleOption`, `visibleItems`. Do NOT import `visibleAnswers` here — it is a boundary filter for
`QuizContainer` only, and calling it during render would blank fields the patient is actively
looking at (RESEARCH.md Pitfall 2). Add a short comment saying so, so a future reader does not
"tidy up" the asymmetry.

Delete the `const takingMeds = answers.taking_meds;` line and the
`question.part === 5 && (question.id === "med_list" || question.id === "med_control")` guard. Their
replacement is `visibleItems(items, answers).map(...)` at the top of the render — the filter now
comes from the data, not from the file.

Add the info-block branch ABOVE the existing `switch (item.type)`, gated on `item.kind === "info"`,
not as a new case inside the switch. RESEARCH.md Pattern 2 is explicit about this: an info block
has no `QuestionType`, so a case inside the type switch would defeat the narrowing that gives
D-09 its compile-time force. After the early return, TypeScript narrows `item` to `QuizQuestion`
and the six existing cases compile unchanged.

Add a local presentational component `InfoBlockCard({ block }: { block: QuizInfoBlock })` in this
file. It renders, inside a `styles.questionCard` wrapper: the optional `heading` in a
`styles.questionCard__label` element; each entry of `paragraphs` as its own `<p>` with
`styles.questionCard__subtitle`; and, when `bullets` is present and non-empty, a `<ul>` with one
`<li>` per bullet. All values are passed as React children so escaping is automatic. It must not
call `onAnswerChange`, must not read `answers`, and must not render an input of any kind — D-11's
"leaves no trace in the submission" is satisfied by there being no way for it to produce one. Do
not add `dangerouslySetInnerHTML`, a markdown renderer, or a sanitizer; Phase 1 closed a reflected
XSS on this exact page and D-10 keeps that sink closed. No new CSS class and no stylesheet edit —
reuse the three existing classes named in the interfaces block.

In the `checkbox_multi` / `radio_multi` case, replace the three literal-driven locals:
  - `getMultiAnswer(answers[question.id])` becomes `selectedValues(answers[item.id])`
  - `exclusiveNone` / `hasNone` / `isNone` / `disableOthers` collapse into one call:
    the input's `disabled` prop becomes `disabled || isOptionDisabledByExclusive(item, raw, opt)`
  - the entire `onChange` body — currently eleven lines of exclusive-none branching — becomes a
    single call: `onAnswerChange(item.id, toggleOption(item, raw, opt.value))`

That last substitution is the heart of DIR-01. The toggle decision is no longer expressible as a
literal because it no longer lives here.

Leave the six per-type JSX cases, the `FREQUENCY_LABELS` / `BOTHER_LABELS` constants, the `key`
construction, the ARIA wiring, and `default: return null` otherwise untouched. This refactor
changes guards, not markup.

**`isPartComplete(items: QuizItem[], answers: QuizAnswers)`** — replace the entire body. Iterate
`visibleItems(items, answers)`. For each item: `if (!isQuestion(item)) continue;` FIRST, before any
required or answered logic runs, so the info-block skip is one line at the top of the loop rather
than a case in the requiredness switch (RESEARCH.md Pitfall 3 — "without special-casing" is doing
real work in D-12). Then `if (item.required === false) continue;` (D-05: default is required, so
only an explicit `false` opts out). Then `if (!isAnswered(item, answers[item.id])) return false;`.
Return `true` after the loop.

The whole per-type switch, both `takingMeds` reads, and both ID-literal special cases disappear.
Extend the existing one-line doc comment to say that non-question items are skipped without a
required check (D-12) and that required-ness now defaults to true (D-05) — match this file's terse
comment style, not `app/lib/quiz/*.ts`'s longer form.

**`app/components/quiz/QuizContainer.tsx`** — mechanical only in this task: rename `questions=` to
`items=` at the two render sites (:453-457 and :547-551). Change nothing else in this file; the
`visibleAnswers` wiring and the `handleAnswerChange` cleanup belong to Plan 02-04.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/quiz-part-renderer-no-literals.test.ts app/components/quiz/QuizPartRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` now PASSES. Together with the
      recorded RED run from Plan 02-01 Task 1, this is the non-vacuous SCH-02 proof. Paste both the
      old failure counts and the new pass into the SUMMARY.
    - Independent confirmation of the flip, not routed through the test file:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizPartRenderer.tsx","utf-8"); for (const n of ["\"none\"","\"timing_triggers\"","\"symptoms_nasal\"","\"symptoms_eye\"","\"symptoms_sinus\"","\"med_list\"","\"med_control\"","taking_meds","isExclusiveNoneQuestion","getMultiAnswer"]) console.log(n, s.split(n).length-1)'`
      must print `0` for every needle.
    - `npx vitest run app/components/quiz/QuizPartRenderer.test.ts` passes, and
      `git diff main -- app/components/quiz/QuizPartRenderer.test.ts` is EMPTY. Editing that file
      to make it pass would disprove Success Criterion 4; if any of its 12 assertions goes red,
      stop and reconcile against CONTEXT.md rather than changing the test.
    - `npm run typecheck` exits 0.
    - The renderer does not import the boundary filter and holds no HTML sink:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizPartRenderer.tsx","utf-8"); console.log("visibleAnswers",s.split("visibleAnswers").length-1,"dsi",s.split("dangerouslySetInnerHTML").length-1)'`
      must print `visibleAnswers 0 dsi 0`.
    - The info branch sits above the type switch, not inside it:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizPartRenderer.tsx","utf-8"); console.log(s.indexOf("kind === \"info\"") < s.indexOf("switch (item.type)"))'`
      must print `true`.
    - `isPartComplete`'s body contains no `switch` and no `case`:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizPartRenderer.tsx","utf-8"); const b=s.slice(s.indexOf("export function isPartComplete")); console.log("switch",b.split("switch").length-1,"case",b.split("case ").length-1)'`
      must print `switch 0 case 0`.
    - No stylesheet was modified: `git diff main -- app/styles/quiz.module.css` is empty.
    - No new dependency: `git diff main -- package.json package-lock.json` is empty.
    - Full suite: every previously-passing test still passes; the count has not decreased.
  </acceptance_criteria>
  <done>The renderer maps items to markup and delegates every decision to `schema.ts`; the
  literal-inventory test has flipped from red to green.</done>
</task>

<task type="auto">
  <name>Task 2: Append the D-06, Part 6, and info-block assertions without touching the existing 12</name>
  <files>app/components/quiz/QuizPartRenderer.test.ts</files>
  <read_first>
    - app/components/quiz/QuizPartRenderer.test.ts (its 9 `it` blocks / 12 `expect` assertions and
      its convention of importing real question constants rather than fixtures — you are APPENDING
      below line 67 only)
    - app/components/quiz/QuizPartRenderer.tsx (the new isPartComplete)
    - app/lib/quiz/questions.ts (PART1_SYMPTOM_CHECKLIST, PART5_TREATMENT, PART6_MEDICAL_HISTORY,
      and the two `required: false` declarations Plan 02-01 added)
    - .planning/phases/02-quiz-schema-foundation/02-VALIDATION.md "Rule 2" and "Rule 3"
  </read_first>
  <action>
Append new `describe` blocks BELOW the existing content of
`app/components/quiz/QuizPartRenderer.test.ts`. Do not modify, reorder, reformat, or reflow a
single existing line — this file is the behavior-identical harness, and a diff that touches lines
1-67 is itself evidence the refactor changed behavior it should not have.

**Block: `isPartComplete — an empty array no longer counts as answered (D-06)`.** Assert that
`isPartComplete(PART1_SYMPTOM_CHECKLIST, { symptoms_nasal: [], symptoms_eye: ["none"],
symptoms_sinus: ["none"] })` is `false`. Comment that this is a deliberate, patient-visible change
signed off in D-06: a patient could previously tick a symptom box, untick it, and advance with
nothing recorded, and the submission could not distinguish "no symptoms" from "did not engage" even
though every one of these questions offers an explicit "None of the above". Add the companion
assertion that `["none"]` on all three is still `true`, so the change is scoped to `[]` and not to
the none-of-the-above path.

**Block: `isPartComplete — Part 6 does not become un-completable (D-05 / D-06 interaction)`.**
Assert that `isPartComplete(PART6_MEDICAL_HISTORY, { history_personal: [], history_family: [] })`
is `true`. Comment the reason in full, because it contradicts a supporting claim in D-06 and a
future reader will otherwise "fix" it: D-06 states that all seven checklist questions carry an
explicit "None of the above", which is true of the five Part 1 / Part 2 checklists and FALSE of
these two. `QuizContainer`'s effect seeds both to `[]` on entering the medical-history step, so
under a naive default-required reading a patient with no personal or family history could never
advance. `required: false` on both (added in Plan 02-01) reproduces today's behavior exactly, and
Phase 3 replaces this part wholesale.

**Block: `isPartComplete — showIf replaces the med_list / med_control special case`.** Assert
`isPartComplete(PART5_TREATMENT, { taking_meds: "no" })` is `true` (both conditional questions are
hidden, so neither is required) and that `isPartComplete(PART5_TREATMENT, { taking_meds: "yes" })`
is `false` (both are now visible and unanswered). These pin the SCH-02 substitution behaviorally
rather than only as a source-text count.

**Block: `isPartComplete — info block (D-11 / D-12)`.** Build a test-local `QuizItem[]` containing
one `kind: "info"` block and one real question copied from `PART1_SYMPTOM_CHECKLIST`. Assert:
  - the part is complete when only the question is answered — the info block never demands one
  - a part consisting of an info block alone is complete
  - an info block whose `showIf` is unsatisfied does not block completion either
  - no answers key is required for the info block's `id`, and adding one does not change the result
Do not add an info block to `questions.ts`; this phase ships mechanism, not content.
  </action>
  <verify>
    <automated>npx vitest run app/components/quiz/QuizPartRenderer.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/components/quiz/QuizPartRenderer.test.ts` passes with zero skips.
    - The first 67 lines are byte-identical to `main`:
      `git diff main -- app/components/quiz/QuizPartRenderer.test.ts | grep -c '^-' ` must print
      `1` (the single `---` file header line) — i.e. the diff contains additions only. If it
      prints more, existing assertions were modified and the task is not done.
    - The four new describe blocks exist and their names contain `empty array`, `Part 6`,
      `showIf`, and `info block` respectively (VALIDATION.md's `-t` targets).
    - `npm test` shows a NET ADD versus the 173 baseline. Report the exact new total and confirm
      0 skipped, 0 removed.
    - `npm run typecheck` exits 0.
    - `git diff main -- app/lib/quiz/questions.ts` shows no `kind: "info"` addition — the info
      fixture is test-local.
  </acceptance_criteria>
  <done>The two intended behavior changes and the info-block contract are positively asserted, and
  the original 12 assertions are provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `questions.ts` info-block content -> rendered DOM on `/quiz-embed` | The one new render path this plan opens on a PHI-collecting page. Content is developer-authored, not patient-supplied, but the page's history matters: Phase 1 closed a reflected XSS here. |
| item list + live answers -> what the patient is asked -> what `isPartComplete` requires | Both now flow through `visibleItems`. A bug here either shows a question that should be hidden (recoverable) or hides one that should be asked (not recoverable). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-11 | Tampering / Information Disclosure | `InfoBlockCard` reintroducing an HTML sink on `/quiz-embed` | mitigate | D-10. Content renders as JSX children only; JSX escapes string children automatically. Acceptance criterion asserts zero occurrences of `dangerouslySetInnerHTML` in the file, and Plan 02-01 already ensured `QuizInfoBlock` exposes no HTML-shaped field to bind to. No markdown or sanitizer dependency is added (DIR-01). |
| T-2-12 | Information Disclosure | A clinical question silently not asked | mitigate | `visibleItems` drives both rendering and the required check from one evaluator (D-07), so a question cannot be visible-but-not-required or required-but-not-visible. Plan 02-02's D-04 fail-open plus its reference-integrity guard bound the residual risk. Behaviorally pinned by the `taking_meds: "yes"` -> false assertion in Task 2. |
| T-2-13 | Elevation of Privilege | An info block acquiring required-ness or an answers key | mitigate | The `isQuestion(item)` narrow is the FIRST statement in `isPartComplete`'s loop body, before any required logic, per RESEARCH.md Pitfall 3. `InfoBlockCard` receives no `onAnswerChange` and renders no input. Asserted by the info-block describe block in Task 2 and by Plan 02-01's `@ts-expect-error` fixture. |
| T-2-14 | Repudiation | Claiming "behavior is identical" without a harness that could have caught otherwise | mitigate | The 12 original assertions must pass unmodified; the acceptance criterion counts diff deletions to prove they were not edited. VALIDATION.md Rule 2: a plan that edits them to make them pass has disproven its own success criterion. |
| T-2-15 | Denial of Service | Part 6 becoming un-completable under D-06 | mitigate | Positively asserted in Task 2's Part 6 block. This is the one real regression risk the D-06 change created, and it exists because D-06's supporting claim about all seven checklists offering "None of the above" is false for the two Part 6 questions. |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed; asserted by an empty `package.json` / `package-lock.json` diff. |
</threat_model>

<verification>
- `tests/quiz-part-renderer-no-literals.test.ts` passes, and the RED-to-GREEN flip is recorded with
  the pre-refactor counts from Plan 02-01 alongside the post-refactor zeros.
- The independent `node` occurrence count over `QuizPartRenderer.tsx` prints `0` for all ten
  needles.
- `git diff main -- app/components/quiz/QuizPartRenderer.test.ts` contains additions only.
- `npm run typecheck` exits 0; `npm test` shows a net add versus 173 with 0 skips.
- `git diff main -- app/styles/quiz.module.css package.json package-lock.json` is empty.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1: a required question blocks advance with no per-ID code.
- ROADMAP Success Criterion 2: a `showIf` question appears and disappears with no per-ID code.
- ROADMAP Success Criterion 3: a static info block renders inside a part and collects no answer.
- ROADMAP Success Criterion 4: `med_list` / `med_control` behavior identical, existing suite green
  and unmodified.
- SCH-02 satisfied against its actual text, not only its two line-cites (D-13).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-schema-foundation/02-03-SUMMARY.md` when done. Record: the RED
counts from Plan 02-01 next to the GREEN zeros here, side by side; the exact `npm test` total; and
confirmation that the 12 original assertions passed unmodified, with the diff-deletion count as
evidence.
</output>
</content>
