---
phase: 02-quiz-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/quiz-part-renderer-no-literals.test.ts
  - app/lib/quiz/types.ts
  - app/lib/quiz/questions.ts
  - tests/quiz-schema-type-guarantees.test.ts
autonomous: true
requirements: [SCH-01, SCH-02]

must_haves:
  truths:
    - "A test exists that FAILS against today's QuizPartRenderer.tsx by counting its forbidden literals, proving the SCH-02 claim is measurable before any refactor (VALIDATION Rule 1)"
    - "A quiz question can declare `required` and `showIf` declaratively (D-01, D-02, D-05, SCH-01)"
    - "A static info block is a discriminated-union member, not a QuestionType (D-09)"
    - "Info block content is structured data — heading plus paragraph array plus optional bullets — with no markdown, HTML, or sanitizer dependency (D-10)"
    - "An info block cannot enter ALL_SCORED_QUESTIONS, cannot be passed to calculateTotalScore, and cannot carry `required` — and the compiler, not a reviewer, enforces it (D-09, VALIDATION Rule 4)"
    - "Exclusivity is declared per-option via `exclusive: true`, independent of excludeFromScore (D-13, D-14, D-15)"
    - "`timing_season`'s `only_rarely` is NOT marked exclusive — the proof that exclusivity was not derived from excludeFromScore (D-14)"
    - "med_list and med_control declare showIf against taking_meds instead of relying on renderer code (D-01, SCH-02)"
  artifacts:
    - path: "app/lib/quiz/types.ts"
      provides: "QuizItem union, QuizInfoBlock, ShowIfCondition, required/showIf on QuizQuestion, exclusive on QuizOption"
      contains: "export type QuizItem"
    - path: "app/lib/quiz/questions.ts"
      provides: "kind discriminants, exclusive flags, showIf declarations, ALL_ITEMS export"
      contains: "ALL_ITEMS"
    - path: "tests/quiz-part-renderer-no-literals.test.ts"
      provides: "SCH-02 literal-inventory contract test, proven non-vacuous"
      contains: "split("
    - path: "tests/quiz-schema-type-guarantees.test.ts"
      provides: "D-09 compile-time negative fixture"
      contains: "@ts-expect-error"
  key_links:
    - from: "app/lib/quiz/questions.ts"
      to: "app/lib/quiz/types.ts"
      via: "QuizItem / QuizQuestion type imports"
      pattern: "from \\\"./types\\\""
    - from: "tests/quiz-part-renderer-no-literals.test.ts"
      to: "app/components/quiz/QuizPartRenderer.tsx"
      via: "readFileSync source-text read"
      pattern: "QuizPartRenderer.tsx"
---

<objective>
Lay the declarative schema and the two proof harnesses that everything downstream depends on.

Purpose: SCH-02's claim ("no question-ID literals remain") is worthless unless a test can be shown
to FAIL against today's file first — this project has three documented cases of a count-based check
passing vacuously. And D-09's claim ("an info block cannot reach scoring") is a *typecheck* claim,
so it needs a compile-time negative fixture, not a runtime observation.

Output: a failing literal-inventory test, the widened schema types, the updated question data, and
a `@ts-expect-error` fixture that breaks the build if the union ever widens.

Branch: create and work on `phase-2-quiz-schema-foundation` (CLAUDE.md forbids committing to main).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-schema-foundation/02-CONTEXT.md
@.planning/phases/02-quiz-schema-foundation/02-PATTERNS.md
@.planning/phases/02-quiz-schema-foundation/02-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Current contracts the executor must respect. Extracted from the codebase — do not re-explore. -->

app/lib/quiz/types.ts exports today:
  type QuestionType = "checkbox_multi" | "radio_multi" | "severity_0_3" | "frequency_0_4"
                    | "bother_0_4" | "yesno" | "text_input" | "control_0_3"
  interface QuizQuestion { id, type, text, subtitle?, options?, excludeFromScore?, order, part, scoreWeight? }
  interface QuizOption { value, label, score? }
  interface QuizCategory { name, part, questions }
  type QuizAnswers = Record<string, string | string[] | number>
  interface QuizSubmission { ... }
  interface QuizConfig { ... }

app/lib/quiz/questions.ts exports today:
  PART1_SYMPTOM_CHECKLIST: QuizQuestion[]   (symptoms_nasal, symptoms_eye, symptoms_sinus)
  PART2_TIMING_TRIGGERS:   QuizQuestion[]   (timing_season, timing_triggers)
  PART3_SEVERITY:          QuizQuestion[]   (5 severity_0_3)
  PART4_IMPACT:            QuizQuestion[]   (4 frequency_0_4 + bother_overall)
  PART5_TREATMENT:         QuizQuestion[]   (taking_meds, med_list, med_control)
  PART6_MEDICAL_HISTORY:   QuizQuestion[]   (history_personal, history_family)
  ALL_SCORED_QUESTIONS:    QuizQuestion[]   (parts 1-5 concatenated)
  QUIZ_PARTS:              QuizQuestion[][] (parts 1-5)
  getQuestionById(id: string): QuizQuestion | undefined
    -> searches [...ALL_SCORED_QUESTIONS, ...PART6_MEDICAL_HISTORY]; currently ZERO callers

app/lib/quiz/scoring.ts (NOT modified this phase):
  scoreQuestion(question: QuizQuestion, answer: string | string[] | number | undefined): number
  calculateTotalScore(questions: QuizQuestion[], answers: QuizAnswers): number

Measured occurrence counts in app/components/quiz/QuizPartRenderer.tsx on current main
(counted with split(needle).length - 1, NOT grep -c):
  "none"                  -> 5
  "timing_triggers"       -> 1
  "symptoms_nasal"        -> 1
  "symptoms_eye"          -> 1
  "symptoms_sinus"        -> 1
  "med_list"              -> 3
  "med_control"           -> 3
  taking_meds (unquoted)  -> 2   (answers.taking_meds property reads at :31 and :273)
  isExclusiveNoneQuestion -> 2

Baseline suite: 173 tests / 17 files passing; `npm run typecheck` exits 0.
tsconfig.json `include` is ["env.d.ts", "**/*.ts", "**/*.tsx", ".react-router/types/**/*"], so
test files ARE typechecked — that is what makes the @ts-expect-error fixture load-bearing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SCH-02 literal-inventory test and prove it FAILS against today's renderer</name>
  <files>tests/quiz-part-renderer-no-literals.test.ts</files>
  <read_first>
    - tests/entry-theme-contract.test.ts (the project's only source-text contract test; copy its
      readFileSync(join(process.cwd(), ...)) idiom, its module-level SOURCE const, and its
      "WHY THIS FILE EXISTS" header-comment convention verbatim in shape)
    - tests/quiz-embed-contract.test.ts lines 118-160 (the in-repo precedent for
      split(needle).length - 1 occurrence counting)
    - app/components/quiz/QuizPartRenderer.tsx (the file under inspection, current state)
    - .planning/STATE.md "Accumulated Context" (the grep -c line-vs-occurrence trap that three
      executors and the orchestrator each hit)
  </read_first>
  <action>
Create `tests/quiz-part-renderer-no-literals.test.ts` on the new `phase-2-quiz-schema-foundation`
branch. Load the renderer source once into a module-level const named SOURCE via
`readFileSync(join(process.cwd(), "app", "components", "quiz", "QuizPartRenderer.tsx"), "utf-8")`.

Open with a header doc-comment stating: this file guards SCH-02; D-13 corrected REQUIREMENTS.md's
two line-cites, which under-counted the real inventory (`isExclusiveNoneQuestion` plus five `"none"`
literals were missing from the enumeration); and occurrence counting must use
`split(needle).length - 1` because `grep -c` counts matching LINES, a trap this project hit four
times.

Name the top-level describe block exactly `QuizPartRenderer.tsx has no question-ID or 'none'-value
literals (SCH-02)` — VALIDATION.md's per-task verification map targets that string with
`vitest run -t`.

Write one `it` per forbidden-literal class, each asserting `SOURCE.split(needle).length - 1` equals
`0`:
  - the quoted option value `"none"`
  - each of the four quoted question IDs `"timing_triggers"`, `"symptoms_nasal"`, `"symptoms_eye"`,
    `"symptoms_sinus"` (loop over an array; the `it` name should say "four hardcoded question IDs")
  - the quoted IDs `"med_list"` and `"med_control"`
  - the BARE identifier `taking_meds` (unquoted) — the renderer reads it as `answers.taking_meds`
    at two sites, and a quoted-only needle would miss both. This is the same class of error as
    STATE.md's `apntly` vs `appointly` retraction: a count against the wrong needle read as absence.
  - the helper name `isExclusiveNoneQuestion`

Build each needle from fragments where the needle would otherwise appear as a plain literal in this
test file, following the `const ANNOTATION = \`(${"required"})\`` convention already used in
`app/components/quiz/QuizPartRenderer.test.ts` — the habit of not reproducing a forbidden token is
the point, and it also keeps this test file from matching its own grep during review.

Do NOT modify `QuizPartRenderer.tsx` in this task. Run the test and capture the failure output.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-part-renderer-no-literals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` EXITS NON-ZERO on this task's
      commit. A passing run here means the test is measuring nothing and the task is not done.
    - The failure output shows received values matching the measured pre-refactor inventory:
      `"none"` = 5; each of `"timing_triggers"`, `"symptoms_nasal"`, `"symptoms_eye"`,
      `"symptoms_sinus"` = 1; `"med_list"` = 3; `"med_control"` = 3; bare `taking_meds` = 2;
      `isExclusiveNoneQuestion` = 2. Record these numbers verbatim in the plan SUMMARY.
    - `grep -c` appears zero times in the new file; every count uses `split(` and `.length - 1`.
      Verify with: `node -e 'const s=require("fs").readFileSync("tests/quiz-part-renderer-no-literals.test.ts","utf-8"); console.log("splitCounts", s.split(".length - 1").length-1, "grepC", s.split("grep -c").length-1)'`
      — splitCounts must be >= 8 and grepC must be 0 (the header comment may reference grep -c
      only as prose; if it does, adjust the assertion to allow exactly that one prose occurrence
      and state which line it is).
    - The describe block name is byte-identical to
      `QuizPartRenderer.tsx has no question-ID or 'none'-value literals (SCH-02)`.
    - The current git branch is `phase-2-quiz-schema-foundation`, not `main`
      (`git branch --show-current`).
  </acceptance_criteria>
  <done>The literal-inventory test exists, is committed, and is proven to fail against the
  pre-refactor renderer with the exact counts above.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the schema types and re-express the four hardcodes as data</name>
  <files>app/lib/quiz/types.ts, app/lib/quiz/questions.ts</files>
  <read_first>
    - app/lib/quiz/types.ts (current interfaces and its inline-comment-per-field convention)
    - app/lib/quiz/questions.ts (all 20 question literals, the per-part banner comments, and
      getQuestionById's current search scope)
    - app/lib/quiz/scoring.ts (calculateTotalScore's signature — it must keep taking QuizQuestion[])
    - app/components/quiz/QuizPartRenderer.tsx lines 26-38 and 272-310 (the exact hardcoded
      behavior this data must reproduce)
    - .planning/phases/02-quiz-schema-foundation/02-CONTEXT.md (D-01, D-02, D-05, D-09, D-10,
      D-12, D-13, D-14, D-15)
    - .planning/phases/02-quiz-schema-foundation/02-RESEARCH.md "Pattern 1" and "Pitfall 5"
  </read_first>
  <action>
**`app/lib/quiz/types.ts`** — additive changes only; keep the existing module header and the
inline-comment-per-field style already used on `excludeFromScore`.

Add `kind: "question"` as the FIRST field of `QuizQuestion`, required (not optional). D-09's whole
argument is compiler enforcement; an optional discriminant weakens narrowing.

Add to `QuizQuestion`: `required?: boolean` (comment: defaults to true per D-05, so omission is the
common case and a question added in Phase 3/4 that forgets the flag is required, not silently
optional) and `showIf?: ShowIfCondition`.

Add `exclusive?: boolean` to `QuizOption`, directly beside `score?`, with a comment stating it is
independent of `excludeFromScore` and citing D-14's counter-example (`timing_season`'s
`only_rarely` is excluded from score but is deliberately NOT exclusive).

Add `ShowIfCondition` as a THREE-MEMBER UNION, not a single interface with three optional fields.
This resolves RESEARCH.md Open Question 1 in favor of D-01's literal locked shape
(`{ questionId: "taking_meds", equals: "yes" }` — no redundant `op` field) while still making it
syntactically impossible to write two operators on one condition:
  - `{ questionId: string; equals: string }`
  - `{ questionId: string; includes: string }`
  - `{ questionId: string; isAnswered: true }`
Comment it with D-02: exactly these three operators, each with a named Phase 3/4 consumer; no
`notEquals`, no `allOf`/`anyOf`.

Add `QuizInfoBlock` with `kind: "info"`, `id: string`, `heading?: string`, `paragraphs: string[]`,
`bullets?: string[]`, `order: number`, `part: number`, `showIf?: ShowIfCondition`. Per D-10 the
content is structured strings rendered as React children — comment that no markdown, HTML, or
sanitizer is permitted here because Phase 1 closed a reflected XSS on this exact page. Per the
RESEARCH "Anti-Patterns" section, `QuizInfoBlock` MUST NOT declare `required`, not even as
`required?: false`.

Add `export type QuizItem = QuizQuestion | QuizInfoBlock;`.

Leave `QuizAnswers`, `QuizSubmission`, `QuizCategory`, and `QuizConfig` untouched — D-11 requires
info blocks to leave no trace in the submission, which is satisfied by them never acquiring an
answers key, not by any type change here.

**`app/lib/quiz/questions.ts`** — data changes; no new questions, no copy changes, no info blocks
(this phase adds mechanism only).

Add `kind: "question"` to all 20 question literals across PART1 through PART6. Mechanical; the
compiler will list any you miss.

Add `exclusive: true` to the `none` option of exactly four questions: `symptoms_nasal`,
`symptoms_eye`, `symptoms_sinus` (Part 1) and `timing_triggers` (Part 2). These are precisely the
four IDs `isExclusiveNoneQuestion` hardcodes today, so this is behavior-preserving. Do NOT add it
to `timing_season`'s `only_rarely` (D-14) and do NOT derive the flag from `excludeFromScore`
anywhere.

Add `showIf: { questionId: "taking_meds", equals: "yes" }` to `med_list` and to `med_control`. Do
not add a `part === 5` guard equivalent — question IDs are unique across the set, so the current
renderer's part check is redundant.

Add `required: false` to BOTH `history_personal` and `history_family`, each with an inline comment
citing D-05/D-06 and this reason: **D-06's supporting claim that "all seven checklist questions
carry an explicit 'None of the above'" is true of the five Part 1/Part 2 checklists but FALSE of
these two — neither offers a none-of-the-above option.** Today `isPartComplete` accepts `[]` for
them, and `QuizContainer`'s effect seeds both to `[]` on entering the medical-history step, so the
part is always complete. Under D-06 (`[]` is no longer answered) they would become permanently
un-completable for a patient with no personal or family history — a new dead end, not the intended
behavior change. `required: false` reproduces today's behavior exactly. Note in the comment that
Phase 3 replaces this part wholesale (HIST-01..HIST-05), which is when required-ness for medical
history gets designed properly. Add no other `required` declaration anywhere — D-05's opt-out model
means the other 18 questions stay silent.

Widen `QUIZ_PARTS` to `QuizItem[][]` so Phase 3 can place info blocks inside a part without a type
change. Keep `PART1`..`PART5`, `PART6_MEDICAL_HISTORY`, and `ALL_SCORED_QUESTIONS` declared as
`QuizQuestion[]` — `ALL_SCORED_QUESTIONS` staying narrow is what makes D-09's scoring exclusion a
compiler fact, and keeping `PART6_MEDICAL_HISTORY` narrow keeps `getQuestionById` working without
importing a type predicate from `schema.ts`, which would create a circular import.

Add `export const ALL_ITEMS: QuizItem[] = [...ALL_SCORED_QUESTIONS, ...PART6_MEDICAL_HISTORY];`
with a comment: this is the full item set `visibleAnswers` is called with, deliberately including
Part 6 so `history_personal` / `history_family` are known-and-visible rather than unknown keys
(RESEARCH.md Open Question 2 — a Part-6-blind item set would silently strip them from the clinical
record).

Leave `getQuestionById` unchanged: return type stays `QuizQuestion | undefined` and its search
scope stays `QuizQuestion`-only, per RESEARCH.md Pitfall 5 — a `showIf.questionId` must never
resolve to an info block.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run app/components/quiz/QuizPartRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npx vitest run` full suite still reports 173 passed / 17 files (this task changes data
      shape, not behavior; the one new failing file from Task 1 is expected and is counted
      separately — state both numbers in the SUMMARY).
    - `app/lib/quiz/types.ts` contains `export type QuizItem = QuizQuestion | QuizInfoBlock`.
    - `ShowIfCondition` is declared as a union of three object types. Verify no single object type
      carries more than one operator field:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/types.ts","utf-8"); const b=s.slice(s.indexOf("ShowIfCondition"), s.indexOf("QuizInfoBlock")); console.log("pipes", b.split("|").length-1)'`
      must print `pipes 2` or more.
    - `QuizInfoBlock` does not declare `required`:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/types.ts","utf-8"); const b=s.slice(s.indexOf("interface QuizInfoBlock"), s.indexOf("export type QuizItem")); console.log(b.split("required").length-1)'`
      must print `0`.
    - Exactly four `exclusive: true` declarations exist in questions.ts:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/questions.ts","utf-8"); console.log(s.split("exclusive: true").length-1)'`
      must print `4`.
    - `only_rarely` is not marked exclusive:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/questions.ts","utf-8"); const i=s.indexOf("only_rarely"); console.log(s.slice(i, i+120).includes("exclusive"))'`
      must print `false`.
    - Exactly two `showIf` declarations exist in questions.ts and both target `taking_meds`:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/questions.ts","utf-8"); console.log(s.split("showIf:").length-1, s.split("questionId: \"taking_meds\"").length-1)'`
      must print `2 2`.
    - Exactly two `required: false` declarations exist, and both are inside PART6_MEDICAL_HISTORY:
      `node -e 'const s=require("fs").readFileSync("app/lib/quiz/questions.ts","utf-8"); const p6=s.slice(s.indexOf("PART6_MEDICAL_HISTORY"), s.indexOf("ALL_SCORED_QUESTIONS")); console.log(s.split("required: false").length-1, p6.split("required: false").length-1)'`
      must print `2 2`.
    - `kind: "question"` appears exactly 20 times in questions.ts.
    - `ALL_SCORED_QUESTIONS` is still declared `QuizQuestion[]` and `ALL_ITEMS` is exported as
      `QuizItem[]`.
    - `getQuestionById`'s signature and body are unchanged from `main`
      (`git diff main -- app/lib/quiz/questions.ts` shows no hunk touching it).
  </acceptance_criteria>
  <done>Types express required, showIf, exclusive, and the QuizItem union; the four renderer
  hardcodes now exist as data; typecheck is clean and the existing suite is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prove D-09's exclusion at compile time with a @ts-expect-error negative fixture</name>
  <files>tests/quiz-schema-type-guarantees.test.ts</files>
  <read_first>
    - app/lib/quiz/types.ts (as modified by Task 2)
    - app/lib/quiz/questions.ts (as modified by Task 2)
    - app/lib/quiz/scoring.ts (calculateTotalScore's exact signature)
    - tsconfig.json (confirm `include` covers `**/*.ts`, which is why tsc checks this file)
    - .planning/phases/02-quiz-schema-foundation/02-VALIDATION.md "Rule 4"
  </read_first>
  <action>
Create `tests/quiz-schema-type-guarantees.test.ts`. Its purpose is stated in a header comment: a
runtime test that observes info blocks scoring 0 would pass VACUOUSLY, because `scoreQuestion`
already returns 0 from its `default` branch. D-09's claim is a typecheck claim, so it is proven by
`npm run typecheck` failing if the guarantee is ever lost.

Declare a module-local `const INFO_FIXTURE: QuizInfoBlock` with a `kind: "info"` literal, an `id`,
one `paragraphs` entry, and `order`/`part` values. This fixture is test-local — do NOT add an info
block to `questions.ts`.

Then write four `@ts-expect-error`-guarded lines, each with a comment naming the invariant it
pins:
  1. `QuizInfoBlock` is not assignable to `QuizQuestion` — assign INFO_FIXTURE to a
     `QuizQuestion`-typed const.
  2. An info block cannot enter a `QuizQuestion[]` — build a `QuizQuestion[]`-typed array literal
     containing INFO_FIXTURE.
  3. `calculateTotalScore` refuses a `QuizItem[]` — call it with an array literal containing
     INFO_FIXTURE and an empty answers object.
  4. `QuizInfoBlock` has no `required` property — read `.required` off INFO_FIXTURE.

Each `@ts-expect-error` is a two-way assertion: if the type error disappears because the union
widened, `tsc` raises TS2578 ("Unused '@ts-expect-error' directive") and the build breaks. That is
the proof mechanism — say so in the comment.

Add at least one runtime `it` so Vitest does not report a file with zero tests. Assert that every
member of `ALL_SCORED_QUESTIONS` has `kind === "question"`, and that `ALL_ITEMS` length equals
`ALL_SCORED_QUESTIONS.length + PART6_MEDICAL_HISTORY.length`. Name the describe block
`quiz schema type guarantees (D-09)`.

If any expression needs a value the fixture cannot supply, keep the assignment target as an
explicitly-typed `const` rather than reaching for `as` — a cast anywhere in this file defeats the
file's own purpose.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/quiz-schema-type-guarantees.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 with the fixture in place (each `@ts-expect-error` is consumed by
      a real error).
    - `npx vitest run tests/quiz-schema-type-guarantees.test.ts` passes.
    - The fixture contains exactly four `@ts-expect-error` directives:
      `node -e 'const s=require("fs").readFileSync("tests/quiz-schema-type-guarantees.test.ts","utf-8"); console.log(s.split("@ts-expect-error").length-1)'`
      must print `4`.
    - Zero `as ` casts and zero `any` in the file:
      `node -e 'const s=require("fs").readFileSync("tests/quiz-schema-type-guarantees.test.ts","utf-8"); console.log(s.split(" as ").length-1, s.split("any").length-1)'`
      must print `0 0`.
    - Negative-control check, run and then reverted: temporarily change `kind: "info"` to
      `kind: "question"` in INFO_FIXTURE and confirm `npm run typecheck` now FAILS with TS2578 on
      at least one directive. Record the observed error in the SUMMARY, then revert. A fixture
      that never fails proves nothing.
    - `questions.ts` contains zero occurrences of `kind: "info"` — this phase ships no info-block
      content.
  </acceptance_criteria>
  <done>The compiler, not a reviewer, now enforces that an info block cannot become a question,
  cannot enter the scored set, cannot reach calculateTotalScore, and cannot carry required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored `questions.ts` -> rendered quiz page | The only new content channel this plan opens. Info-block copy is authored in-repo, not patient-supplied. |
| React state -> `POST /api/quiz/submit` | Unchanged by this plan; no route file is touched. Listed so the reviewer can confirm the boundary was considered and found untouched. |

Per the phase security note, the surface here is genuinely low: this plan modifies two library
files and adds two test files. It touches no route, no API, no PHI persistence path, and adds no
dependency. If a later plan in this phase touches `app/routes/api.*`, `app/lib/db.ts`, or
`app/lib/submissions.ts`, that is scope drift and should be flagged, not built.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | `QuizInfoBlock` content shape (`app/lib/quiz/types.ts`) | mitigate | Content is typed `string[]` / `string` only. No `html`, `markdown`, or `content` field is added, so `dangerouslySetInnerHTML` has nothing to bind to in Plan 02-03. Phase 1 closed a reflected XSS on `/quiz-embed`; D-10 keeps that sink closed by construction. Acceptance criteria assert `QuizInfoBlock` carries no field beyond heading/paragraphs/bullets/id/order/part/showIf/kind. |
| T-2-02 | Information Disclosure | Clinical-record completeness via `required` semantics | mitigate | D-05's default-true plus the explicit `required: false` on the two Part 6 questions. Without the latter, D-06 would make Part 6 un-completable for a patient with no history — a new dead end in a clinical intake. Pinned by an acceptance criterion counting exactly two declarations, both inside Part 6. |
| T-2-03 | Tampering | `exclusive` derived from `excludeFromScore` | mitigate | D-14. Acceptance criteria assert exactly four `exclusive: true` declarations and that `only_rarely` carries none. Deriving it would silently make a rarely-symptomatic patient unable to also report spring symptoms. |
| T-2-04 | Repudiation | A vacuously-passing SCH-02 proof | mitigate | Task 1 must exit NON-ZERO. Counting uses `split(needle).length - 1`; `grep -c` is banned by acceptance criterion. Four documented instances of the line-vs-occurrence trap in this project. |
| T-2-05 | Elevation of Privilege | Info block acquiring question capabilities | mitigate | Task 3's four `@ts-expect-error` directives plus TS2578 on an unused directive. Negative control required. |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs zero packages. RESEARCH.md's Package Legitimacy Audit records the gate as skipped by its own preconditions. Adding any dependency here — including a dev dependency — is out of scope and forbidden by DIR-01. |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` exits NON-ZERO with the nine
  recorded counts (this is the intended state at the end of this plan).
- `npx vitest run tests/quiz-schema-type-guarantees.test.ts` passes.
- `npx vitest run app/components/quiz/QuizPartRenderer.test.ts` passes with its 9 `it` blocks
  unmodified — `git diff main -- app/components/quiz/QuizPartRenderer.test.ts` must be empty.
- Full suite: 173 pre-existing tests still pass; the only failing file is the deliberately-red
  literal-inventory test.
</verification>

<success_criteria>
- The SCH-02 claim is measurable and currently measures FALSE, with recorded numbers.
- `required`, `showIf`, `exclusive`, `QuizInfoBlock`, and `QuizItem` are declarable in
  `app/lib/quiz/types.ts` (SCH-01).
- The four renderer hardcodes now exist as data in `questions.ts`, ready for Plan 02-03 to delete
  their code counterparts.
- D-09's exclusion is enforced by `tsc`, with a negative control proving the fixture can fail.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-schema-foundation/02-01-SUMMARY.md` when done. Record: the nine
pre-refactor literal counts verbatim; the observed TS2578 error text from the Task 3 negative
control; and the Part 6 `required: false` finding, since it contradicts a supporting claim in
D-06 and Phase 3 planning needs to know.
</output>
</content>
</invoke>
