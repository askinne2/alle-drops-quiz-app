---
phase: 02-quiz-schema-foundation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/components/quiz/QuizContainer.tsx
autonomous: false
requirements: [SCH-01, SCH-02]

must_haves:
  truths:
    - "Nothing the patient could not see reaches the score, Cloud SQL, or the clinical PDF (D-03)"
    - "visibleAnswers is applied at all THREE calculateTotalScore call sites, including the Test Mode button that D-03 did not account for (D-03, DIR-03)"
    - "A patient who toggles taking_meds off and back on does not lose the medication list they typed (D-03)"
    - "A stale hidden med_control value cannot inflate the score (D-03)"
    - "visibleAnswers is imported only in QuizContainer.tsx, never in QuizPartRenderer.tsx (RESEARCH Pitfall 2)"
    - "The full suite net-adds tests against the 173 / 17-file baseline, with typecheck and build clean (VALIDATION standing rule)"
    - "The two patient-visible behavior changes, D-06 and D-16, are confirmed in a real browser — pure-function tests prove the decisions, not that the renderer honors them"
  artifacts:
    - path: "app/components/quiz/QuizContainer.tsx"
      provides: "D-03 boundary pass at all three score sites and the submit payload"
      contains: "visibleAnswers"
  key_links:
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/lib/quiz/schema.ts"
      via: "visibleAnswers import"
      pattern: "visibleAnswers"
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/lib/quiz/questions.ts"
      via: "ALL_ITEMS import"
      pattern: "ALL_ITEMS"
---

<objective>
Wire D-03's boundary pass and close the phase.

Purpose: everything before this plan is mechanism. This is where the mechanism starts protecting
the clinical record — today, answering the medication list, going back, and flipping "Are you
currently taking any allergy medications?" to "no" sends an orphan answer that contradicts the
answer above it into the row Dr. Sullivan reads. It also removes a real data-loss bug in the
opposite direction: the current `handleAnswerChange` deletes the patient's typed medication list on
that same toggle, with no undo.

Output: three wired call sites, one deleted special case, a full phase gate, and the two manual
checks that are the only thing standing between a wiring bug and production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-schema-foundation/02-CONTEXT.md
@.planning/phases/02-quiz-schema-foundation/02-PATTERNS.md
@.planning/phases/02-quiz-schema-foundation/02-VALIDATION.md
@.planning/phases/02-quiz-schema-foundation/02-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Everything below is already delivered by Plans 02-01 through 02-03. -->

app/lib/quiz/schema.ts:
  visibleAnswers(items: QuizItem[], answers: QuizAnswers): QuizAnswers
    - strips only KNOWN-and-HIDDEN keys; unknown keys pass through (DIR-02)
    - does not mutate its input
    - licensed call sites: immediately before calculateTotalScore, and when building the POST body

app/lib/quiz/questions.ts:
  ALL_ITEMS: QuizItem[]   = [...ALL_SCORED_QUESTIONS, ...PART6_MEDICAL_HISTORY]
  ALL_SCORED_QUESTIONS: QuizQuestion[]   (still narrow — scoring signature unchanged)

The THREE calculateTotalScore call sites in QuizContainer.tsx (DIR-03 — RESEARCH.md found a third
that CONTEXT.md's D-03 did not account for):
  1. buildPayload, :188   `const s = score ?? calculateTotalScore(ALL_SCORED_QUESTIONS, answers);`
                          and the returned object's `answers,` shorthand field at :200
  2. goToOutcome, :235    `const s = calculateTotalScore(ALL_SCORED_QUESTIONS, answers);`
  3. Test Mode onClick, :629  `const s = calculateTotalScore(ALL_SCORED_QUESTIONS, sample);`

The special case to DELETE, handleAnswerChange :174-183:
  the `if (questionId === "taking_meds" && value === "no") { delete next.med_list;
  delete next.med_control; }` branch

useCallback dependency arrays: `visibleAnswers` and `ALL_ITEMS` are pure module imports, not React
state, so they are NOT added to any dependency array — consistent with how `calculateTotalScore`,
`getScoreBracket`, and `ALL_SCORED_QUESTIONS` are already omitted from these same arrays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply visibleAnswers at all three score sites and the payload, and delete the contradicting answer-deletion special case</name>
  <files>app/components/quiz/QuizContainer.tsx</files>
  <read_first>
    - app/components/quiz/QuizContainer.tsx (full file — particularly handleAnswerChange :174-183,
      buildPayload :185-207, goToOutcome :234-240, handleConsentSubmit :301-315, and the Test Mode
      onClick :600-634)
    - app/lib/quiz/schema.ts (visibleAnswers' JSDoc, which names its licensed call sites)
    - app/lib/quiz/questions.ts (ALL_ITEMS)
    - CLAUDE.md "Self-review checklist for PHI-handling changes"
  </read_first>
  <action>
Add `import { visibleAnswers } from "../../lib/quiz/schema";` alongside the existing quiz-lib
imports, matching this file's relative-path depth and named-import style.

Add `ALL_ITEMS` to the existing `from "../../lib/quiz/questions"` import.

**Site 1 — `buildPayload`.** Insert `const visible = visibleAnswers(ALL_ITEMS, answers);` as the
first statement after the `if (!patientState || !symptomProfileId) throw` guard. Change the score
line to compute from `visible`, and change the returned object's `answers,` shorthand to
`answers: visible,`. This is D-03's boundary #1 and #2 in one function — the score the patient is
shown and the answers written to Cloud SQL now come from the same filtered set, which is what keeps
them from disagreeing.

**Site 2 — `goToOutcome`.** Insert the same `const visible = visibleAnswers(ALL_ITEMS, answers);`
line before the `calculateTotalScore` call and pass `visible`. Leave the dependency array `[answers]`
unchanged.

**Site 3 — the Test Mode button's `onClick` (DIR-03).** Insert
`const visible = visibleAnswers(ALL_ITEMS, sample);` after `setAnswers(sample)` and pass `visible`
to `calculateTotalScore`. RESEARCH.md notes this site is optional for correctness, because the
sample object contains no hidden conditional keys — apply it anyway. Three sites computing a score
from three different shapes of the same data is how the next drift starts, and consistency is worth
one line. Note in a comment that the sample carries `symptoms_sinus: []`, which under D-06 is no
longer "answered"; Test Mode bypasses `isPartComplete` entirely, so this is not a behavior change,
but a future reader comparing the sample against the required rules should not be surprised.

**Delete the answer-deletion special case in `handleAnswerChange`.** Remove the
`questionId === "taking_meds"` branch and its two `delete` statements, leaving the callback as a
plain spread-and-set. Comment the removal with its reason: D-03 explicitly requires a hidden
question's answer to STAY in React state and be stripped at the boundary instead. The current
deletion is the failure D-03 names — losing a long medication list to a mis-click has no undo, and
abandonment is this milestone's headline risk. The submitted payload is unchanged, because
`visibleAnswers` strips `med_list` and `med_control` whenever `taking_meds` is not "yes"; the score
is unchanged for the same reason, and `med_control` is a scored question, so this is a live
guarantee rather than a hypothetical one. Deleting the branch also removes the last three
question-ID literals from this file.

Leave `handleConsentSubmit`'s reads of `answers.history_personal` / `answers.history_family`
against the RAW answers object — both Part 6 questions are unconditional, so they are always
visible, and routing them through the filter would add a dependency without changing the result.

Change nothing else. Do not touch any file under `app/routes/`, `app/lib/db.ts`, or
`app/lib/submissions.ts` — RESEARCH.md's Architectural Responsibility Map places every capability
in this phase in the Browser/Client tier, and a diff reaching those files is scope drift.

Before committing, walk CLAUDE.md's self-review checklist for PHI-handling changes. This phase is
expected to trip zero items; if one trips, that is a signal the scope drifted into the PHI path,
not something to reason around.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `visibleAnswers` is called exactly three times in the file and `ALL_ITEMS` is imported:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizContainer.tsx","utf-8"); console.log("calls",s.split("visibleAnswers(ALL_ITEMS").length-1,"import",s.split("ALL_ITEMS").length-1)'`
      must print `calls 3` and an `ALL_ITEMS` count of at least `4`.
    - The payload no longer sends raw answers:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizContainer.tsx","utf-8"); const b=s.slice(s.indexOf("const buildPayload"), s.indexOf("const submitPayload")); console.log("visible",b.split("answers: visible").length-1,"raw",b.split(/\n\s+answers,\n/).length-1)'`
      must print `visible 1` and `raw 0`.
    - Zero question-ID literals remain in QuizContainer.tsx:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizContainer.tsx","utf-8"); for (const n of ["\"taking_meds\"","med_list","med_control"]) console.log(n, s.split(n).length-1)'`
      must print `0` for all three.
    - No `delete ` statement remains in `handleAnswerChange`:
      `node -e 'const s=require("fs").readFileSync("app/components/quiz/QuizContainer.tsx","utf-8"); const b=s.slice(s.indexOf("const handleAnswerChange"), s.indexOf("const buildPayload")); console.log(b.split("delete ").length-1)'`
      must print `0`.
    - Score-equivalence check, run as a throwaway node/tsx snippet and recorded: with
      `{ ...fullyAnsweredParts1to4, taking_meds: "no", med_list: "X", med_control: "not_at_all" }`,
      `calculateTotalScore(ALL_SCORED_QUESTIONS, visibleAnswers(ALL_ITEMS, answers))` must equal the
      score computed with `med_list` / `med_control` absent entirely. `med_control` is worth 3
      points at `not_at_all`, so a filter that failed to strip it would show up as a 3-point
      difference. Record both numbers.
    - `npm run typecheck` exits 0, `npm test` shows a net add versus 173 with 0 skips, and
      `npm run build` succeeds.
    - `git diff main --name-only` lists no file under `app/routes/`, and does not list
      `app/lib/db.ts` or `app/lib/submissions.ts`.
    - `git diff main -- package.json package-lock.json` is empty.
  </acceptance_criteria>
  <done>All three score sites and the submit payload read filtered answers; the contradicting
  deletion is gone; nothing outside the client tier was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gate and open the PR</name>
  <files>none — verification and delivery only</files>
  <read_first>
    - .planning/phases/02-quiz-schema-foundation/02-VALIDATION.md ("Sampling Rate", "Non-Vacuous
      Proof Strategy", "Validation Sign-Off")
    - .planning/phases/02-quiz-schema-foundation/02-01-SUMMARY.md, 02-02-SUMMARY.md,
      02-03-SUMMARY.md (the recorded RED runs and negative controls this gate consolidates)
    - CLAUDE.md "Development workflow" and "Self-review checklist for PHI-handling changes"
  </read_first>
  <action>
Run the phase gate and assemble the evidence. Do not fix anything by weakening a test; if something
is red, stop and report rather than adjusting an assertion.

1. `npm run typecheck` — must exit 0.
2. `npm test` — record the exact `Test Files` and `Tests` totals. The baseline is 173 tests / 17
   files. The phase must NET-ADD. A total at or below 173, or any skipped test, is a gate failure.
3. `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` — must pass, and the SUMMARY must
   show it beside Plan 02-01's recorded failure counts. A passing run alone proves nothing; the
   pair does.
4. Re-run the Plan 02-01 Task 3 negative control one final time: flip `kind: "info"` to
   `kind: "question"` in the type-guarantee fixture, confirm `npm run typecheck` fails with TS2578,
   revert, confirm it passes. VALIDATION.md Rule 4 requires the D-09 guarantee to be a compile-time
   proof, and a guarantee nobody has watched fail is not one.
5. `npm run build` — must succeed.
6. Confirm `git diff main -- app/components/quiz/QuizPartRenderer.test.ts` contains additions only.
7. Confirm `git diff main --name-only` touches no route, no `app/lib/db.ts`, no
   `app/lib/submissions.ts`, no `app/lib/shopify/`, and no `package.json` / `package-lock.json`.
8. Walk CLAUDE.md's PHI self-review checklist and record the result. Expected: zero items tripped.
   Note explicitly for the reviewer that this phase adds no `console.log` of any answer value, adds
   no route that returns PHI, and adds no dependency.
9. Push `phase-2-quiz-schema-foundation` and open a PR. The description must call out: no PHI-path
   file was touched; the payload's `answers` field is now the `visibleAnswers()` output, which
   narrows what reaches Cloud SQL rather than widening it; two deliberate patient-visible behavior
   changes ship (D-06 and D-16); the `handleAnswerChange` deletion removal is a data-loss fix; and
   Part 6's two questions carry `required: false` for the reason recorded in Plan 02-01.
   Do NOT merge — Andrew reviews and merges. Do NOT run `fly deploy` from the branch.
  </action>
  <verify>
    <automated>npm run typecheck && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; `npm run build` exits 0.
    - `npm test` reports strictly more than 173 tests across at least 19 files, with 0 skipped and
      0 failed. Exact totals recorded in the SUMMARY.
    - The literal-inventory RED (Plan 02-01) and GREEN (Plan 02-03) results appear side by side in
      the SUMMARY with their numbers.
    - The TS2578 negative control was re-run and both outcomes are recorded.
    - `git diff main --name-only` lists exactly these files and no others:
      `app/lib/quiz/types.ts`, `app/lib/quiz/questions.ts`, `app/lib/quiz/schema.ts`,
      `app/lib/quiz/schema.test.ts`, `app/components/quiz/QuizPartRenderer.tsx`,
      `app/components/quiz/QuizPartRenderer.test.ts`, `app/components/quiz/QuizContainer.tsx`,
      `tests/quiz-part-renderer-no-literals.test.ts`, `tests/quiz-schema-type-guarantees.test.ts`,
      plus `.planning/**`. Any additional file is scope drift and must be explained or reverted.
    - The PHI self-review checklist result is recorded with zero items tripped.
    - A PR exists against `main` from `phase-2-quiz-schema-foundation`, unmerged, with the
      PHI-relevant callouts listed above. `main` has received no direct commit
      (`git log main..phase-2-quiz-schema-foundation --oneline` is non-empty and
      `git log origin/main --oneline -1` is unchanged from the phase start).
  </acceptance_criteria>
  <done>The phase gate is green with recorded non-vacuous evidence, and the work is in review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the two patient-visible changes and the info-block render in a real browser</name>
  <files>none — manual verification</files>
  <read_first>
    - .planning/phases/02-quiz-schema-foundation/02-VALIDATION.md "Manual-Only Verifications" and
      its "Known coverage limit, accepted" note
    - .planning/phases/02-quiz-schema-foundation/02-CONTEXT.md (D-06, D-16, D-10)
  </read_first>
  <action>
Pause for human verification. Present the four checks in "How to verify" below, wait for the
result, and do not proceed or mark the phase complete without it. This checkpoint is BLOCKING and
is not auto-approvable: VALIDATION.md states that pure-function extraction proves the decisions are
correct but not that the renderer honors them, and calls these checks non-optional.

After the human reports, record all four outcomes verbatim in the plan SUMMARY, confirm the Check 4
throwaway fixture was reverted, and file any visual note about the info block as a Phase 3 item
rather than fixing it here — this phase ships mechanism, not presentation.
  </action>
  <what-built>
Every decision the renderer used to make from a question-ID literal now lives in
`app/lib/quiz/schema.ts` as a pure, unit-tested function: `isAnswered`, `evaluateShowIf`,
`visibleItems`, `visibleAnswers`, `toggleOption`, `isOptionDisabledByExclusive`. The renderer maps
items to markup. `QuizContainer` filters answers at three score sites and the submit payload.

Why this checkpoint is not optional: pure-function extraction proves the DECISIONS are correct, not
that the RENDERER honors them. A wiring bug — `toggleOption` correct but called with the wrong
argument, or `visibleItems` correct but its result ignored — would pass every automated test in
this phase. There is no DOM test infrastructure in this repo and none is being added (DIR-01).
These checks are the only thing standing between that class of bug and production.
  </what-built>
  <how-to-verify>
Start the app locally (`npm run dev`) and open the quiz. If the local Shopify dev flow is
inconvenient, `/quiz-embed` on a local server renders the same React tree.

**Check 1 — D-06, empty selection disables Next.**
  1. Reach Part 1 (nasal / eye / sinus symptom checklists).
  2. Answer the eye and sinus questions with "None of the above".
  3. On the nasal question, tick any symptom, then untick it so nothing is selected.
  4. EXPECTED: the "Next" button is DISABLED. Before this phase it was enabled and the patient
     could advance with nothing recorded.
  5. Tick "None of the above" on the nasal question. EXPECTED: "Next" ENABLES.

**Check 2 — D-16, an exclusive option still deselects.**
  1. On the same nasal question, with "None of the above" selected, confirm the other five options
     are visibly DISABLED but "None of the above" itself is still clickable.
  2. Click "None of the above" again. EXPECTED: it deselects, the other options become enabled
     again, nothing is selected, and "Next" is DISABLED (this is D-06 and D-16 composing, and it is
     intended, signed-off, patient-visible behavior — not a regression).

**Check 3 — D-03, the medication list survives a toggle.**
  1. Reach Part 5. Answer "yes" to "Are you currently taking any allergy medications?".
  2. Type a distinctive string into the medication list, e.g. `Cetirizine 10mg daily`.
  3. Switch the answer to "no". EXPECTED: the medication list and the control question DISAPPEAR,
     and "Next" enables.
  4. Switch back to "yes". EXPECTED: the medication list reappears WITH `Cetirizine 10mg daily`
     still in it. Before this phase the text was deleted with no undo.

**Check 4 — D-10, an info block renders and collects nothing.**
This phase ships mechanism, not content, so there is no info block in `questions.ts` to look at.
Verify with a throwaway fixture:
  1. Temporarily add one `kind: "info"` item to `PART1_SYMPTOM_CHECKLIST` in
     `app/lib/quiz/questions.ts` with a `heading`, two `paragraphs`, and two `bullets`.
  2. Reload Part 1. EXPECTED: the heading, both paragraphs, and both bullets render inside a card;
     there is NO input, checkbox, radio, or textarea attached to it; and it does not affect whether
     "Next" is enabled.
  3. Add `showIf: { questionId: "symptoms_nasal", includes: "sneezing" }` to the same fixture.
     EXPECTED: the block appears only once "Sneezing" is ticked and disappears when it is unticked.
  4. **REVERT the fixture and confirm `git diff main -- app/lib/quiz/questions.ts` shows no
     `kind: "info"`.** This content must not ship — Phase 3's HIST-04 is the first real info block.

Note any visual problem with the info block's presentation. It reuses the existing `questionCard`
classes and no new CSS was written, so imperfect spacing is expected and is a Phase 3 concern, not
a blocker here — but say so explicitly rather than leaving it unrecorded.
  </how-to-verify>
  <verify>
    <human-check>Four checks reported with EXPECTED or a description of what differed; Check 4 fixture confirmed reverted.</human-check>
  </verify>
  <acceptance_criteria>
    - Checks 1, 2, 3, and 4 each report EXPECTED or an explicit description of what differed.
    - The Check 4 fixture is reverted and `git diff main -- app/lib/quiz/questions.ts` contains no
      `kind: "info"`.
    - Any visual issue with the info block is recorded as a Phase 3 note rather than silently
      accepted or silently fixed.
    - If Check 1 or Check 2 fails, the cause is a renderer wiring bug, not a `schema.ts` bug —
      `toggleOption` and `isAnswered` are already proven correct by unit tests. Report which
      argument or return value was mishandled.
  </acceptance_criteria>
  <done>The two deliberate patient-visible changes and the info-block render are confirmed by
  observation, closing the coverage gap VALIDATION.md flags as accepted-but-not-optional.</done>
  <resume-signal>Type "approved" with the four check results, or describe what differed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React answers state -> `visibleAnswers()` -> `POST /api/quiz/submit` -> Cloud SQL / clinical PDF | The PHI boundary. This plan changes only what the client puts in the body; the route, its validation, the DB layer, and the PDF generator are untouched. The `answers` shape (`Record<string, string \| string[] \| number>`) is unchanged, so no downstream contract moves. |
| React answers state -> displayed score | Now computed from the same filtered set as the payload, so the score shown and the score stored cannot disagree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-16 | Tampering | Orphan hidden answer reaching Cloud SQL and the clinical PDF | mitigate | `visibleAnswers(ALL_ITEMS, answers)` applied in `buildPayload` before both the score and the `answers` field. Verified by the acceptance criterion asserting `answers: visible` appears once and the raw `answers,` shorthand zero times inside `buildPayload`. |
| T-2-17 | Tampering | A stale hidden `med_control` inflating the score | mitigate | Same filter at all three `calculateTotalScore` sites (DIR-03). `med_control` scores up to 3 at `not_at_all`, so the score-equivalence acceptance check would show a 3-point gap if the filter missed it. Live, not hypothetical. |
| T-2-18 | Information Disclosure | Losing a patient's typed medication list | mitigate | The `handleAnswerChange` deletion branch is removed; D-03 keeps the answer in React state and strips it at the boundary. Confirmed by human Check 3, which is the only place this is observable. |
| T-2-19 | Information Disclosure | PHI in logs | accept | This plan adds no logging. The one existing `console.warn` in this file logs a rejected navigation path only, and CLAUDE.md's checklist walk in Task 2 records the result. No answer value, name, DOB, email, phone, score, or bracket appears anywhere in the diff. |
| T-2-20 | Repudiation | A wiring bug passing every automated test | mitigate | Task 3's blocking human checkpoint. VALIDATION.md states this coverage limit explicitly and calls the manual checks non-optional; it is not auto-approvable. |
| T-2-21 | Tampering | Info-block content shipping accidentally | mitigate | Check 4's fixture is throwaway. The acceptance criterion requires `git diff main -- app/lib/quiz/questions.ts` to contain no `kind: "info"` after reverting, and Task 2's file-list criterion would surface it too. |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed across the entire phase; asserted by an empty `package.json` / `package-lock.json` diff in Tasks 1 and 2. RESEARCH.md records the Package Legitimacy Gate as skipped by its own preconditions. |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm run build` exits 0.
- `npm test` reports more than 173 tests across at least 19 files, 0 skipped, 0 failed.
- `visibleAnswers(ALL_ITEMS` appears exactly three times in `QuizContainer.tsx`.
- Zero occurrences of `taking_meds`, `med_list`, or `med_control` remain in `QuizContainer.tsx`.
- `git diff main --name-only` is confined to the nine source/test files plus `.planning/**`.
- The four human checks are reported, and the Check 4 fixture is confirmed reverted.
</verification>

<success_criteria>
- D-03 is live at all three score sites and the submit payload (DIR-03).
- The medication-list data-loss bug is gone and the fix is confirmed by observation, not inference.
- The phase gate is green with recorded non-vacuous evidence for SCH-02 (RED then GREEN) and for
  D-09 (TS2578 observed then reverted).
- A PR is open and unmerged; `main` received no direct commit; nothing was deployed from a branch.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-schema-foundation/02-04-SUMMARY.md` when done. Record: the final
`npm test` totals versus 173 / 17; the score-equivalence numbers; the four human check results; the
PR URL; and a Phase 3 handoff note covering the three findings this phase produced —
`required: false` on the two Part 6 questions and why, the NON-TRANSITIVE chained-`showIf` rule and
its forward guard, and the fact that `visibleAnswers` strips known-and-hidden rather than keeping
known-and-visible so Phase 3 does not "simplify" it into a whitelist.
</output>
</content>
