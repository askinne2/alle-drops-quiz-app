---
phase: 01-live-defect-fixes
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - public/quiz-bundle.js
  - CLAUDE.md
  - .planning/STATE.md
autonomous: true
requirements: [DEF-01, DEF-02, DEF-03, DEF-04]

must_haves:
  truths:
    - "The committed public/quiz-bundle.js contains the corrected handles and the corrected label"
    - "The committed bundle no longer contains either allerdrops misspelling or the (required) label suffix"
    - "The full test suite and typecheck are green with no fewer than 51 pre-existing tests passing"
    - "CLAUDE.md no longer instructs future agents that this is not a GSD project"
    - "Every out-of-scope defect found during this phase is recorded rather than lost"
  artifacts:
    - path: "public/quiz-bundle.js"
      provides: "Rebuilt committed theme bundle matching the changed source"
      contains: "tennessee-alledrops"
    - path: "CLAUDE.md"
      provides: "Corrected project guidance without the stale anti-GSD line"
    - path: ".planning/STATE.md"
      provides: "Recorded follow-up candidates surfaced by this phase"
      contains: "frame-ancestors"
  key_links:
    - from: "app/lib/quiz/product-links.ts"
      to: "public/quiz-bundle.js"
      via: "npm run build:theme bundles the corrected handle into the committed artifact"
      pattern: "tennessee-alledrops"
    - from: "app/lib/quiz/questions.ts"
      to: "public/quiz-bundle.js"
      via: "npm run build:theme bundles the corrected label into the committed artifact"
      pattern: "and dosages"
---

<objective>
Rebuild and commit the theme bundle so the committed artifact agrees with the changed source, correct
the stale instruction in `CLAUDE.md` that tells every future agent to stop, and record the
out-of-scope defects this phase surfaced.

Purpose: `public/quiz-bundle.js` is a **committed static artifact** read off disk at request time by
`app/routes/quiz-bundle-js.tsx`. The Dockerfile now runs `npm run build && npm run build:theme`, so
the image does rebuild it — but the committed copy is not gitignored and can silently diverge from
source in the working tree. Session 28 was burned by exactly that divergence: `fly deploy` succeeded,
`/health` returned 200, headers matched, and the app served a bundle the build had never touched.
Committing a freshly built artifact alongside the source change removes the ambiguity before the
deploy rather than after it.

This plan is also the last fully autonomous gate. Every remaining plan needs Andrew — for merge and
deploy authorization, for theme-editor access, and for Cloud SQL. The suite must be unambiguously
green here.

Output: a rebuilt committed bundle whose content assertions all pass locally, a corrected `CLAUDE.md`,
and a recorded set of follow-ups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-defect-fixes/01-VALIDATION.md
@.planning/phases/01-live-defect-fixes/01-02-SUMMARY.md
@.planning/phases/01-live-defect-fixes/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild and commit the theme bundle, then assert its content locally</name>

  <read_first>
    - `package.json` — confirm the script name is `build:theme` (it maps to `vite build --config vite.theme.config.ts`). Confirm `@vitejs/plugin-react` is listed under `dependencies`, not `devDependencies`; the Dockerfile runs `npm ci --omit=dev` and would otherwise strip it, breaking `build:theme` in the image. Do not "tidy" it into `devDependencies`.
    - `Dockerfile` — confirm it runs both `npm run build` and `npm run build:theme`, and note that `COPY package.json package-lock.json* ./` followed by `npm ci` means a **lockfile must exist** for the deploy to succeed. `package-lock.json` is gitignored (`.gitignore:7`), which is unusual and is recorded in `HANDOFF.md:127`.
    - `.dockerignore` — it contains only `.cache`, `build`, `node_modules`. `public/` is copied in and then overwritten by the in-image build; the ordering is load-bearing and undocumented.
    - `app/routes/quiz-bundle-js.tsx` — confirms the served bytes are read off disk from `public/quiz-bundle.js` at request time.
    - `.planning/phases/01-live-defect-fixes/01-VALIDATION.md` §"Deploy-Artifact Provenance Gates" — Gate B's marker list and today's baselines.
    - `.planning/phases/01-live-defect-fixes/01-02-SUMMARY.md` — for the recorded pre-change byte size.
  </read_first>

  <files>public/quiz-bundle.js</files>

  <action>
    Confirm a `package-lock.json` exists in the working tree before doing anything else. `npm ci` in the
    Dockerfile requires one; a deploy from a tree without it fails, and a deploy from a stale one
    installs stale dependencies. If it is absent, generate it with `npm install --package-lock-only` and
    say so in the summary — do not silently proceed, because the file is gitignored and will not travel
    with the commit.

    Run `npm run build:theme`. Then run `npm run typecheck && npm test` and require both to exit 0 with
    the full suite green, including `tests/liquid-block-contract.test.ts` and
    `tests/quiz-embed-contract.test.ts`, which Plans 02 and 03 turned green.

    Assert the rebuilt artifact's content locally, before any deploy. These are the same markers Gate B
    will assert against the served bytes, and running them locally first separates "the build is wrong"
    from "the deploy is stale" — two failure modes that session 28 conflated. Against
    `public/quiz-bundle.js`: `tennessee-alledrops` occurs at least once; `texas-alledrops` occurs at
    least once; `tennessee-allerdrops` occurs zero times; `texas-allerdrops` occurs zero times; the
    label-with-suffix string occurs zero times; `quiz:scrollToTop` occurs at least once; and
    `window.location.assign` does not appear as a reassignment target. Record the new byte count and
    compare it to the pre-change 183691 — it must differ. Compute and record
    `shasum -a 256 public/quiz-bundle.js`; Plan 05 needs the pre-deploy hash, because a served hash that
    **matches** the pre-deploy value is the stale-artifact signature.

    Commit `public/quiz-bundle.js` in the same commit as, or immediately alongside, the source changes
    so the committed artifact and the source agree. Stay on `fix-phase1-live-defects`; do not merge and
    do not deploy from this branch (CLAUDE.md:133).

    Do not add an ETag or change either bundle route's `Cache-Control` in this phase. Both are worth
    doing — the absence of `ETag` and `Last-Modified` is precisely what makes deploy verification a
    string-counting exercise, and the two routes `quiz-bundle.js.tsx` and `quiz-bundle-js.tsx` disagree
    on `max-age` (3600 versus 300) — but both are out of DEF-01..04 scope. Record them in Task 2.
  </action>

  <verify>
    <automated>npm run typecheck && npm test && printf '%s' "$(cat public/quiz-bundle.js)" | grep -c "tennessee-alledrops" && [ "$(grep -c 'tennessee-allerdrops' public/quiz-bundle.js)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm test` exits 0, reporting no fewer than 51 passing tests and reporting `tests/liquid-block-contract.test.ts` and `tests/quiz-embed-contract.test.ts` as passing files.
    - `grep -c "tennessee-alledrops" public/quiz-bundle.js` returns at least 1.
    - `grep -c "texas-alledrops" public/quiz-bundle.js` returns at least 1.
    - `grep -c "tennessee-allerdrops" public/quiz-bundle.js` returns 0.
    - `grep -c "texas-allerdrops" public/quiz-bundle.js` returns 0.
    - `grep -c "and dosages (required)" public/quiz-bundle.js` returns 0.
    - `grep -c "quiz:scrollToTop" public/quiz-bundle.js` returns at least 1.
    - `wc -c < public/quiz-bundle.js` differs from 183691.
    - `ls package-lock.json` succeeds.
    - `git status --short public/quiz-bundle.js` shows the file staged or committed, not untracked-and-ignored.
    - `shasum -a 256 public/quiz-bundle.js` is recorded in the summary as the pre-deploy hash.
    - `git branch --show-current` returns `fix-phase1-live-defects`.
  </acceptance_criteria>

  <done>
    A freshly built `public/quiz-bundle.js` is committed, every Gate B marker passes against the local
    artifact, its byte count differs from the pre-change baseline, its pre-deploy hash is recorded, and
    the full suite plus typecheck are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend the stale CLAUDE.md instruction and record out-of-scope findings</name>

  <read_first>
    - `CLAUDE.md` — read the whole file. Line 13 currently reads `## THIS IS NOT a GSD project. never try to start GSD.` It sits between the compliance-rules block and `## What this app is`. Everything else in this file is genuine, current guidance and must be preserved untouched: the six PHI compliance rules at `:3-10`, the branch and PR workflow at `:126-134`, the PHI self-review checklist at `:135-145`, and the common pitfalls at `:147-154`.
    - `.planning/STATE.md` — read the `## Deferred Items` table and `## Blockers/Concerns` so new entries match the existing shape and do not duplicate what is already recorded.
    - `.planning/phases/01-live-defect-fixes/01-RESEARCH.md` §"Open Questions" (line 1338) and §"Pitfall 6" side note (line 966) — the sources for the follow-ups below.
  </read_first>

  <files>CLAUDE.md, .planning/STATE.md</files>

  <action>
    Remove the line in `CLAUDE.md` that asserts this is not a GSD project and instructs agents never to
    start GSD. It is out of date: the repo owner directed GSD setup, `.planning/` now holds a committed
    scaffold with a full eight-phase roadmap, and this very phase was produced by `/gsd:discuss-phase`.
    Left in place it will keep stopping every future agent that reads the file, which has real cost.

    Replace it with a short, accurate section stating that this repo **is** GSD-managed, that
    `.planning/` holds the roadmap, requirements, and per-phase plans, and that agents should read
    `.planning/ROADMAP.md` and `.planning/STATE.md` before starting work. Change nothing else in the
    file. In particular do not reflow, reword, or reorder the six PHI compliance rules, the development
    workflow, the self-review checklist, or the common pitfalls.

    Add the following to `.planning/STATE.md` — five items surfaced by this phase's research that are
    real and out of DEF-01..04 scope. Each needs enough detail that a future planner does not have to
    rediscover it. Match the existing table or bullet formatting in the file.

    First, `Content-Security-Policy: frame-ancestors *` on `/quiz-embed` lets any site frame a
    PHI-collecting form. Clickjacking exposure. Phase 8 candidate. The `e.origin` guard added in Plan 03
    narrows what a hostile framer can cause but does not prevent the framing.

    Second, neither bundle route emits `ETag` or `Last-Modified`, and `app/routes/quiz-bundle.js.tsx`
    and `app/routes/quiz-bundle-js.tsx` serve the same file with disagreeing `max-age` values (3600
    versus 300). Adding a content-hash ETag is roughly a three-line change and would convert every
    future deploy verification from string-counting into a single conditional request. Phase 8
    candidate. Note that all gates in this phase deliberately assert against `/quiz-bundle-js`, the
    300-second variant, because that is the one `quiz-embed.tsx` references.

    Third, a latent double-submit defect: on the `3-6` bracket a patient can click "Schedule a
    Telehealth Appointment", which submits, navigate back, then take "Continue to Purchase" through
    consent and submit again — violating the `NOT NULL UNIQUE` constraint on
    `submissions.symptom_profile_id`, because `generateSymptomProfileId()` returns `AOD_${Date.now()}`
    and is called once per session. Real and patient-facing. Phase 4 (TEST-05) deletes the `3-6`
    purchase jump entirely and removes it for free, so it needs no separate fix — but it must be
    recorded, because reproducing it during verification would otherwise look like a Phase 1
    regression.

    Fourth, `app/entry.theme.tsx`'s `injectIframe()` message handler is correct code on a dead path. Its
    existence is what made DEF-01 look implemented for two months. It also carries the same open-redirect
    pattern D-05 just closed on the live path, plus `behavior: "smooth"` and a wrapper-scroll that both
    violate D-06. Deletion candidate for Phase 8; deliberately untouched in this phase.

    Fifth, the sticky-header scroll offset is hardcoded at 100px in the Liquid block's `{%- style -%}`
    region rather than exposed as a `range` setting, because whether a newly added non-`product` schema
    setting receives its default on an **already-placed** block is unverified. If tuning it ever
    requires a deploy, verify that behavior first, then promote it to a setting.

    Also confirm and record that Klaviyo is **still live** on `/pages/allergy-quiz` — 4 occurrences in
    the HTML fetched 2026-07-30. It is a reportable-breach trigger owned by Phase 8 / LAUNCH-01. State
    explicitly in `STATE.md` that Phase 1's verification pass does **not** clear LAUNCH-01, so that a
    green Phase 1 is not misread as a clean patient-facing page.
  </action>

  <verify>
    <automated>[ "$(grep -c 'THIS IS NOT a GSD project' CLAUDE.md)" = "0" ] && grep -c "frame-ancestors" .planning/STATE.md && grep -c "LAUNCH-01" .planning/STATE.md && npm run typecheck && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "THIS IS NOT a GSD project" CLAUDE.md` returns 0.
    - `grep -c "never try to start GSD" CLAUDE.md` returns 0.
    - `grep -c ".planning/ROADMAP.md" CLAUDE.md` returns at least 1 — the replacement points agents at the roadmap.
    - `grep -c "Never write PHI to Shopify metafields" CLAUDE.md` returns 1 — compliance rule 2 is intact.
    - `grep -c "Never add analytics, session-replay" CLAUDE.md` returns 1 — compliance rule 4 is intact.
    - `grep -c "Always work on a feature branch" CLAUDE.md` returns 1 — the workflow rule is intact.
    - `git diff --stat CLAUDE.md` shows fewer than 12 changed lines — the edit is surgical, not a rewrite.
    - `grep -c "frame-ancestors" .planning/STATE.md` returns at least 1.
    - `grep -c "ETag" .planning/STATE.md` returns at least 1.
    - `grep -c "symptom_profile_id" .planning/STATE.md` returns at least 1.
    - `grep -c "entry.theme.tsx" .planning/STATE.md` returns at least 1.
    - `grep -c "scroll-margin-top" .planning/STATE.md` returns at least 1.
    - `grep -c "LAUNCH-01" .planning/STATE.md` returns at least 1 and the surrounding text states that Phase 1 does not clear it.
    - `npm run typecheck && npm test` both exit 0 — a docs edit must not break the suite.
  </acceptance_criteria>

  <done>
    The stale anti-GSD instruction is gone and replaced with accurate guidance, every PHI compliance
    rule and workflow rule in `CLAUDE.md` is byte-intact, five follow-up candidates plus the LAUNCH-01
    caveat are recorded in `STATE.md`, and the suite is still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → committed build artifact → Docker image → served bytes | Four representations of the same code that can silently disagree. The documented session-28 failure lived here. |
| repo documentation → future agent behavior | `CLAUDE.md` is read as instruction, not description. A stale line there is an operational defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | The open redirect in `symptom-quiz.liquid` — closed in Plan 03 | mitigate | This plan does not touch it. It does carry it toward production: the Liquid file ships through `shopify app deploy` in Plan 05, and Gate C proves the guard is present in the rendered live page. Until Plan 05 completes, **the open redirect is still live in production** regardless of the state of this branch. |
| T-1-17 | Tampering | **Stale-artifact false positive.** A deploy is declared successful while the app serves a `public/quiz-bundle.js` the build never touched — the documented session-28 incident, caused by `Cache-Control` and exit-code reasoning substituting for content assertion. | mitigate | Rebuild with `build:theme`, commit the artifact so source and committed bytes agree, assert all seven Gate B markers against the local file **before** deploying, record the byte count and the pre-deploy SHA-256. Plan 05 then treats a served hash matching the pre-deploy hash as a failure signal. Never assert on `Cache-Control`, a deploy exit code, or a 200 from `/health` — `/health` returns static JSON and never touches the database. |
| T-1-18 | Denial of Service | `package-lock.json` is gitignored yet `npm ci --omit=dev` in the Dockerfile requires one. A deploy from a tree without it fails; a deploy from a stale one installs stale dependencies. | mitigate | Task 1 confirms a lockfile exists before anything else and records the fact in the summary. Related trap recorded but not changed: `@vitejs/plugin-react` must stay in `dependencies` or `build:theme` breaks inside the image. |
| T-1-19 | Repudiation | Out-of-scope defects found during research are lost when the phase closes, and are rediscovered at cost later | mitigate | Task 2 records five specific findings plus the LAUNCH-01 caveat in `STATE.md` with enough detail to act on without re-deriving. |
| T-1-16 | Information Disclosure | Klaviyo on `/pages/allergy-quiz` — a third-party script on a PHI-collecting page, 4 occurrences confirmed live 2026-07-30. A reportable-breach trigger per `docs/breach-response-runbook.md:16`. | transfer | Owned by Phase 8 / LAUNCH-01, not fixable from this repo. Task 2 records it explicitly **and** records that a green Phase 1 does not clear it, so the two are never conflated in a status report. |
| T-1-09 | Information Disclosure | `frame-ancestors *` clickjacking exposure on the PHI form | accept | Recorded as a Phase 8 candidate in Task 2. Out of DEF-01..04 scope. |
| T-1-SC | Tampering | Supply chain — npm/pip/cargo installs | accept | Zero package installs. If Task 1 must run `npm install --package-lock-only` to regenerate a missing lockfile, that resolves existing declared dependencies and adds none; `git diff package.json` must still be empty. Any actual new dependency requires CLAUDE.md:144's ask-before-adding path plus a Package Legitimacy Gate pass. |
</threat_model>

<verification>
- `npm run typecheck && npm test` both exit 0, full suite green.
- All seven Gate B markers pass against the local `public/quiz-bundle.js`.
- `wc -c < public/quiz-bundle.js` differs from 183691.
- `package-lock.json` exists.
- `git diff --stat package.json` is empty.
- `grep -c "THIS IS NOT a GSD project" CLAUDE.md` returns 0 with every compliance rule intact.
</verification>

<success_criteria>
- The committed theme bundle contains both corrected handles, contains no `allerdrops` spelling, contains no `(required)` label suffix, and contains `quiz:scrollToTop`.
- The pre-deploy SHA-256 and byte count are recorded for Plan 05.
- A lockfile is confirmed present.
- `CLAUDE.md` no longer stops future agents, with all compliance and workflow content byte-intact.
- Five follow-up candidates and the LAUNCH-01 caveat are recorded in `STATE.md`.
</success_criteria>

<output>
Create `.planning/phases/01-live-defect-fixes/01-04-SUMMARY.md` when done. Record: the pre-deploy
SHA-256 of `public/quiz-bundle.js`, its new byte count, the lockfile status, the exact replacement text
used in `CLAUDE.md`, and the list of follow-ups written to `STATE.md`.
</output>
