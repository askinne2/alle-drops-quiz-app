---
phase: 04-mandatory-allergy-testing
plan: 07
type: execute
wave: 3
depends_on: ["04-06"]
files_modified:
  - app/components/quiz/QuizPartRenderer.tsx
  - tests/quiz-part-renderer-dom.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Part 7 renders two radio options and nothing else until a choice is made"
    - "Selecting 'I've already had allergy testing' reveals three input fields in the DOM"
    - "Selecting 'I need allergy testing' leaves those three fields out of the DOM"
    - "Next is disabled until testing_status is answered, and until all three revealed fields are answered on the had_testing branch"
  artifacts:
    - path: "app/components/quiz/QuizPartRenderer.tsx"
      provides: "radio_single and text_input_short render branches"
      contains: "text_input_short"
    - path: "tests/quiz-part-renderer-dom.test.ts"
      provides: "Part 7 DOM coverage through the real renderer"
      contains: "testing_status"
  key_links:
    - from: "app/components/quiz/QuizPartRenderer.tsx"
      to: "app/lib/quiz/schema.ts isAnswered"
      via: "isPartComplete's generic per-item call — no renderer-level gating code"
      pattern: "isAnswered"
---

<objective>
Give `QuizPartRenderer` the two render branches Part 7's unblocked questions need — `radio_single`
and `text_input_short` — and cover Part 7 with DOM tests that render through the real renderer.

Both branches are structural copies of existing cases (`control_0_3` for radios,
`text_input` for the text field), which is what "content plus wiring, no new mechanism" means here.
`file_multi`'s branch is genuinely new interaction logic and belongs to plan 04-16 in the
blocker-gated upload wave.

DOM coverage is not optional on this file. Three of the five defects that shipped past a fully green
suite on this project were wiring bugs in exactly this component, invisible to every test that did
not render it.

Purpose: TEST-01 (two options, gated Next), TEST-02 (needs-testing branch reveals nothing),
TEST-03 (year, location, allergens actually reachable and capturable).
Output: two new `case` branches and a `renderPart7` DOM test block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md
@.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md
@app/components/quiz/QuizPartRenderer.tsx
@tests/quiz-part-renderer-dom.test.ts
@app/lib/quiz/schema.ts

<interfaces>
Structural donor for radio_single — QuizPartRenderer.tsx `case "control_0_3":` (lines 314-342).
It renders `.questionCard__optionsVertical` with `role="radiogroup"` and `aria-labelledby={key}`,
maps `question.options` to `<label className={optionVertical + (selected ? optionSelected : "")}>`
wrapping a true `<input type="radio" name={question.id}>`, and calls
`onAnswerChange(question.id, opt.value)`.

Structural donor for text_input_short — QuizPartRenderer.tsx `case "text_input":` (lines 293-312).
It renders `<textarea id={question.id} className={styles.quizContainer__input} rows={4} ...>`.
`.quizContainer__input` is already used on single-line `<input type="text">` elements by
`PatientInfoStep.tsx` for name/dob/email/phone — this is class reuse, not new CSS.

Gating — QuizPartRenderer.tsx `isPartComplete` (lines 359-366) already calls
`isAnswered(item, answers[item.id])` generically for every visible question. After plan 04-02 widened
that switch, gating works with ZERO renderer-level change. Do not add a per-type gating branch.

Fusion detector — `isGateItem` / `isRevealItem` (lines 89-103) key on `showIf` present AND
`required === false`. Part 7's children are all required, so they do not match. 04-UI-SPEC.md §4
decides they must NOT be made to match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the radio_single and text_input_short render branches</name>
  <files>app/components/quiz/QuizPartRenderer.tsx</files>
  <read_first>
    - `app/components/quiz/QuizPartRenderer.tsx` — the whole file. Read the `switch` in full, the
      `control_0_3` case (314-342), the `text_input` case (293-312), `isGateItem`/`isRevealItem`
      (89-103), `InfoBlockCard`, and `isPartComplete` (359-366).
    - `app/components/quiz/PatientInfoStep.tsx` — confirm how `.quizContainer__input` is applied to
      a single-line `<input type="text">` today, so `text_input_short` reuses the existing shape
      rather than inventing one.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` Component Inventory §1 and §4 —
      the `radio_single` naming rationale, the `text_input_short` rationale, and the explicit
      decision that Part 7 does NOT use the gate/reveal fusion.
    - `.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md`
      §`app/components/quiz/QuizPartRenderer.tsx` — the two diffs, spelled out.
  </read_first>
  <action>
    Add `case "radio_single":` as a near-verbatim structural copy of the existing `case "control_0_3":`
    block — same `cardClassName`, same `.questionCard__label` with `id={key}`, same optional
    `.questionCard__subtitle`, same `.questionCard__optionsVertical` with `role="radiogroup"` and
    `aria-labelledby={key}`, same `question.options` map producing true `<input type="radio"
    name={question.id}>` elements inside `.questionCard__optionVertical` labels with
    `.questionCard__optionSelected` applied when selected, same `onAnswerChange(question.id,
    opt.value)` handler, same `disabled` propagation. The only difference is the case label.

    Prefer sharing the JSX between `control_0_3` and `radio_single` by falling both case labels
    through to one block rather than duplicating ~28 lines. If a shared block would obscure the
    `control_0_3`-specific comment, duplicate instead and add a comment on each pointing at the other
    so they cannot drift apart silently.

    Add `case "text_input_short":` modeled on `case "text_input":`, differing only in the control:
    render `<input type="text" id={question.id} className={styles.quizContainer__input}
    value={val as string} disabled={disabled} onChange={...} />` instead of the 4-row `<textarea>`.
    Keep the label, optional subtitle, and `onAnswerChange` wiring identical.

    Do NOT touch `isPartComplete`, `isGateItem`, `isRevealItem`, `InfoBlockCard`, or any existing
    case. Do NOT add a `file_multi` case — plan 04-16 owns it. Do NOT introduce a question-ID literal
    anywhere; `tests/quiz-part-renderer-no-literals.test.ts` guards that and Phase 2 exists to
    prevent it.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/quiz-part-renderer-no-literals.test.ts tests/quiz-part-renderer-exclusive-clickable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/quiz/QuizPartRenderer.tsx` contains `case "radio_single":` and
      `case "text_input_short":`.
    - It contains zero occurrences of `case "file_multi"`.
    - It contains zero occurrences of the string `testing_status`, `testing_year`,
      `testing_location`, or `testing_allergens` (no question-ID literals).
    - `git diff app/components/quiz/QuizPartRenderer.tsx` shows no hunk inside `isPartComplete`,
      `isGateItem`, or `isRevealItem`.
    - `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` passes.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The renderer handles radio_single and text_input_short by reusing existing card structure and classes, with gating still delegated entirely to isAnswered.</done>
</task>

<task type="auto">
  <name>Task 2: Cover Part 7 in the DOM test suite through the real renderer</name>
  <files>tests/quiz-part-renderer-dom.test.ts</files>
  <read_first>
    - `tests/quiz-part-renderer-dom.test.ts` — the whole file. Note the `// @vitest-environment jsdom`
      header, the `React.createElement` (not JSX) construction, the `cleanup()` in `afterEach`, and
      the `renderPart6` helper at approximately lines 51-67 which is the template for `renderPart7`.
    - `app/components/quiz/QuizPartRenderer.tsx` (post-Task-1).
    - `app/lib/quiz/questions.ts` (post-04-06) — `PART7_ALLERGY_TESTING` and `QUIZ_PARTS` index 6.
    - `.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md` §"Per-Task Verification Map"
      rows TEST-01, TEST-02, TEST-03 — the behaviors this file must cover.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §"Locked / verbatim copy" — the
      two option labels, asserted verbatim below.
  </read_first>
  <action>
    Add a `renderPart7` helper mirroring `renderPart6`: render `itemsForPart(QUIZ_PARTS, 6)` through
    the real `QuizPartRenderer` with a supplied `answers` object and a `vi.fn()` `onAnswerChange`.
    Do NOT hand-build a synthetic question array — the point of this file is that it exercises the
    real `QUIZ_PARTS` data through the real renderer.

    Cover, at minimum:

    1. With `answers = {}`: exactly two radio inputs render; their accessible labels are exactly
       `I need allergy testing` and `I've already had allergy testing` (assert verbatim — these are
       locked copy); and there is no third option and no skip control.
    2. With `answers = {}`: querying for a text input or textarea returns nothing — none of the three
       children render before a choice is made.
    3. With `answers = { testing_status: "needs_testing" }`: still zero text inputs/textareas. This
       is TEST-02's structural guarantee — the needs-testing branch collects nothing.
    4. With `answers = { testing_status: "had_testing" }`: three additional controls render. Assert
       each by its visible question text — `What year did you have your allergy testing done?`,
       `Where did you have your allergy testing done?` (substring match is fine), and
       `What Allergens Did You React To?` (assert this one verbatim — locked copy).
    5. Typing into the year field fires `onAnswerChange` with `("testing_year", <typed value>)`.
       Use `fireEvent.change`.
    6. Selecting a radio fires `onAnswerChange` with `("testing_status", "had_testing")`.
    7. Gating, asserted against the real `isPartComplete` imported from the renderer module (not
       re-derived in the test):
       - `{}` -> false
       - `{ testing_status: "needs_testing" }` -> true (no children to satisfy)
       - `{ testing_status: "had_testing" }` -> false
       - `{ testing_status: "had_testing", testing_year: "2019", testing_location: "X",
          testing_allergens: "Y" }` -> true
       - `{ testing_status: "had_testing", testing_year: "  ", testing_location: "X",
          testing_allergens: "Y" }` -> false (whitespace-only does not satisfy `text_input_short`)
    8. Non-vacuity control: assert Part 6 still renders as before by keeping the existing
       `renderPart6` tests untouched and green.

    Keep the file at `.test.ts` and keep using `React.createElement` — `vitest.config.ts`'s include
    glob does not match `.tsx`, and this cost time in Phase 3.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-part-renderer-dom.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The suite asserts the two option labels verbatim, including the apostrophe in
      `I've already had allergy testing`.
    - The suite asserts zero text inputs render for both `{}` and `{ testing_status: "needs_testing" }`.
    - The suite asserts all three children render for `{ testing_status: "had_testing" }`, one of
      them matched on the verbatim string `What Allergens Did You React To?`.
    - The gating assertions use the real `isPartComplete` export, and the whitespace-only case
      returns `false`.
    - Existing `renderPart6` tests are unmodified and still pass.
    - `npm test` full suite green; total count strictly greater than plan 04-06's recorded count.
  </acceptance_criteria>
  <done>Part 7 is exercised end to end through real question data and the real renderer, covering both branches, both gate outcomes, and the locked copy strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declarative question data → rendered DOM | The renderer is where five shipped defects lived; a green non-DOM suite has repeatedly proven insufficient here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-23 | Tampering | Required-field gate bypass via whitespace | mitigate | Task 2's gating case asserts `testing_year: "  "` yields `isPartComplete === false`, exercising `text_input_short`'s `trim()` group from plan 04-02 |
| T-4-24 | Information Disclosure | `needs_testing` branch silently collecting clinical fields | mitigate | Task 2 case 3 asserts zero text inputs render on that branch, so a `showIf` regression that fails open is caught in the DOM, not just in schema unit tests |
| T-4-25 | Tampering | Question-ID literals creeping back into the renderer | mitigate | Task 1 forbids them and `tests/quiz-part-renderer-no-literals.test.ts` runs in this plan's verify command |
| T-4-26 | Denial of Service | A required field that traps a patient with no escape | accept for this plan, mitigated later | This is defect-5's shape. No structural test can catch it; plan 04-19's human browser pass is the control, and Part 7's `needs_testing` branch is itself the escape (D-06) |

No packages installed in this plan.
</threat_model>

<verification>
- `npm run typecheck` clean; `npm test` full suite green with a higher count than 04-06.
- `npx vitest run tests/quiz-part-renderer-no-literals.test.ts` green — no ID literals added.
- `public/quiz-bundle.js` is deliberately NOT rebuilt here — plan 04-09 owns the single rebuild.
</verification>

<success_criteria>
- A patient on Part 7 sees exactly two options, can advance only after choosing one, and on the
  `had_testing` branch only after filling all three revealed fields with non-whitespace text.
- The `needs_testing` branch collects nothing beyond the choice itself.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-07-SUMMARY.md` when done.
</output>
