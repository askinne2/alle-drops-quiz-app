---
phase: 04-mandatory-allergy-testing
plan: 18
type: execute
wave: 10
depends_on: ["04-14", "04-15", "04-16", "04-17"]
files_modified:
  - extensions/quiz-history/src/QuizHistoryBlock.jsx
  - extensions/quiz-history/src/QuizHistoryBlock.js
  - public/quiz-bundle.js
  - tests/quiz-bundle-freshness.test.ts
autonomous: false
blocked_on:
  - "Blocker 1 — William agrees to test-result upload AND it is priced"
requirements: [TEST-04]
must_haves:
  truths:
    - "A logged-in patient sees a download link per uploaded file in their assessment ledger"
    - "The committed theme bundle contains the file-upload widget and the testing_files question"
    - "Every new freshness marker was measured 0-before and >=1-after"
  artifacts:
    - path: "extensions/quiz-history/src/QuizHistoryBlock.jsx"
      provides: "Per-file download links in the patient ledger"
      contains: "files"
    - path: "public/quiz-bundle.js"
      provides: "Rebuilt bundle including the file_multi widget"
      contains: "file_multi"
  key_links:
    - from: "extensions/quiz-history/src/QuizHistoryBlock.jsx"
      to: "GET /api/me/assessment/:id/files/:fileId"
      via: "s-link with the existing ?token= convention, using the route's redirect variant"
      pattern: "/files/"
---

<objective>
⛔ **BLOCKED** — see `blocked_on`. Do not start until plan 04-10's checkpoint clears.

Two closing items for the upload track: D-05's patient-facing surface, and the second theme-bundle
rebuild.

**The `quiz-history` extension is NOT broken.** CONTEXT.md originally claimed it still read deleted
PHI metafields; that was retracted in place. Measured: `extensions/quiz-history/src/` contains zero
`metafield` references and already calls `GET /api/me/assessments` with a Bearer token. The refactor
landed in `ca3c3f4` and was hardened by `f762aaa` on 2026-05-08, and `REQUIREMENTS.md` records it as
DONE-07. **There is no refactor task here** — this plan adds file links to a working component. Its
live render is verified in plan 04-19's 10-minute check, not rebuilt.

The rebuild is the second and final one for this phase. Plan 04-09 folded in the unblocked track;
this one folds in plan 04-16's widget and questions.

Purpose: TEST-04's third retrieval surface reaching a patient, and the storefront actually receiving
the widget.
Output: extension file links, a rebuilt bundle, and measured Phase-4 upload markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md
@.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md
@extensions/quiz-history/src/QuizHistoryBlock.jsx
@app/routes/api.me.assessments.tsx
@app/routes/api.me.assessment.$id.files.$fileId.tsx
@tests/quiz-bundle-freshness.test.ts

<interfaces>
Existing per-assessment row (QuizHistoryBlock.jsx:63-76) — the template to extend:

  {assessments.map(a => (
    <s-stack key={a.id} direction="inline" gap="base" align-items="center">
      <s-text>{formatDate(a.completed_at)}</s-text>
      <s-link href={`${FLY_BASE}/api/me/assessment/${a.id}/pdf?token=${encodeURIComponent(token)}`}>
        Download PDF
      </s-link>
    </s-stack>
  ))}

This is Preact (`preact/hooks` at line 3), NOT React. Do not import from `react` here.

Plan 04-14 resolved the JSON-vs-navigation shape mismatch by making
GET /api/me/assessment/:id/files/:fileId support BOTH: `Accept: application/json` (or `?as=json`)
returns `{ url }`; anything else returns a 302 to the signed URL. So a plain `<s-link href>` works
with no fetch-then-navigate rewrite.

The freshness guard's counting helper (tests/quiz-bundle-freshness.test.ts):
  const count = (needle: string): number => SOURCE.split(needle).length - 1;
NEVER `grep -c` — the bundle is a single line, so line counting collapses every count to 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-file download links to the patient ledger</name>
  <files>extensions/quiz-history/src/QuizHistoryBlock.jsx, extensions/quiz-history/src/QuizHistoryBlock.js, app/routes/api.me.assessments.tsx</files>
  <read_first>
    - `extensions/quiz-history/src/QuizHistoryBlock.jsx` — the whole file: the Preact imports, the
      token handling, the fetch to `/api/me/assessments`, and the per-assessment row at 63-76.
    - `extensions/quiz-history/src/QuizHistoryBlock.js` — confirm whether it is a build artifact of
      the `.jsx` or a hand-maintained parallel file, and treat it accordingly. If it is generated,
      regenerate it rather than hand-editing; if hand-maintained, apply the same change to both.
    - `app/routes/api.me.assessments.tsx` — the ledger response shape, and whether it already returns
      anything per-file.
    - `app/routes/api.me.assessment.$id.files.$fileId.tsx` (plan 04-14) — the dual JSON/redirect
      response and the `?token=` support.
    - `app/lib/submission-files.ts` (04-11) — `listFilesForSubmission`.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-05 — including the retraction
      that this extension already works.
  </read_first>
  <action>
    Extend the ledger response in `app/routes/api.me.assessments.tsx` so each assessment carries a
    `files` array of `{ id, filename, sizeBytes }`, sourced from `listFilesForSubmission`. Keep the
    route's existing JWT Bearer verification and ownership scoping exactly as they are — the ledger
    is already ownership-bounded and that boundary must not move. Do not add a filename to any log
    line in this route.

    In `QuizHistoryBlock.jsx`, render one `<s-link>` per file inside the existing assessment row,
    pointing at `${FLY_BASE}/api/me/assessment/${a.id}/files/${f.id}?token=${encodeURIComponent(token)}`
    — the same `?token=` convention the PDF link already uses. Label each link with the filename.
    Render nothing extra when `files` is empty, so an assessment with no uploads looks exactly as it
    does today.

    This is Preact. Do not import from `react`. Do not restructure the component, change its data
    fetching, or alter its auth handling — it works, and the phase's job here is additive.

    Keep the filename out of every log line and out of the request path. It appears only as link text
    inside the authenticated customer-account surface.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/assessments-ledger.test.ts &amp;&amp; node -e "const s=require('fs').readFileSync('extensions/quiz-history/src/QuizHistoryBlock.jsx','utf8');const c=(n)=>s.split(n).length-1;if(c('/files/')<1)throw new Error('file link missing');if(c(\"from 'react'\")+c('from \"react\"')>0)throw new Error('react import in a preact component');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `api.me.assessments.tsx` returns a `files` array per assessment and its Bearer verification and
      ownership scoping are unchanged (`git diff` shows no change to those blocks).
    - `QuizHistoryBlock.jsx` renders one `<s-link>` per file to
      `/api/me/assessment/:id/files/:fileId?token=...` and nothing when `files` is empty.
    - The component imports only from `preact/hooks`; there is no `react` import.
    - No filename appears in any request path or query string, or in any log line.
    - The existing `tests/assessments-ledger.test.ts` passes, extended if needed for the new field.
    - `.js` and `.jsx` are consistent — state in the SUMMARY which is generated and how you kept them
      in sync.
  </acceptance_criteria>
  <done>A patient sees a download link per uploaded file in a component that already worked, with the ownership boundary and auth flow untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the theme bundle and add measured upload markers</name>
  <files>public/quiz-bundle.js, tests/quiz-bundle-freshness.test.ts</files>
  <read_first>
    - `tests/quiz-bundle-freshness.test.ts` — the whole file including plan 04-09's Phase 4 block, so
      this task extends rather than duplicates it.
    - `.planning/phases/04-mandatory-allergy-testing/04-09-SUMMARY.md` — the recorded byte size and
      hash after the first rebuild, which is this rebuild's before-value.
    - `app/lib/quiz/questions.ts` and `app/components/quiz/QuizPartRenderer.tsx` (post-plan-04-16) —
      the source of the new markers.
    - `.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md` — the counting rule and the
      same-commit rebuild requirement.
  </read_first>
  <action>
    Record the committed bundle's byte size and SHA-256 before doing anything. Run
    `npm run build:theme`, record the new size and hash, run it a second time, and confirm the hash is
    identical — determinism must be re-proved after new code enters the bundle, not inherited from
    plan 04-09.

    Measure each candidate marker against BOTH the pre-rebuild bundle (restore it from git into a
    scratch file; do not overwrite the new one) and the new bundle. A presence marker must count 0
    before and >= 1 after, or it cannot detect staleness and must be replaced.

    Candidate presence markers, all string literals that survive minification:
      `file_multi`                  (quoted QuestionType in the renderer switch)
      `testing_files`               (quoted question id in the question data)
      `Add files`                   (dropzone label)
      `No files added yet.`         (empty state)
      `Add at least one file to continue.`   (required-but-empty error)
      `Upload allergy test results` (the input's aria-label)
      `/api/quiz/upload`            (the endpoint path in the widget)

    Extend the guard with an `it()` per marker, each carrying a comment recording its measured stale
    count and fresh count, in the same style as plan 04-09's block. Extend, do not replace; do not
    weaken any existing Phase 2, Phase 3, or plan-04-09 marker.

    Add a header note recording this rebuild's date, its byte-size move, and that determinism was
    re-verified by two consecutive builds.

    Commit `public/quiz-bundle.js` and the guard extension in the SAME commit — session 32's invisible
    phase shipped because the artifact and the source diverged across commits.
  </action>
  <verify>
    <automated>npm run build:theme &amp;&amp; npx vitest run tests/quiz-bundle-freshness.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Two consecutive `npm run build:theme` runs produce byte-identical output; both hashes recorded.
    - All seven presence markers count 0 against the pre-rebuild bundle and >= 1 against the new one,
      with both figures recorded per marker.
    - Every existing marker from Phase 2, Phase 3, and plan 04-09 still passes unmodified.
    - Every count uses `split(needle).length - 1`; the SUMMARY states `grep -c` was not used.
    - `public/quiz-bundle.js` and `tests/quiz-bundle-freshness.test.ts` are in the same commit.
    - `npm test`, `npm run typecheck`, and `npm run build` all pass.
  </acceptance_criteria>
  <done>The committed artifact the storefront loads contains the upload widget and its question, with seven new markers each proven able to detect staleness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer-account extension → patient PHI files | The extension runs in Shopify's surface but the ownership check lives on the Fly route |
| repo source → storefront-served bundle | The artifact is committed, not built at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-94 | Elevation of Privilege | A file link in the extension bypassing ownership | mitigate | The link is inert; every check is server-side in `api.me.assessment.$id.files.$fileId.tsx` (plan 04-14), which verifies the Bearer token and re-proves ownership in SQL before signing |
| T-4-95 | Information Disclosure | The `?token=` query parameter appearing in referrer headers or logs | accept | Pre-existing convention already used by the PDF link since 2026-05-08; changing it is out of this phase's scope. Recorded as a Phase 8 hardening candidate alongside the `frame-ancestors` item |
| T-4-96 | Information Disclosure | Filename in a URL path or a log line | mitigate | The route takes opaque IDs only; the filename appears solely as link text inside the authenticated customer-account surface, and Task 1 forbids logging it |
| T-4-97 | Tampering | Stale committed bundle shipping the widget invisibly | mitigate | Seven presence markers each measured 0-before / >=1-after, determinism re-proved, and the artifact committed alongside the guard |
| T-4-98 | Spoofing | A vacuous freshness gate from line counting | mitigate | Every count uses `split(needle).length - 1`, asserted in the acceptance criteria |

No packages installed in this plan (plan 04-10 owns the install).
</threat_model>

<verification>
- `npm test`, `npm run typecheck`, `npm run build`, and a deterministic `npm run build:theme` all
  green; suite count above plan 04-17's.
- `git show --stat HEAD` for Task 2's commit lists both `public/quiz-bundle.js` and
  `tests/quiz-bundle-freshness.test.ts`.
- `git grep -n "from 'react'" extensions/quiz-history/src/` returns nothing.
</verification>

<success_criteria>
- All three of D-05's retrieval surfaces exist: admin, patient ledger, and inline in the clinical PDF.
- The storefront-loaded artifact contains the upload widget, proven by markers with measured
  detection power.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-18-SUMMARY.md` when done.
</output>
