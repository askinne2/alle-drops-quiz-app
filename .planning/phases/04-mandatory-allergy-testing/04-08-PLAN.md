---
phase: 04-mandatory-allergy-testing
plan: 08
type: execute
wave: 4
depends_on: ["04-03", "04-07"]
files_modified:
  - tests/quiz-testing-bypass-deletion.test.ts
  - app/components/quiz/ResultsDisplay.tsx
  - app/components/quiz/QuizContainer.tsx
autonomous: true
requirements: [TEST-05, TEST-07]
must_haves:
  truths:
    - "Every bracket takes one path: Part 7 -> consent -> submit -> results"
    - "A 0-2 patient sees ConsentStep before anything is submitted"
    - "ResultsDisplay has zero callback props and is the sole terminal screen"
    - "No surface in the quiz offers a purchase path from the results screen to a patient who has not been tested"
    - "The last quiz part's Next button says Continue, not See results"
  artifacts:
    - path: "tests/quiz-testing-bypass-deletion.test.ts"
      provides: "Source-text guard for the six D-09 deletions, proven RED first"
      contains: "split(needle).length"
    - path: "app/components/quiz/ResultsDisplay.tsx"
      provides: "Terminal results screen with data-only props"
      contains: "testingStatus"
    - path: "app/components/quiz/QuizContainer.tsx"
      provides: "Single-path flow with consent between Part 7 and results"
      contains: "consent"
  key_links:
    - from: "app/components/quiz/QuizContainer.tsx"
      to: "app/components/quiz/ConsentStep.tsx"
      via: "the only route out of the last quiz part"
      pattern: "setStep\\(\"consent\"\\)"
    - from: "app/components/quiz/ResultsDisplay.tsx"
      to: "app/lib/quiz/redirects.ts"
      via: "getRedirectUrl(\"testOptions\") on a plain anchor, intercepted by quiz-embed.tsx"
      pattern: "testOptions"
---

<objective>
Implement D-09. Today a 0–2 patient auto-submits with `consent_version` stamped in the payload while
never seeing `ConsentStep` (`QuizContainer.tsx:205-219`) — the field records consent the patient did
not give. TEST-07 is violated in production right now. This plan makes consent the single mandatory
step between Part 7 and the results screen, for every bracket.

That restructuring deletes the last no-testing bypass (TEST-05: the `3–6` "Continue to Purchase
AlleDrops" jump — Phase 3's D-11 already removed the `7+` proceed-without-testing chain), makes
`ResultsDisplay` terminal with zero callback props, and resolves for free the `symptom_profile_id`
double-submit defect recorded in `STATE.md` §Deferred Items.

Purpose: TEST-05 and TEST-07, and closing a live consent defect.
Output: a source-text deletion guard proven RED then GREEN, a data-only `ResultsDisplay`, and a
single-path `QuizContainer`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md
@app/components/quiz/QuizContainer.tsx
@app/components/quiz/ResultsDisplay.tsx
@tests/quiz-medical-history-deletion.test.ts
@app/lib/quiz/redirects.ts

<interfaces>
ResultsDisplayProps today (ResultsDisplay.tsx:5-13):
  { score, scoreBracket, patientState, symptomProfileId,
    onScheduleConsult: () => void, onProceedToPurchase: () => void, onTestFirst: () => void }

ResultsDisplayProps after this plan (04-UI-SPEC.md Component Inventory §5) — zero callbacks:
  { score, scoreBracket, patientState, symptomProfileId,
    testingStatus: "needs_testing" | "had_testing" }

QuizContainer deletion targets with current line numbers (04-PATTERNS.md):
  savedToServer state + all references .......... 142, 212, 246, 260, 280
  autoSubmit0to2Attempted ref ................... 144, 206, 208, 215
  the 0-2 auto-submit useEffect ................. 204-219
  handleScheduleConsult ......................... 241-254
  handleTestFirst ............................... 256-267
  handleProceedToPurchase ....................... 269-272
  consent Previous target setStep("outcome") .... 500
  "See results" terminal button label ........... 468
  the "completed" step block .................... 328-370 (and the "completed" FlowStep at :41)

Flow after this plan (04-UI-SPEC.md Flow Contract):
  state_gate -> patient_info -> quiz_parts (Part 7 last) -> consent -> submitting -> ResultsDisplay

Score and bracket are still COMPUTED where `goToOutcome` computes them today (on leaving Part 7,
via visibleAnswers / calculateTotalScore / getScoreBracket) — they are held in state and not
displayed until after a successful submit. Only the `setStep` target changes.

Anchor navigation: `quiz-embed.tsx:62-72` intercepts plain `<a href>` clicks and navigates the parent
storefront. `app/lib/quiz/redirects.ts` exports `REDIRECT_FALLBACK.testOptions = "/pages/test-options"`,
verified live. This is why the results CTA is an anchor, not a callback prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the deletion guard and prove it RED against pre-change source</name>
  <files>tests/quiz-testing-bypass-deletion.test.ts</files>
  <read_first>
    - `tests/quiz-medical-history-deletion.test.ts` — the whole file. This is the established
      convention: `readFileSync` the source, `const count = (source, needle) => source.split(needle).length - 1`,
      needles assembled from string fragments so the test's own prose cannot self-match, and a block
      of positive controls proving non-vacuity. Copy the scaffolding shape exactly, including the
      header comment explaining why `grep -c` is forbidden.
    - `app/components/quiz/QuizContainer.tsx` — the whole file, pre-change. You need the current
      occurrence counts to prove RED.
    - `app/components/quiz/ResultsDisplay.tsx` — the whole file, pre-change.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-09 — the deletion list.
    - `.planning/STATE.md` — the 03-03 entry recording that the Phase 3 guard was proven RED (11
      QuizContainer + 1 ResultsDisplay assertions failing) before being trusted.
  </read_first>
  <action>
    Create `tests/quiz-testing-bypass-deletion.test.ts` reading both `QuizContainer.tsx` and
    `ResultsDisplay.tsx` from disk.

    Absence assertions, count exactly 0, needles assembled from fragments:
      `autoSubmit` + `0to2Attempted`
      `handleSchedule` + `Consult`
      `handleTest` + `First`
      `handleProceedTo` + `Purchase`
      `savedTo` + `Server`
      `onSchedule` + `Consult`, `onProceedTo` + `Purchase`, `onTest` + `First`  (in BOTH files)
      `See res` + `ults`            (the stale terminal button label)
      `Continue to Purchase ` + `AlleDrops`   (the 3-6 bypass button, in ResultsDisplay)
      `We recommend proceeding with allergy testing`  (the 7+ clause deleted by D-10)
      `setStep("out` + `come")`     (the step no longer precedes consent)

    Positive controls, count >= 1, proving the read hit the real files and the assertions are not
    vacuous:
      QuizContainer: `itemsForPart`, `QUIZ_PARTS`, `quizPartsTotal`, `handleAnswerChange`,
        `setStep("consent")`, `CONSENT_VERSION`
      ResultsDisplay: `Schedule Allergy Testing`, `testingStatus`,
        `Your responses have been submitted.`

    Then RUN the guard against the unmodified source and record, in the plan's SUMMARY, the exact
    number of failing assertions and the pre-change count for every absence needle. A guard that was
    never observed RED is not evidence — this is the project's standing rule.

    Add a header comment stating the measured RED result and the date, matching the Phase 3 guard's
    convention. Note in the comment that this file's own prose deliberately contains some of these
    identifiers, which is why every needle is fragment-assembled.

    File extension `.test.ts`, never `.tsx`.
  </action>
  <verify>
    <automated>! npx vitest run tests/quiz-testing-bypass-deletion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` FAILS against unmodified source
      (the verify command above inverts the exit code, so it passes only while the guard is red).
    - The SUMMARY records the failing-assertion count and each absence needle's pre-change count;
      at least 10 absence assertions fail.
    - Every count uses `split(needle).length - 1`; the file contains zero occurrences of `grep -c`
      other than inside the explanatory header comment.
    - Every absence needle is assembled from two or more string fragments.
    - Positive controls all pass even in the RED state (they assert on code that already exists,
      except `setStep("consent")`, `testingStatus`, `Schedule Allergy Testing`, and
      `Your responses have been submitted.` which are expected to fail until Tasks 2-3 land — record
      which controls are red-by-design).
  </acceptance_criteria>
  <done>The guard exists, is measurably RED against pre-change source with recorded counts, and its needles cannot self-match.</done>
</task>

<task type="auto">
  <name>Task 2: Make ResultsDisplay terminal and update its call site</name>
  <files>app/components/quiz/ResultsDisplay.tsx, app/components/quiz/QuizContainer.tsx</files>
  <read_first>
    - `app/components/quiz/ResultsDisplay.tsx` — the whole file: the props interface (5-13), the
      three bracket-conditional `.quizResults__recommendation` blocks (57-132), and every
      `.quizResults__actions` block.
    - `app/components/quiz/QuizContainer.tsx` — lines 320-380 (the `completed` step block, whose
      "Return Home" and "Go to AlleDrops Product Page" actions fold into ResultsDisplay) and
      lines 480-495 (the `ResultsDisplay` call site passing three callbacks).
    - `app/styles/quiz.module.css` — confirm `.quizResults__subtitle` exists (approximately lines
      725-736) and is currently unused, and confirm `.quizResults__actions` is already
      `flex-direction: column`.
    - `app/lib/quiz/redirects.ts` — `getRedirectUrl` and `REDIRECT_FALLBACK.testOptions`.
    - `app/lib/quiz/product-links.ts` — `getProductHandle`, used by the product-page anchor.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` Component Inventory §5 — the
      full action-area contract, the locked copy deletions, and the compliance-motivated
      recommendation about the product link.
  </read_first>
  <action>
    Rewrite `ResultsDisplayProps` to the data-only shape: `score`, `scoreBracket`, `patientState`,
    `symptomProfileId`, `testingStatus: "needs_testing" | "had_testing"`. Delete
    `onScheduleConsult`, `onProceedToPurchase`, `onTestFirst` from the interface and every usage.

    Header: immediately under the existing `<h2>Your Assessment Results</h2>`, add
    `<p className={styles.quizResults__subtitle}>Your responses have been submitted.</p>`. Pure reuse
    of an existing unused class — add no CSS.

    Copy deletions, locked verbatim:
    - 7+ bracket: delete the sentence "We recommend proceeding with allergy testing, to identify
      specific allergens that may be causing your symptoms." The paragraph now ends at
      "...which may include prescription medications or immunotherapy."
    - 3–6 bracket: delete the entire "Continue to Purchase AlleDrops" `<button>` and its
      `onProceedToPurchase` wiring. Do NOT edit the surrounding paragraph text (D-10).
    - 0–2 bracket: message text unchanged; its action button is removed with the shared action area
      below.

    Replace all three per-bracket `.quizResults__actions` blocks with ONE shared action area,
    conditioned on `testingStatus` and independent of `scoreBracket`, rendered inside the existing
    `.quizResults__actions` container:
    - `testingStatus === "needs_testing"`: (1) `<a href={getRedirectUrl("testOptions")}>` styled
      `.quizNavigation__button .quizNavigation__buttonNext` with the LOCKED text
      "Schedule Allergy Testing"; (2) a "Return Home" action styled `.quizNavigation__buttonPrev`
      using the same `navigateParent("/")` call the `completed` block uses today; (3) NO
      "Go to AlleDrops Product Page" link.
    - `testingStatus === "had_testing"`: (1) "Return Home" styled `.quizNavigation__buttonNext`;
      (2) "Go to AlleDrops Product Page" styled `.quizNavigation__buttonPrev`, using the same
      `href={\`/products/${getProductHandle(...)}\`}` shape as today's `completed` block.

    Omitting the product link on the `needs_testing` branch is a PLANNER-RATIFIED decision, not a
    CONTEXT.md lock: 04-UI-SPEC.md flagged it for ratification, and it directly serves TEST-06's
    "no surface... offers or implies a path to purchase without testing." Record it in a code comment
    citing TEST-06 and 04-UI-SPEC.md §5, and surface it for override at plan 04-19's UAT checkpoint.

    Every action is a plain `<a>` or a `navigateParent` call — no callback prop may be reintroduced.
    That is what "terminal" means here.

    At the `QuizContainer.tsx` call site only: stop passing the three callbacks and pass
    `testingStatus={answers.testing_status === "had_testing" ? "had_testing" : "needs_testing"}`.
    Leave the now-unused handler definitions in place for Task 3 to delete — this keeps typecheck
    green at this task's commit boundary.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `ResultsDisplayProps` contains zero properties whose type is a function.
    - `app/components/quiz/ResultsDisplay.tsx` contains zero occurrences of `onScheduleConsult`,
      `onProceedToPurchase`, `onTestFirst`, `Continue to Purchase AlleDrops`, and
      `We recommend proceeding with allergy testing`.
    - It contains exactly one occurrence of `Schedule Allergy Testing` and exactly one of
      `Your responses have been submitted.`
    - `getProductHandle` is referenced only inside the `had_testing` branch.
    - `git diff app/styles/quiz.module.css` is empty — no CSS was added.
    - `npm run typecheck` exits 0 and `npx vitest run` shows only
      `tests/quiz-testing-bypass-deletion.test.ts` still failing.
  </acceptance_criteria>
  <done>ResultsDisplay takes data only, renders one testingStatus-conditioned action area, drops both deleted copy items, and hides the product link from untested patients.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire QuizContainer to the single consent-first path and take the guard GREEN</name>
  <files>app/components/quiz/QuizContainer.tsx</files>
  <read_first>
    - `app/components/quiz/QuizContainer.tsx` — the whole file, again, post-Task-2. Every deletion
      target is listed in this plan's `<interfaces>` block with current line numbers.
    - `app/components/quiz/ConsentStep.tsx` — its props, so the Previous-button rewiring targets the
      right handler.
    - `tests/quiz-testing-bypass-deletion.test.ts` (from Task 1) — the exact needles that must reach
      zero and the positive controls that must stay >= 1.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §"Flow Contract" and
      §"Required copy fix" — the new step sequence and the "See results" -> "Continue" fix.
    - `.planning/STATE.md` §Deferred Items — the `symptom_profile_id` double-submit entry, which this
      rewiring resolves for free; confirm in the SUMMARY that it is now unreachable.
  </read_first>
  <action>
    Delete, per D-09:
    - the `savedToServer` state and every reference to it
    - the `autoSubmit0to2Attempted` ref and every reference to it
    - the entire 0–2 auto-submit `useEffect`
    - `handleScheduleConsult`, `handleTestFirst`, `handleProceedToPurchase`
    - the `"completed"` step block and `"completed"` from the step union — `ResultsDisplay` is now
      the sole terminal screen and absorbed that block's two actions in Task 2

    Rewire:
    - `goToOutcome` keeps its computation exactly as-is (`visibleAnswers`, `calculateTotalScore`,
      `getScoreBracket`, holding score and bracket in state) and changes only its `setStep` target
      from `"outcome"` to `"consent"`. Rename it to reflect the new target if that reads better, but
      do not change what it computes or when.
    - Decide whether the `"outcome"` step member survives at all. If nothing routes to it after
      these deletions, remove it from the step union — a dead `FlowStep` is exactly how the
      `entry.theme.tsx` open redirect survived two independent reviews. State the decision and the
      reason in the SUMMARY.
    - The consent step's Previous button target changes from `setStep("outcome")` to re-entering the
      quiz at the last part: set the part index to `quizPartsTotal - 1` and the step to
      `"quiz_parts"`.
    - Submit flow: consent -> `"submitting"` -> on success `setStep` to the results screen; on
      failure the existing `"error"` step, unchanged. `consent_version` must still be stamped on the
      payload exactly as it is today — the point of D-09 is that the patient now actually saw the
      document, not that the field changes.
    - Terminal-part button label: change `"See results"` to `"Continue"` on the
      `currentPartIndex === quizPartsTotal - 1` branch. The button one part earlier still reads
      `"Next →"`. This is a required accuracy fix, not a style preference: under the new flow that
      click leads to consent, not results.

    Must NOT change (these are the guard's positive controls): `itemsForPart` usage, `QUIZ_PARTS`
    reads, the `quizPartsTotal` computation, `handleAnswerChange`'s no-special-case behavior, and
    the `TOTAL_FLOW_STEPS` / "Part N of {quizPartsTotal}" progress derivation — Part 7 makes that
    read "Part 7 of 7" automatically with no edit.

    Do not add any question-ID literal. `answers.testing_status` is read once, at the
    `ResultsDisplay` call site established in Task 2.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/quiz-testing-bypass-deletion.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` passes, including every positive
      control that was red-by-design in Task 1.
    - `app/components/quiz/QuizContainer.tsx` contains zero occurrences of `savedToServer`,
      `autoSubmit0to2Attempted`, `handleScheduleConsult`, `handleTestFirst`,
      `handleProceedToPurchase`, `See results`, and `setStep("outcome")`.
    - It contains at least one occurrence of `setStep("consent")` and still contains `itemsForPart`,
      `QUIZ_PARTS`, `quizPartsTotal`, and `handleAnswerChange`.
    - There is exactly one code path from the last quiz part forward, and it passes through the
      consent step — state in the SUMMARY how you verified this (enumerate every `setStep` call and
      its reachable predecessors).
    - The `"completed"` step is gone from both the union and the render tree.
    - `npm run typecheck` exits 0; `npm test` full suite green with a higher count than plan 04-07.
    - The SUMMARY states explicitly that the `symptom_profile_id` double-submit path recorded in
      STATE.md is now unreachable, and how that was verified.
  </acceptance_criteria>
  <done>One path leads from Part 7 to the results screen and it runs through consent; all six D-09 deletions are gone and the guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient consent action → stored `consent_version` | Today the field is stamped on a path where the document was never rendered |
| quiz results screen → storefront purchase | The results screen has been a routing hub with three exits; two of them bypassed testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-27 | Repudiation | 0–2 auto-submit stamping `consent_version` without rendering `ConsentStep` | mitigate | Task 3 deletes the auto-submit `useEffect` and routes every bracket through `setStep("consent")`; Task 1's guard asserts `autoSubmit0to2Attempted` reaches zero occurrences. This is a LIVE defect being closed, not a hypothetical |
| T-4-28 | Elevation of Privilege | `3–6` "Continue to Purchase AlleDrops" jump reaching purchase without testing | mitigate | Task 2 deletes the button and its wiring; the guard asserts the string count is zero in `ResultsDisplay.tsx` |
| T-4-29 | Elevation of Privilege | Product-page link shown to a `needs_testing` patient | mitigate | Task 2's shared action area omits it on that branch, per TEST-06's intent. Planner-ratified, flagged for override at 04-19 |
| T-4-30 | Tampering | Double submit violating `submissions.symptom_profile_id` NOT NULL UNIQUE | mitigate | The single-path rewiring removes the back-navigate-and-resubmit route entirely; Task 3 requires an explicit reachability statement in the SUMMARY rather than an assumption |
| T-4-31 | Tampering | A surviving dead `"outcome"` FlowStep | mitigate | Task 3 requires an explicit keep/remove decision with reasoning. Dead FlowSteps are how the `entry.theme.tsx` open redirect survived two reviews |
| T-4-32 | Spoofing | Stale "See results" label promising the wrong destination | mitigate | Task 3 changes it to "Continue"; the guard asserts `See results` reaches zero occurrences |

No packages installed in this plan.
</threat_model>

<verification>
- `npm run typecheck` clean; `npm test` full suite green with a higher count than plan 04-07.
- `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` green, having been observed RED in
  Task 1 with recorded counts.
- `git diff app/styles/quiz.module.css` empty.
- `public/quiz-bundle.js` is deliberately NOT rebuilt here — plan 04-09 owns the single rebuild for
  the whole unblocked track and folds in plans 04-02, 04-03, 04-06, 04-07 and this one.
</verification>

<success_criteria>
- Every completion path renders `ConsentStep` before anything is POSTed.
- `ResultsDisplay` has zero function-typed props and is the only terminal screen.
- No quiz surface offers a purchase route to a patient who selected `needs_testing`.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-08-SUMMARY.md` when done.
</output>
