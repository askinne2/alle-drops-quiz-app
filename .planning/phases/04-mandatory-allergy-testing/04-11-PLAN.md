---
phase: 04-mandatory-allergy-testing
plan: 11
type: execute
wave: 7
depends_on: ["04-10"]
files_modified:
  - migrations/004_create_submission_files.sql
  - app/lib/submission-files.ts
  - tests/submission-files.test.ts
autonomous: false
blocked_on:
  - "Blocker 1 — William agrees to test-result upload AND it is priced"
  - "Cloud SQL DDL requires a named on-demand backup and a human-confirmed row-count precondition"
requirements: [TEST-04]
must_haves:
  truths:
    - "A file row can only be reached by joining to a submissions row the caller is entitled to see"
    - "submission_files can be inserted into and selected from, and nothing else"
    - "N file rows for one submission insert atomically or not at all"
    - "The migration is its own commit and no DDL runs until the app code is proven live"
  artifacts:
    - path: "migrations/004_create_submission_files.sql"
      provides: "submission_files join table, insert-only grants, access-log action widening"
      contains: "GRANT SELECT, INSERT"
    - path: "app/lib/submission-files.ts"
      provides: "insertSubmissionFiles, getSubmissionFileForCustomer, getSubmissionFileForAdmin, listFilesForSubmission"
      exports: ["insertSubmissionFiles", "getSubmissionFileForCustomer", "getSubmissionFileForAdmin", "listFilesForSubmission"]
  key_links:
    - from: "app/lib/submission-files.ts"
      to: "submissions table"
      via: "every customer-facing query joins through submissions and constrains by customer_id_shopify OR patient_email"
      pattern: "customer_id_shopify"
---

<objective>
⛔ **BLOCKED** — see `blocked_on`. Do not start until plan 04-10's checkpoint clears.

Create the `submission_files` join table and its data-access layer.

D-08 keeps `submissions` insert-only and this plan does not touch that. `submission_files` is a
SECOND, separate, insert-only table — 04-RESEARCH.md Pitfall 2 is explicit that this does not violate
D-08. It also does not go in `answers_json`: a file cannot live in a JSON blob, and stuffing tokens
there would lose queryability for the admin and patient surfaces. This is D-03's one legitimate
exception to the "extend `answers_json`, never the top-level schema" rule, and it is justified by
shape, not by convenience.

The migration ships as its own commit (Phase 3 D-01, non-negotiable) and **no DDL executes in this
plan**. Plan 04-19 runs it, after the app code is independently proven live on Fly — the same
ordering Phase 3 used for `003_drop_medical_history_legacy_columns.sql`.

Purpose: TEST-04's persistence layer, and the ownership boundary every retrieval surface depends on.
Output: an additive migration file, four ownership-aware data-access functions, and integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md
@.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md
@.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md
@CLAUDE.md
@app/lib/submissions.ts
@app/lib/db.ts
@migrations/002_create_submission_access_log.sql
@migrations/003_drop_medical_history_legacy_columns.sql

<interfaces>
Ownership-bounded query pattern to replicate EXACTLY (app/lib/submissions.ts:150-174). This is a
HIPAA requirement, not a style preference:

  export async function getSubmissionByIdForCustomer(args: {
    id: string; customer_id_shopify?: string | null; email?: string | null;
  }): Promise<SubmissionFullRow | null> {
    const pool = getPool();
    const sql = `
      SELECT * FROM submissions
       WHERE id = $1
         AND ( ($2::text IS NOT NULL AND customer_id_shopify = $2)
            OR ($3::text IS NOT NULL AND patient_email = $3) )
       LIMIT 1`;
    const result = await pool.query<SubmissionFullRow>(sql, [
      args.id, args.customer_id_shopify ?? null, args.email ?? null ]);
    return result.rows[0] ?? null;
  }

Admin counterpart (submissions.ts:251-261) — no ownership filter, because Shopify session auth is
the gate: `SELECT * FROM submissions WHERE id = $1 LIMIT 1`.

Access log (submissions.ts:264-279): logSubmissionAccess({ submission_id, actor_shop, action }),
action currently constrained to 'list' | 'detail' | 'pdf'.

Child-table migration pattern (migrations/002_create_submission_access_log.sql): UUID PK with
gen_random_uuid(), submission_id UUID REFERENCES submissions(id), TIMESTAMPTZ NOT NULL DEFAULT now(),
two CREATE INDEX IF NOT EXISTS lines, then explicit GRANTs to alledrops_app.

Constants ratified in 04-UPLOAD-DECISIONS.md: GCS_PENDING_PREFIX, GCS_PERMANENT_PREFIX,
MAX_FILE_BYTES, MAX_TOTAL_BYTES, MAX_FILES.

NOTE: app/lib/submissions.ts today calls pool.query directly everywhere; it has NO client-level
transaction precedent. This plan introduces pool.connect() + BEGIN/COMMIT/ROLLBACK for the first time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 004 (own commit, no DDL executed)</name>
  <files>migrations/004_create_submission_files.sql</files>
  <read_first>
    - `migrations/002_create_submission_access_log.sql` — the full child-table pattern: UUID PK,
      FK to `submissions(id)`, `TIMESTAMPTZ NOT NULL DEFAULT now()`, indexes, and explicit GRANTs.
    - `migrations/001_create_submissions.sql` — the "Identity (PHI)" column-commenting convention,
      and note that line 47 grants `UPDATE` on `submissions` even though nothing uses it. Do NOT
      copy that unused grant forward.
    - `migrations/003_drop_medical_history_legacy_columns.sql` header (lines 1-29) — the
      migration-safety commentary convention: backup taken first with ID/timestamp/status read back
      rather than trusted from an exit code, deploy-before-migrate ordering, and a pre-migration row
      count to diff against.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` §`<specifics>` — "The
      `submissions` table is test data only... If a real patient has completed the quiz by the time
      Phase 4 runs, any migration decision must stop and be re-raised."
    - `CLAUDE.md` compliance rule 5 (as amended by plan 04-01 to include uploaded filenames).
  </read_first>
  <action>
    Create `migrations/004_create_submission_files.sql`. Additive only — `CREATE TABLE IF NOT EXISTS`
    plus indexes, grants, and one `ALTER TABLE` on the access log's CHECK constraint.

    Header comment must carry, in the 003 style: that this migration is additive and non-destructive;
    that DDL must not run until the app code that reads and writes the table is independently proven
    live on Fly (plan 04-19 executes it); that a named on-demand Cloud SQL backup must be taken and
    its ID/status read back via `gcloud sql backups list`/`describe` before execution, never trusted
    from an exit code; and that a pre-migration `SELECT COUNT(*) FROM submissions` must be recorded
    and re-checked afterwards. Add the CONTEXT.md `<specifics>` warning verbatim: if a real patient
    has completed the quiz by execution time, stop and re-raise.

    Table `submission_files`:
      id                     UUID        PRIMARY KEY DEFAULT gen_random_uuid()
      submission_id          UUID        NOT NULL REFERENCES submissions(id)
      storage_object_key     TEXT        NOT NULL     -- permanent GCS key under submissions/
      original_filename      TEXT        NOT NULL     -- ⚠ PHI: a filename can carry a patient name
      content_type           TEXT        NOT NULL     -- effective type AFTER any HEIC conversion
      original_content_type  TEXT        NOT NULL     -- sniffed type BEFORE conversion (audit trail)
      size_bytes             INTEGER     NOT NULL
      uploaded_at            TIMESTAMPTZ NOT NULL DEFAULT now()

    `submission_id` is `NOT NULL` here, unlike the access log's nullable FK — every file row belongs
    to exactly one submission and there is no "list action" analog. Comment `original_filename` under
    a PHI heading, matching `001`'s "Identity (PHI)" convention, and note that it must never be
    logged.

    Indexes: `idx_submission_files_submission ON submission_files(submission_id)` and
    `idx_submission_files_uploaded ON submission_files(uploaded_at DESC)`, both
    `CREATE INDEX IF NOT EXISTS`.

    Grants: `GRANT SELECT, INSERT ON submission_files TO alledrops_app;` — **no UPDATE, no DELETE**.
    D-08's insert-only posture is stricter here than `001` was for `submissions`, deliberately. Add
    `GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO alledrops_app;` matching `002`.

    Also widen the access log so file downloads are auditable: drop and re-add
    `submission_access_log`'s `action` CHECK constraint to include `'file'` alongside `'list'`,
    `'detail'`, `'pdf'`. Do this inside `004` rather than a separate `005` — it is the same logical
    change and keeps the migration-per-commit rule to one file.

    **Execute no SQL.** This task produces a file. Commit it alone, touching no other file, per
    Phase 3 D-01.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('migrations/004_create_submission_files.sql','utf8');const c=(n)=>s.split(n).length-1;const req=['CREATE TABLE IF NOT EXISTS submission_files','submission_id  UUID','REFERENCES submissions(id)','original_filename','original_content_type','GRANT SELECT, INSERT ON submission_files','CREATE INDEX IF NOT EXISTS idx_submission_files_submission','ALTER TABLE submission_access_log',\"'file'\",'backup','COUNT(*)'];for(const r of req){if(c(r)<1)throw new Error('migration missing: '+r)}if(/GRANT[^;]*UPDATE[^;]*submission_files/i.test(s))throw new Error('UPDATE granted on submission_files');if(/GRANT[^;]*DELETE[^;]*submission_files/i.test(s))throw new Error('DELETE granted on submission_files');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/004_create_submission_files.sql` exists with all eight columns, both indexes, and
      the access-log CHECK widening including `'file'`.
    - `submission_id` is `NOT NULL` and references `submissions(id)`.
    - The grants for `submission_files` are exactly `SELECT, INSERT` — no `UPDATE`, no `DELETE`.
    - `original_filename` carries a PHI comment and a never-log instruction.
    - The header names the required backup, the deploy-before-migrate ordering, the row-count
      precondition, and the "test data only / stop if a real patient has submitted" warning.
    - No SQL was executed: `gcloud sql` was not invoked, and the SUMMARY says so.
    - `git show --stat HEAD` for this task's commit lists exactly one file.
  </acceptance_criteria>
  <done>An additive, insert-only migration exists as its own commit, with execution preconditions documented in its own header and no DDL run.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the ownership-bounded data-access layer</name>
  <files>app/lib/submission-files.ts</files>
  <read_first>
    - `app/lib/submissions.ts` — the whole file. Specifically `insertSubmission` (55-102) for the
      parameterized-INSERT-with-RETURNING convention, `getSubmissionByIdForCustomer` (150-174) for
      the ownership join, `getSubmissionByIdForAdmin` (251-261), and `logSubmissionAccess` (264-279).
    - `app/lib/db.ts` — `getPool()`, its lazy-init singleton shape and its `max: 5` pool size, which
      constrains how long a transaction may hold a client.
    - `migrations/004_create_submission_files.sql` (Task 1) — the exact column names and types.
    - `.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md` §`app/lib/submission-files.ts`
      and §"Shared Patterns / Ownership-bounded queries".
    - `.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md` Pitfall 3 — the partial-failure
      analysis behind the transaction requirement.
    - `CLAUDE.md` §"Self-review checklist for PHI-handling changes".
  </read_first>
  <action>
    Create `app/lib/submission-files.ts` exporting a `SubmissionFileRow` type matching the migration's
    columns and exactly four functions:

    1. `insertSubmissionFiles(submissionId: string, files: NewSubmissionFile[]): Promise<SubmissionFileRow[]>`
       — inserts N rows for one submission ATOMICALLY. This is the codebase's first client-level
       transaction: `const client = await getPool().connect()`, then `BEGIN`, the parameterized
       inserts, `COMMIT`, with `ROLLBACK` in the catch and `client.release()` in a `finally`. Use a
       single multi-row `INSERT ... VALUES ($1,$2,...),($n,...) RETURNING *` or a loop inside the
       transaction; either is acceptable, but the release must be unconditional. Explicit column list,
       parameterized placeholders, `RETURNING` clause — never string-concatenated SQL.

    2. `getSubmissionFileForCustomer(args: { submissionId: string; fileId: string;
       customer_id_shopify?: string | null; email?: string | null }): Promise<SubmissionFileRow | null>`
       — **must JOIN through `submissions`** and constrain by `customer_id_shopify` OR
       `patient_email`, exactly like `getSubmissionByIdForCustomer`. A bare
       `SELECT * FROM submission_files WHERE id = $1` is forbidden: ownership is proven by joining to
       a `submissions` row the caller is entitled to see, never by the file id alone. Also constrain
       `submission_files.submission_id = $submissionId` so a valid file id from a different
       submission cannot be paired with a submission the caller does own.

    3. `getSubmissionFileForAdmin(submissionId: string, fileId: string): Promise<SubmissionFileRow | null>`
       — no ownership filter (Shopify session auth is the gate at the route), but still constrained
       by `submission_id` so an admin cannot accidentally fetch a file under the wrong submission id.

    4. `listFilesForSubmission(submissionId: string): Promise<SubmissionFileRow[]>` — used by
       `app/lib/pdf.ts` for embedding. Ordered by `uploaded_at ASC` so PDF page order is stable
       across regenerations.

    Logging discipline for the whole file: log IDs and counts only. Never log `original_filename`,
    never log a row object, never log a `storage_object_key` alongside a submission id in a way that
    reconstructs a patient's file inventory in plaintext logs. Follow
    `api.quiz.submit.tsx:188-193`'s shape (`[submission-files] ...` prefix, ids and counts).

    Do NOT add an update or delete function. Do NOT modify `app/lib/submissions.ts` — the promotion
    caller wires these two modules together in plan 04-17, keeping `insertSubmission` untouched.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; node -e "const s=require('fs').readFileSync('app/lib/submission-files.ts','utf8');const c=(n)=>s.split(n).length-1;for(const k of ['insertSubmissionFiles','getSubmissionFileForCustomer','getSubmissionFileForAdmin','listFilesForSubmission','customer_id_shopify','patient_email','BEGIN','ROLLBACK','client.release']){if(c(k)<1)throw new Error('missing: '+k)}if(/UPDATE\s+submission_files/i.test(s))throw new Error('UPDATE present');if(/DELETE\s+FROM\s+submission_files/i.test(s))throw new Error('DELETE present');if(c('original_filename')>0 &amp;&amp; /console\.(log|error)[^\n]*original_filename/.test(s))throw new Error('filename logged');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All four functions are exported with the signatures above.
    - `getSubmissionFileForCustomer`'s SQL contains a join or subquery against `submissions` AND both
      `customer_id_shopify` and `patient_email` predicates; it contains no bare
      `WHERE id = $1` on `submission_files` alone.
    - Both single-file getters constrain `submission_files.submission_id`.
    - `insertSubmissionFiles` uses `pool.connect()` with `BEGIN`, `COMMIT`, `ROLLBACK`, and an
      unconditional `client.release()` in a `finally`.
    - The file contains no `UPDATE submission_files` and no `DELETE FROM submission_files`.
    - No `console.log`/`console.error` call in the file references `original_filename` or a full row
      object.
    - `listFilesForSubmission` orders by `uploaded_at ASC`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Four data-access functions exist; every customer-facing read proves ownership by joining through submissions, the multi-row insert is transactional, and nothing can update or delete a file row.</done>
</task>

<task type="auto">
  <name>Task 3: Integration-test insert and ownership-bounded retrieval</name>
  <files>tests/submission-files.test.ts</files>
  <read_first>
    - `tests/assessments-ledger.test.ts` — the whole file. It is the mocking-pattern donor: `vi.mock`
      on the lib layer, real module under test imported after the mocks, declarative status/shape
      assertions, and its ownership-scoping assertions (a customer-scoped query must not return
      another customer's rows).
    - `tests/pdf.test.ts` — confirm its mocking style before writing, so this file stays consistent
      with how the repo tests lib modules directly rather than through a route.
    - `app/lib/submission-files.ts` (Task 2) and `app/lib/db.ts` — the `getPool()` seam to mock.
    - `.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md` §"Wave 0 Requirements" — the
      `tests/submission-files.test.ts` entry.
  </read_first>
  <action>
    Create `tests/submission-files.test.ts`. Mock `app/lib/db`'s `getPool` with a fake pool exposing
    `query` and `connect` — the repo's existing route tests all mock their lib dependencies directly
    rather than spinning up a live service, so follow that, not a real Postgres.

    Cover:

    1. `insertSubmissionFiles` issues `BEGIN`, then the insert(s), then `COMMIT`, and calls
       `client.release()` exactly once on the success path. Assert on the ordered sequence of
       `client.query` calls.
    2. `insertSubmissionFiles` issues `ROLLBACK` and still calls `client.release()` exactly once when
       an insert rejects, and the rejection propagates to the caller (it must not be swallowed).
    3. `insertSubmissionFiles` never issues a statement containing `UPDATE` or `DELETE`.
    4. `getSubmissionFileForCustomer` — assert on the SQL text actually passed to `query`: it
       references `submissions`, contains both `customer_id_shopify` and `patient_email`, and
       contains `submission_id`. Assert the parameter array carries the customer id and email in the
       positions the SQL expects. This is the ownership boundary; assert its shape, not just its
       return value.
    5. `getSubmissionFileForCustomer` returns `null` when the fake pool returns zero rows — the
       "wrong customer" case — and the caller therefore cannot distinguish "not yours" from "does not
       exist". Assert that no branch throws or returns a distinguishing error.
    6. `getSubmissionFileForAdmin` issues SQL WITHOUT `customer_id_shopify`/`patient_email` but WITH
       `submission_id`.
    7. `listFilesForSubmission` issues SQL containing `ORDER BY uploaded_at ASC`.
    8. Non-vacuity control: assert at least one test would fail if the ownership predicates were
       removed — implement this by asserting the exact predicate substrings rather than a boolean, so
       deleting them breaks the assertion directly. State in a comment how this was confirmed.

    Keep the file at `.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/submission-files.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All eight cases above exist and pass.
    - The ownership tests assert on the SQL text passed to `query`, not only on the returned value.
    - The rollback test asserts `client.release()` was called exactly once and that the error
      propagated.
    - The "wrong customer" case asserts a plain `null` with no distinguishing error message.
    - `npm test` full suite green; total count strictly greater than plan 04-10's recorded count.
    - The SUMMARY records that the ownership assertions were confirmed to fail when the predicates
      are removed (state how this was checked).
  </acceptance_criteria>
  <done>Insert atomicity, rollback-and-release, and the ownership boundary are all asserted at the SQL level, with the ownership assertions proven non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied fileId → PHI file row | A file id alone must never be sufficient to reach a file |
| GCS object → database row | Files exist in storage before the row exists; the linkage step can partially fail |
| database role → PHI table | The app role's grants are the last line of defence against a mutation path nobody intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-43 | Elevation of Privilege | IDOR on `submission_files.id` | mitigate | `getSubmissionFileForCustomer` joins through `submissions` and constrains by `customer_id_shopify` OR `patient_email` AND `submission_id`; Task 3 asserts those predicates in the SQL text, not just the result |
| T-4-44 | Information Disclosure | Error responses distinguishing "not yours" from "does not exist" | mitigate | Both getters return plain `null`; Task 3 case 5 asserts no distinguishing error. Routes turn that into an undifferentiated 404 in plan 04-14 |
| T-4-45 | Tampering | An UPDATE or DELETE path appearing on PHI file metadata | mitigate | Migration grants `SELECT, INSERT` only — no `UPDATE`, no `DELETE` — and Task 2's acceptance criteria assert neither statement exists in the module. Defence at both the role and the code layer |
| T-4-46 | Tampering | Partial linkage after a mid-promotion crash (RESEARCH Pitfall 3) | mitigate | `insertSubmissionFiles` is transactional; Task 3 cases 1–2 assert BEGIN/COMMIT and ROLLBACK-with-release. GCS copy failures are handled separately in plan 04-17 |
| T-4-47 | Denial of Service | A leaked pool client exhausting the `max: 5` pool | mitigate | `client.release()` in an unconditional `finally`; asserted on both success and failure paths |
| T-4-48 | Information Disclosure | `original_filename` reaching application logs | mitigate | Column carries a PHI comment; Task 2 forbids logging it and the verify script greps for the pattern; CLAUDE.md rule 5 was amended in plan 04-01 to name it |
| T-4-49 | Tampering | DDL executed against Cloud SQL before the app code is live | mitigate | No DDL runs in this plan. Migration header requires a named on-demand backup read back via `gcloud sql backups describe`, a pre-migration row count, and deploy-before-migrate ordering; plan 04-19 executes it |

No packages installed in this plan (plan 04-10 owns the install).
</threat_model>

<verification>
- `npm run typecheck` clean; `npm test` full suite green with a higher count than plan 04-10.
- `migrations/004_create_submission_files.sql` committed alone in its own commit.
- No `gcloud sql` command was executed; the SUMMARY states so explicitly.
- `git grep -n "UPDATE submission_files\|DELETE FROM submission_files"` returns nothing.
</verification>

<success_criteria>
- No code path can read a patient's file without first proving, in SQL, that the caller owns the
  submission it belongs to.
- No code path and no database grant can mutate or delete a file row.
- N file rows for one submission land atomically or not at all.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-11-SUMMARY.md` when done.
</output>
