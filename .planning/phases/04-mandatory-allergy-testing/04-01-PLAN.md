---
phase: 04-mandatory-allergy-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "Anyone reading DEC-testing-results-by-email-not-upload in PROJECT.md sees the D-01 retraction in place, not the original decision alone"
    - "REQUIREMENTS.md TEST-04 describes a required multi-file upload, not an email instruction"
    - "CLAUDE.md's PHI field enumeration names uploaded filenames"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Retracted-in-place DEC-testing-results-by-email-not-upload"
      contains: "RETRACTED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Rewritten TEST-04"
      contains: "TEST-04"
    - path: "CLAUDE.md"
      provides: "PHI field list including uploaded filenames"
      contains: "filename"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md D-01"
      via: "retraction cross-reference"
      pattern: "D-01"
---

<objective>
Reconcile the three source-of-truth documents that D-01 invalidated, before any Phase 4 code is
written. `DEC-testing-results-by-email-not-upload` is still marked LOCKED in `PROJECT.md`, TEST-04
in `REQUIREMENTS.md` still says "no file input, multipart parsing, object storage, or upload column",
and `CLAUDE.md` rule 5's PHI field enumeration does not name uploaded filenames — which is the exact
gap RESEARCH.md Pitfall 5 says will get a filename logged during debugging.

Purpose: every later plan in this phase contradicts these three documents. Leaving them stale means
a future reader (or a future Claude) resolves the conflict the wrong way.
Output: three updated documents, each using this project's established retract-in-place convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retract DEC-testing-results-by-email-not-upload in place</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` — the `<decisions>` block; `DEC-testing-results-by-email-not-upload`
      is at line 229 with `status="LOCKED" date="2026-07-29"`.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` §`<decisions>` D-01 — the
      reversing decision and its exact required wording ("Retract ... **in place**").
    - `.planning/STATE.md` — the `injectIframe` and Apntly entries, which are this project's
      canonical examples of the retract-in-place convention (original text struck through with
      `~~...~~`, correction stated immediately below, original never deleted).
  </read_first>
  <action>
    Edit the `<decision id="DEC-testing-results-by-email-not-upload">` element in place. Do NOT
    delete it and do NOT delete its original body text.

    Change the opening tag's `status` attribute from `LOCKED` to `RETRACTED`, and add
    `retracted="2026-08-09"` and `retracted_by="04-CONTEXT.md D-01"` attributes on the same tag.

    Wrap the existing body text (the three paragraphs beginning "File upload for allergy test
    results is dropped." and ending "...depends on the unresolved domain-spelling question.") in
    `~~` strikethrough markers so the original wording stays readable but visibly superseded.

    Immediately after the struck-through body and before `</decision>`, add a retraction block that
    states, in this order: (a) that Andrew reversed this on 2026-08-09 in the Phase 4 discussion;
    (b) that upload is now IN Phase 4 and required on the `had_testing` branch per D-01 and D-02;
    (c) that the reversal re-adds the 3–4 day estimate this decision had removed; (d) that the
    reversal introduced three client-side blockers — William's agreement plus pricing, the Fly.io
    BAA, and the AOD GCP cutover; (e) that the `testing@alledrops.com` email address disappears from
    the copy entirely, so the unresolved domain-spelling question no longer gates Phase 4 (it still
    gates LAUNCH-07); (f) a pointer to
    `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` §`<decisions>` D-01 as the source.

    Do not touch `DEC-mandatory-allergy-testing`, `DEC-purchase-gating-is-honor-system`,
    `DEC-no-approval-promise-copy`, `DEC-derive-max-score-from-question-set`, or
    `DEC-medical-history-before-testing-split`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf8');const c=(n)=>s.split(n).length-1;const ok=c('DEC-testing-results-by-email-not-upload')>=1 && c('status=\"RETRACTED\"')>=1 && c('retracted_by=\"04-CONTEXT.md D-01\"')===1 && c('it\\'s fine if')===1 && c('status=\"LOCKED\" date=\"2026-07-29\" source=\"docs/REQUIREMENTS-AND-GAPS-2026-07-29.md (R5)\"')===0;if(!ok)process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `status="RETRACTED"` on the
      `DEC-testing-results-by-email-not-upload` element and no longer contains `status="LOCKED"` on
      that element.
    - The original sentence fragment `it's fine if` (William's quoted words) still appears exactly
      once — the original body was struck through, not deleted.
    - The retraction text names all three blockers by owner: `William`, `Fly.io BAA`, `GCP`.
    - The retraction text contains the literal string `04-CONTEXT.md D-01`.
    - No other `<decision id=...>` element's `status` attribute changed (diff review).
  </acceptance_criteria>
  <done>PROJECT.md's email-not-upload decision reads as retracted-in-place with the reversal, its cost, and its three blockers stated, and the original wording still visible.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md TEST-04 from email-only to required upload</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 89–110 — the `### Mandatory Allergy Testing` block holding
      TEST-01…TEST-07, and line 239–245's traceability table.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-01, D-02, D-03, D-04, D-05.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §Copywriting Contract — the
      error-state strings the requirement's acceptance depends on.
  </read_first>
  <action>
    Replace TEST-04's body. New text must state: the `had_testing` branch requires the patient to
    upload at least one copy of their allergy test results before they can continue (D-02, no
    optional-with-email-fallback); the allowlist is PDF, JPEG, PNG, and HEIC with multiple files per
    submission supported (D-03); uploaded files never touch Shopify and never leave the BAA chain
    (D-04); and files are retrievable from three surfaces — the embedded admin at `/app/quiz-results`,
    the patient ledger via `/api/me/*`, and inline in the clinical PDF (D-05). Keep the intel slug in
    the same position but change it from `REQ-testing-results-by-email` to
    `REQ-testing-results-upload`, and delete the trailing "⚠ email address depends on the domain-spelling
    decision" clause — D-01 removed that dependency.

    Add one line immediately under the rewritten TEST-04, in the same retract-in-place convention as
    Task 1: a `~~`-struck copy of the original TEST-04 sentence plus a one-line note that it was
    reversed by 04-CONTEXT.md D-01 on 2026-08-09, so the traceability back to
    `docs/REQUIREMENTS-AND-GAPS-2026-07-29.md` R5 is not silently lost.

    Also amend TEST-05's body to record that half of it already shipped in Phase 3: Phase 3's D-11
    deleted the `7+` proceed-without-testing chain and the `"medical_history"` FlowStep. The
    remaining Phase 4 work under TEST-05 is the `3–6` "Continue to Purchase AlleDrops" jump and
    making `ResultsDisplay` terminal with zero callback props. Do not remove the "**must land AFTER
    HIST-05**" note.

    Leave TEST-01, TEST-02, TEST-03, TEST-06, TEST-07 and the traceability table rows unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const c=(n)=>s.split(n).length-1;const ok=c('REQ-testing-results-upload')===1 && c('with no file input')===1 && c('testing@alledrops.com')>=1 && c('REQ-testing-results-by-email')<=1 && c('HEIC')>=1 && c('Phase 3')>=1;if(!ok)process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - TEST-04's active (non-struck) text contains `PDF`, `JPEG`, `PNG`, and `HEIC`.
    - TEST-04's active text contains the slug `REQ-testing-results-upload`.
    - TEST-04's active text does NOT contain the phrase `no file input`; that phrase survives only
      inside the struck-through original.
    - TEST-04's active text does NOT contain `domain-spelling`.
    - TEST-05's text names Phase 3 as having already delivered the `7+` chain deletion, and still
      contains `must land AFTER HIST-05`.
    - `npx vitest run` is unaffected (no source file touched).
  </acceptance_criteria>
  <done>TEST-04 describes required multi-file upload with the allowlist, BAA constraint, and three retrieval surfaces; the original email-only wording survives struck through with its reversal noted; TEST-05 records Phase 3's partial delivery.</done>
</task>

<task type="auto">
  <name>Task 3: Add uploaded filenames to CLAUDE.md's PHI field list and self-review checklist</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — compliance rule 5 (the PHI field enumeration: `name`, `dob`, `phone`, `email`,
      `state`, `quiz_score`, `score_bracket`, `answers`, `personal_history`, `family_history`), and
      §"Self-review checklist for PHI-handling changes" (its first bullet enumerates the same fields
      for the no-`console.log` rule).
    - `.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md` Pitfall 5 — the reasoning
      (a patient photographing their own paperwork produces `Jane_Doe_allergy_panel.pdf`).
    - `.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md` §"PHI logging discipline".
  </read_first>
  <action>
    Three edits to `CLAUDE.md`, all additive:

    1. In compliance rule 5, extend the enumerated PHI field list with `uploaded_filename` and
       `submission_files` (the whole join table), and add a parenthetical stating that a filename can
       itself carry a patient name, so filenames are PHI even though they are not a database column
       on `submissions`.

    2. In §"Self-review checklist for PHI-handling changes", extend the first checklist bullet's
       field enumeration with "uploaded filenames" so the no-`console.log` rule covers them
       explicitly, and add one new checklist bullet: uploaded file bytes are read and written only
       server-side against GCS, never proxied through a browser-facing public URL, and downloads are
       always `Content-Disposition: attachment`, never inline-rendered.

    3. In §"Common pitfalls", correct the stale claim that "The Customer Account UI extension
       currently still reads PHI metafields that no longer exist." Use the retract-in-place
       convention: strike the sentence with `~~` and state that the refactor landed in `ca3c3f4`
       and was hardened by `f762aaa` on 2026-05-08, that `extensions/quiz-history/src/` contains
       zero `metafield` references, that it calls `GET /api/me/assessments` with a Bearer token, and
       that `.planning/REQUIREMENTS.md` records it as DONE-07.

    Do not change compliance rules 1–4 or rule 6. Do not change the non-PHI metafield allowlist
    (`alledrops.last_completed_at`, `alledrops.quiz_count`).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('CLAUDE.md','utf8');const c=(n)=>s.split(n).length-1;const ok=c('uploaded_filename')>=1 && c('submission_files')>=1 && c('uploaded filenames')>=1 && c('Content-Disposition: attachment')>=1 && c('ca3c3f4')===1 && c('f762aaa')===1 && c('alledrops.last_completed_at')===1 && c('alledrops.quiz_count')===1;if(!ok)process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` compliance rule 5 lists `uploaded_filename` and `submission_files`.
    - The self-review checklist contains the literal `uploaded filenames` and a bullet containing
      `Content-Disposition: attachment`.
    - The common-pitfalls quiz-history claim is struck through (`~~`) and followed by the commits
      `ca3c3f4` and `f762aaa`.
    - The non-PHI metafield allowlist still names exactly `alledrops.last_completed_at` and
      `alledrops.quiz_count` and nothing else.
  </acceptance_criteria>
  <done>CLAUDE.md treats uploaded filenames as PHI, forbids proxying/inline-rendering uploaded bytes, and no longer claims the quiz-history extension is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future implementer | A stale compliance rule is read as current policy and licenses a PHI leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information Disclosure | `CLAUDE.md` rule 5 PHI field list | mitigate | Task 3 adds `uploaded_filename` and `submission_files` to the enumeration so no later executor reads filename logging as permitted |
| T-4-02 | Repudiation | `.planning/PROJECT.md` decision log | mitigate | Task 1 retracts in place rather than deleting, so the audit trail from the 2026-07-29 William call to the 2026-08-09 reversal stays intact |
| T-4-03 | Information Disclosure | uploaded-file download responses | mitigate | Task 3 adds an explicit `Content-Disposition: attachment` / no-inline-render checklist bullet, binding every later retrieval plan in this phase |

No package installs in this plan, so no `T-4-SC` supply-chain checkpoint applies here (see 04-09).
</threat_model>

<verification>
- `npx vitest run` — full suite still 361/27 green (no source file touched by this plan).
- `npm run typecheck` — clean (no source file touched).
- `git diff --stat` shows exactly three files changed: `.planning/PROJECT.md`,
  `.planning/REQUIREMENTS.md`, `CLAUDE.md`.
</verification>

<success_criteria>
- Reading `PROJECT.md`'s decision block top to bottom, a reader cannot conclude that upload is out
  of scope.
- Reading `REQUIREMENTS.md` TEST-04, a reader knows upload is required, what the allowlist is, and
  which three surfaces must retrieve files.
- Reading `CLAUDE.md`, an implementer knows filenames are PHI before writing the first log line.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-01-SUMMARY.md` when done.
</output>
