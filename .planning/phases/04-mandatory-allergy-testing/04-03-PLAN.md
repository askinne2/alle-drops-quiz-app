---
phase: 04-mandatory-allergy-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/quiz/ConsentStep.tsx
  - app/lib/consent-version.ts
  - tests/consent-version.test.ts
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "The rendered consent document contains no bracketed placeholder text"
    - "Section 4 reads as a coherent paragraph without referencing a treatment-policy page"
    - "CONSENT_VERSION identifies the new text, so a stored consent_version never misattributes which document a patient agreed to"
  artifacts:
    - path: "app/components/quiz/ConsentStep.tsx"
      provides: "Placeholder-free interim consent copy marked UNCONFIRMED"
      contains: "UNCONFIRMED"
    - path: "app/lib/consent-version.ts"
      provides: "Bumped CONSENT_VERSION constant"
      contains: "draft-2026-08-09"
    - path: "tests/consent-version.test.ts"
      provides: "Version assertion plus a no-placeholder guard on the rendered consent source"
      contains: "PENDING"
  key_links:
    - from: "app/components/quiz/ConsentStep.tsx"
      to: "app/lib/consent-version.ts"
      via: "consent text and its version identifier change together"
      pattern: "CONSENT_VERSION"
---

<objective>
Close D-11. `ConsentStep.tsx` renders a literal `[PENDING — Treatment policy page language]` inside
section 4 ("Laboratory Testing Authorization"). Phase 4 routes 100% of patients through consent (up
from only the 3–6 purchase path), so this phase materially increases the exposure rather than
inheriting it.

Because the rendered consent text changes, `CONSENT_VERSION` bumps in the same commit. A stored
`consent_version` is the record of *which document* a patient agreed to; leaving it at
`draft-2026-05-09` while the text changes would misattribute every future submission. `submissions`
is insert-only, so existing rows keep their original value untouched — no migration, no backfill.

Purpose: TEST-07's "every submission records a `consent_version`" is only meaningful if the version
string actually identifies the text.
Output: coherent placeholder-free section 4, marked UNCONFIRMED in code, with a bumped version and a
regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@app/components/quiz/ConsentStep.tsx
@app/lib/consent-version.ts
@tests/consent-version.test.ts

<interfaces>
app/lib/consent-version.ts (whole file):

  // Bump this string whenever the consent text in ConsentStep.tsx changes.
  // Format: "draft-YYYY-MM-DD" during development, "v1.0-YYYY-MM-DD" after counsel approval.
  export const CONSENT_VERSION = 'draft-2026-05-09'

ConsentStep.tsx section 4 as it stands (lines ~44-50):

  <section>
    <h3>4. Laboratory Testing Authorization</h3>
    <p>
      Provider may recommend IgE testing via Labcorp or Quest. Billed separately by lab. Insurance may not
      cover. [PENDING — Treatment policy page language]
    </p>
  </section>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the [PENDING] placeholder with D-11's interim copy, marked UNCONFIRMED</name>
  <files>app/components/quiz/ConsentStep.tsx</files>
  <read_first>
    - `app/components/quiz/ConsentStep.tsx` — the whole file. Section 4 is at approximately lines
      44-50; read the surrounding sections 3 and 5 so the replacement matches their register.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §"Interim consent copy (D-11)"
      — the proposed replacement paragraph, verbatim, and its warning that the section is about
      *future* IgE lab testing a provider might order, a different concept from Phase 4's upload of
      testing the patient already had. Do not conflate them.
    - `app/lib/quiz/questions.ts` — search for the `UNCONFIRMED` comment convention Phase 3
      established for HIST-03's third label, and match its exact comment syntax.
    - `~/Documents/Claude/Projects/AoD/aod-consent-text.md` — the verbatim consent source, if
      present, to confirm the replacement does not contradict it.
  </read_first>
  <action>
    Replace section 4's paragraph body with 04-UI-SPEC.md's proposed interim text verbatim:

    "Provider may recommend IgE testing via Labcorp or Quest. This testing is billed separately by
    the lab, and insurance coverage is not guaranteed. You are responsible for reviewing your
    insurance benefits and any out-of-pocket costs before scheduling recommended testing."

    Immediately above the `<p>`, add a JSX comment in the same syntax Phase 3 used for HIST-03,
    stating: interim copy, UNCONFIRMED, awaiting William/counsel approval, owned by LAUNCH-03, and
    that it replaced a `[PENDING — Treatment policy page language]` placeholder under 04-CONTEXT.md
    D-11.

    Constraints on the replacement text, all of which the verbatim string already satisfies —
    confirm, do not re-author: no approval or unlock promise (`DEC-no-approval-promise-copy`), no
    new clinical claim, no reference to a treatment-policy page, and no reference to the Phase 4
    upload feature.

    Change nothing else in this file: no layout, spacing, class, section-ordering, or checkbox-wiring
    changes (04-UI-SPEC.md Component Inventory §6 is explicit that ConsentStep is visually
    unchanged).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/quiz/ConsentStep.tsx','utf8');const c=(n)=>s.split(n).length-1;const ok=c('[PENDING')===0 && c('PENDING —')===0 && c('UNCONFIRMED')>=1 && c('insurance coverage is not guaranteed')===1 && c('Laboratory Testing Authorization')===1;if(!ok)process.exit(1);console.log('OK')" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/quiz/ConsentStep.tsx` contains zero occurrences of `[PENDING` (counted with
      `split(needle).length - 1`, never `grep -c`).
    - The file contains the literal `insurance coverage is not guaranteed` exactly once.
    - The file contains `UNCONFIRMED` at least once, in a JSX comment directly above section 4's
      `<p>`.
    - The file contains no occurrence of `approved`, `approval`, or `unlock` in section 4.
    - `git diff app/components/quiz/ConsentStep.tsx` shows changes confined to section 4 — no
      `className`, `<section>` count, or checkbox handler appears in the diff.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Section 4 is a coherent, placeholder-free paragraph carrying an UNCONFIRMED marker; nothing else in ConsentStep changed.</done>
</task>

<task type="auto">
  <name>Task 2: Bump CONSENT_VERSION and extend its regression test</name>
  <files>app/lib/consent-version.ts, tests/consent-version.test.ts</files>
  <read_first>
    - `app/lib/consent-version.ts` — three lines; its header comment already states the bump rule.
    - `tests/consent-version.test.ts` — the whole file (13 lines); the existing
      `expect(CONSENT_VERSION).toBe('draft-2026-05-09')` assertion.
    - `app/components/quiz/ConsentStep.tsx` (post-Task-1) — the text whose change forces the bump.
    - `app/routes/api.quiz.submit.tsx` — confirm how `consent_version` reaches `insertSubmission`,
      so the new test asserts against the real path rather than an invented one.
    - `.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md` — the entry "Extend
      `tests/consent-version.test.ts` for the single-path consent flow" (extend, do not replace).
  </read_first>
  <action>
    Set `CONSENT_VERSION` to `'draft-2026-08-09'`. Extend the file's header comment with one line
    recording that the 2026-08-09 bump was caused by D-11's section-4 interim rewrite, and that
    existing `submissions` rows keep their prior value because the table is insert-only — no
    backfill, no migration.

    In `tests/consent-version.test.ts`, extend (do not replace) the existing describe block:

    1. Update the `toBe(...)` assertion to `'draft-2026-08-09'`.
    2. Add a source-text guard that reads `app/components/quiz/ConsentStep.tsx` from disk with
       `readFileSync` and asserts the literal `[PENDING` appears zero times, counted with
       `SOURCE.split(needle).length - 1`. Assemble the needle from fragments (for example
       `"[PEND" + "ING"`) so this test's own prose cannot self-match a future repo-wide search.
    3. Add a positive control in the same guard asserting `Laboratory Testing Authorization` appears
       at least once, proving the read pointed at the real file and the zero-count is not vacuous.
    4. Add an assertion that `CONSENT_VERSION` matches `/^(draft|v\d+\.\d+)-\d{4}-\d{2}-\d{2}$/`,
       so a future bump cannot silently break the documented format.

    Keep the file at `.test.ts`. Do not add DOM rendering here — plan 04-07 owns proving consent is
    reachable on every path.
  </action>
  <verify>
    <automated>npx vitest run tests/consent-version.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/consent-version.ts` exports `CONSENT_VERSION = 'draft-2026-08-09'`.
    - The header comment names D-11 and states existing rows are not backfilled.
    - `tests/consent-version.test.ts` reads `ConsentStep.tsx` from disk and asserts a `[PENDING`
      count of exactly 0 using `split(needle).length - 1`.
    - The same test asserts `Laboratory Testing Authorization` count >= 1 (non-vacuity control).
    - The format regex assertion passes for `draft-2026-08-09` and would fail for `2026-08-09`
      (state in the SUMMARY that you confirmed this by temporarily inverting the value locally, then
      restoring it).
    - `npm test` full suite green; count above the 361 baseline.
  </acceptance_criteria>
  <done>CONSENT_VERSION identifies the new text, the placeholder can never silently return, and the guard is proven non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered consent document → stored `consent_version` | The stored string is the only record of which legal text a patient agreed to |
| interim clinical copy → patient | Copy shipped without counsel approval is a standing project constraint being deliberately brushed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-07 | Repudiation | `consent_version` vs. rendered consent text | mitigate | Task 2 bumps `CONSENT_VERSION` to `draft-2026-08-09` in the same commit as the text change, so no submission records agreement to a document it never saw |
| T-4-08 | Tampering | Unapproved clinical copy shipping as approved | mitigate | Task 1's `UNCONFIRMED` JSX comment plus the `draft-` version prefix; the copy also rides on the William/counsel message drafted in plan 04-18. LAUNCH-03 remains the owner of final language |
| T-4-09 | Information Disclosure | Placeholder text visible to a patient on a clinical consent screen | mitigate | Task 2's zero-count source guard with a non-vacuity positive control, so the placeholder cannot silently reappear |

No packages installed in this plan.
</threat_model>

<verification>
- `npx vitest run tests/consent-version.test.ts` green.
- `npm test` full suite green; `npm run typecheck` clean.
- `public/quiz-bundle.js` is deliberately NOT rebuilt here — `ConsentStep.tsx` is quiz source, so
  plan 04-08 folds this change into the single theme-bundle rebuild for the unblocked track and adds
  a Phase-4 freshness marker for it.
</verification>

<success_criteria>
- No bracketed placeholder renders anywhere in the consent document.
- `CONSENT_VERSION` and the consent text moved together in one commit.
- The regression guard is proven non-vacuous by its positive control.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-03-SUMMARY.md` when done.
</output>
