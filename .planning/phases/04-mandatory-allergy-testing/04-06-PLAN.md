---
phase: 04-mandatory-allergy-testing
plan: 06
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - app/lib/quiz/questions.ts
  - app/lib/format.ts
  - tests/quiz-schema-type-guarantees.test.ts
  - tests/answer-labels.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "QUIZ_PARTS has seven parts and Part 7 is the allergy-testing split"
    - "testing_status offers exactly two options and is required"
    - "Choosing 'I've already had allergy testing' reveals year, location, and allergens, all required"
    - "Choosing 'I need allergy testing' reveals none of those three fields"
    - "ALL_SCORED_QUESTIONS did not grow — Part 7 carries no score"
    - "Every Part 7 question ID renders a clinical label in the PDF and admin modal"
  artifacts:
    - path: "app/lib/quiz/questions.ts"
      provides: "PART7_ALLERGY_TESTING attached to QUIZ_PARTS"
      contains: "PART7_ALLERGY_TESTING"
    - path: "app/lib/format.ts"
      provides: "ANSWER_LABELS entries for the Part 7 question IDs"
      contains: "testing_status"
  key_links:
    - from: "app/lib/quiz/questions.ts"
      to: "app/lib/quiz/schema.ts evaluateShowIf"
      via: "showIf: { questionId: 'testing_status', equals: 'had_testing' }"
      pattern: "testing_status"
    - from: "app/lib/format.ts"
      to: "app/lib/pdf.ts and app/routes/app.quiz-results.tsx"
      via: "getAnswerLabel, the single shared label map"
      pattern: "getAnswerLabel"
---

<objective>
Add Part 7 — the allergy-testing split — to `QUIZ_PARTS` as declarative question data, and give its
IDs clinical labels in the one shared label map both PHI renderers consume.

D-06 locks the mechanism: a 7th part in `QUIZ_PARTS`, not a new `FlowStep`. Phase 3 deleted the
`"medical_history"` FlowStep for being exactly the shape a new step would take, and dead FlowSteps
are how the `entry.theme.tsx` open redirect survived two independent reviews.

`testing_files` (the `file_multi` upload question) is deliberately NOT added here. It belongs to the
blocker-gated upload wave and lands in plan 04-16, which appends it to this same array. Everything
in this plan is unblocked work.

Purpose: TEST-01 (two options, no skip), TEST-02 (the needs-testing branch), TEST-03 (year, location,
allergens persisted into `answers_json`).
Output: `PART7_ALLERGY_TESTING`, wired into `QUIZ_PARTS`, `getQuestionById`, and `ANSWER_LABELS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md
@app/lib/quiz/questions.ts
@app/lib/quiz/types.ts
@app/lib/format.ts

<interfaces>
Attachment point (app/lib/quiz/questions.ts:476-488):

  export const QUIZ_PARTS: QuizItem[][] = [
    PART1_SYMPTOM_CHECKLIST, PART2_TIMING_TRIGGERS, PART3_SEVERITY,
    PART4_IMPACT, PART5_TREATMENT, PART6_MEDICAL_HISTORY,
  ];
  export const ALL_ITEMS: QuizItem[] = QUIZ_PARTS.flat();

Scoring boundary (questions.ts:455-466) — must NOT grow:

  export const ALL_SCORED_QUESTIONS: QuizQuestion[] = [
    ...PART1_SYMPTOM_CHECKLIST, ...PART2_TIMING_TRIGGERS, ...PART3_SEVERITY,
    ...PART4_IMPACT, ...PART5_TREATMENT,
  ];

Lookup used by evaluateShowIf (questions.ts:468-474) — MUST grow, or Part 7's showIf references
fail open and render every child unconditionally:

  export function getQuestionById(id: string): QuizQuestion | undefined {
    return [...ALL_SCORED_QUESTIONS, ...PART6_MEDICAL_HISTORY]
      .filter((item): item is QuizQuestion => item.kind === "question")
      .find((q) => q.id === id);
  }

Label map (app/lib/format.ts:24-49) — consumed by BOTH app/lib/pdf.ts and
app/routes/app.quiz-results.tsx:

  const ANSWER_LABELS: Record<string, string> = { history_comorbidities: '...', ... };
  export function getAnswerLabel(key: string): string {
    return ANSWER_LABELS[key] ?? capitalize(key.replace(/_/g, ' '))
  }

QuestionType values available after plan 04-02: radio_single, text_input_short, text_input,
file_multi (unused until 04-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define PART7_ALLERGY_TESTING and attach it to QUIZ_PARTS</name>
  <files>app/lib/quiz/questions.ts</files>
  <read_first>
    - `app/lib/quiz/questions.ts` — read `PART6_MEDICAL_HISTORY` in full (from its section banner at
      ~line 265 to its closing bracket at ~line 452). It is this repo's only working example of a
      `showIf`-gated multi-child part with an info block, and it is the direct structural donor.
      Also read `ALL_SCORED_QUESTIONS`, `getQuestionById`, `QUIZ_PARTS`, and `ALL_ITEMS` at the tail.
    - `app/lib/quiz/types.ts` (post-04-02) — the `QuizQuestion` fields, `ShowIfCondition`, and the
      `required` default-true comment.
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §"Locked / verbatim copy" and
      §"Proposed copy" — the two option labels and `testing_allergens`'s question text are locked
      verbatim; the rest is proposed and must carry an UNCONFIRMED comment.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-06 and D-08.
  </read_first>
  <action>
    Add a section banner comment and `export const PART7_ALLERGY_TESTING: QuizItem[]` immediately
    after `PART6_MEDICAL_HISTORY`'s closing bracket, matching PART6's banner style. The banner must
    state: mandatory, no score, `ALL_SCORED_QUESTIONS` stays Parts 1–5 only, and that `testing_files`
    (`file_multi`) is appended by the blocker-gated upload wave (plan 04-16) — so a reader does not
    conclude the upload requirement was dropped.

    Four items, all `part: 7`:

    1. `testing_status` — `kind: "question"`, `type: "radio_single"`, `order: 70`.
       `text`: "Have you already had allergy testing?"
       `subtitle`: 'If you choose "I\'ve already had allergy testing," you\'ll be asked to upload a
       copy of your results (PDF or photo) to continue.'
       `options`: exactly two, in this order —
         `{ value: "needs_testing", label: "I need allergy testing" }`
         `{ value: "had_testing", label: "I've already had allergy testing" }`
       Both labels are LOCKED verbatim (quoted identically in ROADMAP.md and REQUIREMENTS.md
       TEST-02/TEST-03) — do not reword, do not add a third option, do not add a skip option.
       Omit `required` so it defaults to `true` (Phase 2 D-05).
       Add an UNCONFIRMED comment on `text` and `subtitle` only (the option labels are locked).
       Add a comment recording D-08: the choice is honor-system, recorded in `answers_json`, never
       enforced — no account flag, no server-side gate.

    2. `testing_year` — `type: "text_input_short"`, `order: 71`,
       `showIf: { questionId: "testing_status", equals: "had_testing" }`,
       `text`: "What year did you have your allergy testing done?" (UNCONFIRMED comment).

    3. `testing_location` — `type: "text_input_short"`, `order: 72`, same `showIf`,
       `text`: "Where did you have your allergy testing done? (Clinic or lab name and city)"
       (UNCONFIRMED comment).

    4. `testing_allergens` — `type: "text_input"`, `order: 73`, same `showIf`,
       `text`: "What Allergens Did You React To?" — LOCKED verbatim, title case exactly as written,
       quoted in REQUIREMENTS.md TEST-03. Add a comment saying so.

    All four omit `required`, so all four default to required. That is intentional and is why
    04-UI-SPEC.md §4 excludes Part 7 from Phase 3's gate/reveal fusion (which keys on `showIf` +
    `required === false`).

    Use a flat `showIf` on each child pointing at `testing_status` directly. Do NOT chain a `showIf`
    through another Part 7 child — `evaluateShowIf` is non-transitive by design.

    Then make exactly three wiring edits:
    - Append `PART7_ALLERGY_TESTING` to `QUIZ_PARTS` after `PART6_MEDICAL_HISTORY`.
    - Widen `getQuestionById`'s spread to `[...ALL_SCORED_QUESTIONS, ...PART6_MEDICAL_HISTORY,
      ...PART7_ALLERGY_TESTING]`. Without this, `evaluateShowIf` cannot resolve `testing_status` and
      fails OPEN (Phase 2 D-04), rendering all three children unconditionally.
    - Update `QUIZ_PARTS`'s doc comment from "Ordered parts 1–6" to "Ordered parts 1–7" and note
      Part 7 is reached by 100% of patients.

    Do NOT add Part 7 to `ALL_SCORED_QUESTIONS`. Do NOT widen `getAllQuestionsForScoring`. Do NOT
    add a section heading for Part 7 (04-UI-SPEC.md §4: no part in this flow carries a
    `.questionCategory__title`).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --experimental-strip-types -e "import('./app/lib/quiz/questions.ts').then(m=>{const p=m.QUIZ_PARTS;if(p.length!==7)throw new Error('QUIZ_PARTS length '+p.length);const s=p[6].find(q=>q.id==='testing_status');if(!s||s.options.length!==2)throw new Error('testing_status options');if(s.options[0].value!=='needs_testing'||s.options[1].value!=='had_testing')throw new Error('option values');const kids=p[6].filter(q=>q.showIf);if(kids.length!==3)throw new Error('expected 3 gated children, got '+kids.length);for(const k of kids){if(k.showIf.questionId!=='testing_status'||k.showIf.equals!=='had_testing')throw new Error('bad showIf on '+k.id);if(k.required===false)throw new Error(k.id+' must be required')}if(m.ALL_SCORED_QUESTIONS.some(q=>q.part===7))throw new Error('Part 7 leaked into ALL_SCORED_QUESTIONS');if(!m.getQuestionById('testing_status'))throw new Error('getQuestionById cannot resolve testing_status');console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `QUIZ_PARTS.length === 7` and `QUIZ_PARTS[6] === PART7_ALLERGY_TESTING`.
    - `testing_status` has exactly two options with values `needs_testing` and `had_testing`, and
      labels exactly `I need allergy testing` and `I've already had allergy testing`.
    - `testing_year`, `testing_location`, `testing_allergens` each carry
      `showIf: { questionId: "testing_status", equals: "had_testing" }` and none sets
      `required: false`.
    - `getQuestionById("testing_status")` returns the question (proves the spread was widened).
    - No member of `ALL_SCORED_QUESTIONS` has `part === 7`.
    - `app/lib/quiz/questions.ts` contains zero occurrences of `testing_files` (that question lands
      in plan 04-16).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Part 7 exists as declarative data with a two-option required gate and three required showIf children, resolvable by evaluateShowIf, carrying no score.</done>
</task>

<task type="auto">
  <name>Task 2: Add Part 7 clinical labels to the shared ANSWER_LABELS map</name>
  <files>app/lib/format.ts, tests/answer-labels.test.ts</files>
  <read_first>
    - `app/lib/format.ts` — the whole file: the `ANSWER_LABELS` header comment (it states the map is
      consumed by `app/lib/pdf.ts` and `app/routes/app.quiz-results.tsx`, and that an unmapped key
      falls back to `capitalize(key.replace(/_/g,' '))` so an existing row can never regress), the
      Phase 3 HIST block's grouping-comment convention, and `getAnswerLabel`.
    - `tests/answer-labels.test.ts` — the whole file; match its existing assertion style.
    - `app/lib/quiz/questions.ts` (post-Task-1) — the four Part 7 question IDs.
  </read_first>
  <action>
    Add a Phase 4 grouping comment and four entries to `ANSWER_LABELS`, following the existing
    per-phase comment convention:

      testing_status     -> 'Allergy testing status'
      testing_year       -> 'Year of prior allergy testing'
      testing_location   -> 'Where prior allergy testing was done'
      testing_allergens  -> 'Allergens reacted to on prior testing'

    Do NOT add a `testing_files` entry — that question does not exist yet and plan 04-16 owns it.
    Do NOT create a second label map anywhere; both PHI renderers must keep consuming this one.

    Extend `tests/answer-labels.test.ts` with: one assertion per new key that `getAnswerLabel` returns
    the exact mapped string; and one non-vacuity control asserting an unmapped key still falls back
    to the capitalize-and-underscore-replace behavior (for example `getAnswerLabel('made_up_key')`
    returns `Made up key`), so a future refactor that breaks the fallback fails here.
  </action>
  <verify>
    <automated>npx vitest run tests/answer-labels.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `getAnswerLabel('testing_status')` returns `Allergy testing status` and the other three keys
      return their mapped strings exactly.
    - `getAnswerLabel` on an unmapped key still returns the capitalized underscore-replaced form.
    - `app/lib/format.ts` contains zero occurrences of `testing_files`.
    - `grep -n "ANSWER_LABELS" app/lib/` returns exactly one definition site.
    - `npx vitest run tests/answer-labels.test.ts` passes with at least 5 more assertions than before.
  </acceptance_criteria>
  <done>All four Part 7 IDs render clinical labels in both PHI renderers via the single shared map, with the fallback path proven intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update the ALL_ITEMS arithmetic guard and prove the scoring boundary held</name>
  <files>tests/quiz-schema-type-guarantees.test.ts</files>
  <read_first>
    - `tests/quiz-schema-type-guarantees.test.ts` — the whole file, especially the existing
      positive-control assertion `expect(ALL_ITEMS.length).toBe(ALL_SCORED_QUESTIONS.length +
      PART6_MEDICAL_HISTORY.length)` and the Phase 4 `isAnswered` block added by plan 04-02.
    - `app/lib/quiz/questions.ts` (post-Task-1) — `ALL_ITEMS`, `ALL_SCORED_QUESTIONS`,
      `PART6_MEDICAL_HISTORY`, `PART7_ALLERGY_TESTING`.
    - `.planning/phases/04-mandatory-allergy-testing/04-PATTERNS.md`
      §`tests/quiz-schema-type-guarantees.test.ts` — the instruction to update the arithmetic and add
      a parallel non-vacuity control proving `ALL_SCORED_QUESTIONS` did NOT grow.
  </read_first>
  <action>
    Update the existing arithmetic assertion to
    `ALL_SCORED_QUESTIONS.length + PART6_MEDICAL_HISTORY.length + PART7_ALLERGY_TESTING.length`, and
    rename the test title from "covers parts 1-6" to "covers parts 1-7".

    Add four new assertions:
    - `QUIZ_PARTS.length` is `7`.
    - No member of `ALL_SCORED_QUESTIONS` has `part === 7` — the scoring boundary held.
    - `getQuestionById('testing_status')` is defined, and `getQuestionById('testing_year')` is
      defined — proving the spread widening in Task 1 actually took effect rather than being masked
      by `evaluateShowIf`'s fail-open behavior.
    - `PART7_ALLERGY_TESTING.filter(i => i.showIf).length` is `3`, and every one of those three has
      `showIf.questionId === 'testing_status'` — a flat gate, no chained `showIf`.

    Do not touch the compile-time `QuizInfoBlock` vs `QuizQuestion` invariants or plan 04-02's
    `isAnswered` block.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-schema-type-guarantees.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The arithmetic assertion includes `PART7_ALLERGY_TESTING.length` and passes.
    - An assertion proves `ALL_SCORED_QUESTIONS` contains no `part === 7` question.
    - An assertion proves `getQuestionById` resolves both `testing_status` and `testing_year`.
    - An assertion proves exactly three Part 7 items carry `showIf`, all pointing at
      `testing_status`.
    - `npm test` full suite green; total count strictly greater than plan 04-02's recorded count.
  </acceptance_criteria>
  <done>The type-guarantee suite pins Part 7's shape, its flat gate, and the unchanged scoring boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient branch choice → clinical record | `testing_status` is honor-system per D-08; it records intent, it does not enforce anything |
| question ID → PHI renderers | An unmapped ID reaches the clinical PDF and admin modal as a machine-shaped key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-19 | Elevation of Privilege | `testing_status` honor-system branch | accept | D-08 and `DEC-purchase-gating-is-honor-system` (LOCKED). A patient choosing `needs_testing` to dodge the later upload is routed to *buy testing* and is not purchasing SLIT today, so the branch is a slower path, not a purchase bypass. Enforcement stays human at fulfillment. Task 1 records this in a code comment so nobody "hardens" it into a server-side gate |
| T-4-20 | Tampering | `getQuestionById` not widened | mitigate | `evaluateShowIf` fails OPEN on an unresolvable reference (Phase 2 D-04), so a missed widening silently renders all three children to every patient instead of erroring. Task 1's wiring edit plus Task 3's explicit `getQuestionById` assertions close it |
| T-4-21 | Tampering | Part 7 leaking into the score | mitigate | Task 1 forbids touching `ALL_SCORED_QUESTIONS` / `getAllQuestionsForScoring`; Task 3 asserts no `part === 7` question is in `ALL_SCORED_QUESTIONS` |
| T-4-22 | Information Disclosure | Part 7 answers rendered as raw keys in the clinical PDF | mitigate | Task 2 adds all four IDs to the single shared `ANSWER_LABELS` map both PHI renderers consume; the fallback path is separately asserted so an unmapped key still degrades safely |

No packages installed in this plan.
</threat_model>

<verification>
- `npm run typecheck` clean; `npm test` full suite green with a higher count than 04-02.
- `app/lib/quiz/questions.ts` contains zero occurrences of `testing_files`.
- `public/quiz-bundle.js` is deliberately NOT rebuilt here — plan 04-09 owns the single rebuild for
  the unblocked track.
</verification>

<success_criteria>
- Every patient reaching the quiz's last part sees exactly two testing options and cannot advance
  without choosing one.
- Choosing `had_testing` reveals exactly three required fields; choosing `needs_testing` reveals none.
- The score is unchanged for every existing answer set.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-06-SUMMARY.md` when done.
</output>
