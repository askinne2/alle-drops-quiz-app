---
phase: 04-mandatory-allergy-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/andrewskinner/Local Sites/allergist-on-demand/config/settings_data.json
  - /Users/andrewskinner/Local Sites/allergist-on-demand/templates/page.quiz.json
  - /Users/andrewskinner/Local Sites/allergist-on-demand/templates/page.testing-options.json
  - .planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md
  - .planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md
autonomous: false
requirements: [TEST-06]
must_haves:
  truths:
    - "The allergist-on-demand theme working tree, if pushed, would not re-enable Klaviyo on the PHI-collecting quiz page"
    - "The theme working tree carries no 'no longer a need for needles or allergy tests' clause on either product page"
    - "The theme working tree carries no proceed-without-testing content on /pages/test-options"
    - "Replacement copy exists as a draft for William, and is not shipped"
  artifacts:
    - path: ".planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md"
      provides: "Measured before/after diff of every known drift item, with counts"
      contains: "settings_data.json"
    - path: ".planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md"
      provides: "Held-for-approval replacement copy for the deleted clauses"
      contains: "UNCONFIRMED"
  key_links:
    - from: "/Users/andrewskinner/Local Sites/allergist-on-demand/config/settings_data.json"
      to: "live /pages/allergy-quiz served bytes"
      via: "shopify theme push would overwrite live app-embed state"
      pattern: "klaviyo-onsite-embed"
---

<objective>
Prepare the `allergist-on-demand` theme repo so a `shopify theme push` is safe, then delete the
storefront clauses that promise testing can be skipped. **No push happens in this plan** — the push
and its served-bytes verification are plan 04-05, behind Andrew's explicit authorization.

D-12 makes this mandatory rather than optional: a push from that repo *today* would re-enable the
Klaviyo onsite embed on `/pages/allergy-quiz`, a third-party script on a PHI-collecting page and a
reportable-breach trigger per `docs/breach-response-runbook.md:16`. Andrew disabled Klaviyo in the
theme editor on 2026-08-09; the local `config/settings_data.json` still reads `disabled: false`.

D-13 splits the copy work: deleting an inaccurate clinical claim needs no approval, authoring a
replacement does. Delete now, draft the replacement, hold it for William.

Purpose: TEST-06 requires that no storefront surface offers or implies a no-testing path.
Output: a reconciled theme working tree, the deletions applied locally, a reconciliation report with
measured counts, and a held draft of replacement copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@CLAUDE.md
@docs/breach-response-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the theme working tree against live and record measured counts</name>
  <files>/Users/andrewskinner/Local Sites/allergist-on-demand/config/settings_data.json, /Users/andrewskinner/Local Sites/allergist-on-demand/templates/page.quiz.json, /Users/andrewskinner/Local Sites/allergist-on-demand/templates/page.testing-options.json, .planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md</files>
  <read_first>
    - `.planning/STATE.md` §"Live exposures to close immediately" — the Klaviyo entry (CLOSED live,
      but local `settings_data.json` still reads `disabled: false`), the Appointly retraction
      (`apntly` = 0, `appointly` = 15 on served bytes), the Gate D `templates/page.quiz.json` drift
      note (git HEAD still references a `quiz-kit-smart-product-finder` block), and the
      `/pages/testing-options` 404 / orphaned-template entry.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-12 and D-14 — the three named
      drift items and the served-bytes counting protocol.
    - `/Users/andrewskinner/Local Sites/allergist-on-demand/config/settings_data.json` — the
      `current.blocks` object; locate every `shopify://apps/.../blocks/...` entry and its `disabled`
      value.
    - `/Users/andrewskinner/Local Sites/allergist-on-demand/templates/page.quiz.json` — the app
      block's `consult_redirect_url` and `test_options_redirect_url` settings.
    - `docs/breach-response-runbook.md:16` — the reportable-breach trigger this protects against.
  </read_first>
  <action>
    Work in `/Users/andrewskinner/Local Sites/allergist-on-demand`. Start with `git status` and
    `git diff` to capture existing uncommitted drift BEFORE changing anything — record it verbatim in
    the report. Never `git reset --hard` in this repo; uncommitted tracked-file modifications there
    are load-bearing (they carry Andrew's theme-editor fixes).

    Apply three reconciliations to the working tree:

    1. `config/settings_data.json` — set the Klaviyo onsite-embed block
       (`shopify://apps/klaviyo-email-marketing-sms/blocks/klaviyo-onsite-embed/...`) to
       `"disabled": true`, matching live. Do NOT change the Appointly block
       (`shopify://apps/apntly-appointment-booking-app/blocks/main-app-embed/...`) — it is
       deliberately left enabled pending a Phase 8 keep/disable decision; record its current
       `disabled` value in the report so the push does not silently alter it either way.
    2. `templates/page.quiz.json` — confirm the working tree matches live:
       `consult_redirect_url` resolving to `/products/allergy-consultation` and
       `test_options_redirect_url` resolving to `/pages/test-options`. Confirm no
       `quiz-kit-smart-product-finder` block reference remains in the working tree; if one does,
       remove it and record that as a fourth drift item.
    3. `templates/page.testing-options.json` — this template is orphaned (`/pages/testing-options`
       is a 404 while `/pages/test-options` is the live 200). Delete the orphaned template file.
       Confirm first, by grepping the whole theme repo, that no other template, section, or snippet
       references `testing-options` — if anything does, do not delete; record it and stop.

    Write `.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md` in the quiz-app
    repo (not the theme repo). It must contain, per drift item: the file, the pre-change value, the
    post-change value, and the evidence the target value is correct (which STATE.md entry or which
    live measurement). Count every occurrence with `SOURCE.split(needle).length - 1` — never
    `grep -c`, which counts lines and collapses multi-match lines to 1.

    Do not run `shopify theme push`, `shopify theme pull`, `git commit`, or `git push` in the theme
    repo. This task ends with an uncommitted, reconciled working tree.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const T='/Users/andrewskinner/Local Sites/allergist-on-demand';const s=fs.readFileSync(T+'/config/settings_data.json','utf8');const c=(x,n)=>x.split(n).length-1;const kIdx=s.indexOf('klaviyo-onsite-embed');if(kIdx<0)throw new Error('klaviyo block not found');const win=s.slice(Math.max(0,kIdx-400),kIdx+400);if(c(win,'\"disabled\": true')<1)throw new Error('klaviyo block not disabled');if(fs.existsSync(T+'/templates/page.testing-options.json'))throw new Error('orphaned template still present');const r=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md','utf8');if(c(r,'settings_data.json')<1||c(r,'appointly')+c(r,'Appointly')<1)throw new Error('reconciliation report incomplete');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - In `config/settings_data.json`, the Klaviyo onsite-embed block's `disabled` value is `true`.
    - The Appointly block's `disabled` value is unchanged from its pre-task value, and the report
      records both the pre and post values explicitly.
    - `templates/page.testing-options.json` no longer exists, and the report records the repo-wide
      grep proving nothing referenced it.
    - `templates/page.quiz.json` contains `/pages/test-options` and does not contain
      `quiz-kit-smart-product-finder`.
    - `04-THEME-RECONCILIATION.md` records a pre-value, post-value, and evidence source for every
      drift item, with every count produced by `split(needle).length - 1`.
    - `git -C "/Users/andrewskinner/Local Sites/allergist-on-demand" log -1` shows the same commit
      SHA as before the task (nothing was committed).
  </acceptance_criteria>
  <done>The theme working tree, if pushed, would leave Klaviyo off, leave Appointly exactly as it is today, keep the corrected redirect URLs, and carry no orphaned template — with every claim backed by a recorded count.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the no-testing-required clauses and draft (do not ship) the replacement</name>
  <files>/Users/andrewskinner/Local Sites/allergist-on-demand, .planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md</files>
  <read_first>
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-13 — the exact deletion
      targets ("no longer a need for needles or allergy tests" on both product pages; the
      proceed-without-testing content on `/pages/test-options`) and the delete-now / draft-and-hold
      split.
    - `.planning/REQUIREMENTS.md` TEST-06 — verbatim.
    - `.planning/PROJECT.md` §Key Decisions — `DEC-mandatory-allergy-testing` and
      `DEC-no-approval-promise-copy`, which bound what the draft may say.
    - `.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md` (from Task 1) —
      so this task appends to the same evidence trail rather than starting a new one.
  </read_first>
  <action>
    Locate the clause text in the theme repo before assuming where it lives. Product-page body copy
    on Shopify usually lives in product metafields or the product description in the Admin, not in
    the theme repo — grep the whole theme repo for `needles`, `allergy test`, and `no longer a need`
    and record what you find. Report which surfaces are theme-repo-editable and which are
    Admin-only; do not invent a theme-file edit for content that is not in the theme repo.

    For every occurrence that IS in the theme repo (templates, sections, snippets, locale files,
    `settings_data.json` block settings), delete the clause. Deletion only — do not substitute
    replacement wording anywhere.

    For `/pages/test-options`: remove the proceed-without-testing content from whichever theme file
    renders it. If that page's body is Admin-managed rather than theme-managed, record that in the
    report; plan 04-05's checkpoint then routes it to Andrew as an Admin edit rather than a push.

    Write `.planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md` in the quiz-app
    repo. It holds proposed replacement copy for each deleted clause, clearly marked `UNCONFIRMED —
    held for William/counsel approval, not shipped`. The draft must state that allergy testing is
    required before SLIT and how a patient obtains it, and must contain no approval/unlock promise
    (`DEC-no-approval-promise-copy`), no efficacy claim, and no reference to the Phase 4 upload
    feature. Include, for each item: the exact deleted string, the surface it was on, and the
    proposed replacement.

    Append to `04-THEME-RECONCILIATION.md` a deletions section recording, per surface, the pre-change
    occurrence count and the post-change occurrence count, both via `split(needle).length - 1`.

    Still no `shopify theme push`, no commit, no push.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md','utf8');const c=(x,n)=>x.split(n).length-1;if(c(d,'UNCONFIRMED')<1)throw new Error('draft not marked UNCONFIRMED');if(c(d,'approved')+c(d,'unlock')>0)throw new Error('draft contains an approval/unlock promise');const r=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md','utf8');if(c(r,'needles')<1)throw new Error('deletions section missing');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Every theme-repo occurrence of `no longer a need` and `needles` in a patient-facing string is
      deleted; the report records a post-change count of 0 for each, via `split(needle).length - 1`.
    - Any occurrence that is Admin-managed rather than theme-managed is named explicitly in the
      report as an Admin edit, not silently skipped.
    - `04-STOREFRONT-COPY-DRAFT.md` exists, contains `UNCONFIRMED`, and contains zero occurrences of
      `approved` and `unlock`.
    - No replacement wording was written into any theme file (`git -C <theme> diff` shows deletions
      and the Task-1 reconciliations only — no added patient-facing sentences).
    - The theme repo still has no new commit.
  </acceptance_criteria>
  <done>The false clauses are gone from every theme-repo surface, Admin-only surfaces are named rather than skipped, and the replacement copy exists only as a held draft.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Andrew authorizes the theme push</name>
  <what-built>
    The `allergist-on-demand` theme working tree is reconciled against live (Klaviyo embed set to
    disabled, Appointly untouched, redirect URLs confirmed, orphaned `page.testing-options.json`
    removed) and the D-13 no-testing-required clauses are deleted. Nothing has been committed or
    pushed. `04-THEME-RECONCILIATION.md` records every change with measured occurrence counts, and
    `04-STOREFRONT-COPY-DRAFT.md` holds the replacement copy for William.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md` end to end.
       Every drift item must show a pre-value, a post-value, and an evidence source.
    2. Run `git -C "/Users/andrewskinner/Local Sites/allergist-on-demand" diff` and read the full
       diff. Confirm specifically: the Klaviyo block flips to `"disabled": true`, the Appointly block
       does NOT change, and no patient-facing replacement sentence was added anywhere.
    3. Confirm `templates/page.testing-options.json` is deleted and that the report shows nothing
       referenced it.
    4. Read `.planning/phases/04-mandatory-allergy-testing/04-STOREFRONT-COPY-DRAFT.md` and confirm
       the replacement copy is acceptable to send to William — it does not ship in this phase.
    5. Confirm which surfaces the report flagged as Admin-managed rather than theme-managed, and
       decide whether you will make those edits in the Shopify Admin yourself.
    6. Decide whether plan 04-05 may run `shopify theme push` against the live "Sense" theme.
       A push overwrites live theme state — this is the irreversible step.
  </how-to-verify>
  <action>Present the reconciliation report, the theme-repo diff, and the copy draft to Andrew, then STOP and wait. Do not run `shopify theme push`, `git commit`, or `git push` in the theme repo under any circumstance in this plan — plan 04-05 owns those, and only after this authorization is recorded. Record Andrew's answer verbatim in the SUMMARY, including any surfaces he intends to edit in the Shopify Admin himself.</action>
  <done>Andrew has explicitly authorized or declined the theme push, and his answer is recorded verbatim in the SUMMARY.</done>
  <resume-signal>Type "approved — push authorized" to unblock plan 04-05, or describe what must change first. If you prefer to apply these via the theme editor instead of a push, say so and 04-05 will be re-planned as a verification-only plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local theme working tree → live storefront | `shopify theme push` overwrites live theme state, including app-embed enablement, on a PHI-collecting page |
| storefront copy → patient | Copy that implies purchase without testing is the exact surface TEST-06 exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-10 | Information Disclosure | `config/settings_data.json` Klaviyo onsite embed | mitigate | Task 1 flips `disabled` to `true` in the working tree BEFORE any push is authorized; Task 3's blocking checkpoint requires Andrew to read the diff and confirm it. Re-enabling Klaviyo on `/pages/allergy-quiz` is a reportable-breach trigger per `docs/breach-response-runbook.md:16` |
| T-4-11 | Information Disclosure | Appointly app embed (`staq-cdn.com` / `staqlab.com`) | accept | Deliberately left enabled — Phase 7 may depend on it for booking. Task 1 records its exact pre/post value so a push cannot change it accidentally; the keep/disable decision stays a Phase 8 item |
| T-4-12 | Tampering | Uncommitted theme-repo drift wiped by a careless reset | mitigate | Task 1 forbids `git reset --hard` and captures `git status` / `git diff` before any edit, per the CLAUDE.md pitfall |
| T-4-13 | Spoofing | Verification against the theme editor UI or a deploy exit code | mitigate | D-14: no verification claim in this plan or 04-05 may rest on the editor UI; only authenticated, cache-busted served bytes count, and a single fetch is not evidence |
| T-4-14 | Tampering | Unapproved replacement clinical copy shipping | mitigate | Task 2 deletes only; the replacement lives in `04-STOREFRONT-COPY-DRAFT.md` marked UNCONFIRMED and rides on the William message drafted in plan 04-19 |

No packages installed in this plan.
</threat_model>

<verification>
- `git -C "/Users/andrewskinner/Local Sites/allergist-on-demand" status` shows a modified but
  uncommitted working tree; `git log -1` SHA is unchanged from the start of the plan.
- `npm test` and `npm run typecheck` in the quiz-app repo are unaffected — this plan touches no
  application source.
- `04-THEME-RECONCILIATION.md` and `04-STOREFRONT-COPY-DRAFT.md` both exist and are committed to the
  quiz-app repo.
</verification>

<success_criteria>
- A `shopify theme push` from the reconciled tree would not re-enable Klaviyo and would not alter
  Appointly.
- Every no-testing-required clause reachable from the theme repo is deleted, with a measured
  before/after count.
- Replacement copy exists but has not shipped.
- Andrew has explicitly authorized (or declined) the push.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-04-SUMMARY.md` when done.
</output>
