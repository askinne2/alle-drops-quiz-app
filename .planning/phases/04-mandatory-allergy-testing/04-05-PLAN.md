---
phase: 04-mandatory-allergy-testing
plan: 05
type: execute
wave: 2
depends_on: ["04-04"]
files_modified:
  - /Users/andrewskinner/Local Sites/allergist-on-demand
  - .planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md
autonomous: false
requirements: [TEST-06]
must_haves:
  truths:
    - "The live /pages/allergy-quiz served bytes contain zero Klaviyo occurrences after the push"
    - "Both live product pages contain zero occurrences of the no-testing-required clause"
    - "The live /pages/test-options contains no proceed-without-testing content"
    - "Every count above was taken on authenticated, cache-busted served bytes, twice"
  artifacts:
    - path: ".planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md"
      provides: "Post-push served-bytes verification section with two independent fetches per surface"
      contains: "cache-buster"
  key_links:
    - from: "reconciled theme working tree"
      to: "live Sense theme"
      via: "shopify theme push (authorized in 04-04 Task 3)"
      pattern: "shopify theme push"
---

<objective>
Ship plan 04-04's reconciled theme working tree and prove TEST-06 on live served bytes.

This plan is `autonomous: false` because its first action is irreversible against the live
storefront and is gated on the explicit authorization captured in plan 04-04 Task 3. If Andrew
declined the push in favour of theme-editor edits, this plan runs verification only.

D-14 governs every claim made here: authenticate past the storefront password first (unauthenticated
requests 302 to `/password` and return 200 for the password page, so every unauthenticated check
passes vacuously), use a cache-buster, count with `SOURCE.split(needle).length - 1` and never
`grep -c`, and treat a single fetch as insufficient evidence — the Klaviyo check on 2026-08-09
showed 10 occurrences in a fetch taken ~2 minutes before the theme save and 0 after.

Purpose: TEST-06 is structurally unautomatable inside this repo's vitest suite; served-bytes
measurement is the only evidence that counts.
Output: a pushed theme and a verification section with two independent measurements per surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md
@.planning/phases/04-mandatory-allergy-testing/04-04-SUMMARY.md
@docs/breach-response-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the reconciled theme and record the push provenance</name>
  <files>/Users/andrewskinner/Local Sites/allergist-on-demand</files>
  <read_first>
    - `.planning/phases/04-mandatory-allergy-testing/04-04-SUMMARY.md` — confirm Task 3's
      authorization was granted and read exactly what Andrew authorized. If it was declined or
      conditioned, stop and report rather than pushing.
    - `.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md` — the full drift
      inventory and deletions section, so the push's expected effect is known before it happens.
    - `.planning/STATE.md` §"Live exposures to close immediately" — the Klaviyo entry and its theme
      repo drift warning, and the note that "Sense" is the Active theme.
    - `/Users/andrewskinner/Local Sites/allergist-on-demand` `git status` and `git diff` — re-read
      the working tree immediately before pushing; do not trust 04-04's snapshot.
  </read_first>
  <action>
    Re-verify all three preconditions independently before touching the CLI — do not trust plan
    04-04's SUMMARY alone:
    1. `04-04-SUMMARY.md` records an explicit push authorization from Andrew.
    2. `git -C "<theme>" diff` still shows the Klaviyo block at `"disabled": true` and the Appointly
       block unchanged.
    3. `shopify theme list` confirms which theme is Live/Active and its ID; the target is the Active
       theme "Sense" (15.4.1). Record the theme ID.

    Take a pre-push baseline: fetch `/pages/allergy-quiz`, both product pages
    (`/products/tennessee-alledrops`, `/products/texas-alledrops`), and `/pages/test-options`
    authenticated and cache-busted, and record the occurrence counts for the needles listed in Task 2
    BEFORE the push. Without a pre-push count, a post-push zero proves nothing about whether the push
    did anything.

    Commit the theme repo working tree with a message naming the reconciliation (Klaviyo embed
    disabled to match live, orphaned template removed, D-13 clauses deleted), then run
    `shopify theme push` against the recorded Active theme ID.

    Record in `04-THEME-RECONCILIATION.md`: the theme ID, the commit SHA, the push timestamp, and the
    CLI's reported result. Do NOT treat the push's exit code as evidence that anything changed live —
    that is what Task 2 measures.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md','utf8');const c=(n)=>r.split(n).length-1;if(c('theme ID')+c('Theme ID')<1)throw new Error('theme id not recorded');if(c('pre-push')<1)throw new Error('pre-push baseline not recorded');if(c('shopify theme push')<1)throw new Error('push not recorded');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `04-04-SUMMARY.md` was read and its authorization quoted verbatim in this plan's SUMMARY.
    - The recorded theme ID matches the theme reported Live by `shopify theme list`.
    - A pre-push occurrence count exists for every needle used in Task 2, taken on authenticated,
      cache-busted served bytes.
    - `git -C "<theme>" log -1` shows exactly one new commit whose diff matches 04-04's recorded
      changes and adds no patient-facing sentence.
    - No claim in the SUMMARY rests on the `shopify theme push` exit code.
  </acceptance_criteria>
  <done>The reconciled tree is committed and pushed to the Active theme, with a pre-push baseline recorded so the post-push measurement is non-vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: Prove TEST-06 on authenticated, cache-busted served bytes — twice</name>
  <files>.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md</files>
  <read_first>
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` D-14 — the full verification
      protocol: authenticate first, cache-bust, count with `split(needle).length - 1`, never
      `grep -c`, and a single fetch is not evidence.
    - `.planning/STATE.md` — the corrected page inventory table (`/pages/test-options` 200,
      `/products/allergy-consultation` 200, `/pages/consult` 404, `/pages/testing-options` 404), the
      `apntly` vs `appointly` needle correction, and the Klaviyo stale-fetch caution.
    - `.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md` — Task 1's pre-push
      baseline counts, which every post-push count is diffed against.
  </read_first>
  <action>
    Fetch each surface twice, at least five minutes apart, authenticated past the storefront password
    and with a distinct cache-buster query parameter on each fetch. Surfaces:
    `/pages/allergy-quiz`, `/products/tennessee-alledrops`, `/products/texas-alledrops`,
    `/pages/test-options`.

    For each fetch, save the raw body to a scratch file and count these needles with a Node
    one-liner using `SOURCE.split(needle).length - 1`. Never `grep -c`.

    Absence needles, expected count 0 on every surface:
      `klaviyo`, `static.klaviyo.com`, `_klOnsite`, `gtag`, `googletagmanager`, `google-analytics`,
      `connect.facebook`, `fbq(`, `hotjar`

    Absence needles, expected count 0 on the two product pages:
      `no longer a need`, `needles`

    Absence needles, expected count 0 on `/pages/test-options`:
      the exact proceed-without-testing string(s) recorded in Task 2 of plan 04-04

    Presence needles, non-vacuity controls — a fetch that returns the password page instead of the
    real page would score 0 on everything, so every zero-count claim is void without these:
      `/pages/allergy-quiz` must contain `data-alledrops-quiz` at least once
      each product page must contain its own product handle at least once
      `/pages/test-options` must contain `Test Options` at least once

    Known-nonzero control: count `appointly` on `/pages/allergy-quiz` (NOT `apntly` — that is the
    vendor slug inside the app-block URI and reads 0 even when the script is live). Record the count.
    A nonzero value is expected and correct; it is deliberately left enabled pending a Phase 8
    decision. Recording it proves the fetch reached real page bytes.

    Append a verification section to `04-THEME-RECONCILIATION.md` containing, per surface: both fetch
    timestamps, both cache-buster values, the HTTP status, every needle with its pre-push count and
    both post-push counts, and an explicit statement that no count came from `grep -c`.

    If any absence count is nonzero on either fetch, stop. Do not re-fetch until it reads zero — a
    nonzero count means the surface is not remediated, and the correct output is a report, not a
    retry loop.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-THEME-RECONCILIATION.md','utf8');const c=(n)=>r.split(n).length-1;const need=['data-alledrops-quiz','appointly','cache-buster','no longer a need','klaviyo','split(needle).length'];for(const n of need){if(c(n)<1)throw new Error('verification section missing evidence for: '+n)}if(c('grep -c')<1)throw new Error('must state explicitly that grep -c was not used');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Every absence needle reads exactly 0 on both fetches of every applicable surface.
    - Every presence control reads >= 1 on both fetches, proving the fetch reached real page bytes
      and not the `/password` page.
    - The `appointly` count on `/pages/allergy-quiz` is recorded and nonzero, and the report states
      this is expected and Phase 8-owned.
    - Both fetches per surface are at least five minutes apart with distinct cache-busters, and both
      timestamps are recorded.
    - The report states explicitly that all counts used `split(needle).length - 1` and that `grep -c`
      was not used anywhere.
    - The pre-push count for at least one absence needle was nonzero, proving the push actually
      changed something (if every pre-push count was already 0, say so and explain why the push was
      still required).
  </acceptance_criteria>
  <done>TEST-06 is proven on live served bytes with two independent authenticated, cache-busted measurements per surface, non-vacuity controls, and a recorded pre/post diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI exit code → claimed live state | A successful push says nothing about what a patient's browser receives |
| unauthenticated fetch → claimed live state | The password page returns 200, making every unauthenticated absence check pass vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-15 | Spoofing | Verification evidence | mitigate | Task 2 requires authenticated fetches plus presence controls (`data-alledrops-quiz`, product handles, `Test Options`); a password-page response fails those controls |
| T-4-16 | Information Disclosure | Klaviyo re-enabled on a PHI page by the push | mitigate | Task 1 re-verifies the working tree diff immediately before pushing; Task 2 measures `klaviyo` / `static.klaviyo.com` / `_klOnsite` at 0 on two independent post-push fetches |
| T-4-17 | Repudiation | Stale-fetch false negative | mitigate | Two fetches at least five minutes apart with distinct cache-busters, per the 2026-08-09 incident where a fetch two minutes before the save showed 10 occurrences |
| T-4-18 | Tampering | Counting with the wrong needle or the wrong tool | mitigate | `appointly` (not `apntly`) is used as the known-nonzero control, and every count is asserted to use `split(needle).length - 1` |

No packages installed in this plan.
</threat_model>

<verification>
- `04-THEME-RECONCILIATION.md` carries a complete verification section with two fetches per surface.
- `npm test` / `npm run typecheck` in the quiz-app repo unaffected — no application source touched.
- Neither `main` nor any application file in this repo changed.
</verification>

<success_criteria>
- No live storefront surface offers or implies a path to purchase without testing, proven by
  measurement rather than by a deploy result.
- Klaviyo is still absent from the PHI-collecting page after the push.
- Appointly's state is unchanged and explicitly recorded as a Phase 8 open decision.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-05-SUMMARY.md` when done.
</output>
