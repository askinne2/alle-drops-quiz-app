---
phase: 04-mandatory-allergy-testing
plan: 10
type: execute
wave: 6
depends_on: ["04-09"]
files_modified:
  - .planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md
  - package.json
  - package-lock.json
autonomous: false
blocked_on:
  - "Blocker 1 — William agrees to test-result upload AND it is priced (owner: William / Andrew)"
  - "Blocker 2 — Fly.io BAA signed (owner: Andrew)"
  - "Blocker 3 — Production cutover to AOD's Google Cloud project (owner: William / AOD)"
requirements: [TEST-04]
must_haves:
  truths:
    - "The current status of all three Phase 4 blockers is recorded with evidence, not assumed"
    - "Real per-file and total size caps are decided by Andrew, not silently defaulted"
    - "The upload architecture (Fly-proxied vs direct-to-GCS) is decided with stated reasoning"
    - "Whether GCS dev work may proceed against alledrops-quiz is decided, not assumed"
    - "All four new packages are human-verified on their registry pages before install"
  artifacts:
    - path: ".planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md"
      provides: "Blocker status evidence, package audit, and the ratified decisions"
      contains: "MAX_FILE_BYTES"
    - path: "package.json"
      provides: "The four vetted upload dependencies"
      contains: "@google-cloud/storage"
  key_links:
    - from: ".planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md"
      to: "plans 04-11 through 04-19"
      via: "every downstream upload plan reads its constants and architecture from this file"
      pattern: "MAX_FILE_BYTES|GCS_BUCKET_NAME"
---

<objective>
⛔ **THIS PLAN AND EVERY PLAN AFTER IT IS BLOCKED.** Phase 4's upload work cannot start until three
client-side items clear (see `blocked_on` in the frontmatter). Plans 04-01 through 04-09 are the
unblocked set and can ship without any of this.

This plan is the gate. It records the actual status of the three blockers with evidence, resolves the
three open questions 04-RESEARCH.md left open, runs the package legitimacy gate on the four new
dependencies, and installs them.

The open questions are NOT for an executor to guess:
- **Size caps.** 04-UI-SPEC.md left `{N} MB` / `{M} MB` as literal placeholders and 04-RESEARCH.md's
  15MB/50MB is an unsourced engineering estimate (Assumption A2). Product decision.
- **Upload architecture.** Fly-proxied vs. direct-to-GCS signed PUT is a real fork with different
  threat models. Planner recommends Fly-proxied, with reasoning; Andrew ratifies or overrides.
- **Dev storage target.** Whether GCS engineering may proceed against Andrew's `alledrops-quiz`
  project now (mirroring the Cloud SQL dev precedent), or whether Blocker 1 gates writing any upload
  code at all. This determines whether plans 04-11+ are truly blocked or merely deploy-blocked.

Purpose: TEST-04 is this phase's core new engineering surface and it has four unresolved inputs.
Output: a decisions document every downstream plan reads, and four installed, human-verified packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@CLAUDE.md
@fly.toml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the blocker-status and package-audit evidence brief</name>
  <files>.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md</files>
  <read_first>
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` §`<blockers>` — the three
      blockers verbatim, their owners, and exactly what each blocks.
    - `.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md` §"Package Legitimacy Audit",
      §"Open Questions" 2, 3 and 4, §"Assumptions Log" A1–A5, §"Environment Availability".
    - `.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md` §Copywriting Contract — the
      three error strings containing `{N} MB` and `{M} MB` placeholders the caps decision fills.
    - `fly.toml` — the `[[vm]]` block (`memory = '1gb'`, `cpu_kind = 'shared'`, `cpus = 1`) and its
      commented `memory = '2gb'` option.
    - `.planning/STATE.md` §Blockers/Concerns — the Google Workspace / Gene / Robert escalation
      chain behind Blocker 3.
  </read_first>
  <action>
    Create `.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md` with four sections.

    **Section 1 — Blocker status.** Per blocker: owner, what it blocks, current status, and evidence
    source. Gather what is machine-checkable: `fly apps list`, `fly secrets list -a
    alle-drops-quiz-app` (names only — never print a secret value), `gcloud projects list`,
    `gcloud config get-value project`, and `gcloud storage buckets list --project alledrops-quiz`.
    Blockers 1 and 2 have no CLI evidence; mark them "Andrew-reported — pending checkpoint".

    **Section 2 — Package legitimacy audit.** For `@remix-run/form-data-parser`,
    `@google-cloud/storage`, `heic-convert`, `pdf-lib`, pull data from the LIVE registry rather than
    copying 04-RESEARCH.md: `npm view <pkg> version time.modified repository.url scripts.postinstall`
    plus the weekly download count. Include each canonical `https://npmjs.com/package/<name>` URL so
    the human can open it. Run `slopcheck scan` — **NOT `slopcheck install`**, which actually executes
    `npm install` and modifies the working tree (04-RESEARCH.md hit this during research). Record each
    verdict. Any `[SLOP]` verdict is disqualifying and stops the plan; name any `[SUS]` verdict
    explicitly. Flag Assumption A1 for the human: `pdf-lib`'s last publish is 2022-05-12, roughly four
    years stale, and requires a post-install `npm audit` rather than trusting the research-time finding.

    **Section 3 — Decisions pending.** State each as an answerable question with the planner's
    recommendation and reasoning:

    (a) *Size caps.* Recommend `MAX_FILE_BYTES = 15 * 1024 * 1024`, `MAX_TOTAL_BYTES = 50 * 1024 *
    1024`, `MAX_FILES = 10`. Reasoning: an iPhone HEIC of a paper page runs roughly 2–4 MB, so a
    four-page panel is around 16 MB; 15 MB per file leaves headroom for a high-resolution scan while
    capping single-request memory pressure on a 1 GB VM. Note these numbers substitute into three
    patient-facing error strings, so changing them later is a copy change too.

    (b) *Upload architecture.* Recommend **Fly-proxied**, one file per request, streamed via
    `@remix-run/form-data-parser` directly into a GCS resumable upload. Reasoning: it keeps
    magic-byte validation and HEIC conversion synchronous and inline in one request, the only place
    server-side content validation can happen before bytes land in storage. Direct-to-GCS requires a
    second server-side validation pass triggered by either a client "confirm upload" POST (spoofable)
    or a Pub/Sub finalize notification (new infrastructure), plus CORS on a PHI bucket. Realistic
    concurrency here is individual patients, not bulk traffic. Named mitigation for the 1 GB VM:
    bump `[[vm]] memory` to `2gb` in plan 04-17, a one-line change already documented in `fly.toml`.

    (c) *Dev storage target.* Recommend building against a bucket in Andrew's `alledrops-quiz`
    project now, mirroring the Cloud SQL dev precedent, with bucket and project driven entirely by
    `GCS_BUCKET_NAME` and `GCS_PROJECT_ID` env vars — never hardcoded — so the AOD cutover is a
    config change, not a code change. Reasoning: Blocker 3 blocks *production* storage, not
    dev-environment engineering. **But state the opposing reading too:** 04-RESEARCH.md Open
    Question 4 is explicit that Blocker 1 (William's agreement AND pricing) arguably gates writing
    any upload code at all, not just shipping it. That is commercial, not technical. Present both;
    do not pick.

    (d) *Virus scanning.* Recommend leaving it out of Phase 4 as a documented risk acceptance —
    GCP's own reference architecture needs Cloud Run + Pub/Sub + a ClamAV DB-mirror pipeline and is
    blocked on the same GCP cutover. Compensating controls in this phase: extension-independent
    magic-byte allowlist, size caps, `Content-Disposition: attachment` on every download, and never
    inline-rendering an uploaded file in a browser context.

    **Section 4 — Ratified.** Leave empty; Task 3 fills it after the checkpoint.

    Install nothing in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md','utf8');const c=(n)=>d.split(n).length-1;const req=['Blocker 1','Blocker 2','Blocker 3','@remix-run/form-data-parser','@google-cloud/storage','heic-convert','pdf-lib','npmjs.com/package','slopcheck','MAX_FILE_BYTES','GCS_BUCKET_NAME','Fly-proxied','direct-to-GCS','Assumption A1'];for(const r of req){if(c(r)<1)throw new Error('brief missing: '+r)}console.log('OK')" &amp;&amp; git diff --quiet package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - The brief records, per blocker: owner, what it blocks, current status, and evidence source
      (CLI output, or "Andrew-reported — pending checkpoint").
    - The brief records, per package: live registry version, last-publish date, repository URL,
      `scripts.postinstall` (empty for all four), weekly downloads, the `npmjs.com/package/<name>`
      URL, and the `slopcheck scan` verdict.
    - Zero packages carry a `[SLOP]` verdict; any `[SUS]` verdict is named explicitly.
    - `slopcheck install` was NOT executed — `git diff --quiet package.json package-lock.json` exits 0.
    - All four decisions are stated as answerable questions with a recommendation and reasoning, and
      decision (c) presents both readings without picking one.
    - No secret value appears anywhere in the brief (only secret *names* from `fly secrets list`).
  </acceptance_criteria>
  <done>One document carries blocker status with evidence, live registry data for four packages, and four decisions framed for a human to answer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Andrew clears the blockers, ratifies the decisions, and approves the packages</name>
  <what-built>
    `.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md` holds the CLI-gathered
    status of what could be checked for the three blockers, live npm registry data plus
    `slopcheck scan` verdicts for the four proposed dependencies, and four decisions framed with the
    planner's recommendation and reasoning. Nothing is installed. No upload code exists yet.
  </what-built>
  <how-to-verify>
    **This checkpoint is never auto-approvable.** `workflow.auto_advance` does not apply — package
    legitimacy and blocker clearance both require a human.

    1. **Blocker status.** State plainly for each: cleared or not.
       - Blocker 1: has William agreed to test-result upload AND has it been priced?
       - Blocker 2: is the Fly.io BAA signed? Patient-uploaded results are PHI in a file; they cannot
         transit or land on Fly without it.
       - Blocker 3: has the cutover to AOD's Google Cloud project happened?
       If Blocker 1 is not cleared, stop here. D-01 states plainly that Phase 4 does not ship without
       it, and no amount of engineering changes that.

    2. **Package legitimacy — open each URL and confirm it yourself; do not accept the table.**
       - https://npmjs.com/package/@remix-run/form-data-parser
       - https://npmjs.com/package/@google-cloud/storage
       - https://npmjs.com/package/heic-convert
       - https://npmjs.com/package/pdf-lib
       For each: does the name match what you expect, does the linked repository look real and
       active, is the publisher who you would expect? `pdf-lib` has not published since 2022-05-12 —
       confirm you accept a roughly four-year-stale dependency in the PHI path. Also confirm none of
       the four makes outbound network calls during PHI processing beyond the GCS API itself
       (CLAUDE.md checklist item; `@google-cloud/storage` Node client telemetry is 04-RESEARCH.md
       Assumption A3, unconfirmed — plan 04-12 verifies it with a network capture).

    3. **Size caps.** Approve 15 MB per file / 50 MB total / 10 files max, or give different numbers.
       These substitute into three patient-facing error strings, so they are copy as well as config.

    4. **Upload architecture.** Approve Fly-proxied (recommended) or choose direct-to-GCS signed PUT.
       Fly-proxied keeps content validation synchronous; direct-to-GCS moves bytes off the 1 GB VM
       but needs a second validation pass and CORS on a PHI bucket.

    5. **Dev storage target.** May GCS engineering proceed against a bucket in your `alledrops-quiz`
       project now, mirroring how Cloud SQL is used today, with bucket and project driven by env vars
       so the AOD cutover is a config change? Or does Blocker 1 gate writing any upload code at all?
       This is a commercial call, not a technical one.

    6. **Virus scanning.** Confirm you accept deferring malware scanning out of Phase 4, with
       magic-byte allowlisting, size caps, and attachment-only downloads as compensating controls.
  </how-to-verify>
  <action>Present the six numbered items from `04-UPLOAD-DECISIONS.md` to Andrew and STOP. Install nothing. This checkpoint is never auto-approvable — `workflow.auto_advance` does not apply to a package-legitimacy gate or a blocker clearance. Record every answer verbatim; Task 3 transcribes them into the document's Ratified section.</action>
  <done>Andrew has answered all six items and the answers are captured verbatim, or he has replied "blocked, do not proceed" and the upload track is held.</done>
  <resume-signal>Answer all six numbered items. Reply "packages approved" plus your answers to 3, 4, 5 and 6 to unblock Task 3 — or "blocked, do not proceed" to hold the entire upload track. If Blocker 1 or 2 is not cleared, say so and the phase stops here with plans 04-01..04-09 as the shippable set.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the ratified decisions and install the approved packages</name>
  <files>.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md, package.json, package-lock.json</files>
  <read_first>
    - `.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md` — Task 1's brief and
      the checkpoint's recorded answers.
    - `package.json` — the current `dependencies` block and the `engines` node range
      (`>=20.19 <22 || >=22.12`) the new packages must satisfy.
    - `.planning/phases/04-mandatory-allergy-testing/04-RESEARCH.md` §"Standard Stack" — version
      ranges `@remix-run/form-data-parser` `^0.17.4`, `@google-cloud/storage` `^7.21.0`,
      `heic-convert` `^2.1.0`, `pdf-lib` `^1.17.1`.
  </read_first>
  <action>
    Fill Section 4 ("Ratified") of `04-UPLOAD-DECISIONS.md` with Andrew's verbatim answers to all six
    checkpoint items and the date. Then convert the caps decision into named constants with concrete
    byte values, plus the env var names every downstream plan must use:

      MAX_FILE_BYTES, MAX_TOTAL_BYTES, MAX_FILES        (values from the checkpoint)
      GCS_BUCKET_NAME, GCS_PROJECT_ID                   (env vars — never hardcode a bucket)
      GCS_PENDING_PREFIX      = "pending/"
      GCS_PERMANENT_PREFIX    = "submissions/"
      SIGNED_URL_TTL_SECONDS  = 300
      PENDING_OLM_AGE_DAYS    = 2

    State in the same section that this file is the single source of truth for plans 04-11 through
    04-19, and that plan 04-16 must substitute the caps values into 04-UI-SPEC.md's `{N} MB` /
    `{M} MB` error strings.

    If the checkpoint declined any package, remove it from the plan and record what replaces it — do
    not install it anyway. If the checkpoint declined the whole track, stop: write the SUMMARY,
    install nothing, and report that plans 04-11+ remain blocked.

    Install the approved set with `npm install @remix-run/form-data-parser @google-cloud/storage
    heic-convert pdf-lib`. Do NOT use `slopcheck install` — it runs `npm install` as a side effect
    and is not a dry-run tool.

    Immediately after install: run `npm audit --omit=dev` and record the result, paying specific
    attention to `pdf-lib` (Assumption A1); record the resolved versions from `package-lock.json`;
    and confirm the install graph added no `postinstall` script (`npm ls --all` plus a re-check of
    `npm view <pkg> scripts.postinstall` for all four).

    Run `npm run typecheck`, `npm test`, and `npm run build` to confirm the install did not disturb
    the existing tree. Commit `package.json` and `package-lock.json` together in one commit.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; node -e "const p=require('./package.json');const need=['@remix-run/form-data-parser','@google-cloud/storage','heic-convert','pdf-lib'];const missing=need.filter(n=>!p.dependencies||!p.dependencies[n]);if(missing.length)throw new Error('not installed: '+missing.join(','));const d=require('fs').readFileSync('.planning/phases/04-mandatory-allergy-testing/04-UPLOAD-DECISIONS.md','utf8');const c=(n)=>d.split(n).length-1;for(const k of ['Ratified','MAX_FILE_BYTES','MAX_TOTAL_BYTES','MAX_FILES','GCS_BUCKET_NAME','GCS_PROJECT_ID','SIGNED_URL_TTL_SECONDS','PENDING_OLM_AGE_DAYS']){if(c(k)<1)throw new Error('constant not recorded: '+k)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Section 4 records Andrew's verbatim answers to all six checkpoint items, dated.
    - All eight named constants and env var names appear in the document with concrete values.
    - `package.json` `dependencies` contains all four approved packages at the recorded versions.
    - `npm audit --omit=dev` output is recorded in the SUMMARY, with `pdf-lib` findings called out.
    - `npm view <pkg> scripts.postinstall` is empty for all four, re-checked post-install.
    - `npm run typecheck`, `npm test`, and `npm run build` all pass.
    - No upload source file was created in this plan — the install is the only code-adjacent change.
  </acceptance_criteria>
  <done>Every downstream upload plan now has a single ratified source for caps, prefixes, TTLs, env var names, and architecture, and the four approved packages are installed with a recorded audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → PHI-path runtime | A compromised or typosquatted package would execute inside the process that handles binary PHI |
| unratified engineering estimate → patient-facing copy | Size caps appear verbatim in three error strings a patient reads while stuck |
| dev GCP project → BAA chain | A bucket outside AOD's BAA holding real PHI is a compliance failure, not a config detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SC | Tampering | `npm install` of four new dependencies | mitigate | `slopcheck scan` on all four, live registry checks, `scripts.postinstall` empty verified pre- and post-install, plus Task 2's blocking-human verification of each `npmjs.com/package` page. Never auto-approvable |
| T-4-37 | Tampering | `pdf-lib` unmaintained since 2022-05-12 (Assumption A1) | accept with control | Human accepts it explicitly at Task 2 item 2; `npm audit --omit=dev` runs at install and its result is recorded. Residual risk is bounded because pdf-lib only merges PDFs the app itself produced plus patient uploads, and never renders untrusted PDF structure |
| T-4-38 | Information Disclosure | `@google-cloud/storage` Node client telemetry (Assumption A3) | mitigate | Unconfirmed for the Node REST client; plan 04-12 verifies with a network capture. Even if present, metrics land in Cloud Monitoring inside the same BAA-covered GCP project, not a third party |
| T-4-39 | Information Disclosure | PHI landing in a bucket outside AOD's BAA | mitigate | Bucket and project are env-var-driven (`GCS_BUCKET_NAME`, `GCS_PROJECT_ID`), never hardcoded, so the AOD cutover is a config change. Task 2 item 5 makes the dev-vs-production posture an explicit human decision |
| T-4-40 | Denial of Service | Unbounded upload size on a 1 GB shared-CPU VM | mitigate | `MAX_FILE_BYTES` / `MAX_TOTAL_BYTES` / `MAX_FILES` are ratified here and enforced during streaming by plan 04-13; plan 04-17 bumps `[[vm]] memory` to 2gb |
| T-4-41 | Tampering | Malicious file content reaching a provider | accept | Task 2 item 6 records the risk acceptance. Compensating controls: magic-byte allowlist, size caps, attachment-only downloads, no inline rendering. Revisit after the GCP cutover gives Cloud Run access in the AOD project |
| T-4-42 | Information Disclosure | Secret values printed while gathering blocker evidence | mitigate | Task 1 restricts `fly secrets list` usage to names only; acceptance criteria assert no secret value appears in the brief |
</threat_model>

<verification>
- `npm run typecheck`, `npm test`, `npm run build` all green after install.
- `04-UPLOAD-DECISIONS.md` contains a Ratified section with all eight named constants.
- `npm audit --omit=dev` result recorded.
- No application source file changed in this plan.
</verification>

<success_criteria>
- No downstream plan has to guess a size cap, a bucket name, a prefix, a TTL, or an architecture.
- Four packages entered the PHI path only after a human opened each registry page.
- If a blocker is not cleared, the phase stops here cleanly with plans 04-01..04-09 shippable.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-10-SUMMARY.md` when done.
</output>
