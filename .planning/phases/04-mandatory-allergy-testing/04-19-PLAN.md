---
phase: 04-mandatory-allergy-testing
plan: 19
type: execute
wave: 11
depends_on: ["04-05", "04-18"]
files_modified:
  - .planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md
  - .planning/phases/04-mandatory-allergy-testing/04-WILLIAM-MESSAGE.md
  - HANDOFF.md
autonomous: false
blocked_on:
  - "Blocker 1 — William agrees to test-result upload AND it is priced"
  - "Blocker 2 — Fly.io BAA signed (required before any PHI file transits Fly in production)"
  - "Blocker 3 — Production cutover to AOD's Google Cloud project"
  - "Human browser pass and merge/deploy authorization from Andrew"
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07]
must_haves:
  truths:
    - "A human has clicked through the whole flow on a real phone, including a real HEIC photo"
    - "Migration 004 ran only after a verified backup, a recorded row count, and proof the app code is live"
    - "The six-item William message exists as a draft ready to send"
    - "Every ROADMAP Phase 4 success criterion is checked against measured evidence"
  artifacts:
    - path: ".planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md"
      provides: "The executed browser-pass script with per-check results"
      contains: "HEIC"
    - path: ".planning/phases/04-mandatory-allergy-testing/04-WILLIAM-MESSAGE.md"
      provides: "Six-item draft for William and counsel"
      contains: "UNCONFIRMED"
  key_links:
    - from: "migrations/004_create_submission_files.sql"
      to: "alledrops_quiz_dev"
      via: "executed only after backup + row count + proof the app code is live on Fly"
      pattern: "004_create_submission_files"
---

<objective>
⛔ **BLOCKED** — see `blocked_on`. This is the phase's closing plan: the human browser pass, the
migration execution, the deploy, and the William message.

**A green suite is not the finish line on this phase.** Five defects have shipped past a fully green
suite on this project and all five were caught by a human clicking. Three were wiring bugs that
Phase 3's DOM infrastructure now catches. Two were judgment failures no structural test can catch:
a question that read as redundant, and a required field that trapped a healthy patient. D-02 puts a
required file upload on the single highest-abandonment step in the entire flow — structurally that
second defect, one step further along, with a materially worse cost. A patient who gets stuck loses a
fully-completed questionnaire, and resume is out of scope for v1.0.

Run the browser pass against **local, not production**. A full run through to submit writes a PHI row.

Purpose: closing every TEST requirement against measured evidence rather than a passing suite.
Output: an executed UAT record, an executed migration with recorded preconditions, a deployed app, and
a drafted client message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md
@.planning/phases/04-mandatory-allergy-testing/04-VALIDATION.md
@.planning/phases/04-mandatory-allergy-testing/04-UI-SPEC.md
@HANDOFF.md
@CLAUDE.md
@migrations/004_create_submission_files.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human browser pass on a real device</name>
  <what-built>
    The full Phase 4 flow: Part 7's two-option testing split with three text fields and a required
    multi-file upload, consent between Part 7 and the results screen for every bracket, a terminal
    results screen with no callback props, uploaded files retrievable from the admin, the patient
    ledger and the clinical PDF, and the storefront copy remediated and verified on served bytes.
  </what-built>
  <how-to-verify>
    Run local, NOT production — a full run writes a PHI row.
    `SHOPIFY_APP_URL=http://localhost:3000 npx react-router dev`, then
    `http://localhost:3000/quiz-embed`. `npm run dev` will NOT work: it is `shopify app dev` and
    blocks on an interactive store prompt. The page nests an iframe — query
    `document.querySelector('iframe').contentDocument`, not the top document.

    Record a pass/fail and a note for each of these fourteen checks in
    `.planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md`:

    1. Part 7 shows exactly two options, no skip, and Next is disabled until one is chosen.
    2. "I need allergy testing" reveals no extra fields; the flow continues to consent.
    3. "I've already had allergy testing" reveals year, location, allergens, and the upload widget,
       and the guidance block's second paragraph tells you how to finish today without your results.
    4. On a real phone at 375px or narrower, pick a real HEIC photo from the camera roll. Watch the
       row go uploading -> uploaded. **Is the state readable without relying on the colour cue alone?**
    5. Attempt a `.txt` renamed to `.pdf`. Does the exact UI-SPEC wrong-type sentence render, prefixed
       with ⚠, rather than a generic browser error?
    6. Attempt a file over the per-file cap. Same question for the too-large sentence, with the real
       MB number substituted — not `{N}`.
    7. Attempt Next with zero uploaded files. Does "⚠ Add at least one file to continue." render?
    8. Pick files twice. Does the second pick APPEND rather than replace?
    9. Upload a file on `had_testing`, flip to `needs_testing`, flip back. Is the file list intact?
       Then flip to `needs_testing` and submit — confirm the staged object is not linked.
    10. Complete the flow with a 0–2 scoring answer set. **Does ConsentStep render?** This path never
        showed consent before; it is the live defect D-09 closes.
    11. On the results screen after choosing `needs_testing`: is "Schedule Allergy Testing" present,
        and is the AlleDrops product link ABSENT? Hiding it is a planner-ratified decision, not a
        CONTEXT.md lock — **override it here if you disagree.**
    12. Mobile sticky-header clearance — the one open item from Phase 1's `01-HUMAN-UAT.md`, now
        measurable for the first time because plan 04-04 reconciled the theme repo.
    13. Log into a customer account with at least one submission and confirm the `quiz-history` ledger
        paints and lists it, with a download link per uploaded file. **10-minute check, not a
        refactor** — the extension already calls the right API.
    14. Watch `fly logs -a alle-drops-quiz-app` (or local console) during an upload-heavy run. Any OOM
        kills at the new 2gb VM size?

    Judgment questions no test can answer, and the real reason this pass exists — answer them in
    prose, not as checkboxes:
    - Standing in an exam room with your phone, uncertain whether your photo uploaded: does this UI
      tell you? Does the error copy read as forgiving or as blame?
    - Does anything in Part 7 read as redundant against Part 6, the way DIAG-01 duplicated HIST-01?
    - Is there any state where a patient who genuinely cannot produce a file is stuck with no way
      forward other than abandoning?
  </how-to-verify>
  <action>Start the local dev server (`SHOPIFY_APP_URL=http://localhost:3000 npx react-router dev`), create `.planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md` pre-populated with the fourteen checks and the three prose questions, hand it to Andrew, and STOP. Do not run this against production — a full run to submit writes a PHI row. Do not proceed to Task 2 until every check has a recorded result and every found defect is closed.</action>
  <done>All fourteen checks and the three prose answers are recorded in 04-HUMAN-UAT.md and any defect found has been closed.</done>
  <resume-signal>Record all fourteen results plus the three prose answers in 04-HUMAN-UAT.md, then reply "UAT complete" with any defects found. Any defect stops the phase here and is closed before Task 2 — this is exactly the point at which the last five defects would have been caught.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute migration 004 behind four independently re-verified preconditions</name>
  <files>.planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md</files>
  <read_first>
    - `migrations/004_create_submission_files.sql` — its own header, which lists the execution
      preconditions plan 04-11 recorded.
    - `.planning/STATE.md` — the Phase 3 03-06 and 03-07 entries recording exactly how this was done
      last time: backup ID 1786306233540 read back via `gcloud sql backups list`/`describe` rather
      than trusted from an exit code, and DROP COLUMN executed only after independently re-verifying
      all four preconditions.
    - `.planning/phases/04-mandatory-allergy-testing/04-CONTEXT.md` §`<specifics>` — "The
      `submissions` table is test data only... If a real patient has completed the quiz by the time
      Phase 4 runs, any migration decision must stop and be re-raised."
    - `CLAUDE.md` §"Testing the submission pipeline E2E" — the Cloud SQL access route, and the note
      that Prisma on the Fly machine is the SQLite session store, not the PHI database.
  </read_first>
  <action>
    Re-verify all four preconditions independently. Do not trust any earlier SUMMARY.

    1. **Backup.** Take a named on-demand Cloud SQL backup of `alledrops-quiz-data`. Read the result
       back with `gcloud sql backups list` and `gcloud sql backups describe`, and record the backup
       ID and a status of `SUCCESSFUL`. An exit code is not evidence.
    2. **Row count and patient check.** Record `SELECT COUNT(*) FROM submissions` immediately before
       any DDL. Also determine whether any row looks like a real patient rather than test data. If it
       does, **stop and re-raise** per CONTEXT.md — do not proceed on your own judgment. Select counts
       only; never read, print, or record a PHI field value.
    3. **Merge state.** Confirm the Phase 4 branch is merged to `main` and record the merge SHA.
    4. **App code live.** Confirm the deployed Fly release actually serves the new code, measured on
       served bytes rather than inferred from a deploy result — the same standard Phase 3 held for
       release v50.

    Only with all four recorded, execute `migrations/004_create_submission_files.sql` against
    `alledrops_quiz_dev`. Then verify: `submission_files` exists with all eight columns; its grants
    are `SELECT, INSERT` only; both indexes exist; the `submission_access_log` `action` CHECK now
    accepts `'file'`; and `SELECT COUNT(*) FROM submissions` is unchanged from the pre-DDL figure.

    Finally, prove the write path survived: complete one synthetic submission end to end with one
    uploaded file, then confirm exactly one new `submissions` row and one new `submission_files` row
    by count. Delete the synthetic rows and the promoted GCS object afterwards, and record the
    before/after counts. Never print a PHI field value at any point.

    Append all of this to `04-HUMAN-UAT.md` under a Migration Execution heading.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('.planning/phases/04-mandatory-allergy-testing/04-HUMAN-UAT.md','utf8');const c=(n)=>d.split(n).length-1;for(const k of ['SUCCESSFUL','backup','COUNT(*)','submission_files','merge','served bytes']){if(c(k)<1)throw new Error('migration record missing: '+k)}console.log('OK')" &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - A backup ID and a `SUCCESSFUL` status are recorded, read back via `gcloud sql backups describe`.
    - A pre-DDL `submissions` row count is recorded, and a post-DDL count matching it exactly.
    - A statement is recorded on whether any row appears to be a real patient, with the decision taken.
    - The merge SHA is recorded, and the live-code check is described in terms of served bytes.
    - `submission_files` exists with eight columns and `SELECT, INSERT` grants only; verified by query,
      not assumed from the migration file.
    - The access-log CHECK accepts `'file'`, verified by an actual insert with that value.
    - The synthetic end-to-end submission produced exactly one new row in each table, and both were
      cleaned up with counts recorded.
    - No PHI field value appears anywhere in the record — counts and IDs only.
  </acceptance_criteria>
  <done>The table exists in the dev database, created only after a verified backup, a recorded row count, a merged branch, and live-code proof, with the write path proven end to end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Draft the William message and authorize merge and deploy</name>
  <what-built>
    Phase 4 is complete in the repo: Part 7, the required upload with GCS staging and promotion, all
    three retrieval surfaces, the consent-first flow, the terminal results screen, the storefront
    remediation verified on served bytes, migration 004 executed against dev with recorded
    preconditions, and the human browser pass recorded in `04-HUMAN-UAT.md`.
  </what-built>
  <how-to-verify>
    First, review the drafted `.planning/phases/04-mandatory-allergy-testing/04-WILLIAM-MESSAGE.md`.
    It carries six items — five inherited from Phase 3 plus one new:
      1. **Test-result upload** — agreement and pricing. This blocked the whole phase (D-01).
      2. Domain spelling — no longer blocks Phase 4; still blocks LAUNCH-07.
      3. DIAG-01 scope — confirms rather than blocks.
      4. HIST-03's third label — unconfirmed copy in `questions.ts`.
      5. HIST-02's medications gate copy.
      6. **New:** the D-13 storefront replacement copy from `04-STOREFRONT-COPY-DRAFT.md`, plus
         D-11's interim consent paragraph for counsel review.
    Every copy item is marked UNCONFIRMED in code and none of it is presented as approved.

    Then check each ROADMAP Phase 4 success criterion against the recorded evidence:
      1. Every patient reaches the testing step before their score, two choices, no way past it.
      2. "I need allergy testing" routes to the storefront testing-options page; "I've already had
         allergy testing" collects year, location, and allergens.
      3. **This criterion is stale.** It reads "no file upload exists anywhere" — reversed by D-01.
         Read it as: a patient with existing results uploads them, required, before continuing.
      4. No surface offers or implies a path to purchase without testing: both bypasses gone,
         `ResultsDisplay` terminal, and the product-page and test-options copy remediated and verified
         on authenticated cache-busted served bytes.
      5. Every completed submission passed through consent with a recorded `consent_version`.

    Finally, decide:
    - May the Phase 4 branch be merged to `main`? (CLAUDE.md's default is that you merge; PRs #16–19
      were merged by Claude under your explicit in-session authorization.)
    - May `fly deploy -a alle-drops-quiz-app` run? **This puts binary PHI handling into production.
      Confirm the Fly.io BAA is signed before authorizing** — that is Blocker 2 and it has no
      technical workaround.
    - Is production storage pointed at a bucket inside AOD's BAA-covered GCP project, or is the app
      still pointed at the `alledrops-quiz` dev project? If dev, the deploy must not receive real
      patient traffic.
  </how-to-verify>
  <action>Draft `.planning/phases/04-mandatory-allergy-testing/04-WILLIAM-MESSAGE.md` with the six items listed above, sourcing item 6 from `04-STOREFRONT-COPY-DRAFT.md` and the D-11 consent paragraph from `ConsentStep.tsx`. Present it alongside the five ROADMAP success criteria and the evidence for each, then STOP. Do not merge and do not run `fly deploy` before the authorization is recorded. After authorization, perform the merge and deploy, then update `HANDOFF.md` with the phase state, remaining blockers, and next steps.</action>
  <done>The William message is drafted, every ROADMAP criterion is checked against recorded evidence, and Andrew has either authorized merge and deploy or held it with a stated reason.</done>
  <resume-signal>Reply "merge and deploy authorized" plus your confirmation on the Fly BAA and the production bucket, or name what must change first. If the Fly BAA is not signed, reply "hold deploy" — the code stays merged and unshipped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a green suite → shipped behaviour | Five defects have crossed this boundary on this project already |
| dev environment → production PHI | Deploying binary PHI handling without the Fly BAA is a compliance failure, not a config detail |
| DDL → a database holding clinical records | Migration execution is the only irreversible database action in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-99 | Denial of Service | A required upload trapping a patient, losing a completed questionnaire | mitigate | Task 1 checks 3, 7 and 9 plus the three prose judgment questions. This is defect-5's shape and no structural test catches it |
| T-4-100 | Tampering | Migration executed against a database holding real patient data | mitigate | Task 2 precondition 2 requires a real-patient determination and a stop-and-re-raise if one is found, per CONTEXT.md `<specifics>` |
| T-4-101 | Denial of Service | Irreversible DDL with no recovery path | mitigate | A named on-demand backup with its ID and `SUCCESSFUL` status read back via `gcloud sql backups describe`, never trusted from an exit code |
| T-4-102 | Information Disclosure | Binary PHI transiting Fly without a signed BAA | mitigate | Task 3 makes the Fly BAA an explicit precondition of deploy authorization, with "hold deploy" as a first-class outcome |
| T-4-103 | Information Disclosure | Production traffic landing in a non-BAA dev bucket | mitigate | Task 3 requires an explicit statement of which GCP project production storage points at, and that a dev-pointed deploy must not receive real patient traffic |
| T-4-104 | Information Disclosure | PHI values read or printed during migration verification | mitigate | Task 2 restricts every query to `COUNT(*)` and IDs, matching the Phase 3 PHI-cleanup precedent |
| T-4-105 | Tampering | Unapproved clinical copy shipping as approved | mitigate | Every copy item is UNCONFIRMED in code, `CONSENT_VERSION` carries a `draft-` prefix, and the William message presents all six items for approval. LAUNCH-03 remains the owner of final language |
| T-4-106 | Repudiation | A ROADMAP criterion marked met against a stale requirement | mitigate | Task 3 explicitly flags criterion 3 as stale (reversed by D-01) and restates how to read it, rather than silently passing it |

No packages installed in this plan.
</threat_model>

<verification>
- `npm test` full suite green, `npm run typecheck` clean, `npm run build` and `npm run build:theme`
  clean.
- `04-HUMAN-UAT.md` records fourteen check results, three prose answers, and the migration execution
  block with backup ID, status, and matching row counts.
- `04-WILLIAM-MESSAGE.md` exists with all six items.
- `HANDOFF.md` updated with the phase's current state, the blockers that remain, and the next steps.
</verification>

<success_criteria>
- Every TEST-01…TEST-07 requirement is closed against measured evidence, not against a passing suite.
- A human has used the flow on a real phone with a real HEIC photo and answered the three judgment
  questions in prose.
- Nothing reached production without an explicit statement that the Fly BAA is signed and that
  storage sits inside a BAA-covered project.
</success_criteria>

<output>
Create `.planning/phases/04-mandatory-allergy-testing/04-19-SUMMARY.md` when done.
</output>
