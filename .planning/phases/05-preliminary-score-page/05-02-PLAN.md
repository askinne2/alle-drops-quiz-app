---
phase: 05-preliminary-score-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/styles/quiz.module.css
autonomous: true
requirements: [SCORE-03]
tags: [css-modules, scale-bar, legacy-cleanup]

must_haves:
  truths:
    - "The four legacy severity tone classes and the chip's three wrapper classes no longer exist in the stylesheet"
    - "Five tone tokens exist, so a fourth or fifth zone needs zero new CSS"
    - "Zone colour is selected by a data attribute, never by a per-bracket class name"
    - "A visible section boundary separates the scale bar from the recommendation"
  artifacts:
    - path: "app/styles/quiz.module.css"
      provides: "The .scaleBar__* class family, five --quiz-color-tone-* tokens, and the retirement of the legacy severity chip"
      contains: ".scaleBar__zone[data-tone="
  key_links:
    - from: "app/styles/quiz.module.css"
      to: "the zone tone tokens"
      via: "attribute selector on data-tone"
      pattern: "\\.scaleBar__zone\\[data-tone="
    - from: ".scaleBar__legendItem[data-current='true']"
      to: "the current-zone non-colour signal"
      via: "font-weight 700"
      pattern: "data-current="
---

<objective>
Retire the legacy severity chip's CSS and build the `.scaleBar__*` class family the new scale bar
renders through, exactly as `05-UI-SPEC.md` Component Inventory §2 and §3 specify.

Purpose: D-07 replaces four hardcoded per-bracket colour classes with an arbitrary-N tone scale
driven by a data attribute, so three-versus-four zones stops being a code decision. D-06's two-axis
separation needs a real visible boundary, not just a heading.
Output: seven legacy classes deleted, five tone tokens added, and the `.scaleBar__*` rules with their
mobile-first `@media (min-width: 750px)` overrides.

**Deliberate transient state:** this plan deletes the legacy classes while `ResultsDisplay.tsx` still
references them (plan 05-03 removes those references). For one wave the retired chip renders
unstyled. Nothing breaks — no test asserts on those class names, and CSS Modules yields `undefined`
for a missing key rather than a type error (there is no generated `.d.ts` enumerating class names in
this repo). The full suite and typecheck must still be proven green, which is Task 1's acceptance
criterion.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-preliminary-score-page/05-CONTEXT.md
@.planning/phases/05-preliminary-score-page/05-UI-SPEC.md
@.planning/phases/05-preliminary-score-page/05-PATTERNS.md
@app/styles/quiz.module.css

<interfaces>
<!-- Exact current stylesheet facts, measured 2026-08-11. Do not re-derive. -->

`app/styles/quiz.module.css` regions this plan touches:

  lines 15-34     :root token block. Ends with --quiz-color-success #4CAF50 (31),
                  --quiz-color-warning #FF9800 (32), --quiz-color-error #F44336 (33).
  lines 948-1004  The legacy chip: .quizResults__severity (948) + its @media (956-960),
                  .quizResults__severityLabel (962) + @media (968-973),
                  .quizResults__severityValue (975) + @media (982-987),
                  the "/* Severity color classes ... */" comment (989), and the four tone classes
                  .quizResults__severityValueMinimal (990), ...Mild (994), ...Moderate (998, #FF5722),
                  ...Severe (1002).
  lines 295-303   .questionCategory__title — the border-bottom: 2px solid
                  rgba(var(--color-foreground, 32, 34, 35), 0.1) divider treatment to reuse.
  lines 613-627   .quizNavigation__button — the existing font-weight: 600 (Label role source).
  line 1111+      .quizResults__scoreCircle — RETAINED, do not touch.

Values to reuse verbatim for `.scaleBar`'s container treatment (they are the deleted
`.quizResults__severity`'s own values — same visual container, new content inside it):
  background-color: rgba(var(--color-foreground, 32, 34, 35), 0.03);
  border-radius: var(--quiz-border-radius);
  padding: var(--quiz-spacing-md);   /* mobile */  -> var(--quiz-spacing-lg) at >= 750px

Out of scope, deliberately — do NOT delete these even though they are also orphaned:
  lines 938-946   .quizResults__scoreMax (+ @media) — unreferenced by ResultsDisplay.tsx.
  lines 1006-1017 .quizResults__severityDescription (+ @media) — unreferenced by ResultsDisplay.tsx.
  Neither is named by D-07 or by 05-UI-SPEC.md's Interaction Contract Summary, which lists exactly
  seven classes for deletion. Record both in the SUMMARY as observed orphans for a future cleanup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the seven legacy severity classes and add the five tone tokens</name>
  <files>app/styles/quiz.module.css</files>
  <read_first>
    - `app/styles/quiz.module.css` lines 1-50 (file header conventions and the `:root` token block)
    - `app/styles/quiz.module.css` lines 930-1020 (the exact deletion region and its orphan neighbours)
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Color (the five tone token values and the mid-high hex-reuse rationale)
  </read_first>
  <action>
    Delete exactly seven class rules and their `@media (min-width: 750px)` override blocks from the
    948-1004 region: `.quizResults__severity`, `.quizResults__severityLabel`,
    `.quizResults__severityValue`, `.quizResults__severityValueMinimal`,
    `.quizResults__severityValueMild`, `.quizResults__severityValueModerate`,
    `.quizResults__severityValueSevere`. Delete the
    `/* Severity color classes - these are specific accent colors */` comment with them.

    Leave `.quizResults__scoreMax` (938-946) and `.quizResults__severityDescription` (1006-1017) in
    place. Both are unreferenced orphans, neither is named for deletion by D-07 or the UI-SPEC's
    seven-class list, and widening the deletion beyond the locked list is scope creep. Record both in
    the SUMMARY instead.

    Add five custom properties to the existing `:root` block immediately after `--quiz-color-error`
    (line 33), under a comment stating these are the scale-bar zone tones (D-07) and that five slots
    ship so Phase 5.1 can add a fourth or fifth zone without new CSS:
    `--quiz-color-tone-low: var(--quiz-color-success, #4CAF50)`,
    `--quiz-color-tone-low-mid: #CDDC39`,
    `--quiz-color-tone-mid: var(--quiz-color-warning, #FF9800)`,
    `--quiz-color-tone-mid-high: #FF5722`,
    `--quiz-color-tone-high: var(--quiz-color-error, #F44336)`.

    On the `--quiz-color-tone-mid-high` line, add a comment recording that `#FF5722` is the exact hex
    the deleted `.quizResults__severityValueModerate` used, so D-07's cleanup introduces zero
    net-new hex values in that slot.

    Add no `.scaleBar__*` rule in this task — that is Task 2, so the deletion and the addition land as
    two independently reviewable hunks.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/styles/quiz.module.css','utf8');const c=n=>s.split(n).length-1;const gone=['severityValueMinimal','severityValueMild','severityValueModerate','severityValueSevere','severityLabel','severityValue'];const bad=gone.filter(n=>c(n)!==0);if(bad.length)throw new Error('legacy remains: '+bad.join(','));if(c('--quiz-color-tone-')<5)throw new Error('fewer than 5 tone tokens');if(c('severityDescription')<2)throw new Error('out-of-scope orphan deleted');if(c('scoreMax')<2)throw new Error('out-of-scope orphan deleted');console.log('ok')" && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Occurrence counts in `app/styles/quiz.module.css`, measured with `split(needle).length - 1` and never `grep -c`: `severityValueMinimal` 0, `severityValueMild` 0, `severityValueModerate` 0, `severityValueSevere` 0, `severityLabel` 0, `severityValue` 0.
    - `--quiz-color-tone-` occurs at least 5 times (the five declarations).
    - `severityDescription` and `scoreMax` each still occur at least twice (rule plus `@media` override) — the two out-of-scope orphans survived.
    - `npm run typecheck` exits 0 and `npm test` exits 0 despite `ResultsDisplay.tsx` still referencing the deleted keys — this is the transient state proven harmless, and the SUMMARY must state that it was measured, not assumed.
    - `git diff app/styles/quiz.module.css` shows no change to `.quizResults__scoreCircle`, `.quizResults__message`, `.quizResults__recommendation`, or `.quizResults__disclaimer`.
  </acceptance_criteria>
  <done>Seven legacy classes are gone, five tone tokens exist, both out-of-scope orphans survive, and the suite plus typecheck are green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the .scaleBar__* class family, the five data-tone selectors, and the D-06 section boundary</name>
  <files>app/styles/quiz.module.css</files>
  <read_first>
    - `app/styles/quiz.module.css` lines 290-310 (the `.questionCategory__title` divider treatment being reused)
    - `app/styles/quiz.module.css` lines 610-630 (the `font-weight: 600` Label-role source)
    - `app/styles/quiz.module.css` lines 930-1010 (the post-deletion region where the new family goes)
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Component Inventory 2 and 3, §Spacing Scale, §Typography, §Responsive Behavior
  </read_first>
  <action>
    Insert the whole `.scaleBar__*` family into the gap left by Task 1's deletion (immediately after
    `.quizResults__scoreMax`'s `@media` block and before `.quizResults__severityDescription`), so the
    new bar's rules sit where the retired chip's rules were. Follow the file's existing convention
    throughout: base rule first, then its own `@media (min-width: 750px)` override block immediately
    after, one property per line, spacing via the `--quiz-spacing-*` tokens only.

    `.scaleBar` — the block container. `background-color: rgba(var(--color-foreground, 32, 34, 35), 0.03)`,
    `border-radius: var(--quiz-border-radius)`, `padding: var(--quiz-spacing-md)` rising to
    `var(--quiz-spacing-lg)` at 750px. These are the deleted `.quizResults__severity`'s exact values,
    reused rather than reinvented.

    `.scaleBar__axisRow` — `display: flex`, `justify-content: space-between`,
    `align-items: baseline`, `margin-bottom: var(--quiz-spacing-xs)`.
    `.scaleBar__axisLabel` — `font-size: 1.2rem` rising to `1.4rem`, `font-weight: 600`,
    `line-height: 1.3`, `font-family: var(--font-body-family, inherit)`,
    `color: rgba(var(--color-foreground, 32, 34, 35), 0.75)`.
    `.scaleBar__value` — same sizes and line-height as the axis label but `font-weight: 700` and full
    `rgb(var(--color-foreground, 32, 34, 35))`; this is the number the patient came for.

    `.scaleBar__track` — the OUTER positioning element. `position: relative`, `height: 12px` rising to
    `16px`. It must carry no `overflow` and no `border-radius`. Add a comment stating that both moved
    to `.scaleBar__zones` deliberately, and that clipping this element would slice the marker into a
    half-moon at score 0 and at score max — the blocking finding UI-SPEC revision 1 fixed.

    `.scaleBar__zones` — the INNER clipped wrapper. `display: flex`, `width: 100%`, `height: 100%`,
    `border-radius: var(--quiz-border-radius)`, `overflow: hidden`.

    `.scaleBar__zone` — `height: 100%`, `min-width: 0`, and
    `border-right: 2px solid rgb(var(--color-foreground, 32, 34, 35))`. Add
    `.scaleBar__zone:last-child { border-right: none; }` so the seam appears only between zones. Do
    not declare `flex` here — the proportional `flex: <span> 0 0` is set inline per zone by the
    component from the config data (plan 05-03), because the spans are data, not style.

    The five tone selectors, all shipped even though the provisional default exercises only three
    (this is what makes the mechanism arbitrary-N rather than arbitrary-N-up-to-3):
    `.scaleBar__zone[data-tone="low"]`, `[data-tone="low-mid"]`, `[data-tone="mid"]`,
    `[data-tone="mid-high"]`, `[data-tone="high"]`, each setting `background-color` to the matching
    `--quiz-color-tone-*` token from Task 1. These are the file's first attribute selectors; standard
    CSS, no adaptation needed.

    `.scaleBar__marker` — `position: absolute`, `top: 50%`, `width` and `height` `14px` rising to
    `18px`, `border-radius: 50%`, `background: rgb(var(--color-background, 255, 255, 255))`,
    `border: 3px solid rgb(var(--color-foreground, 32, 34, 35))`, `box-shadow: var(--quiz-shadow)`,
    `transform: translate(-50%, -50%)`, `pointer-events: none`. Do not declare `left` — the component
    sets it inline from the score. Add no `transition` and no `animation`: UI-SPEC §5 rules out
    animating the marker so a final value never reads as a loading state.

    `.scaleBar__legend` — `display: flex`, `margin-top: var(--quiz-spacing-xs)`.
    `.scaleBar__legendItem` — `min-width: 0`, `text-align: center`, `white-space: nowrap`,
    `overflow: hidden`, `text-overflow: ellipsis`, `font-size: 1.0rem` rising to `1.2rem`,
    `font-weight: 400`, `line-height: 1.3`,
    `color: rgba(var(--color-foreground, 32, 34, 35), 0.75)`. Like the zones, its proportional `flex`
    is set inline by the component. The colour must never be tinted to match its zone's tone — zone
    identity is carried by the word and its position, which is what satisfies WCAG 1.4.1 here.

    `.scaleBar__legendItem[data-current="true"] { font-weight: 700; }` — the current-zone highlight.
    Add a comment recording that the current zone is flagged with a `data-current` attribute rather
    than a modifier class, for the same reason `data-tone` is an attribute: CSS Modules hashes class
    names, so a jsdom test can assert an attribute but cannot reliably assert a generated class name
    or a computed font-weight. This mechanism choice is a planner ratification of UI-SPEC's
    "the current legend item gets font-weight: 700" requirement, consistent with its own `data-tone`
    precedent.

    `.scaleBar__meaningSection` — the D-06 boundary wrapper.
    `border-top: 2px solid rgba(var(--color-foreground, 32, 34, 35), 0.1)`,
    `padding-top: var(--quiz-spacing-md)` rising to `var(--quiz-spacing-lg)`,
    `margin-top: var(--quiz-spacing-lg)` rising to `var(--quiz-spacing-xl)`. The border is the same
    treatment `.questionCategory__title` already establishes, applied as `border-top`. Add a comment
    stating this line IS the visual implementation of D-06's two-axes requirement, so it is not
    decorative and must not be removed as visual noise.

    `.scaleBar__meaningHeading` — `font-size: 1.2rem` rising to `1.4rem`, `font-weight: 600`,
    `line-height: 1.3`, `margin: 0 0 var(--quiz-spacing-sm)`,
    `color: rgba(var(--color-foreground, 32, 34, 35), 0.75)`. Deliberately the same visual weight as
    `.scaleBar__axisLabel`: D-06's point is that the two read as parallel axis labels, not as a
    primary and a secondary heading.

    Introduce no new spacing token, no new font weight beyond 400/600/700, no new breakpoint, and no
    use of `rgb(var(--color-button, ...))` anywhere in this family — the accent colour is reserved for
    interactive controls and the bar is data, not a call to action.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/styles/quiz.module.css','utf8');const c=n=>s.split(n).length-1;const need=['.scaleBar {','.scaleBar__axisRow','.scaleBar__axisLabel','.scaleBar__value','.scaleBar__track','.scaleBar__zones','.scaleBar__zone','.scaleBar__marker','.scaleBar__legend','.scaleBar__legendItem','.scaleBar__meaningSection','.scaleBar__meaningHeading','[data-current=\"true\"]'];const missing=need.filter(n=>c(n)===0);if(missing.length)throw new Error('missing: '+missing.join(','));for(const t of ['low','low-mid','mid','mid-high','high'])if(c('[data-tone=\"'+t+'\"]')===0)throw new Error('missing tone selector '+t);const track=s.slice(s.indexOf('.scaleBar__track'),s.indexOf('.scaleBar__zones'));if(/overflow/.test(track))throw new Error('track must not clip');if(c('--color-button')<1)console.log('note');console.log('ok')" && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All twelve `.scaleBar__*` selectors plus `[data-current="true"]` occur at least once, counted with `split(needle).length - 1`.
    - All five `[data-tone="..."]` selectors are present, including `low-mid` and `mid-high`, which the provisional 3-zone default does not exercise.
    - The source text between `.scaleBar__track` and `.scaleBar__zones` contains no `overflow` declaration — the marker-clipping regression guard at the CSS level.
    - `.scaleBar__zones` declares both `overflow: hidden` and `border-radius`.
    - Neither `.scaleBar__zone` nor `.scaleBar__legendItem` declares a `flex` shorthand with a numeric grow value — the proportional spans are set inline by the component from config data.
    - No rule in the `.scaleBar__*` family references `--color-button`.
    - `npm run typecheck` exits 0, `npm test` exits 0.
  </acceptance_criteria>
  <done>The full `.scaleBar__*` family exists with five tone selectors, an unclipped track, an attribute-driven current-zone highlight, and the D-06 border boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | A stylesheet edit. No input is parsed, no data crosses a boundary, no script is added. This is a PHI-collecting surface, so the relevant control is what is NOT added: no `@import`, no remote font, no remote image, no third-party URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07 | Information Disclosure (third-party asset on a PHI page) | `app/styles/quiz.module.css` | mitigate | CLAUDE.md rule 4 forbids third-party requests from any PHI-collecting surface. This plan adds only literal hex values and existing `var()` tokens — no `@import`, no `url()`, no remote font. Verified by reviewing the diff for `@import` and `url(` occurrences before commit. |
| T-5-08 | Tampering (clinical misreading through a broken visual) | `.scaleBar__track` / `.scaleBar__zones` overflow split | mitigate | Clipping on the wrong element renders the marker as a half-moon at score 0, which is a genuine minimal-symptom outcome and not an edge case. Mitigated by Task 2's source assertion that no `overflow` declaration appears between `.scaleBar__track` and `.scaleBar__zones`, plus the DOM structural guard in plan 05-04 and the human visual check in 05-06. |
| T-5-09 | Tampering (accessibility regression) | `.scaleBar__legendItem` colour | mitigate | Tinting legend text to match its zone would make hue the only channel conveying zone identity, failing WCAG 1.4.1 on a clinical surface. Mitigated by specifying a fixed foreground colour for every legend item plus the `data-current` bold weight as an independent second channel. |
| T-5-10 | Denial of Service / availability | Deleting a class still referenced by a component | accept | The transient unstyled chip lasts one wave, is never deployed, and cannot throw — CSS Modules returns `undefined` for a missing key and React renders `class="undefined"`. Accepted with the explicit requirement that `npm test` and `npm run typecheck` are proven green in the transient state rather than assumed. |
| T-5-11 | Injection / XSS (ASVS V5, V12) | n/a | accept | No script, no HTML sink, no interpolated string. The attribute selectors match values that originate from a compiled-in union type in `score-scale.ts`, never from user input. |
</threat_model>

<verification>
- `npm test` — full suite green in the transient state (legacy classes deleted, component not yet updated).
- `npm run typecheck` — exit 0.
- `git diff --stat` touches only `app/styles/quiz.module.css`.
- No `@import` or `url(` added anywhere in the diff.
- `public/quiz-bundle.css` is NOT rebuilt in this plan — plan 05-05 owns the single rebuild, so the
  freshness markers are measured once against a final source tree.
</verification>

<success_criteria>
- Seven legacy severity classes and their media blocks are gone; the two unrelated orphans survive.
- Five `--quiz-color-tone-*` tokens exist and five `[data-tone]` selectors consume them.
- `.scaleBar__track` does not clip; `.scaleBar__zones` does.
- The current-zone highlight is attribute-driven and testable under jsdom.
- The D-06 section boundary exists as a real border, reusing the established divider treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/05-preliminary-score-page/05-02-SUMMARY.md`, recording the
measured legacy-class occurrence counts (before and after), the two out-of-scope orphans left in
place, and confirmation that the suite was green in the transient state.
</output>
