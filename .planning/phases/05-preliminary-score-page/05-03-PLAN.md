---
phase: 05-preliminary-score-page
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - app/components/quiz/ResultsDisplay.tsx
  - tests/quiz-testing-bypass-deletion.test.ts
  - tests/quiz-resume-write-gate.test.ts
  - tests/quiz-resume-payload-parity.test.ts
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
tags: [results-display, copy, scale-bar, coupled-tests]

must_haves:
  truths:
    - "The results page is titled Preliminary Score and states the 1-2 business day clinical-review sentence"
    - "The Symptom Score bracket chip no longer renders anywhere"
    - "A colour-banded bar above the score shows the patient's linear position on the full derived range"
    - "The bar's colour comes from the raw score's zone, never from the clinical bracket"
    - "The recommendation sits under its own What this means for you heading, separated by a visible boundary"
    - "The three band explanations and the disclaimer render with their text unchanged"
  artifacts:
    - path: "app/components/quiz/ResultsDisplay.tsx"
      provides: "Preliminary Score header, the scale-bar block, and the D-06 meaning section"
      contains: "Preliminary Score"
    - path: "tests/quiz-testing-bypass-deletion.test.ts"
      provides: "Repointed subtitle positive control, terminal-component guard still green"
  key_links:
    - from: "app/components/quiz/ResultsDisplay.tsx"
      to: "app/lib/quiz/score-scale.ts"
      via: "getScoreScale() called once in the component body"
      pattern: "getScoreScale\\(\\)"
    - from: "the rendered zone elements"
      to: "the zone tone tokens"
      via: "data-tone attribute per zone"
      pattern: "data-tone"
    - from: "the marker element"
      to: "the raw score"
      via: "inline left percentage against scale.max"
      pattern: "left:"
---

<objective>
Turn `ResultsDisplay` into the Preliminary Score page: change the two header strings, delete the
bracket chip, render the data-driven scale bar, wrap the recommendation in its own labelled section,
and repoint the three existing test assertions that are coupled to the retiring copy.

Purpose: this is the patient-visible deliverable for all three requirements. SCORE-01 is the copy,
SCORE-02 surfaces the derived ceiling in the readout, SCORE-03 is the bar itself.
Output: one rewritten component region, one new section wrapper, and three repointed test assertions
so the suite stays green on the new strings rather than the retired ones.

**Hard locks this plan must not violate:**
- `ResultsDisplayProps` stays byte-identical. No new prop, and above all no callback prop — that is
  Phase 4's TEST-05 terminal-component invariant, guarded by `tests/quiz-testing-bypass-deletion.test.ts`.
- `getScoreScale()` is called internally, never threaded in as a prop (05-RESEARCH.md Pitfall 5).
- The bar's tone and the `scoreBracket` conditional share no variable, no helper, and no class name
  (D-05/D-06, 05-RESEARCH.md Pitfall 4). A `7+` patient scoring 7 of 60 must render a low-tone bar.
- The three band explanations and the disclaimer are copy-frozen (D-09). Their JSX may be reparented
  into the new section wrapper; their text may not change by one character.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-preliminary-score-page/05-CONTEXT.md
@.planning/phases/05-preliminary-score-page/05-UI-SPEC.md
@.planning/phases/05-preliminary-score-page/05-RESEARCH.md
@.planning/phases/05-preliminary-score-page/05-PATTERNS.md
@app/components/quiz/ResultsDisplay.tsx
@app/lib/quiz/score-scale.ts
@app/components/quiz/QuizProgress.tsx

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these. -->

From `app/lib/quiz/score-scale.ts` (created by plan 05-01):
    export type ScaleTone = "low" | "low-mid" | "mid" | "mid-high" | "high";
    export interface ScaleZone { upTo: number; tone: ScaleTone; label: string }
    export interface ScoreScale { max: number; zones: ScaleZone[]; isProvisional: true }
    export function getScoreScale(): ScoreScale        // no arguments

  Phase 5 runtime values: max 60; zones [{20,"low","Low"},{40,"mid","Moderate"},{60,"high","High"}].

From `app/components/quiz/ResultsDisplay.tsx` — prop interface, UNCHANGED by this plan:
    export interface ResultsDisplayProps {
      score: number;
      scoreBracket: ScoreBracket;                      // "0-2" | "3-6" | "7+"
      patientState: "tennessee" | "texas";
      symptomProfileId: string;
      testingStatus: "needs_testing" | "had_testing";
    }

CSS Module keys available after plan 05-02 (consume via `styles.<key>`):
    scaleBar, scaleBar__axisRow, scaleBar__axisLabel, scaleBar__value, scaleBar__track,
    scaleBar__zones, scaleBar__zone, scaleBar__marker, scaleBar__legend, scaleBar__legendItem,
    scaleBar__meaningSection, scaleBar__meaningHeading

  Zone colour is selected by the `data-tone` attribute, the current legend item by
  `data-current="true"` — both attributes, not class names, because CSS Modules hashes class names
  and jsdom cannot assert a hashed name or a computed font-weight reliably.

Inline-style precedent in this codebase (proportional/computed values are set inline, not in CSS):
    app/components/quiz/QuizProgress.tsx:14 — style={{ width: `${fillPct}%` }}

Locked strings, verbatim:
    h2        "Preliminary Score"
    subtitle  "Our Clinical Team is reviewing your information, and will send you email confirmation of your final results within the next 1-2 business days."
    axis      "Symptom burden"
    readout   `${score} of ${scale.max}`
    heading   "What this means for you"
    aria      `Symptom burden position: ${currentZone.label.toLowerCase()} zone, ${score} of ${scale.max} on a 0 to ${scale.max} scale.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCORE-01 header copy, chip retirement, and the scale-bar render</name>
  <files>app/components/quiz/ResultsDisplay.tsx</files>
  <read_first>
    - `app/components/quiz/ResultsDisplay.tsx` (full file — lines 80-81 are the copy, 90-99 the chip being replaced, 163 and 186 the inline-accessor call convention)
    - `app/lib/quiz/score-scale.ts` (the shape actually shipped by plan 05-01)
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Component Inventory 2, §Accessibility Contract, §Interaction Contract Summary
    - `.planning/phases/05-preliminary-score-page/05-RESEARCH.md` Pitfalls 4 and 5
    - `app/components/quiz/QuizProgress.tsx` (the inline computed-style precedent)
  </read_first>
  <action>
    Add a fifth lib import, `import { getScoreScale } from "../../lib/quiz/score-scale";`, alongside
    the existing `redirects`/`product-links`/`navigation` imports at the top of the file.

    Change line 80's `h2` text from "Your Assessment Results" to "Preliminary Score" and line 81's
    subtitle from "Your responses have been submitted." to the locked sentence in this plan's
    `<interfaces>` block, including its trailing period. Keep both `className` values
    (`quizResults__title`, `quizResults__subtitle`) exactly as they are — this is a copy change, not a
    structural one.

    Near the top of the component body, after the `copied` state and before `handleCopy`, compute
    three values once: `const scale = getScoreScale();`, the current zone as
    `scale.zones.find((z) => score <= z.upTo) ?? scale.zones[scale.zones.length - 1]`, and a
    `markerPercent` guarding a zero or negative `scale.max` by falling back to 0 rather than emitting
    `NaN%`. Do not call `getScoreScale()` more than once and do not memoize it — it returns a module
    constant, and 05-CONTEXT.md leaves the memoization question to discretion; a single call is the
    cheapest correct answer.

    Delete the entire chip block currently at lines 90-99 — the `quizResults__severity` wrapper, the
    `quizResults__severityLabel` span reading "Symptom Score:", and the `quizResults__severityValue`
    span with its three-way class ternary on `scoreBracket`. Nothing from it survives (D-08). The
    score circle immediately above it (`quizResults__scoreContainer`, `quizResults__scoreCircle`,
    `quizResults__scoreNumber`) is retained untouched — neither D-08 nor D-09 names it for removal and
    it still serves the large-focal-number role the bar's small readout does not.

    In its place render the scale bar, nested exactly as follows and no other way. Outermost is a
    `div` with `styles.scaleBar`. Its first child is a `div` with `styles.scaleBar__axisRow`
    containing two spans: `styles.scaleBar__axisLabel` reading "Symptom burden", and
    `styles.scaleBar__value` reading the score-of-max readout. Its second child is a `div` with
    `styles.scaleBar__track`, carrying `role="img"` and the locked `aria-label` template. That track
    has exactly two children, in this order and at this level: first a `div` with
    `styles.scaleBar__zones` and `aria-hidden="true"`, holding one `div` per config zone; second a
    `div` with `styles.scaleBar__marker` and `aria-hidden="true"`. The marker is a SIBLING of the
    zones wrapper, never a descendant of it — the zones wrapper is the only element that clips, and
    placing the marker inside it slices it into a half-moon at score 0 and at score max, which is the
    blocking defect UI-SPEC revision 1 exists to prevent. The track's third and final sibling, outside
    it, is a `div` with `styles.scaleBar__legend` holding one span per zone.

    Each zone `div` carries `key={zone.tone + zone.upTo}`, `className={styles.scaleBar__zone}`,
    `data-tone={zone.tone}`, and an inline `style` setting `flex` to the string
    `"<span> 0 0"` where `<span>` is `zone.upTo` minus the previous zone's `upTo` (0 for the first
    zone). Because the spans sum to `scale.max`, flexbox lays the zones out proportionally at any
    container width with no resize listener. Each legend span carries the same computed `flex`
    value so each label sits under its own segment, plus `className={styles.scaleBar__legendItem}`,
    the zone's `label` as its text, and `data-current="true"` on the current zone only (omit the
    attribute entirely on the others rather than emitting `data-current="false"`).

    The marker carries an inline `style` setting `left` to `markerPercent` as a percentage string.
    Leave it unclamped per UI-SPEC — it is safe precisely because the marker is not clipped.

    Render the zones and the legend by mapping over `scale.zones`. A fixed three-way or four-way JSX
    branch is a D-07 violation even if it produces identical output today.

    Critical semantic rule, restated because it is the single easiest thing to get wrong here: every
    value feeding the bar — zone widths, `data-tone`, `data-current`, the marker position, and the
    `aria-label` — derives from `score` and `scale` only. The `scoreBracket` prop must not appear
    anywhere inside the `styles.scaleBar` subtree, and the current zone must not be passed to, or
    derived from, anything that also decides which recommendation block renders. The proof that this
    is right is that a patient at score 7 with bracket `7+` shows a low-tone bar and the SLIT
    recommendation at the same time; plan 05-04 asserts exactly that.

    Change nothing else in the file. The actions area, the Symptom Profile ID block, and the
    disclaimer keep their classes, copy, and conditional logic. Add no prop, add no callback, add no
    `useEffect`, and add no animation or transition.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "const s=require('fs').readFileSync('app/components/quiz/ResultsDisplay.tsx','utf8');const c=n=>s.split(n).length-1;if(c('Preliminary Score')!==1)throw new Error('title');if(c('1-2 business days')!==1)throw new Error('subtitle');if(c('Your Assessment Results')!==0)throw new Error('old title remains');if(c('Symptom Score:')!==0)throw new Error('chip label remains');if(c('severityValue')!==0)throw new Error('chip classes remain');if(c('getScoreScale()')!==1)throw new Error('accessor call count');if(c('data-tone')<1||c('role=\"img\"')!==1)throw new Error('bar markup');const bar=s.slice(s.indexOf('styles.scaleBar}'),s.indexOf('styles.scaleBar__legend'));if(/scoreBracket/.test(bar))throw new Error('D-05 violation: scoreBracket inside the scale bar');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/quiz/ResultsDisplay.tsx` contains "Preliminary Score" exactly once and the full locked subtitle sentence exactly once, both measured with `split(needle).length - 1`.
    - "Your Assessment Results", "Your responses have been submitted.", "Symptom Score:", and `severityValue` each occur 0 times.
    - `getScoreScale()` occurs exactly once — called internally, in the component body.
    - `role="img"` occurs exactly once; `data-tone` occurs at least once; `aria-hidden="true"` occurs at least twice (the zones wrapper and the marker).
    - The source slice from `styles.scaleBar}` to `styles.scaleBar__legend` contains zero occurrences of `scoreBracket`.
    - `git diff app/components/quiz/ResultsDisplay.tsx` shows `ResultsDisplayProps` unchanged, no new prop, and no `on[A-Z]` callback identifier added.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The header reads Preliminary Score with the review sentence, the chip is gone, and a data-driven bar renders with an unclipped marker and no bracket coupling.</done>
</task>

<task type="auto">
  <name>Task 2: The D-06 meaning section wrapping the three unedited recommendation blocks</name>
  <files>app/components/quiz/ResultsDisplay.tsx</files>
  <read_first>
    - `app/components/quiz/ResultsDisplay.tsx` lines 102-142 (the three conditional blocks, post-Task-1 line numbers may shift)
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Component Inventory 3
    - `.planning/phases/05-preliminary-score-page/05-CONTEXT.md` D-06 and D-09
  </read_first>
  <action>
    Wrap the three `scoreBracket === "0-2" | "3-6" | "7+"` conditional blocks in a single `div` with
    `className={styles.scaleBar__meaningSection}`, opening immediately before the `0-2` conditional and
    closing immediately after the `7+` conditional. As the wrapper's first child, before the three
    conditionals, render one `p` element with `className={styles.scaleBar__meaningHeading}` reading
    "What this means for you".

    The heading renders exactly once for every bracket — it is a sibling of the three conditionals, not
    a child of any of them, and it must not be duplicated inside each branch. Use a `p`, not an `h3` or
    `h4`: the three band explanations already own an `h3` each, and inserting a heading element above
    them would change this screen's heading hierarchy, which is not something D-09's structural
    minimum authorises.

    The three conditional blocks themselves are copy-frozen under D-09. Reparenting them into the new
    wrapper is the only permitted change: same conditions, same `quizResults__recommendation` and
    `quizResults__message` classes, same `h3` text, same paragraph text down to the character. Do not
    reflow, rewrap, or reindent their prose while moving them — a reindent that changes a line break
    inside a JSX text node changes the rendered whitespace and makes the D-09 verbatim assertions in
    plan 05-04 fail for a reason that looks like a copy edit.

    Add a short comment above the wrapper recording that the border on `.scaleBar__meaningSection` is
    D-06's two-axes boundary and that the heading is deliberately the same visual weight as the bar's
    axis label, so a future tidy-up does not "promote" it to a heading element or delete the border as
    decoration.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "const s=require('fs').readFileSync('app/components/quiz/ResultsDisplay.tsx','utf8');const c=n=>s.split(n).length-1;if(c('What this means for you')!==1)throw new Error('heading must render once');if(c('scaleBar__meaningSection')!==1)throw new Error('section wrapper count');for(const h of ['Your Symptoms Appear Mild and Well-Controlled','You May Benefit From Seeing an Allergist','Sublingual Immunotherapy May Significantly Help You'])if(c(h)!==1)throw new Error('band heading changed: '+h);if(c('This assessment is a clinical symptom screening tool.')!==1)throw new Error('disclaimer changed');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - "What this means for you" occurs exactly once in the file, and `scaleBar__meaningSection` exactly once.
    - The three band `h3` strings each occur exactly once, unchanged.
    - The disclaimer sentence "This assessment is a clinical symptom screening tool." occurs exactly once, unchanged.
    - `git diff` for the three conditional blocks shows indentation-only changes plus the new wrapper lines — zero changes to any JSX text node's words or internal line breaks.
    - The heading is a `p` element, not `h1`-`h6`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>One meaning section with one heading wraps all three unedited recommendation blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the three test assertions coupled to the retired copy</name>
  <files>tests/quiz-testing-bypass-deletion.test.ts, tests/quiz-resume-write-gate.test.ts, tests/quiz-resume-payload-parity.test.ts</files>
  <read_first>
    - `tests/quiz-testing-bypass-deletion.test.ts` lines 1-60 and 150-184 (the source-text guard header and the `ResultsDisplay` describe block, including the subtitle positive control at 180-182)
    - `tests/quiz-resume-write-gate.test.ts` lines 200-215 (the post-submit results assertion)
    - `tests/quiz-resume-payload-parity.test.ts` lines 260-272 (the `submitAndAwaitResults` helper)
  </read_first>
  <action>
    Three existing assertions are coupled to strings SCORE-01 retires. They were green before Task 1
    and are RED after it. Repoint each to the new locked string rather than deleting it — each exists
    as a positive control that would otherwise silently stop proving anything.

    In `tests/quiz-testing-bypass-deletion.test.ts`, the `it` at roughly line 180 asserts
    `"Your responses have been submitted."` is present in `ResultsDisplay.tsx`'s source. Change both
    its needle and its `it` title to the new subtitle sentence — the 1-2 business day clinical-review
    copy. Keep it in the same describe block, keep it a `toBeGreaterThan(0)` presence assertion, and
    add a one-line comment stating that the original needle was the pre-Phase-5 subtitle, retired by
    SCORE-01, and that the assertion's purpose is unchanged: proving the file still renders a
    submission-confirmation line rather than having lost it in a refactor. Leave every other assertion
    in that file alone — the five callback-prop and bypass-copy absence checks are Phase 4's TEST-05
    invariant and must stay exactly as they are.

    In `tests/quiz-resume-write-gate.test.ts`, the `waitFor` that queries `getByText("Your Assessment
    Results")` as the "submission landed on the results screen" signal becomes `getByText("Preliminary
    Score")`. Do not change what the test then asserts about `localStorage`.

    In `tests/quiz-resume-payload-parity.test.ts`, the identical query inside `submitAndAwaitResults`
    becomes `getByText("Preliminary Score")`. Nothing else in that file changes — its payload-parity
    assertions are Phase 4.2's D-10 deliverable.

    Do not add new assertions in this task. Coverage of the new markup belongs to plan 05-04's
    dedicated DOM test; this task only keeps the existing suite honest about the new strings.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-testing-bypass-deletion.test.ts tests/quiz-resume-write-gate.test.ts tests/quiz-resume-payload-parity.test.ts && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-testing-bypass-deletion.test.ts tests/quiz-resume-write-gate.test.ts tests/quiz-resume-payload-parity.test.ts` exits 0.
    - `npm test` exits 0 with a test count at or above the 677 baseline recorded in STATE.md, and zero skipped tests added.
    - `node -e "const {execSync}=require('child_process');const out=execSync('git grep -c \"Your Assessment Results\" -- app tests || true').toString();if(out.trim())throw new Error('retired string still referenced: '+out);console.log('ok')"` succeeds — no file under `app/` or `tests/` references the retired title.
    - The five absence assertions in `tests/quiz-testing-bypass-deletion.test.ts`'s `ResultsDisplay` describe block are byte-identical to their pre-change text.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>All three coupled assertions target the new locked strings, the full suite is green, and the retired title appears nowhere in app or tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process props → rendered DOM | `score` and `scoreBracket` are already-validated in-process values computed upstream by `calculateTotalScore`/`getScoreBracket`. This plan renders them plus compiled-in zone labels. No new external input crosses into the component. |
| component → PHI-collecting page | `ResultsDisplay` renders inside the quiz iframe, a PHI-collecting surface governed by CLAUDE.md rule 4. The control is what must not be added: no script, no dependency, no remote asset, no analytics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-12 | Tampering (clinically misleading display) | The bar's tone selection in `ResultsDisplay.tsx` | mitigate | Wiring `data-tone` from `scoreBracket` would render 90% of the bar red and place a patient scoring 7 of 60 deep in it — the outcome D-05 exists to prevent. Mitigated by Task 1's source assertion that `scoreBracket` appears nowhere in the `styles.scaleBar` subtree, and by plan 05-04's behavioural assertion that score 7 with bracket `7+` renders a low tone. |
| T-5-13 | Tampering (loss of the terminal-component invariant) | `ResultsDisplayProps` | mitigate | Reintroducing any callback prop reopens the purchase-without-testing path Phase 4 closed (TEST-05). Mitigated by keeping the prop interface byte-identical, by `tests/quiz-testing-bypass-deletion.test.ts`'s five untouched absence assertions, and by the diff review in Task 1's acceptance criteria. |
| T-5-14 | Injection / XSS (ASVS V5.3, V12) | The `aria-label` template and the score readout | mitigate | Both interpolate only a number (`score`, `scale.max`) and a zone `label` originating from a compiled-in constant typed against a five-member union — never a patient-supplied string. React's default JSX and attribute escaping applies. No `dangerouslySetInnerHTML`, no markdown renderer, no HTML sink is introduced; Phase 1 closed a reflected XSS on this exact page and that posture is preserved. |
| T-5-15 | Information Disclosure (PHI in logs) | `ResultsDisplay.tsx` | mitigate | No `console.*` call may be added. The existing `console.warn` in `navigateParent` logs a navigation path only and is not touched. `score` and `scoreBracket` are not PHI per CLAUDE.md's field list, but they must still not be logged. Verified by diff review. |
| T-5-16 | Information Disclosure (accessible-name leak of the clinical bracket) | The track's `aria-label` | mitigate | Naming the clinical bracket in the accessible name would reintroduce the bracket/bar coupling for screen-reader users only. Mitigated by the locked template, which uses the zone's display label and never the `0-2`/`3-6`/`7+` values. |
| T-5-17 | Repudiation (a green suite that proves nothing) | The three repointed assertions | mitigate | Deleting a coupled assertion instead of repointing it silently drops a positive control. Mitigated by Task 3 repointing all three and by the `git grep` check proving the retired string is referenced nowhere. |
| T-5-18 | Elevation of Privilege | n/a | accept | No auth, session, or access-controlled resource is touched. ASVS V2/V3/V4 have no applicable control (05-RESEARCH.md §Security Domain). |
</threat_model>

<verification>
- `npm test` — full suite green, count at or above 677.
- `npm run typecheck` — exit 0.
- `npm run build` — exit 0 (the React Router build, proving the new import resolves in the app build too).
- `git grep "Your Assessment Results" -- app tests` returns nothing.
- `public/quiz-bundle.js` / `.css` are NOT rebuilt here — plan 05-05 owns the single rebuild.
</verification>

<success_criteria>
- The page title is "Preliminary Score" and the subtitle is the locked 1-2 business day sentence.
- The bracket chip and its CSS references are gone from the component.
- The bar renders one zone per config entry with a correct `data-tone`, a proportional width, an
  unclipped marker positioned from the raw score, and a legend whose current item is attribute-flagged.
- "What this means for you" renders once above the three unedited recommendation blocks.
- The prop interface is unchanged and no callback prop exists.
- The full suite is green on the new strings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-preliminary-score-page/05-03-SUMMARY.md`, recording the
measured occurrence counts for every locked and retired string, and confirming the marker's parent is
the `role="img"` track rather than the clipped zones wrapper.
</output>
