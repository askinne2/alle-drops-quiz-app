---
phase: 05-preliminary-score-page
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - tests/quiz-results-scale-bar-dom.test.ts
  - tests/quiz-testing-bypass-deletion.test.ts
  - .planning/phases/05-preliminary-score-page/05-VALIDATION.md
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
tags: [dom-tests, jsdom, accessibility, compliance-copy]

must_haves:
  truths:
    - "A rendering test proves the bar's tone comes from the raw score and not the clinical bracket"
    - "A rendering test proves the marker is not a descendant of the clipped zones wrapper"
    - "A rendering test proves the accessible name carries score, ceiling, and zone without naming the bracket"
    - "Score 0 and score max both render without error"
    - "No approval-or-purchase promise exists in any band message, proven by assertion rather than by reading"
    - "No patient-facing text describes the band values as provisional"
  artifacts:
    - path: "tests/quiz-results-scale-bar-dom.test.ts"
      provides: "The repo's first dedicated ResultsDisplay DOM test — SCORE-01/02/03 rendering proof"
      min_lines: 120
    - path: "tests/quiz-testing-bypass-deletion.test.ts"
      provides: "D-10 no-approval-promise source guard alongside the existing TEST-05 absence checks"
  key_links:
    - from: "tests/quiz-results-scale-bar-dom.test.ts"
      to: "app/components/quiz/ResultsDisplay.tsx"
      via: "render(React.createElement(ResultsDisplay, props))"
      pattern: "React.createElement\\(ResultsDisplay"
    - from: "tests/quiz-results-scale-bar-dom.test.ts"
      to: "app/lib/quiz/score-scale.ts"
      via: "getScoreScale() imported so assertions track config rather than hardcoding three zones"
      pattern: "getScoreScale"
---

<objective>
Prove the new page renders what the phase promised, with tests that fail for the right reasons rather
than passing vacuously — and close D-10 with an assertion instead of a reading.

Purpose: this project has shipped six defects past a fully green suite, every one of them in code no
test rendered. `ResultsDisplay` has never had a dedicated test file. The scale bar's two most
dangerous failure modes — tone wired from the clinical bracket, and a marker clipped into a half-moon
at score 0 — are both invisible to a source-text guard and both cheap to catch in jsdom.
Output: the repo's first `ResultsDisplay` DOM test, a D-10 source guard, and a filled-in validation map.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-preliminary-score-page/05-CONTEXT.md
@.planning/phases/05-preliminary-score-page/05-UI-SPEC.md
@.planning/phases/05-preliminary-score-page/05-RESEARCH.md
@.planning/phases/05-preliminary-score-page/05-VALIDATION.md
@app/components/quiz/ResultsDisplay.tsx
@app/lib/quiz/score-scale.ts
@tests/quiz-part-renderer-dom.test.ts

<interfaces>
<!-- DOM test infrastructure facts. Do not rediscover these; getting them wrong costs a config change. -->

`vitest.config.ts` include glob is ["app/**/*.test.ts", "tests/**/*.test.ts"] — it does NOT match
`.test.tsx`. The new file MUST be `.test.ts`, elements MUST be built with `React.createElement`
rather than JSX, and the file MUST open with the pragma comment `// @vitest-environment jsdom` on
line 1. Widening the glob is out of scope (see `tests/quiz-part-renderer-dom.test.ts` lines 34-37).

Precedent file to copy the infrastructure shape from: `tests/quiz-part-renderer-dom.test.ts`
  line 1        // @vitest-environment jsdom
  lines 40-46   imports from "vitest", "@testing-library/react", "react", then the subject
  lines 48-50   afterEach(() => { cleanup(); })
  lines 60-68   a local render helper returning the render utils

Subject under test:
  import { ResultsDisplay } from "../app/components/quiz/ResultsDisplay";
  props: { score: number; scoreBracket: "0-2"|"3-6"|"7+"; patientState: "tennessee"|"texas";
           symptomProfileId: string; testingStatus: "needs_testing"|"had_testing" }

Config source of truth for assertions:
  import { getScoreScale } from "../app/lib/quiz/score-scale";
  Phase 5 values: max 60; zones [{20,"low","Low"},{40,"mid","Moderate"},{60,"high","High"}]

Query strategy — CSS Modules class names are hashed at build time, so assertions must go through
roles, text, and data attributes, never through a literal class-name string:
  container.querySelector('[role="img"]')            the track
  container.querySelectorAll('[data-tone]')          the zones, in document order
  container.querySelector('[data-current="true"]')   the current legend item
  the marker: the track's child element that is NOT the zones wrapper (the zones wrapper is the one
  containing the [data-tone] children)

Locked strings this test asserts against:
  "Preliminary Score"
  "Our Clinical Team is reviewing your information, and will send you email confirmation of your final results within the next 1-2 business days."
  "Symptom burden"          "What this means for you"
  "Your Symptoms Appear Mild and Well-Controlled"          (bracket 0-2)
  "You May Benefit From Seeing an Allergist"               (bracket 3-6)
  "Sublingual Immunotherapy May Significantly Help You"    (bracket 7+)
  "This assessment is a clinical symptom screening tool."  (disclaimer, counsel-owned)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ResultsDisplay scale-bar DOM test</name>
  <files>tests/quiz-results-scale-bar-dom.test.ts</files>
  <read_first>
    - `tests/quiz-part-renderer-dom.test.ts` lines 1-100 (jsdom pragma, imports, cleanup, render-helper shape, and the `.test.ts` discipline comment)
    - `app/components/quiz/ResultsDisplay.tsx` (the markup as plan 05-03 actually shipped it)
    - `app/lib/quiz/score-scale.ts`
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Accessibility Contract and §Component Inventory 2
  </read_first>
  <behavior>
    - SCORE-01 copy: the `h2` text is "Preliminary Score"; the locked subtitle sentence is present; "Your Assessment Results", "Your responses have been submitted.", and "Symptom Score:" are all absent from the rendered output.
    - SCORE-02 readout: with score 7 the readout text is "7 of 60", and the 60 is asserted against `getScoreScale().max` rather than the literal — so a future scored question moves the expectation automatically.
    - SCORE-03 zone rendering: the count of `[data-tone]` elements equals `getScoreScale().zones.length`, and their `data-tone` values in document order equal `getScoreScale().zones.map(z => z.tone)`.
    - SCORE-03 proportional widths: each zone's inline `flex` grow value equals its `upTo` minus the previous zone's `upTo`, and the grow values sum to `getScoreScale().max`.
    - D-05 decoupling (the load-bearing assertion): rendered with score 7 and `scoreBracket: "7+"`, the current legend item reads "Low" AND the `7+` band heading is present. The same render must not produce a high tone as the current zone.
    - D-05 second sample: score 45 with `scoreBracket: "7+"` puts the current zone at "High" — proving the current-zone lookup actually tracks the score rather than being constant.
    - Structural regression guard: the marker element's `parentElement` is the same node as the `[role="img"]` track, and is NOT the element holding the `[data-tone]` children.
    - Accessible name: the track's `aria-label` contains "Symptom burden position", the score, the ceiling, and the current zone's label — and contains none of "0-2", "3-6", "7+".
    - Normal-flow announcement: the element containing the "7 of 60" readout has no ancestor carrying `aria-hidden="true"`.
    - Edge scores: score 0 renders with a marker `left` of "0%" and score 60 with "100%", neither throwing, and both still rendering exactly one `[data-current="true"]` element.
    - D-06: "What this means for you" appears exactly once, and appears for all three bracket values.
    - D-09 verbatim: for each of the three brackets, its band heading renders exactly as locked, and the disclaimer sentence renders in all three cases.
    - D-04: the rendered `textContent` matches nothing case-insensitively resembling "provisional".
  </behavior>
  <action>
    Create `tests/quiz-results-scale-bar-dom.test.ts` with `// @vitest-environment jsdom` as line 1.
    Import `afterEach`, `describe`, `expect`, `it` from "vitest"; `cleanup`, `render`, `screen` from
    "@testing-library/react"; `React` from "react"; `ResultsDisplay` from
    "../app/components/quiz/ResultsDisplay"; and `getScoreScale` from
    "../app/lib/quiz/score-scale". Register `afterEach(() => { cleanup(); })`.

    Add a file header comment explaining why this file exists in this project's terms: `ResultsDisplay`
    had no dedicated test before Phase 5, six prior defects on this project shipped past a green suite
    from exactly that kind of gap, and the two failure modes this file targets — tone derived from the
    clinical bracket, and a marker clipped by the zones wrapper — are invisible to a source-text guard.
    Include the `.test.ts`-not-`.test.tsx` note and the reason (the vitest include glob), copying the
    substance of `tests/quiz-part-renderer-dom.test.ts`'s own explanation.

    Write one local helper, `renderResults(overrides)`, that renders `ResultsDisplay` through
    `React.createElement` with a baseline prop set (`score: 7`, `scoreBracket: "7+"`,
    `patientState: "tennessee"`, `symptomProfileId: "AOD_TEST_0001"`,
    `testingStatus: "had_testing"`) merged with per-test overrides, and returns the render utils. Use
    a synthetic profile ID and no realistic patient identifiers — this is a PHI-adjacent component and
    a test fixture must not carry anything resembling a real name, DOB, email, or phone.

    Write a second helper that, given the render container, returns the track element, the zones
    wrapper, the marker, the ordered zone elements, and the current legend item. Derive the marker as
    the track child that is not the zones wrapper, rather than by class name — the class name is
    hashed. Derive the zones wrapper as the track child that contains the `[data-tone]` elements.

    Organise the assertions into one `describe` per invariant, following `scoring.test.ts`'s
    describe-per-invariant style: SCORE-01 copy, SCORE-02 derived readout, SCORE-03 zone rendering and
    proportional widths, D-05 bar/bracket independence, DOM structure (the marker-parent guard), the
    accessibility contract, edge scores, and D-06/D-09/D-04 copy invariants. Every numeric expectation
    about the ceiling or the zone count must read from `getScoreScale()`, never from a literal 60 or a
    literal 3 — a test that hardcodes the ceiling reintroduces the rot SCORE-02 exists to remove.

    Parse each zone's inline flex grow value from its `style.flex` (or `style.flexGrow`) rather than
    asserting the whole style string, so a formatting difference in how React serialises the shorthand
    does not fail the test for the wrong reason. If `style.flex` reads back empty under jsdom, fall
    back to reading the `style` attribute text and extracting the leading number — and record which
    read worked in the SUMMARY, because plan 05-05's marker choices and any future refactor depend on
    knowing.

    For the D-05 assertion, add a comment naming it as the phase's load-bearing test: the whole reason
    D-05, this bar's design, and Phase 5.1's split exist is that bracket-driven colour would render a
    patient at 7 of 60 deep in red. A future refactor that "simplifies" by deriving the tone from
    `scoreBracket` must fail here.

    Prove non-vacuity before finishing. Temporarily edit `app/components/quiz/ResultsDisplay.tsx` in
    the working tree to break each of the three highest-value guards in turn — move the marker inside
    the zones wrapper, then derive the current zone from `scoreBracket`, then revert the `h2` to the old
    title — and confirm the corresponding assertion fails each time. Restore the file exactly (verify
    with `git diff --exit-code app/components/quiz/ResultsDisplay.tsx`) and record all three observed
    failure messages in the SUMMARY. Do not commit any of the temporary mutations.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-results-scale-bar-dom.test.ts && npm run typecheck && git diff --exit-code app/components/quiz/ResultsDisplay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-results-scale-bar-dom.test.ts` exits 0 with at least 15 passing assertions across the describe blocks named in the action.
    - Line 1 of the file is exactly `// @vitest-environment jsdom`, and the filename ends `.test.ts`, not `.test.tsx`.
    - `node -e "const s=require('fs').readFileSync('tests/quiz-results-scale-bar-dom.test.ts','utf8');if(/\btoBe\(60\)/.test(s))throw new Error('hardcoded ceiling');if(s.split('getScoreScale').length-1<3)throw new Error('assertions must read config');console.log('ok')"` succeeds.
    - The three non-vacuity mutations each produced a real failure, and all three failure messages are quoted in the SUMMARY.
    - `git diff --exit-code app/components/quiz/ResultsDisplay.tsx` exits 0 — no temporary mutation was left behind.
    - `npm test` exits 0 and the suite count rose by at least 15 over the pre-plan count.
  </acceptance_criteria>
  <done>The DOM test exists, covers every behavior bullet, reads the ceiling and zone count from config, and has been proven to fail when the three guarded properties are broken.</done>
</task>

<task type="auto">
  <name>Task 2: Close D-10 with an assertion, and fill in the validation map</name>
  <files>tests/quiz-testing-bypass-deletion.test.ts, .planning/phases/05-preliminary-score-page/05-VALIDATION.md</files>
  <read_first>
    - `app/components/quiz/ResultsDisplay.tsx` — the three band-message paragraphs in full
    - `tests/quiz-testing-bypass-deletion.test.ts` lines 1-60 and 150-184 (the source-guard needle convention and the `ResultsDisplay` describe block)
    - `.planning/phases/05-preliminary-score-page/05-CONTEXT.md` D-10
    - `.planning/phases/05-preliminary-score-page/05-RESEARCH.md` §"D-10 Verification"
    - `.planning/phases/05-preliminary-score-page/05-VALIDATION.md`
  </read_first>
  <action>
    First, actually re-read the three band messages in the current `ResultsDisplay.tsx` — post-05-03,
    not the version RESEARCH.md read — and confirm none of them promises the patient will be able to
    purchase if approved. Record the reading in the SUMMARY as a quoted finding per bracket, naming
    the specific clause that carries the conditional, clinician-mediated framing in the `7+` message.
    If any message does contain a promise, stop and report rather than rewriting it: the band copy is
    frozen under D-09 and a rewrite needs William, not an executor.

    Then convert that reading into a standing assertion. Add one new `it` to the existing
    `describe("ResultsDisplay.tsx is terminal: no callback props, no bypass copy ...")` block in
    `tests/quiz-testing-bypass-deletion.test.ts` that asserts zero occurrences, in
    `ResultsDisplay.tsx`'s source, of each of these needles, case-insensitively where the file's
    existing helper allows it: "if approved", "once approved", "upon approval", "you will be able to
    purchase", "you can purchase", "unlock". Use the file's existing occurrence-counting helper —
    `split(needle).length - 1`, never `grep -c`. Name the `it` after the decision it enforces
    (`DEC-no-approval-promise-copy`, D-10) and comment that the needle list is the approval-promise
    vocabulary, not an exhaustive semantic check, so a reviewer knows its limits.

    Prove that assertion is non-vacuous: temporarily insert "if approved" into one band paragraph in
    the working tree, confirm the new `it` fails, remove it, and confirm `git diff --exit-code` on the
    component is clean. Record the observed failure in the SUMMARY.

    Then update `.planning/phases/05-preliminary-score-page/05-VALIDATION.md`'s Per-Task Verification
    Map: replace each `⬜ pending` status with `✅ green` for the rows whose automated commands now pass,
    leaving the bundle row pending until plan 05-05 runs. Set `wave_0_complete: true` in the
    frontmatter, since every Wave 0 gap listed there — `score-scale.ts`, the unit coverage, and this
    phase's DOM test — now exists. Leave `nyquist_compliant` as the planner set it. Do not edit the
    Manual-Only Verifications table; plan 05-06 owns those.

    Do not touch `.planning/REQUIREMENTS.md` in this plan — its SCORE row bookkeeping is plan 05-06's
    closing task, after the human verification pass, so a requirement is never marked complete on the
    strength of a green suite alone.
  </action>
  <verify>
    <automated>npx vitest run tests/quiz-testing-bypass-deletion.test.ts && npm test && git diff --exit-code app/components/quiz/ResultsDisplay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` exits 0, and the file now contains one additional `it` naming D-10.
    - `node -e "const s=require('fs').readFileSync('tests/quiz-testing-bypass-deletion.test.ts','utf8');for(const n of ['if approved','you will be able to purchase','unlock'])if(s.split(n).length-1<1)throw new Error('needle missing from guard: '+n);console.log('ok')"` succeeds.
    - The temporary "if approved" insertion was observed to fail the new assertion, and that failure message is quoted in the SUMMARY.
    - `git diff --exit-code app/components/quiz/ResultsDisplay.tsx` exits 0.
    - `05-VALIDATION.md` has `wave_0_complete: true` and no remaining `⬜ pending` row except the bundle-freshness row.
    - The five pre-existing absence assertions in the same describe block are byte-identical to their pre-change text.
  </acceptance_criteria>
  <done>D-10 is closed by a non-vacuous assertion rather than a reading, and the validation map reflects reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → nothing | Tests run locally and in CI against jsdom. No fixture leaves the process, no database is touched, no network call is made. The relevant control is that fixtures carry no realistic patient identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-19 | Repudiation (a green test that proves nothing) | `tests/quiz-results-scale-bar-dom.test.ts` | mitigate | This project has been burned six times by "passes, therefore correct". Mitigated by requiring three observed mutation-induced failures (marker reparented, tone derived from the bracket, title reverted) before the test is trusted, with the failure text quoted in the SUMMARY. |
| T-5-20 | Repudiation (a vacuous compliance assertion) | The D-10 needle guard | mitigate | An absence assertion over needles that were never present is satisfied by an empty file. Mitigated by the temporary "if approved" insertion proving the guard fires, and by naming the needle list's limits in a comment so it is not mistaken for a semantic check. |
| T-5-21 | Information Disclosure (PHI in a test fixture) | `renderResults` baseline props | mitigate | `ResultsDisplay` is a PHI-adjacent surface and fixtures live in git forever. Mitigated by requiring a synthetic `symptomProfileId` and no name, DOB, email, or phone in any fixture — the component takes none of those props, and none may be introduced. |
| T-5-22 | Tampering (a hardcoded expectation that defeats SCORE-02) | Ceiling and zone-count assertions | mitigate | Asserting `toBe(60)` would make the test pass while the production ceiling silently rots. Mitigated by reading `getScoreScale()` in the assertions and by the acceptance check rejecting a literal `toBe(60)` in the file. |
| T-5-23 | Tampering (working-tree pollution) | Temporary mutations in `ResultsDisplay.tsx` | mitigate | Both non-vacuity proofs mutate production source. Mitigated by `git diff --exit-code app/components/quiz/ResultsDisplay.tsx` in both tasks' verify commands, which fails if any mutation survives. |
| T-5-24 | Denial of Service / availability | jsdom + Testing Library | accept | Already-installed devDependencies since Phase 3, Andrew-approved. No new package is added. |
</threat_model>

<verification>
- `npx vitest run tests/quiz-results-scale-bar-dom.test.ts tests/quiz-testing-bypass-deletion.test.ts` — green.
- `npm test` — full suite green, count up by at least 15.
- `npm run typecheck` — exit 0.
- `git diff --exit-code app/components/quiz/ResultsDisplay.tsx app/styles/quiz.module.css app/lib/quiz` — clean; this plan adds tests and docs only.
</verification>

<success_criteria>
- The scale bar's independence from the clinical bracket is proven behaviourally, not asserted in prose.
- The marker-clipping structural fix is guarded by a test that fails when the marker is reparented.
- The accessible name is proven self-sufficient and proven not to name the clinical bracket.
- Score 0 and score 60 both render cleanly.
- D-10 is enforced by a non-vacuous absence assertion.
- The validation map records real, measured status.
</success_criteria>

<output>
After completion, create `.planning/phases/05-preliminary-score-page/05-04-SUMMARY.md`, quoting all
four observed non-vacuity failures (three from Task 1, one from Task 2), the per-bracket D-10 reading,
and which inline-style read strategy jsdom actually supported.
</output>
