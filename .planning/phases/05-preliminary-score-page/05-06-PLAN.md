---
phase: 05-preliminary-score-page
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-preliminary-score-page/05-VALIDATION.md
autonomous: false
requirements: [SCORE-01, SCORE-02, SCORE-03]
tags: [checkpoint, human-verify, accessibility, requirements-bookkeeping]

must_haves:
  truths:
    - "A human has seen the Preliminary Score page in a real browser at three score positions"
    - "The colour bands are distinguishable in greyscale, proving WCAG 1.4.1 compliance beyond the code review"
    - "The bar renders correctly at 375px and at desktop width with no marker clipping and no label collision"
    - "SCORE-01, SCORE-02, and SCORE-03 are marked complete only after that human pass, never on a green suite alone"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCORE-01/02/03 marked complete with the Phase 5 provisional-scale caveat recorded"
    - path: ".planning/phases/05-preliminary-score-page/05-VALIDATION.md"
      provides: "Manual-Only Verifications table filled in with observed results"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "the human verification result"
      via: "checkbox flipped only after the checkpoint passes"
      pattern: "SCORE-0"
---

<objective>
Put the page in front of a human at three score positions, in greyscale, and at mobile width — then
close the requirement rows.

Purpose: every automated guard in this phase runs in jsdom, which computes no layout and renders no
colour. Three of this phase's success criteria are irreducibly visual: whether the bands read as bands,
whether the marker lands where the patient's score actually is, and whether the colour is
distinguishable without colour. jsdom cannot answer any of them. This project's history is defects that
passed a green suite, and the phase's own validation strategy names these as manual-only.
Output: an observed verification record and three closed requirement rows.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-preliminary-score-page/05-CONTEXT.md
@.planning/phases/05-preliminary-score-page/05-UI-SPEC.md
@.planning/phases/05-preliminary-score-page/05-VALIDATION.md

<interfaces>
<!-- Where the page lives and how to reach each score position. -->

Local dev:   npm run dev      then the quiz embed route served by the Fly app locally
Deployed:    https://alle-drops-quiz-app.fly.dev  — do NOT deploy for this checkpoint. Andrew authorises
             deploys, and `fly deploy` runs from `main` after a PR merge, never from a branch.

Reaching a given score without twenty clicks: `ResultsDisplay` is a pure props component. The cheapest
honest way to see three positions is a temporary local harness or React DevTools prop edit, NOT a
production code change and NOT a testing bypass — Phase 4's TEST-05 deleted every bypass path and it
must stay deleted. Whatever harness is used must be reverted before the plan closes, proven by
`git status --porcelain`.

Score positions to check, chosen to exercise each zone and each edge:
  score 0   bracket 0-2   marker at the extreme left, low tone
  score 7   bracket 7+    low tone WITH the SLIT recommendation — the D-05 proof, visually
  score 45  bracket 7+    mid-high position, high tone
  score 60  bracket 7+    marker at the extreme right

Phase 5 band values are provisional (D-04) — placeholders until William tunes them in Phase 5.1. The
human pass verifies the mechanism and the rendering, not the clinical correctness of the thresholds.

REQUIREMENTS.md rows to close: lines 155, 159, 162 (the `- [ ]` checkboxes) and the traceability rows at
lines 317-319 (`Pending` → `Complete`). Lines 165 and 349's amendment notes are already correct and must
not be rewritten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the verification surface and capture the evidence</name>
  <files>none — no repository file is modified by this task</files>
  <read_first>
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Accessibility Contract, §Responsive Behaviour, §Component Inventory 2
    - `.planning/phases/05-preliminary-score-page/05-VALIDATION.md` Manual-Only Verifications table
    - `app/components/quiz/ResultsDisplay.tsx` as shipped
  </read_first>
  <action>
    Get the page renderable locally and capture what a human needs to judge, so the checkpoint is a
    two-minute look rather than a twenty-minute setup.

    Start the local dev server and reach the results screen. If a temporary harness is needed to set
    arbitrary props, create it outside the source tree the build compiles — or use React DevTools to edit
    the live props — and do not add a query parameter, a debug flag, an environment check, or any other
    bypass to `ResultsDisplay` or the quiz flow. Phase 4's TEST-05 exists because bypasses shipped to
    production once already, and `tests/quiz-testing-bypass-deletion.test.ts` will fail if one reappears.

    Capture screenshots at each of the four score positions named in this plan's `<interfaces>` block, at
    375px width and at a desktop width, in both normal colour and greyscale. Greyscale is the one that
    matters most: WCAG 1.4.1 requires the bands to carry meaning without colour, and UI-SPEC's answer is
    text labels plus a bolded current label plus visible seams. A greyscale screenshot either shows three
    distinguishable regions with a locatable marker or it does not.

    Note anything that looks wrong rather than deciding it is acceptable — in particular: a marker that
    touches or overflows the track's rounded end at score 0 or score 60, legend labels that collide or
    wrap at 375px, a seam that disappears between two adjacent tones, a marker that is hard to locate in
    greyscale, or the score circle and the bar readout appearing to disagree.

    Stop the dev server and revert the harness. Confirm `git status --porcelain` reports nothing outside
    the files this phase's other plans legitimately changed.
  </action>
  <verify>
    <automated>git status --porcelain && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Screenshots exist for all four score positions, each at 375px and desktop width, in normal colour and greyscale.
    - `git status --porcelain` shows no temporary harness file and no modification to `app/components/quiz/ResultsDisplay.tsx` beyond what plan 05-03 committed.
    - `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` exits 0 — no bypass was introduced to make the verification easier.
    - `npm test` and `npm run typecheck` both exit 0.
    - Every observed anomaly is written down verbatim rather than judged, and is carried into the checkpoint's `<what-built>` section.
  </acceptance_criteria>
  <done>Four score positions captured at two widths in colour and greyscale, with the working tree clean and no bypass introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Andrew verifies the Preliminary Score page</name>
  <read_first>
    - `.planning/phases/05-preliminary-score-page/05-UI-SPEC.md` §Accessibility Contract
    - Task 1's captured screenshots and its list of observed anomalies
  </read_first>
  <files>none — this task presents evidence and waits for Andrew</files>
  <action>
    Present the evidence and stop. Show Andrew the four score-position captures at both widths, in
    colour and greyscale, then walk the seven checks in this task's `<how-to-verify>` block in order.
    Lead with the score-7 case, because it is the one that decides whether the phase goal was met.

    State Task 1's observed anomalies plainly before asking for a verdict, including anything that looked
    merely slightly off. Do not pre-judge an anomaly as acceptable and do not bundle it into a summary
    sentence — Andrew decides what is cosmetic here, and the whole reason this checkpoint is blocking is
    that jsdom rendered no colour and computed no layout.

    Do not modify any file in this task, do not deploy, and do not proceed to Task 3 until Andrew responds.
    If he raises a blocker, fix it in the owning plan's files and re-run this checkpoint rather than
    recording it as accepted.
  </action>
  <verify>
    <automated>echo "blocking checkpoint — Andrew's response is the verification"</automated>
  </verify>
  <done>Andrew has responded with "approved" or with a specific list of problems, and every issue he raised is recorded verbatim with a disposition.</done>
  <what-built>
    The results screen is now the Preliminary Score page. Its heading reads "Preliminary Score" and its
    subtitle states that the Clinical Team is reviewing the information and will email final results
    within 1-2 business days. The "Symptom Score: 7+" chip is gone. Above the recommendation sits a
    colour-banded bar showing where the patient's raw score falls on the full 0-to-60 range, with a
    "Symptom burden" label, a "7 of 60" readout, and Low / Moderate / High labels under their segments
    with the current one bolded. The recommendation now sits under its own "What this means for you"
    heading, separated by a visible boundary. The three band explanations and the disclaimer are
    unchanged, word for word.

    The 60 is derived from the scored question set rather than typed in, so adding a scored question
    moves it automatically. The three band thresholds (20 / 40 / 60) are provisional placeholders — you
    tune them with William in Phase 5.1, which swaps the code constant for a database-backed config
    without touching any of this rendering.
  </what-built>
  <how-to-verify>
    1. Look at the four score positions in the captured screenshots — score 0, 7, 45, and 60. Confirm the
       marker moves proportionally and that at 0 and at 60 it sits at the track's end without being cut
       in half by the rounded corner.
    2. Look at the score-7 capture specifically. The bar should be in the low band AND the SLIT
       recommendation should be showing at the same time. That combination is the whole point of the
       redesign: a patient who scores 7 out of 60 is clinically a `7+` bracket but is not visually in the
       red. If the bar looks red or severe here, that is a blocker, not a nitpick.
    3. Look at the greyscale captures. Without colour, can you still tell there are three bands, and can
       you tell which one the patient is in? If the answer is no, the accessibility contract is not met.
    4. Look at the 375px captures. Confirm the Low / Moderate / High labels do not collide, wrap
       awkwardly, or drift out from under their segments.
    5. Read the subtitle out loud. Confirm it promises a review and an email, and promises nothing about
       being able to buy anything if approved.
    6. Confirm nothing on screen tells the patient the bands are provisional — that status is for the
       code and the plan, not the patient.
    7. Say whether the bar reads as clinically honest to you. That is a judgement no test can make, and
       it is the phase goal in one sentence.
  </how-to-verify>
  <acceptance_criteria>
    - Andrew has viewed all four score positions at both widths, in colour and greyscale.
    - Andrew explicitly confirms the score-7 / `7+` case shows a low-tone bar alongside the SLIT recommendation.
    - Andrew explicitly confirms the greyscale captures remain interpretable.
    - Andrew explicitly confirms no copy promises purchase on approval and no copy mentions provisional bands.
    - Any issue Andrew raises is recorded verbatim in the SUMMARY with a disposition: fixed in this plan, deferred to Phase 5.1 with a note, or accepted.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or describe what looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the requirement rows and the validation record</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/05-preliminary-score-page/05-VALIDATION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 150-170 (the SCORE requirement bodies), lines 310-325 (the traceability table), and lines 340-355 (the amendment notes)
    - `.planning/phases/05-preliminary-score-page/05-VALIDATION.md` Manual-Only Verifications table
    - Task 2's approval and any recorded issues
  </read_first>
  <action>
    Only run this task after Task 2 returns approval. If Andrew raised a blocker, fix it first — or,
    if the fix belongs to Phase 5.1, record it in ROADMAP's Phase 5.1 entry — and do not close a
    requirement row on the strength of a green suite.

    In `.planning/REQUIREMENTS.md`, change the `- [ ]` checkboxes on the SCORE-01, SCORE-02, and SCORE-03
    requirement bodies (lines 155, 159, 162) to `- [x]`. In the traceability table (lines 317-319), change
    each row's status from `Pending` to `Complete`, preserving SCORE-02's and SCORE-03's existing
    parenthetical about the 2026-08-11 unblocking and William's Phase 5.1 tuning — that note is the
    record of why they shipped on provisional values, and deleting it loses the reason. Append to
    SCORE-03's row a short note that Phase 5 shipped the provisional bands as a code constant and that
    Phase 5.1 replaces `getScoreScale()`'s body with a database read behind the same signature.

    Do not touch the AMENDED note at line 165 or the note at line 349 — both are already accurate and
    both are the audit trail for the D-04 decision.

    In `05-VALIDATION.md`, fill in the Manual-Only Verifications table with what was actually observed:
    the greyscale result, the 375px result, the marker behaviour at score 0 and 60, and the score-7
    decoupling check. Write the observation, not "verified" — a table row reading "verified" is worth
    nothing to whoever reads this during the HIPAA review. Set the bundle-freshness row to green, since
    plan 05-05 closed it.

    Add one line to `05-VALIDATION.md` recording the phase's final test count from `npm test`, so the next
    phase's plans have a real baseline instead of STATE.md's stale 677.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');for(const id of ['SCORE-01','SCORE-02','SCORE-03']){const re=new RegExp('- \\\\[x\\\\] \\\\*\\\\*'+id);if(!re.test(s))throw new Error('not checked: '+id);}if(/\\| SCORE-0[123] \\| Phase 5 \\| Pending/.test(s))throw new Error('traceability row still Pending');if(!s.includes('AMENDED 2026-08-11'))throw new Error('amendment note lost');console.log('ok')" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All three SCORE requirement bodies show `- [x]`, and no traceability row for SCORE-01/02/03 still reads `Pending`.
    - SCORE-02's and SCORE-03's existing unblocking parentheticals survive verbatim, and the `AMENDED 2026-08-11` note is unchanged.
    - `05-VALIDATION.md`'s Manual-Only Verifications table contains a written observation per row, not the word "verified" alone, and the bundle-freshness row is green.
    - `05-VALIDATION.md` records the final `npm test` count as a number.
    - `git diff --stat .planning/` shows only `REQUIREMENTS.md` and `05-VALIDATION.md` changed by this task.
  </acceptance_criteria>
  <done>The three SCORE requirements are closed on the strength of an observed human pass, with the provisional-scale caveat preserved for the Phase 5.1 handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev harness → committed source | A shortcut added to reach an arbitrary score is a bypass. Phase 4 deleted every bypass path precisely because one shipped. The boundary is enforced by `git status` and by the TEST-05 guard. |
| human judgement → requirement closure | A requirement marked complete is a claim this project will rely on during a HIPAA readiness review. The control is that closure follows an observed pass, never a green suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-31 | Elevation of Privilege (a bypass reintroduced for convenience) | Local verification harness | mitigate | Reaching score 45 by clicking is tedious, which is exactly how the original bypass got written. Mitigated by forbidding any source-level flag or query parameter, requiring DevTools or an out-of-tree harness, and re-running `tests/quiz-testing-bypass-deletion.test.ts` plus `git status --porcelain` before closing. |
| T-5-32 | Repudiation (a requirement closed without evidence) | `REQUIREMENTS.md` row closure | mitigate | Mitigated by gating Task 3 behind Task 2's explicit approval and by requiring written observations rather than the word "verified" in the validation table. |
| T-5-33 | Information Disclosure (PHI in a screenshot) | Captured verification screenshots | mitigate | Screenshots of a PHI-adjacent surface must not be committed or shared. Mitigated by using the synthetic profile ID convention from plan 05-04, entering no realistic name, DOB, email, or phone into the local flow, and keeping screenshots out of the repository — they are checkpoint evidence for the session, not artifacts. |
| T-5-34 | Information Disclosure (unintended deploy) | `fly deploy` | mitigate | This checkpoint is local only. Mitigated by the plan explicitly forbidding a deploy: `fly deploy` runs from `main` after Andrew merges the PR, and CLAUDE.md requires his authorisation. |
| T-5-35 | Tampering (losing the D-04 audit trail) | `REQUIREMENTS.md` amendment notes | mitigate | The 2026-08-11 amendment is the record of why SCORE-02/03 shipped on provisional values. Mitigated by naming lines 165 and 349 as untouchable and by the verify command asserting the `AMENDED 2026-08-11` string survives. |
</threat_model>

<verification>
- Four score positions captured at two widths, in colour and greyscale.
- `npx vitest run tests/quiz-testing-bypass-deletion.test.ts` — green, no bypass reintroduced.
- `npm test` and `npm run typecheck` — green.
- `git status --porcelain` — no temporary harness surviving.
- `REQUIREMENTS.md` — three rows closed, both amendment notes intact.

**Phase-level verification, run once here:**
- `npm test` — full suite green, count recorded in `05-VALIDATION.md`.
- `npm run typecheck` — exit 0.
- `npm run build:theme` — artifacts unchanged from plan 05-05's commit (a diff here means something drifted after the rebuild).
</verification>

<success_criteria>
- A human has seen the page, not just its tests.
- The greyscale check passed, making the WCAG 1.4.1 claim an observation rather than an assertion.
- The score-7 low-tone-plus-SLIT case was confirmed visually, closing the phase goal's honesty question.
- The three SCORE requirements are closed with the provisional-scale caveat preserved for Phase 5.1.
</success_criteria>

<output>
After completion, create `.planning/phases/05-preliminary-score-page/05-06-SUMMARY.md` recording
Andrew's verdict, every issue he raised with its disposition, the greyscale and 375px observations, and
the final `npm test` count. Note explicitly that Phase 5.1 inherits `getScoreScale()` as its single
swap point and `getMaxScore()` as its ceiling source.
</output>
